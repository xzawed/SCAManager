# fastapi>=0.137.0 — starlette CVE-2024-47874 / CVE-2025-54121 패치 버전 번들
# fastapi>=0.137.0 — bundles starlette with CVE-2024-47874 / CVE-2025-54121 patches
fastapi>=0.137.0
slowapi==0.1.9
uvicorn[standard]==0.49.0
pydantic-settings==2.14.1
sqlalchemy==2.0.50
alembic==1.18.4
# 온프레미스 프로덕션: psycopg2-binary → psycopg2 + libpq-dev(Ubuntu) / postgresql-devel(RHEL) 설치 권장
# On-premises production: replace psycopg2-binary with psycopg2 + libpq-dev (Ubuntu) / postgresql-devel (RHEL).
psycopg2-binary==2.9.12
PyGithub==2.9.1
httpx==0.28.1
defusedxml>=0.7.1
# authlib>=1.7.2 — GHSA-jj8c-mmj3-mmgv (OAuth cache CSRF) fix
authlib>=1.7.2
itsdangerous>=2.2.0
# starlette는 fastapi가 CVE 패치 버전을 번들로 가져옴 — 독립 핀 불필요
# starlette CVE patches are bundled via fastapi>=0.137.0 — no standalone pin needed
python-telegram-bot==22.7
anthropic>=0.104.1
# openai — 머지 검증자(2nd-LLM cross-vendor) 런타임 의존 + (dev) DALL-E 일러스트 도구. 키 미설정 시 비활성
# openai — runtime dep for the 2nd-LLM merge verifier (+ dev DALL-E tool). Inert without OPENAI_API_KEY.
openai>=2.41.0
aiosmtplib>=5.1.1
pylint==4.0.5
flake8==7.3.0
bandit>=1.9.4
semgrep==1.165.0
slither-analyzer>=0.11.5
sqlfluff>=4.2.2
jinja2==3.1.6
# python-multipart>=0.0.30 — CVE-2026-40347 (preamble/epilogue CPU DoS) fix
python-multipart>=0.0.30
yamllint>=1.38.0
# cryptography>=48.0.0 — CVE-2026-26007/34073/39892 fix
cryptography>=48.0.0
