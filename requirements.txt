# fastapi>=0.115.5 — starlette CVE-2024-47874 / CVE-2025-54121 패치 버전 번들
# fastapi>=0.115.5 — bundles starlette with CVE-2024-47874 / CVE-2025-54121 patches
fastapi>=0.115.5
uvicorn[standard]==0.30.6
pydantic-settings==2.3.4
sqlalchemy==2.0.35
alembic==1.13.3
# 온프레미스 프로덕션: psycopg2-binary → psycopg2 + libpq-dev(Ubuntu) / postgresql-devel(RHEL) 설치 권장
# On-premises production: replace psycopg2-binary with psycopg2 + libpq-dev (Ubuntu) / postgresql-devel (RHEL).
psycopg2-binary==2.9.11
PyGithub==2.3.0
httpx==0.27.2
# authlib>=1.6.11 — GHSA-jj8c-mmj3-mmgv (OAuth cache CSRF) fix
authlib>=1.6.11
itsdangerous>=2.1.0
# starlette는 fastapi가 CVE 패치 버전을 번들로 가져옴 — 독립 핀 불필요
# starlette CVE patches are bundled via fastapi>=0.115.5 — no standalone pin needed
python-telegram-bot==21.6
anthropic>=0.25.0
aiosmtplib>=3.0.0
pylint==3.3.1
flake8==7.1.1
bandit>=1.9.4
semgrep==1.136.0
slither-analyzer>=0.10.0
jinja2==3.1.6
# python-multipart>=0.0.26 — CVE-2026-40347 (preamble/epilogue CPU DoS) fix
python-multipart>=0.0.26
# cryptography>=46.0.7 — CVE-2026-26007/34073/39892 fix
cryptography>=46.0.7
# Observability (Phase E.2) — SENTRY_DSN 미설정 시 no-op
# Observability (Phase E.2) — no-op when SENTRY_DSN is not set.
sentry-sdk[fastapi]>=2.0.0
