# fastapi>=0.138.1 — starlette 1.0 지원 첫 릴리스 (include_router → _IncludedRouter 지연 포함).
#   이전 CVE-2024-47874 / CVE-2025-54121(starlette) 패치는 아래 starlette>=1.3.1 로 승계.
# fastapi>=0.138.1 — first release supporting starlette 1.0 (include_router → lazy _IncludedRouter).
#   Prior CVE-2024-47874 / CVE-2025-54121 (starlette) fixes are carried by starlette>=1.3.1 below.
fastapi==0.141.1
slowapi==0.1.10
uvicorn[standard]==0.52.1
pydantic-settings==2.14.2
sqlalchemy==2.0.51
alembic==1.19.0
# 온프레미스 프로덕션: psycopg2-binary → psycopg2 + libpq-dev(Ubuntu) / postgresql-devel(RHEL) 설치 권장
# On-premises production: replace psycopg2-binary with psycopg2 + libpq-dev (Ubuntu) / postgresql-devel (RHEL).
psycopg2-binary==2.9.12
PyGithub==2.9.1
httpx==0.28.1
defusedxml==0.7.1
# authlib>=1.7.2 — GHSA-jj8c-mmj3-mmgv (OAuth cache CSRF) fix
authlib==1.7.2
itsdangerous==2.2.0
# starlette>=1.3.1 — PYSEC-2026-161 / CVE-2026-48710 패치 (Host 헤더 미검증 → request.url.path 오염 → 경로 인증 우회; ≤1.0.0 전체 취약, 1.0.1 단독 수정).
# starlette>=1.3.1 — fixes PYSEC-2026-161 / CVE-2026-48710 (Host header not validated → request.url.path poisoning → path-auth bypass; all ≤1.0.0 affected, fixed only in 1.0.1).
starlette==1.4.1
python-telegram-bot==22.8
anthropic==0.120.2
# openai — 머지 검증자(2nd-LLM cross-vendor) 런타임 의존 + (dev) DALL-E 일러스트 도구. 키 미설정 시 비활성
# openai — runtime dep for the 2nd-LLM merge verifier (+ dev DALL-E tool). Inert without OPENAI_API_KEY.
openai==2.53.0
aiosmtplib==5.1.2
pylint==4.0.6
flake8==7.3.0
bandit==1.9.4
# 🔴 semgrep 은 1.170.1 에 고정 — 1.171.0 은 sqlfluff 와 공존 불가 (2026-07-27 실측)
#   semgrep 1.171.0 → click~=8.4.2 / sqlfluff 4.2.2(PyPI 최신) → click<8.4.0 = ResolutionImpossible.
#   sqlfluff 를 올려 해소할 수 없다(4.2.2 가 최신이고 그 제약이 최신). 올리려면 sqlfluff 를 버려야 하므로
#   SQL 정적분석 유지가 우선. 🔴 반복 제안 노이즈는 `.github/dependabot.yml` ignore 로 억제했으므로
#   (#1227) **dependabot 이 더 이상 알려주지 않는다** — 해제 조건 추적은 `docs/backlog.md` H3.
# 🔴 Pinned to semgrep 1.170.1 — 1.171.0 cannot coexist with sqlfluff (measured 2026-07-27):
#   semgrep 1.171.0 requires click~=8.4.2 while sqlfluff 4.2.2 (latest on PyPI) requires click<8.4.0.
#   Bumping sqlfluff cannot fix it (4.2.2 IS latest). Keeping SQL analysis wins; revisit when sqlfluff relaxes.
semgrep==1.170.1
slither-analyzer==0.11.6
sqlfluff==4.2.2
jinja2==3.1.6
# python-multipart>=0.0.32 — CVE-2026-40347 (preamble/epilogue CPU DoS) fix
python-multipart==0.0.32
yamllint==1.38.0
# cryptography>=50.0.0 — CVE-2026-69247 fix (pkcs7_decrypt_* 가 RecipientInfo 복호 결과를
#   보고해 padding-oracle 성립). 이전 핀 사유: CVE-2026-26007/34073/39892.
cryptography==50.0.0
