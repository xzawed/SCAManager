# 사이클 93 Step 2 — UI 일러스트 5장 저장 위치.
# Cycle 93 Step 2 — UI illustration storage location.
#
# 생성: src/scripts/generate_illustrations.py (사용자 로컬 실행).
# Generated by: src/scripts/generate_illustrations.py (user-side local run).
#
# 본 .gitkeep 은 빈 디렉토리 git tracking 용 — 일러스트 commit 후 제거 가능.
# This .gitkeep keeps the empty directory tracked — remove after illustrations are committed.
