-r requirements.txt
# pytest>=9.1.1 — CVE-2025-71176 (tmpdir race condition) fix
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
# subprocess hang 방지 — Windows 환경 특히 중요 (pytest.ini addopts --timeout=30 페어)
# Prevents subprocess hangs — especially critical on Windows (paired with addopts --timeout=30).
pytest-timeout==2.4.0
playwright==1.61.0
pytest-playwright==0.8.0
# openai 는 머지 검증자(2nd-LLM) 런타임 의존으로 requirements.txt(프로덕션)로 이동 — 위 `-r` 로 포함.
# openai moved to requirements.txt (prod, 2nd-LLM verifier runtime dep); included here via `-r` above.
# 개발 전용 추가 사용처: src/scripts/generate_illustrations.py (DALL-E 3 일러스트 생성, 사이클 93).
