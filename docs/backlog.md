# 미해결 일감 원장 (Engineering Backlog)

> **목적**: 회고·감사·운영 관측에서 **확인됐으나 아직 처리하지 않은** 일감을 한 곳에서 추적한다.
> 회고 보고서는 시점 스냅샷(append-only 아카이브)이라 "지금 뭐가 남았나" 를 답하지 못한다 — 이 파일이 그 질문의 단일 출처다.
>
> **owed 원장과의 차이**: [`runbooks/owed-verification.md`](runbooks/owed-verification.md) = **사용자만 할 수 있는 운영 검증**(SessionStart 훅이 기계 집행). 이 파일 = **코드로 처리 가능한 엔지니어링 일감**(집행 없음 — 우선순위 참조용).
>
> **상태 범례**: 🔴 결정 대기(사용자) · 🟡 착수 가능(Claude 자율) · ⏸️ 보류 · ✅ 완료(다음 정리 시 제거)

---

## ▶️ 다음 세션 시작점 (2026-07-19 세션4 인수인계)

**이 파일부터 읽으면 된다.**

🔴 **2026-07-19 세션4 전수 점검 결과 — 이 파일의 처방문 3건이 틀려 있었다.** Grok 교차 검증 완료.
처방문은 산문이 아니라 **실행될 코드**이므로, 틀린 처방은 틀린 코드와 같은 피해를 낸다:

| 항목 | 무엇이 틀렸나 |
|------|---------------|
| **B2** | 처방(`[deploy] numReplicas = 1`)이 **무효 Railway 키** — 그대로 했으면 cron P0 재현 |
| **B2-b** | 블로커 "CLI 로 확인 불가" 가 거짓 — **안 본 곳이 불가능으로 기록**됨 |
| **B6** | 근거의 **귀속이 틀림** — 자동 머지가 아니라 Claude 가 소유자 토큰으로 머지 |

**따라서 갱신 규칙에 (기전·반증수단) 의무를 추가**했다(맨 아래).

| 상태 | 건수 | 성격 |
|------|------|------|
| 🔴 결정 대기 | **1** (B6-b) | AI 자기 머지 거버넌스 — 사용자 영역 |
| 🟡 착수 가능 | **5** (B3·B4·B5·B6-a·B7) | Claude 자율 진행 가능 |
| ✅ 완료 | 3 (B2·B2-b·H1) | 다음 정리 시 제거 |
| ⏸️ 보류 | 1 (H2) | note 등급·테스트 코드 |

**권장 착수 순서**: **B4**(원장이 반증된 결론을 🔴 로 들고 있어 다음 세션을 오도한다 — 가장 싸고 피해가 즉각적) → **B3**(rules 2건) → **B5**(147 아카이브 복구/소실 선언 — 다른 항목 범위를 좌우) → **B6-a** → **B7**.
🔴 구 순서(**B6 최우선**)는 전제가 틀려 철회한다.

🔴 **owed 원장은 별도다** — [`runbooks/owed-verification.md`](runbooks/owed-verification.md) 안전등급 **`#1062`**(NULL-owner IDOR 오차단) 1건이 여전히 ⏳ 이며 SessionStart 훅이 매 세션 경고한다. 운영등급 `#1072`·`#1075`(20:00 UTC 미도래) 2건.

✅ **D1(#1116) · D3(#1117) · B1(#1114) 완료.** B1 = — `importlib.reload(src.database)` 미복구로 `#1102` 가드가 알파벳 순서에 의존하던 것을 봉인(순서 바꾸면 6건 FAIL → 순열 3종 전부 pass).

## ✅ 결정 완료 (2026-07-19) — D1~D4

사용자가 4건 모두 회신했다. **이 섹션은 다음 정리 시 제거** — 실행 상태만 남긴다.

| # | 결정 | 실행 |
|---|------|------|
| **D1** | `scan-security` **활성화 (일 1회)** | ✅ `#1116` — daily 04:00 UTC · `UNSCHEDULED_CRON_PATHS` 는 빈 dict 로 존치(향후 예외 등재 강제) |
| **D2** | auto-merge **고위험 경로만 제외** | 🟡 **미착수 → B6 으로 이관** (아래) |
| **D3** | pylint **정리 PR 진행** | ✅ `#1117` — 7건 전부 해소, **10.00/10 복원** |
| **D4** | 스케줄러 heartbeat **DB 신설** | 🟡 **미착수 → B7 로 이관** (아래) |

---

## 🔴 사용자 결정 대기

_현재 없음._ (D1~D4 는 2026-07-19 회신 완료 — 위 표 참조)

### (신규 결정 항목이 생기면 여기에 추가)

결정 없이는 착수할 수 없는 항목. **다음 사이클 진입 시 회신 요청 의무**.

| # | 일감 | 왜 결정이 필요한가 | 출처 |
|---|------|-------------------|------|
| **D1** | **`scan-security` cron 주기 실행 여부** — 엔드포인트는 있으나 스케줄러 job 이 없어 **출시 이래 실행 0회**. 선택지 = (a) `JOBS` 추가로 활성화 (b) 엔드포인트 폐기 (c) 수동 트리거 전용 현상 유지 | GHAS 폴링(`scan_all_repos`)이 **GitHub API 쿼터 소모 + 알림 발생** — 운영 기능 활성화라 정책 15 High tier | 회고2 P1 · #1112 |
| **D2** | **auto-merge 거버넌스** — 본 세션 PR **전량이 리뷰 0건·생성 ~5분 후 자동 머지**됐다. 정책 7/10 은 "사용자 머지" 를 전제하는데 실제 기전과 불일치. 선택지 = (a) 정책 본문을 현실에 맞춤 (b) 게이트를 조임(예: 특정 경로 변경 시 자동 머지 제외) | 거버넌스·정책 본문 변경이라 사용자 영역. 오늘 `#1109` 가 CodeQL 로 걸린 것이 **유일한 제동**이었다 | 회고2 P1 #3·#13 |
| **D3** | **pylint 잔여 7건 정리해 10.00 회복할지** — `main.py` C0103 명명 2 · C0411 import-order 2, `scheduler.py` W0718 1 · W0706 1 등. 게이트(≥9.90)는 통과 중 | `main.py` **import-order 재배치**를 포함해 부작용 검토가 필요 | 세션 실측 · #1111 |
| **D4** | **스케줄러 실행 기록 DB 영속화** — job 별 `last_run_at`·`last_status` 1행 upsert 테이블 신설. 그러면 스케줄러 생존이 **SELECT 1회**로 판정되고, owed 원장의 소진성 신호(만료 캐시 8행 보존 제약) 없이 상시 검증 가능 | **신규 테이블 + 마이그레이션** = 데이터 모델 변경(정책 15 High tier). 회고1에서 **3개 에이전트가 독립 권고**한 항목 | 회고1 P1 ×3 |

---

## 🟡 착수 가능 (Claude 자율)

결정 없이 진행 가능. 우선순위 순.

| # | 일감 | 근거 | 출처 |
|---|------|------|------|
| **B2** | ✅ **가드분 완료 (2026-07-19)** — `tests/unit/scripts/test_railway_scaling_guard.py` 8건. 🔴 **이 항목의 원래 지시문("`railway.toml` 에 `numReplicas = 1` 명시 핀")은 틀렸었다** — Railway config-as-code 레퍼런스상 `numReplicas` 는 `[deploy]` 유효 키가 **아니고**(`multiRegionConfig.<region>.numReplicas` 로만 존재), 그대로 썼다면 **2026-07-19 cron P0(무효 키 → 조용히 무시 + 거짓 안심)를 그대로 재현**했다. 가드가 이제 그 무효 키를 차단하고, `multiRegionConfig` 로 replica ≥ 2 시 advisory lock 을 강제한다. **잔여 = B2-b** | 수평 확장 시도가 CI 에서 advisory lock 의무를 강제 표면화 | 회고1 P1 |
| **B2-b** | ✅ **완료 (#1125)** — region `us-east4-eqdc4a` 로 핀. 🔴 **원래 기재된 블로커 "CLI 로 확인 불가" 는 틀렸었다** — 최상위 `regions`·`numReplicas` 는 null 이 맞지만 적용값은 `activeDeployments[].meta.serviceManifest.deploy.multiRegionConfig` 에 있었다. **"내가 안 본 곳" 이 "불가능" 으로 기록**돼 있었다. 배포 후 실측 검증(RUNNING `26bec2c5`·insts 1·스케줄러 6 jobs). ~~구 본문:~~ 유효 형식은 `[deploy.multiRegionConfig.<region>] numReplicas = 1` 뿐인데 **region 을 명시해야** 한다. 현재 region 을 CLI(`railway status --json`)로 확인 불가(`regions`·`numReplicas` 필드 null 반환)하고, 틀린 region 을 쓰면 **서비스가 다른 region 으로 이동**할 수 있다(Supabase pooler 가 region 민감 — 메모리 `feedback-connectivity-probe-first`). 현재 RUNNING 인스턴스는 1개로 실측 확인됨. 선택지 = (a) Railway 대시보드에서 현재 region 확인 후 그 값으로 핀 (b) 핀 없이 가드만 유지(현 상태) | 대시보드 스케일업을 **설정으로 막을지** vs region 이동 위험을 피할지 트레이드오프 | B2 후속 |
| **B3** | ✅ **완료 (2026-07-19)** — 3영역 전부 동기화. `security.md`(#1109+#1122) · **`db.md`**(`fileConfig` 가 앱 로깅을 파괴하는 기전 + `is_configured()` 가드 제거 금지) · **`testing.md`**(`importlib.reload` 세션 오염 + 🔴 가드가 알파벳 순서 덕에 살아있던 문제). **반증 수단**: 두 파일의 해당 키워드 grep 이 0건 → 1건 (착수 전 0건이 이 항목의 근거였다) | rule 본문이 stale 하면 Claude guidance drift | 회고2 P1 #16 |
| **B4** | **owed 원장 내부 모순 정리** — `#1103`·`#1105`·`#1106`·`#1108` 이 표 **셀만** 갱신하고 그 아래 산문 섹션을 반증 처리하지 않아, 반증된 *"cron 은 로그로 판별 불가"* 단정이 🔴 마커를 단 채 존치 | 원장 자신이 경고한 실패(낡은 결론 고착)를 재생산 중 | 회고2 P1 #6·#18 |
| **B6** | 🔴 **전제 정정 (2026-07-19 전수 점검)** — 원래 근거였던 *"본 세션 14 PR 이 전부 ~5분 **자동 머지**됐다"* 는 **귀속이 틀렸다**. 실측: `#1123`·`#1125`·`#1126`·`#1127` 전부 `mergedBy=xzawed`(= `gh` CLI 가 소유자 토큰으로 수행) — `src/gate/engine.py::_run_auto_merge` 가 한 것이 아니다. **`reviews=0` 은 사실**이나 "auto-merge 가 했다"는 거짓. 즉 **위험이 하나가 아니라 둘**이고 서로 다른 통제면을 갖는다 (아래 B6-a/B6-b 로 분리) | 처방이 겨눈 기전과 증거가 나온 기전이 달랐다 | D2 결정 · 전수 점검 |
| **B6-a** | 🟡 **auto-merge 무장 상태 (product surface)** — SCAManager 자신의 `repo_configs` 실측 = `auto_merge=true` · `approve_mode=auto` · `merge_threshold=60`, 그리고 **main 은 브랜치 보호 없음**(`Branch not protected` 실측). 즉 점수 60 이상이면 `src/auth/**`·`alembic/**` 포함 PR 도 자동 머지 가능한 상태로 **무장돼 있다**. 원래 B6 처방(경로 제외 + 회귀 가드, 구현 위치 `src/gate/`)이 겨누는 것은 **이쪽**이며 여전히 유효하다 | 이번 세션엔 발화하지 않았을 뿐 무장은 실재 | 전수 점검 |
| **B6-b** | 🔴 **결정 필요 — AI 자기 머지 거버넌스 (repo governance surface)** — 이번 세션 11 PR 을 **Claude 가 소유자 토큰으로 리뷰 0건 상태에서 직접 머지**했다. 이건 `src/gate/` 경로 필터로 막히지 않는다(다른 통제면). 선택지 = (a) main 브랜치 보호 + required review 설정 (b) 정책 7/10 을 현실에 맞춰 "Claude 머지 허용" 으로 명문화 (c) 특정 경로 변경 시 Claude 머지 금지 규칙 | 거버넌스·정책 본문 변경이라 사용자 영역. 지금은 **어느 쪽도 명시되지 않은 채 관행만 존재** | 전수 점검 |
| **B7** | **스케줄러 실행 기록 DB 영속화** (D4 결정) — 🔴 **긴급도 재평가 필요**: 원 근거 "로그로는 스케줄러 생존을 판정 못 한다" 는 `#1102`(로깅 복구) 이후 **약화됐다** — 실제로 `scheduler started — 6 jobs` + 60초 주기 tick 이 로그로 관측된다. DB 는 여전히 내구성·다중 인스턴스·소진성 신호 회피에 유용하나 **P0 가 아니라 durability 편의**다. D4 우선순위 재확인 권장 — job 별 `last_run_at`·`last_status` 1행 upsert 테이블 + 마이그레이션. 스케줄러 생존이 **SELECT 1회**로 판정되고, owed 원장의 소진성 신호(만료 캐시 8행 보존 제약) 없이 상시 검증 가능 | 회고1에서 **3 에이전트 독립 권고**. 로그 의존 검증 → DB 의존 검증이 이번 사고의 진짜 일반화된 교훈 | D4 결정 |
| **B5** | **회고 잔여 재평가 — 🔴 범위가 269 가 아니다** — 269(P0 19·P1 117·P2 133)는 **아카이브된 2건**(135+134)만의 합이다. 실측: 확정 **147건**(P0 2·P1 55·P2 90·186 에이전트) 회고는 **리포트가 아카이브되지 않아** 그 P1 55·P2 90 이 **어떤 원장에도 없다**(`docs/_archive/reports/` 에 186-에이전트 파일 부재 확인). 따라서 선행 작업은 재평가가 아니라 **아카이브 복구 또는 소실 선언**이다 — 없는 것을 재평가할 수는 없다 | 해소분과 잔여분이 섞여 있어 우선순위 판단 불가 | 회고1·2 |

---

## ⏸️ 보류 (의도적)

| # | 일감 | 보류 사유 |
|---|------|----------|
| **H1** | ✅ **해소 (2026-07-19)** — 사용자가 n8n 미사용 확인 → 리포 3건(`ArcanaInsight`·`CustomWebService`·`SCAManager`)의 `n8n_webhook_url` 을 NULL 로 정리(잔여 0 실측). ConnectError 루프 종료. 부수: 이 경로를 조사하다 **시크릿 유출 2경로 발견 → `#1122`**. (구 line 인용 `n8n.py:36` 은 drift — 36=`client.post`, 37=`raise_for_status`) |
| **H2** | **Code Scanning open alert 2건** — `py/unnecessary-lambda`(note), `tests/unit/scripts/test_guard_git_failclosed.py:26,27` | note 등급·테스트 코드. `#1096` 이 `#1097`(note 게이트)보다 먼저 머지돼 시점상 미포착 |

---

## 갱신 규칙

- 일감 완료 시 ✅ 로 표시하고 **다음 정리 시 제거**(이 파일은 append-only 아님 — 현재 상태를 반영한다).
- 신규 회고·감사 종료 시 **잔여 findings 를 여기로 이관**한다. 보고서는 시점 스냅샷으로 아카이브에 남기고, "지금 뭐가 남았나" 는 이 파일이 답한다.
- 🔴 **결정 대기 항목은 다음 사이클 진입 시 사용자 회신 요청 의무**(정책 5/9 페어).
- 🔴 **모든 🟡 행은 (기전 · 반증 수단)을 함께 적는다** (2026-07-19 전수 점검 신설).
  처방문은 **실행될 코드**다 — B2 의 처방은 무효 키였고, B2-b 의 블로커는 거짓이었으며,
  B6 의 근거는 귀속이 틀렸다. 셋 다 *그럴듯한 산문*이라 검토를 통과했다.
  - **기전** = 어느 코드/설정면이 이 문제를 만드는가 (예: `src/gate/engine.py::_run_auto_merge`)
  - **반증 수단** = 이 주장이 틀렸다면 무엇을 측정하면 드러나는가
    (예: *"`mergedBy` 가 봇이 아니라 토큰 소유자면 gate auto-merge 가 아니다"*)
  - 🔴 **"봉인/불가능/전량" 류 단정은 착수 전 반증 측정 1회 의무** — 위 3건 모두 이 한 번을
    안 해서 원장에 거짓이 남았다.
  - ❌ **기계 린터는 만들지 않는다** (Grok 협의 결론) — 자유 산문의 진위는 정적 검사로 판정할
    수 없고, 구조만 검사하는 린터는 통과가 아무것도 보장하지 않아 **이 세션이 계속 다룬
    observer-lie 를 하나 더 만드는 일**이 된다. 진위 판정은 사람/Grok 의 몫으로 둔다.
