# 미해결 일감 원장 (Engineering Backlog)

> **목적**: 회고·감사·운영 관측에서 **확인됐으나 아직 처리하지 않은** 일감을 한 곳에서 추적한다.
> 회고 보고서는 시점 스냅샷(append-only 아카이브)이라 "지금 뭐가 남았나" 를 답하지 못한다 — 이 파일이 그 질문의 단일 출처다.
>
> **owed 원장과의 차이**: [`runbooks/owed-verification.md`](runbooks/owed-verification.md) = **사용자만 할 수 있는 운영 검증**(SessionStart 훅이 기계 집행). 이 파일 = **코드로 처리 가능한 엔지니어링 일감**(집행 없음 — 우선순위 참조용).
>
> **상태 범례**: 🔴 결정 대기(사용자) · 🟡 착수 가능(Claude 자율) · ⏸️ 보류 · ✅ 완료(다음 정리 시 제거)

---

## ▶️ 다음 세션 시작점 (2026-08-11 세션19 — 문서 체계 감사 + 정리 계획 · 2026-08-04 회고 이관)

🔴 **문서·가드 정리가 진행 중이다. 착수 전 반드시 먼저 읽을 것:**
[`docs/runbooks/docs-consolidation-status.md`](runbooks/docs-consolidation-status.md) — **진행 상태 정본**
(12 PR 중 **PR-1~4 완료**[`#1328`·`#1329`·`#1330`·`#1331`], 다음 = **PR-5**(게이트 예산 거짓 집행자 교체 — 🔴 High tier).
🔴 이 줄은 상태 정본이 아니다. 2026-08-13 까지 *"PR-1 완료, 다음 = PR-2·PR-3"* 로 낡아 있었다 — 착수 전 status 파일을 열 것).
계획 전문 = [`runbooks/docs-consolidation-plan.md`](runbooks/docs-consolidation-plan.md) ·
근거 감사 = [`_archive/reports/2026-08-10-docs-system-audit.md`](_archive/reports/2026-08-10-docs-system-audit.md) ·
채점 = [`_archive/reports/2026-08-11-docs-scoring-report.md`](_archive/reports/2026-08-11-docs-scoring-report.md).

> 🔴 그 계획이 서 있는 전제 4건(뒤집지 말 것): ① 총량은 비용 문제가 아니다(게이트 = 세션비용 0.48%) —
> **용량 절감 근거 정리안은 기각** ② 문서 삭제·경로 이동 **0건**(소비자 grep 증명 없는 이동 금지) ③ 채점 체계는
> **순위를 발행하지 않는다**(검증 2라운드 BROKEN) ④ 정책 17 — `CLAUDE.md`·`AGENTS.md` 축소는 `#1296` 순손실 실증.

**이 파일부터 읽으면 된다.**

🔴 **2026-08-04 정식 5+1 회고 완료** (run `wf_d6314612-d17` · **162 에이전트** · 14.7M 토큰 ·
2h53m · finding **140** · 확정 **123** · FP 기각 17 · 심각도 조정 37 · **verdict_coverage 1.0**).
범위 = 2026-07-31 회고 이후 머지 PR **23건**(`#1250`~`#1272`) + 세션13·14 산출물(정책 8-(5)).
확정 분포 = **P0 1 · P1 60 · P2 62**. 🔴 **P0 후보 8건 중 7건이 cross-verify 에서 강등/기각**
(P0→P1 6 · FP 1) — 검증관이 실제로 반대편에 섰다. Grok claim-review(세션 `019fc81b`,
주장 트리거 단축 패스) = CLAIM-A **HOLDS** / CLAIM-B **BROKEN(기전 오류)**.
상세: [`_archive/reports/2026-08-04-retrospective.md`](_archive/reports/2026-08-04-retrospective.md).

> **지배 주제**: 세션13~14 가 *새로 무장한* 가드들이 **자기 자신을 배신한다**. P1 60건 중
> 24건이 이번 창에서 만들어지거나 손댄 관측면이다(심의 게이트 7 · 회고 워크플로 5 ·
> 카덴스 원장 5 · 정책 19 집행면 2 · pre_push_gate/훅 5). 결함 클래스 (6) *수정이 같은
> 결함을 재생산한다* 의 집중 발현이다.

| ID | 상태 | 항목 | 기전 · 반증 수단 |
|----|------|------|------------------|
| **R35** | ✅ 완료 (`#1275`) | 🔴 **[P0] 문서 심의 게이트가 응답 절단을 '승인'으로 처리 — CRITICAL 문서 편집이 무흔적 통과** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r35) |  |
| **R36** | ✅ 완료 (`#1275` + `#1276` 근본원인) | **심의 게이트 무동작 3형태 중 2형태가 '정상 판정'과 구별 불가 + 예외 원문 폐기** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r36) |  |
| **R37** | ✅ 완료 (`#1276` — 잔여 GROK-1 아래 명시) | **심의 게이트가 6-step ⑤ STATE.md 동기화 편집을 block 할 수 있다 — 필수 절차를 가드가 차단** — 🔴 **잔여**: 🔴 **잔여 = GROK-1**: `unable_to_verify` 는 구조적 검증 없이 **모델 신고에 의존**하므로 consistency 의 critical 거부권을 자발적으로 끌 수 있다(impact 이 하드 백스톱으로 남는다) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r37) |  |
| **R38** | ✅ 완료 (`#1282` — 잔여 3축 아래 명시) | **심의 게이트에 prompt caching 미적용 — 문서 편집 1회당 실측 85,434 입력 토큰** — 🔴 **잔여**: 🔴 **잔여 3축** — (a) `docs/STATE.md` 가 캐시 프리픽스에 있어 trailing sync 가 세션 중간 캐시를 무효화한다 (b) TTL 5분 + 병렬 첫 miss (c) **훅에 usage 관측면이 0** — 컨텍스트 예산이 4096 토큰 아래로 줄면 캐시가 오류 없이 조용히 죽는데 아무도 모른다 | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r38) |  |
| **R39** | 🟡 착수 가능 | **회고 워크플로가 체크포인트 0 · budget floor 1지점 · FP 미반환** | **기전**: `.claude/workflows/retrospective.mjs` 에 `writeFile`/`mkdir`/`checkpoint` **0 hit**(모듈에 fs 접근 자체가 없다) → 중단 = 전량 소실. `BUDGET_FLOOR`(`:237`)는 `:248` **Discover 루프 진입 시점만** 검사하고 verifyAll·completeness·gap·재검증 4 지출 지점을 지키지 않는다. 🔴 **2026-08-02 에 실현**(`wf_d89db046-274` ~30분 소실, 리포트 0건). 부수 = Report 가 **`FALSE_POSITIVE` 17건을 카운트만 하고 반환하지 않아** 무엇이 왜 기각됐는지 소실 → 다음 회고가 재발견한다. **반증 수단**: 라운드 종료마다 부분 finding 을 flush 하고 재진입 시 로드되는가 · 반환 payload 에 `false_positives[]` 가 있는가  🔴 **2026-08-14 Grok 독립 재실측(`01a00087`) — STILL_TRUE, 3축 전부 유효**: 2026-08-14 에 이 워크플로를 고쳤지만(Scope 기계 오라클 + Report 종료 시 범위 재측정) 그 작업은 **다른 클래스**를 닫았을 뿐이다. **(a) 체크포인트 0** — 현재 파일에 `writeFile`·`mkdir`·`checkpoint`·`fs.` **전부 0 hit**. 중단되면 `verified[]` 가 통째로 날아간다. **(b) budget floor 1지점** — `BUDGET_FLOOR` 는 `:295` 정의 · `:306` Discover 루프에서만 읽는다. 그 뒤 Completeness(`:327`) · gap finder · `verifyAll` · UNVERIFIED 재검증(`:348-352`) · Report 종료 에이전트(`:384`)는 **잔여 예산을 안 본다**. **(c) FP 미반환** — `falsePositives` 는 `:404` 에서 계산되는데 반환은 `roi.fp_blocked` 개수뿐이다. 기각 **사유**가 payload 에서 사라진다(`confirmed[]`·`unverified_findings[]` 는 있는데 `false_positives[]` 는 없다). **Grok 권장 다음 슬라이스 = (c) 만** — 반환 형태에 FP 객체를 얹는 작은 변경이고 명백히 옳다. (a) 는 워크플로 런타임에 파일시스템이 없어 **원리적으로 불가**(주석만 다는 것은 observer-lie), (b) 는 *예산 미달 시 중단할지 완주할지* 제품 결정 선행 |
| **R40** | 🟡 착수 가능 | **카덴스 이월 원장의 승인 1행이 window 전체를 무기한 뮤트한다 — 원장이 막으려던 바로 그 실패** | **기전**: `scripts/check_retro_cadence.py:108` 이 승인 셀의 **'비어있지 않음'만** 검사하고 `:127` 이 만료·세션당 재승인을 보지 않는다. 실제로 `docs/runbooks/retro-cadence-deferrals.md:25` 의 승인 인용은 *"현재 토큰량이 없습니다. 세션을 정리해주세요"* — **회고 이월을 승인하는 발화가 아니라 세션 종료 지시**다. '목표 진입 세션' 셀은 파싱만 되고 집행 0. **반증 수단**: 이월 행 1개가 있는 상태에서 다음 세션 배너가 여전히 loud 인가(= 세션당 재승인 요구)  🔴 **2026-08-14 Grok 독립 재실측(`01a00087`) — STILL_TRUE(기전), 현재는 비활성**: 이월 원장에 **현재 창 행이 하나라도 비어있지 않으면** 카운터가 `🔴 승인 없음` → `ℹ️ 기록됨` 으로 바뀌고, PR 이 15/20/100건 쌓여도 그대로다. **목표 세션은 인쇄만 되고 집행되지 않는다**(만료 없음). ⚠️ **지금 라이브는 뮤트 중이 아니다** — 실 원장 행 날짜가 2026-08-02 인데 직전 회고가 2026-08-14 라 그 행이 현재 창 **밖**으로 나갔다. 즉 결함은 살아 있고 우연히 발동 조건이 아닐 뿐이다 |
| **R41** | 🟡 착수 가능 | **backlog 완전성 축이 관측되지 않는다 — 역사 창 15행을 통째로 ✅ 로 뒤집어도 가드 10/10 green** | **기전**: `tests/unit/scripts/test_backlog_shape.py:221` `current_window()` 가 역사 섹션을 잘라내고, 전장 백스톱(`#1271`)은 **legality(마커 형태)만** 보고 completeness(열린 항목이 실제로 열려 있는가)는 보지 않는다. 뮤테이션 실증 = 역사 15행 ✅ 플립에도 전건 green. 동류 = **✅ 마커가 미결 잔여를 흡수**(R30 상태 셀이 *"완료 … 잔여 = 사용자 결정"*, 같은 요약 블록이 *"결정 대기 0건"* 과 *"R33-a 재개방"* 을 동시 주장) · **원장 자기 의무 규칙(모든 🟡 행에 기전·반증 수단)이 15행 중 11행 위반**(R10·R12 는 근거 셀이 비어 있고 하필 R10 이 '원장 정합' 항목). **반증 수단**: 역사 창 열린 행을 ✅ 로 바꾸는 뮤테이션에서 가드가 red 가 되는가  🔴 **2026-08-14 Grok 독립 재현(`01a0007f`) — STILL_TRUE**: 역사 표 36행 중 **비-✅ 14행을 전부 ✅ 로 뒤집었는데** `test_backlog_shape` 가 **10/10 green**(`mutated != orig` 확인 후 `git checkout --` 복원). 원인 확정: `current_window()` 가 **역사 섹션 헤딩**에서 절단하고(`test_backlog_shape.py:219-223`), 전장 검사는 legality 만 보며 ✅ 를 합법으로 받는다. *"이 항목이 정말 끝났는가"* 는 어떤 단언의 입력도 아니다. ⚠️ Grok 미확인: **현재 창** 표를 뒤집으면 요약 bijection 이 red 를 낼 것으로 보이나 그 축은 재현하지 않았다 |
| **R42** | 🟡 착수 가능 | **정책 19 집행면이 PR diff 를 읽지 않아 seal 주장이 STATE/cycle-history 로 이주하면 무검증 통과** | **기전**: `scripts/check_claim_review_trace.py:238` 이 **PR 본문만** 스캔한다. `#1269` 는 어휘 사전만 넓히고 검사 표면은 그대로 뒀다. 또 정책 19 default(*실질 작업마다 claim-review*)와 가드 트리거(seal 어휘)가 어긋나 **무주지대**가 생긴다 — 어휘를 피한 실질 작업은 default 위반인데 가드가 발동조차 않는다(R18 의 설계 천장과 다른 축: 이건 표면 문제다). **반증 수단**: seal 어휘를 PR 본문이 아니라 커밋 body·STATE 블록에만 넣은 PR 이 red 가 되는가  🔴 **2026-08-14 Grok 독립 재현(`01a0007f`) — STILL_TRUE**: 실제 커밋으로 실증했다. 무해한 title/body + `docs/STATE.md` **본문에만** 봉인 어휘를 넣은 커밋(`봉인했고 뮤테이션 3/3 red 로 완결했다`)을 만들고 `PR_BASE_SHA`/`PR_HEAD_SHA` 를 그 범위로 주니 **EXIT 0** — *"seal 주장 없음 · 코드 표면 변경 없음"*. 기전 확정: haystack = `title + body + commit_messages`(`:422-424`)뿐이고 `git diff` 는 **`--name-only`**(`:349`)라 **본문을 한 글자도 읽지 않는다**. 결정적 증거 — 같은 diff 에 `find_seal_claims()` 를 직접 돌리면 `(11, '봉인', '+<!-- R42-MEASURE --> …')` 를 반환한다. **가드가 볼 수 있는데 안 보는 것이다**. ⚠️ 수정 보류 사유(Grok): diff 본문을 스캔하면 STATE·cycle-history 가 과거 `봉인` 을 **인용**하는 trailing sync 마다 false-red 가 난다 — 표면 목록 확대 vs 추가 줄만 스캔 vs 한계 수용 중 **제품 결정**이 선행 |
| **R43** | 🟡 착수 가능 | **path-scoped rules 본문 sync 의무(사용자 명시 결정)의 발화율 ~100% · 실이행률 0%** | **기전**: `CLAUDE.md §신규 파일 추가 시 동기화 의무` 매트릭스가 의무를 정의하지만 **이행 조건도 면제 경로도 없다**. `R27` 이 '해소' 를 선언한 **다음 세션에 3/4 PR 에서 재위반** — 즉 ✅ 플립이 청구한 의무를 이행하지 않았다(원장 자체가 observer-lie). 부수 = **`.github/workflows/**` 가 10종 rule 어디에도 매칭되지 않아** CI 집행면 편집 시 규칙 도달성 0 · 도달성 가드(`tests/unit/scripts/test_rule_reachability.py:41`)의 심볼 축이 **1-핀**이라 주석이 이름 붙인 결함을 그 자신이 재생산 · 매트릭스↔frontmatter drift 는 가드가 설계상 보지 않는 방향. **반증 수단**: `src/<area>` 를 건드리고 대응 `.claude/rules/<area>.md` 를 안 고친 PR 이 red 가 되는가  🔴 **2026-08-14 Grok 독립 실측(`01a0007f`) — STILL_TRUE, 측정 방법 정정**: 🔴 원 지시의 `git log --merges --name-only` 는 **이 리포에서 공허하다** — squash 머지라 최근 이력에 merge 커밋이 없고, 40개 merge 커밋은 `#655`~`#502` 시절이며 `-m` 없이는 **파일을 0개 나열한다**(`0/40`). `--first-parent -40`(= 최근 40 PR)으로 재측정: 사용자 표현대로 **`src/<area>/**` 를 건드린 PR 4건 중 대응 rules 동반 = 0건 (0.0%)**. CLAUDE.md 표 전체 매핑(문서 포함)으로 넓히면 **4/39 = 10.3%** 이고 그 4건마저 대부분 *rules 편집이 본업이던 PR*(#1345·#1335·#1348)이다. 즉 원 서술의 *"이행률 0%"* 는 **src 축에서 재확인**됐고 넓은 축은 0이 아니라 10% 다. ⚠️ 수정 보류 사유(Grok): *"src 변경 ⇒ rule 파일 변경"* 하드 게이트는 **정당한 소규모 수정 대부분을 red 로 만든다**(0/4 가 그 증거) — 면제 정책이 선행돼야 한다 |
| **R44** | 🟡 착수 가능 | **가드·훅 자기 결함 5종 (전부 이번 창 산물 또는 이번 창이 손댄 표면)** | **기전**: (a) `.pre-commit-config.yaml:69` 훅 7종이 bare `python`(Windows Store 스텁 exit 49)을 쓰는데 이번 창이 만든 인터프리터 생존 가드는 `settings.json` 만 스캔 (b) `scripts/pre_push_gate.py:64` 가 **입력이 빈** claim-review 가드에 `OK` 를 찍는다 — 러너의 존재 이유인 '초록의 의미' 자기 위반 (c) `.claude/hooks/posttool_pytest_smoke.py:39` 가 `.claude/hooks/**` 를 감시하지 않는다(이번 창이 훅을 2회 편집했는데도) + `:187` 결과 배너가 plain `print()` 라 **Claude 에게 0회 도달**(guards.md:134-136 자기 규칙 위반) (d) `tests/unit/analyzer/test_procurement_contract.py:62` 조달 계약 대조가 **뮤테이션 GREEN** — 주석/대리 토큰만으로 "조달됨" 성립 (e) `check_memory_refs` 는 이 머신에서 **자동 실행면 0**(`#1250` 이 경로만 고치고 배선은 복구 안 함) + 스코프 밖 메모리 인용 7건 중 4건 dangling, 그중 2건이 자동 로드 `rules/`. **반증 수단**: 각 항목의 보호 대상을 삭제/뒤집었을 때 해당 가드가 red 인가(5건 개별 뮤테이션)  🔴 **2026-08-14 Grok 독립 재실측(`01a00087`) — 하위 5항목이 갈렸다. 단일 🟡 로 두면 안 된다**: **(a) ✅ 해소** — R56 의 `language: python` + `test_precommit_entry_interpreter.py` 가 닫았다. **(b) STILL_TRUE** — 빈 환경에서 OK 를 인쇄하는 축. **(c) STILL_TRUE** — `posttool_pytest_smoke` 의 `_WATCHED_ROOTS` 가 `src`·`alembic`·`scripts` 뿐이라 `.claude/hooks` 편집은 **감시 대상이 아니고**(0 hit), 출력이 `print()` 라 Claude 에게 도달하지 않는다(메모리 `feedback-hook-output-channel`). **(d) STILL_TRUE — 실경로 뮤테이션으로 확정**: `railway.toml` 에서 tflint **실제 설치 줄을 제거**하고 tflint 를 언급하는 `echo` 만 남겼는데 `test_every_contracted_tool_is_actually_provisioned` 가 **16 passed**. `_mentioned()` 가 주석·프록시 토큰을 조달로 센다. 🔴 형제 가드 `test_analyzer_provenance.py` 는 `_ECHO_PROSE` 로 산문을 벗긴다 — **같은 리포 안에 옳은 구현이 이미 있다**. **(e) PARTIALLY_FIXED** — dangling 축은 ✅ 해소(스캔 범위가 rules·runbooks·AGENTS·backlog 로 확대, 실측 6 슬러그 0 dangling)이나 **자동 실행 표면은 여전히 0**: SessionStart 5종에 없고 `ci.yml:145` 가 제외하며 pre-commit `files:` 는 `CLAUDE.md`+`policies/` 뿐이라 rules 편집에 발화하지 않는다. **Grok 권장**: (a)·(e)-dangling 을 ✅ 로 떼어내고 (b)(c)(d) 를 각각 현재 file:line 으로 재기술 |
| **R45** | 🟡 착수 가능 | **문서 정합 4종 — 집행면 수치·트리·앵커·줄바꿈** | **기전**: (a) `.github/workflows/ci.yml:129` — *"repo-integrity 9종"* 주장이 **13 지점에 전파**됐으나 실제 required check job 은 **7종**, 나머지 2종은 머지를 못 막는다 🔴 **(a) 축 정정 + 부분 해소 (2026-08-04 API 실측)**: *"required check 7종"* 도 틀렸다 — `GET /branches/main/protection` 실측 = `required_status_checks.contexts` **1종**(`Repo integrity guards (stdlib backstop)`) · `strict: false`. 즉 pip-audit·pytest·CodeQL·TruffleHog·Sonar·Codecov **전부 머지를 막지 못했다**. Grok claim-review `019fccbd` 가 이것을 observer-lie 로 지목했다(*"CI 전건 green" 이 머지 안전을 뜻하지 않는데 그렇게 읽힌다*). **사용자 결정(2026-08-04)으로 `pip-audit (SCA — 의존성 취약점 게이트)` 를 required 로 승격 → 현재 2종.** 집행 실증 = 승격 직후 `#1277`·`#1273` 이 즉시 `BLOCKED` 로 전환(이전에는 pip-audit red 여도 `MERGEABLE` 이었다). 🔴 **잔여**: 나머지 9 job 은 여전히 non-required — 확대 여부는 사용자 결정 영역(마찰 ↔ 집행력 트레이드오프). 또 `strict: false` 라 **base 가 앞서가도 재검증 없이 머지된다**(`#1273` 의 stale green 이 그 형태였다) (b) `docs/architecture.md:146-147` 최상위 `scripts/` 트리가 27종 중 4종 미등재(3종은 이번 창 신설)이고 **트리 싱크 가드는 `src/` 만 스캔**해 영원히 초록 (c) `CLAUDE.md §주의사항(rules 매트릭스)` 등 정책 detail 링크 **16건이 죽은 앵커** — 정책 17 외부화 설계가 의존하는 도달 경로가 끊겨 있다 (d) `#1272` 가 `docs/cycle-history.md` 를 **전장 CRLF 로 뒤집어** 2383삭제/2399추가 whole-file 재작성 → 서사 SSOT 의 diff 심의·blame 소멸(원인 = 산문에 박힌 lone CR 1바이트, `.gitattributes` 불변식 조용히 해제). **반증 수단**: (a) required check 목록 API 실측 대조 (b) `scripts/` 신규 파일 추가 시 가드 red (c) 앵커 도달성 링크 체커 (d) `git diff --stat` 이 whole-file 재작성이면 red  🔴 **2026-08-14 Grok 독립 재실측(`01a00092`) — PARTIALLY_FIXED**: **(a) 부분 해소** — required check 는 이제 **10종**(라이브 API 실측)이라 *"9 job 이 non-required"* 는 거짓이 됐다. 잔여 2축: `strict: false`(base 가 앞서도 stale green 으로 머지 가능) + 러너 docstring 이 여전히 *"repo-integrity 7종"* 인데 `_INTEGRITY` 튜플은 그보다 길다. 🔴 `ci.yml:605-607` 주석 *"required check 로 승격하지 않았다"* 는 **이제 거짓**(E2E 가 required 다). **(b) 악화** — `docs/architecture.md` scripts 펜스 등재 **25** vs 디스크 **34** → 미등재 **10건**(`check_claim_review_trace`·`check_conflict_markers`·`check_e2e_scope`·`check_main_red`·`check_precommit_installed`·`check_red_budget`·`check_reverse_mutation`·`check_test_count_sync`·`lint_js_ignore_baseline.json`·`pre_push_gate`). 원 서술 *"27 중 4"* 보다 **벌어졌다**. 원인: `check_architecture_tree_sync._SRC` 가 `src/` 만 본다. **(c) ✅ 해소** — CLAUDE.md 정책 fragment 13개가 `active.md`/`history.md` 앵커와 **13/13 일치**, DEAD 0. **(d) ✅ 해소** — `cycle-history.md` CR=0 · LF-only 717줄. ⚠️ **재발 가드는 없다**(CR-in-blob 검사 스크립트 0) |
| **R46** | ✅ 완료 (2026-08-15 — 사용자 승인 후 Grok 구현 + Claude 뮤테이션 검증) | 🔴 **제품·점수 노출면 3종 — 이번 회고에서 유일하게 사용자 리포 점수에 직접 닿는다** | **기전**: (a) `src/worker/pipeline.py:139` — **CLI 훅 분석의 무검증 45점이 대시보드 평균에 그대로 집계**된다(NULL-persist 근거가 비대칭 적용) (b) 점수-신뢰도 고지가 **9개 점수 노출면 중 2곳에만** 존재 — 닫힌 R21 행에 잔여가 흡수돼 승계 행·회귀 가드 **모두 0** (c) `src/notifier/telegram.py:103` — R21 의 *"차단 대신 가시화"* 가 9개 알림 채널 중 1곳에만 있어, **승인 버튼이 있는 Telegram 은 아무 경고도 보여주지 않는다**. **반증 수단**: (a) CLI 훅 분석 1건 후 대시보드 평균이 그 45점을 포함하는가 (b) 미커버 언어 리포의 Telegram 알림에 경고 문구가 나타나는가 🔴 **규모 실측 (2026-08-10, 운영 DB SELECT)** — 이 항목의 범위는 원 서술("CLI 훅 분석")보다 **훨씬 크다**: `repo_configs.ai_review_enabled` 가 **7 리포 중 6곳에서 false**(`KeyCloakSDK` 만 true)라, 그 6 리포의 모든 분석이 `ai_review_status='disabled'` + `"ai_defaults_applied": true` 로 **AI 배점 21점을 무검증 기본값으로 받는다**. 최근 14일 실측: `disabled` 643행 vs `success` 180행 — **78%의 점수가 AI 검증 없이 산출**되고 그대로 대시보드 평균에 들어간다(예: `SCAManager` push 238행 avg 78.4). 즉 (a)의 기전은 CLI 훅 경로 하나가 아니라 **리포 단위 설정으로 이미 다수 경로**다. 🔴 다만 **비활성 자체는 사용자 의도일 수 있다**(비용) — 고쳐야 할 것은 끄는 선택이 아니라 *끈 상태의 점수를 검증된 점수와 구분 없이 집계·표시하는 것*이다. **선행 확인**: 6 리포 비활성이 의도인지 사용자 회신  🔴🔴 **2026-08-14 Grok 독립 재실측(`01a00092`) — STILL_TRUE · 라이브 사용자 노출 점수 결함 확정**: 이 원장에서 **사용자 리포 점수에 직접 닿는 유일한 행**이고 3축 전부 살아 있다. **(a)** `src/api/hook.py:258-292` — CLI 훅이 `calculate_score([], …)` 로 정적 **45점 만점**(code_quality 25 + security 20)을 **score 컬럼에 그대로 저장**한다(`static_analysis_incomplete=True` 는 result dict 에만 찍히고 컬럼은 NULL 이 아니다). `_persisted_score_is_unreliable`(`pipeline.py:116-139`)는 **`ai_review_failed()` 만** 본다 — `source=='cli'` · `ai_defaults_applied` · `static_uncovered_languages` · `disabled` **전부 비대상**. 그 45점이 `dashboard_service._kpi_avg`(`:175-177`)와 `analytics_service`(`:60-65`)의 `func.avg(Analysis.score)` 에 **검증된 점수와 구분 없이** 들어간다. **(b)** 신뢰도 고지가 **9 노출면 중 2곳**뿐 — `github_comment.py:181-217` 정의 + `github_commit_comment.py` 재사용. discord·email·github_issue·n8n·slack·telegram·webhook 은 `uncovered=` False 이고 `analysis_detail.html` 은 `ai_defaults` 배너만 있다. **(c)** `telegram_gate.py:65-88` 은 점수·등급 + **Approve/Reject 인라인 버튼**을 띄우는데 파일 전체에 `uncovered`·`incomplete` **0 히트** — 미조달 언어 PR 이 45/45 '깨끗함' 으로 보이고 경고 없이 승인 버튼이 붙는다. `approve.py` 의 스킵 조건에 `uncovered_language` 가 없다(R21 이 차단→가시화로 내린 축). 🔴 **사용자가 실제로 보는 것 3가지**: ① 대시보드 KPI 평균에 CLI 무검증 45점이 섞인다 ② 리포에서 AI 리뷰를 끄면 매 분석이 기본 44점을 받고 평균·리더보드·카드에 검증된 점수처럼 표시된다 ③ 미커버 언어 PR 이 Telegram 승인 버튼 옆에서 경고 없이 '깨끗함' 으로 보인다. ⚠️ 운영 규모(*14일 disabled 643 vs success 180 = 78%*)는 **재측정 불가**(Supabase 미인증) — 2026-08-10 스냅샷으로 표기할 것. 🔴 **Grok 은 `src/` 를 손대지 않았다**(정책 15 High tier — 점수·집계 변경은 사전 확인 의무). **선행 확인 필요**: AI 비활성이 비용 의도인가? 집계에서 제외하면 운영 평균이 **내려간다**  🔴 **2026-08-15 해소 — 3축 전부**: 사용자가 정책 15 High tier 를 승인해 Grok 이 `src/` 를 구현했고 Claude 가 뮤테이션으로 검증했다. **Axis A(분류)** — `src/scorer/reliability.py` 신설. **flag 방식 채택**(NULL-persist 는 genuine AI 실패만 유지) — 역사 행 재작성 0 · 마이그레이션 0 · 상세 페이지는 점수를 계속 보여 준다. unreliable 조건 6종: AI 실패 · `static_analysis_incomplete` · **`source=='cli'`** · `ai_review_status ∈ {disabled,no_api_key,empty_diff}` · `ai_defaults_applied` · 미커버 언어. `ai_review_truncated` 단독은 **reliable 유지**(C22). **Axis B(집계)** — KPI 평균·추세·usage·weekly·moving average·repo-insight 에서 제외하고 `excluded_unreliable` 건수를 대시보드에 **인쇄**한다(숫자가 내려간 이유가 보인다). 실측: 90(PR) + 45(CLI) + 78(disabled) → 이전 평균 71 → **90.0 · 제외 2건**. 🔴 **Claude 가 `src/ui/routes/overview.py` 의 SQL AVG 를 추가로 통일했다** — Grok 이 남긴 그 자리는 리포 카드 평균에 무검증 점수를 계속 섞어 **같은 제품의 두 평균이 갈렸다**(Grok 이 §미완으로 자진 신고). `func.avg(Analysis.score)` 가 리포 어디에도 남지 못하게 하는 가드 신설 — 신뢰도는 `result` JSON 안에 있어 **SQL 로는 원리적으로 판정 불가**하므로 살아남은 SQL AVG 는 반드시 옛 정의를 쓴다. **Axis C(고지)** — `src/notifier/score_warnings.py` 신설, 점수 렌더 채널 **1/9 → 9/9**. Telegram 승인 버튼 위에 사유 줄 + *"Unverified or incomplete score — review carefully"* 가 붙는다. 레지스트리 순회 가드가 채널 누락을 막는다. 🔴 **뮤테이션 실증(Claude 환경 독립 실행)**: CLI 조건 제거 → red · `ai_defaults` 제거 → 2 failed · AI 상태 제거 → 5 failed · 미커버 제거 → red · overview 를 SQL AVG 로 되돌림 → red · slack 배선 제거 → red · telegram_gate 경고 제거 → red. 🔴 **Grok 초판은 뮤테이션이 0건이었고 그것이 실제로 구멍을 남겼다** — CLI 픽스처가 `static_analysis_incomplete` 를 함께 달아 **`source=='cli'` 분기를 지워도 21 passed** 였다. CLI 단독 대조군을 신설해 닫았다(사용자가 보는 결함의 본체가 무관측이었다). ⚠️ **잔여**: `src/api/hook.py` 는 공유 헬퍼를 쓰지 않는다(CLI 행은 incomplete 마커로 집계에서 빠지므로 동작은 같으나 규칙 문구와 배선이 어긋난다) · 주간 Telegram 알림은 제외 건수를 표시하지 않는다 |
| **R47** | 🟡 착수 가능 | **운영 검증 · smoke 3종 — 원장 intake 0건 + 런북 헤드라인 거짓** | **기전**: (a) owed 원장 intake 가 **172 PR 동안 0건** — 이번 창 두 PR 이 **스스로 "코드로 증명 불가"라 쓴** 운영 검증이 등재되지 않았고 SessionStart 훅은 *"미결 0건"* 초록을 인쇄한다(빈 원장 = green 이라는 R0-2 의 쌍대) (b) `docs/runbooks/operational-smoke-checks.md:171` §8 헤드라인이 거짓 — Railway 빌드는 테스트를 **0건** 실행하고, smoke 자동화 10건은 `TestClient` in-process 라 **런북을 만들게 한 그 사고를 원리적으로 못 본다** (c) `:205` smoke 매트릭스가 *"상호 보완"* 으로 제시한 e2e 열은 **어떤 워크플로도 실행하지 않는 스위트**라 manual 0회 × automation 미실행 = **양 열 공백인 행 5건**(R7 과 같은 뿌리). **반증 수단**: (a) "코드로 증명 불가" 어휘를 담은 PR 이 원장 미기재 시 red 인가 (b) in-process 가 아닌 실 HTTP smoke 1건이 배포본을 때리는가  🔴 **2026-08-14 Grok 독립 재실측(`01a00092`) — PARTIALLY_FIXED**: **(a) 부분 해소** — *"intake 0건"* 은 **stale 이 됐다**. `#1351`(2026-08-14)이 2행을 등재해 원장은 이제 20행 · ⏳ 9 · ✅ 6 · ⏭️ 4 · ❌ 1 이고 카운터도 미결 9건을 인쇄한다. 잔여: *"PR 본문에 '코드로 증명 불가' 를 쓴 PR 이 원장에 없으면 red"* 같은 **강제 가드는 여전히 없다**(축 B 는 촉구일 뿐). ⚠️ 그 가드는 산문 판정이라 이 리포가 이미 기각한 형태 — 축 B 유지가 정직하다는 것이 Grok 판단. **(b) STILL_TRUE** — `operational-smoke-checks.md:169-171` 이 *"CI / Railway 빌드 자동 실행 → 사고 즉시 발견"* 이라 적는데 `railway.toml` 에 **`pytest` 문자열이 0개**다(buildCommand=분석기 설치 · startCommand=uvicorn · preDeploy=alembic). Railway 를 CI 와 같은 집행면으로 읽히게 한다. **한 줄 정정으로 닫힌다**. **(c) ✅ 해소** — e2e job 이 `continue-on-error` 0 이고 `E2E (Playwright)` 가 required check 다 |
| **R48** | 🔴 결정 대기 | **6-step ② 가 "예외 없음" 이라 적힌 채 자기 면제됐다 — 규범을 유지할지 정식화할지 결정 필요** | **기전**: `CLAUDE.md §필수 원칙 완료 6-step` 이 *"인라인 cleanup·docs-only 예외 없음"* + *"예외 없음."* 을 **이중으로** 못박는데, 창의 마지막 PR `#1272` 가 정확히 그 면제를 자기 발급했다(*"전체 단위 스위트는 … 대기 생략 — CI pytest job 이 최종 근거 (docs-only 변경, 정책 3 자율 판단 보고)"*). **정책 3은 보고 의무이지 다른 정책의 금지를 해제하는 권한이 아니다.** 🔴 대조군이 예산 문제임을 확정 — 창 **22/23 PR 이 전체 스위트 실측을 남겼고**(#1259 6206 · #1262 6233 · #1264 6247 · #1267 6543 …) 무너진 것은 **토큰이 바닥난 마지막 PR 하나**다. 🔴 **관측자가 없다**: `.git/hooks/pre-push` 는 `pre_push_gate.py` 를 `--full` **없이** 부르고, `pytest tests/unit` 은 `--full` 분기(`:184`)에만 있다 → ② 는 습관으로만 집행되는 산문이다. **결정 필요**: (a) 예산 무관 의무 유지 + *"잔여 예산이 전체 스위트를 못 돌리면 PR 을 만들지 말고 세션을 종료한다"* 를 6-step 에 명시(면제가 아니라 **작업 중단**이 정답) / (b) docs-only 면제를 정식화하되 `pre_push_gate.py --full` 결과 첨부를 기계 조건으로 / (c) 현행 유지. **권장 = (a)+관측자 신설** — 지금처럼 "예외 없음" 이라 적고 예외를 만드는 상태가 최악이다(다음 세션이 어느 쪽을 믿을지 알 수 없다) 🔴 **2026-08-08 회고 권고 (사용자: "회고 결과와 함께 판단")**: **(a)·(b)·(c) 전부 기각하고 질문을 바꾼다.** *"전체 테스트를 돌렸는가"* 는 자기 신고라 기계가 진위를 못 잰다 — *"본문 숫자가 기계값에서 파생됐는가"* 로 바꾸면 잴 수 있다. 실측 근거: 창 18 PR 이 **전건** 본문에 전체 스위트 수치를 남겼고(예산 고갈 가설의 전제가 이 창에서는 재현되지 않았다) 그중 **4건이 기계값과 불일치**(`#1310` 6841+9=6850 vs STATE 6851 = −1 · `#1312` 6860+9=6869 vs STATE 6885 = −16). 즉 무너지는 축은 *실행 여부*가 아니라 *본문 수치의 정확성*이다. **구현**: `check_test_count_sync` 에 1축 추가 — 본문의 `pytest tests/unit → (\d+) passed / (\d+) skipped` 를 파싱해 `passed + skipped == collect(unit)` 대조. 새 스크립트·새 면제 마커 불필요(그 step 은 이미 `PR_BODY` 를 읽는다). **채택 기준**: `#1305`·`#1306`·`#1310`·`#1312` 4/4 소급 red. **유지**: 6-step ② 산문의 "예외 없음" — 면제 신설 금지. 🔴 **선행 조건 2건은 `#1315` 가 이미 닫았다**(이월 마커 PR↔push 비대칭 · `_EXEMPT`/`_DEFERRED` 3종 HTML 스트리핑) — 안 닫힌 상태로 새 축을 얹었으면 첫날 무력화됐다  🔴 **2026-08-14 Grok 독립 재실측(`01a00061`) — PARTIALLY_FIXED**: 2026-08-08 회고 권고(*"본문 수치가 기계값에서 파생됐는가로 질문을 바꾼다"*)는 **이미 구현·CI 배선됐다** — `check_test_count_sync.py:272-375`(`parse_body_claim`/`check_body_claim`) + `ci.yml:486-492` 가 `PR_BODY` 를 넘긴다. 라이브 실증: 불일치 rc=1 · 일치 rc=0 · **본문에 수치가 없으면 rc=0 이되 '미실행' 을 인쇄**(조용한 ✅ 아님). 관련 테스트 70 passed. 즉 *"관측자가 없다"* 는 원 서술은 **더 이상 참이 아니다**. 🔴 **남은 것은 정책 모순 하나**: 6-step ② 는 여전히 *"예외 없음"* 인데 로컬 `.git/hooks/pre-push` 는 `--full` 없이 부르고 `pytest tests/unit` 은 `--full` 분기에만 있다(`pre_push_gate.py:214-222`). 그리고 그 축은 **실행 여부가 아니라 신선도**를 잰다(R76 이 같은 지적). **결정**: (1) *"예외 없음"* 을 유지할지 (2) R76(`--junitxml`) 선행 후 실행 증거로 올릴지. 면제 신설은 금지 |

| **R49** | ✅ 완료 (`#1279` — 잔여 3축 아래 명시) | 🔴 **[P0] 심의 게이트가 한글을 mojibake 로 읽고 자기 손상을 근거로 차단** — 🔴 **잔여**: 🔴 **잔여 3축** — (a) 게이트가 **비구체적 사유로 차단**한다(무엇을 고칠지 알 수 없는 block 3회 연속) (b) **prepend 편집을 교체로 오독** — 심의자 2/3 가 섹션 앞 삽입을 '기존 규칙 삭제' 로 판정 (c) R38 비용이 이 결함으로 증폭(한 파일 편집에 게이트 5회 호출) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r49) |  |
| **R50** | ✅ 완료 (2026-08-04 — 리포 변경 없음, 로컬 정리) | **`.claude/worktrees/` 에 에이전트 worktree 7개 잔존 — 전 리포 grep 이 8배로 오염** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r50) | **기전**: `git worktree list` 실측 7건(`agent-a0d0eb…` 등, 구 커밋 `d58de9f`·`5556601`·`0220bc0`에 고정). `git status` 는 깨끗하지만(무시 대상) **`grep -rn` 이 같은 심볼을 8번 반환**한다 — 이번 세션의 `from cryptography` 전수 조사가 실제로 그랬고, 인용 실측(정책 6)을 매번 수동 필터링해야 한다. 방치 시 디스크·검색 비용이 계속 는다. **반증 수단**: 각 worktree 에 미머지 커밋이 있는지 `git log <branch> --not main --oneline` 로 확인 후 `git worktree remove` — 제거 뒤 `grep -rn` 결과 수가 1/8 로 떨어지는가 |

| **R51** | ✅ 완료 (`#1286` 훅 + `#1289` 서비스 3경로) | **Anthropic 경로가 free-form JSON 을 파싱한다 — 스키마 drift 방어가 영구 필요해진 이유** — 🔴 **잔여**: **잔여 해소**(`#1289`) — `ai_review.py`·`dashboard_service.py`·`repo_insight_service.py` 3경로 전건 `output_config.format` 배선 완료(사용자 사전 확인 후) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r51) |  |

| **R52** | ✅ 완료 (30 → **0**) — CSP 앱 버그까지 해소 (`#1294`) | ~~**e2e 30건이 Linux CI 에서 실패한다 — 로컬 Windows 에서만 초록이던 스위트**~~ → **e2e 스위트가 앱과 14개월 drift — 플랫폼 무관하게 30건 실패** — 🔴 **잔여**: 🔴 **잔여 1건 — `test_dashboard_no_js_runtime_errors` 는 실제 앱 버그다(테스트를 고치지 않았다)**: `src/main.py` 가 주입하는 CSP 가 `base.html` 의 외부 폰트를 차단해 앱이 자기 폰트를 자기 CSP 로 막는다. 수정 방향이 **보안 완화 ↔ 시각 변경**으로 갈려 정책 15 High tier | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r52) |  |

| **R53** | 🟡 착수 가능 (R52 후속 — Grok `019fce` 미반증 축) | **통과 91건은 감사된 적이 없다 — 빨강만 고치고 초록은 믿었다** | **기전**: R52 는 실패 30건만 다뤘다. 그런데 그 30건을 진단하다 **찾아본 곳마다** 공허(vacuous)한 초록이 나왔다: `test_six_card_titles_present` 는 5개 단언 중 4개가 렌더된 제목이 아니라 **한국어 HTML 주석**에 매칭돼 통과했고(제목을 다 지워도 초록), `test_login_ttfb`/`test_login_load` 는 **github.com 의 응답 속도**를 재고 있었고, `test_navigation.py` 13건은 `assert "SCAManager" in content`(nav 로고) 하나로 *"빈 상태 메시지"* 를 검증한다고 적혀 있었다. 내가 R52 에서 넣은 새 가드조차 **2개가 공허**해 Grok claim-review 에 적발됐다(`label[for=]` 부재로 항상 통과 · OTP 앱 상수 자기참조). 🔴 **비율을 모른다는 것이 핵심**: 무작위 표본이 아니라 **손댄 곳마다** 나왔으므로 나머지 84건의 기저율은 미지수다. 빨강 30건은 관측자가 살아 있다는 증거였지만 **초록 91건에는 그런 증거가 없다**. **반증 수단**: 초록 테스트에서 핵심 단언 1줄을 지우거나 앱 쪽 대상을 제거해도 여전히 통과하는가(뮤테이션). 통과율이 높을수록 스위트 신뢰도가 낮다는 뜻이다. **범위 후보**: `test_navigation.py` 13 · `page.content()` 부분문자열 매칭 전수 · `not_to_be_visible`(요소 부재도 통과) · `status < 500` 류  🔴 **2026-08-14 Grok 독립 재실측(`01a00099`) — STILL_TRUE, 공허 e2e 를 뮤테이션으로 실증**: 스위트는 여전히 **121건**(모수 불변). 표본 5건 실행 후 **실경로 뮤테이션**: `src/templates/overview.html` 의 `.get-started-tutorial`(리포 0개 빈 상태)을 제거·숨겼는데 `test_overview_empty_state` 가 **1 passed** — 그 테스트의 단언이 `"SCAManager" in page.content()` 뿐이기 때문이다. **대조군**: `base.html` 의 `nav-logo` 클래스를 제거하니 `test_nav_logo_is_visible` 이 **FAILED** — 즉 뮤테이션 자체는 유효했고 공허한 것은 그 테스트다(둘 다 복원 확인). ✅ 원 지적 3례 중 2례는 시정됐다 — `test_six_card_titles_present` 는 이제 `.hdr-title` textContent 를 보고, `test_login_ttfb`/`test_login_load`(남의 사이트를 재던 것)는 삭제되고 `test_root_*` 로 교체됐다. `"SCAManager" in content` 는 13건 중 **2곳만** 남았다. 🔴 **그러나 클래스 주장은 그대로다** — 116/121 은 이번에도 뮤테이션하지 않았고 전수 감사 산출물이 없다 |

| **R54** | ✅ 완료 (`#1293` — 문서 감사 P0~P2) | **문서 설계 감사: 총량이 아니라 형상이 문제였다** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r54) |  |

| **R55** | 🟡 착수 가능 (R54 잔여 — Grok `df5ed11d` 미이행) | **`cycle-history.md` dangling 참조 22종이 영구 사각이다** | **기전**: `check_memory_refs` 범위를 3→34파일로 넓히며 `docs/_archive/**` 와 `docs/cycle-history.md` 를 제외했다. 아카이브 제외는 정당하다(그 시점의 사실 기록 — 재작성 금지). 그러나 `cycle-history.md` 는 **매 세션 append 되는 활성 문서**라 Grok 이 *over-exclusion* 이라 지적했다(dangling **41회 / unique 22종**). 제외를 정당화하려면 (a) 그 문서가 행동 지시 문서가 아니고 (b) 그 dangling 이 미래 세션 행동을 바꾸지 않아야 하는데, **(a)(b) 모두 약하다**. 🔴 **그런데 단순 포함도 답이 아니다**: 22종 중 상당수가 **구 PC 슬러그 유실**로 복원 불가라, red 로 만들면 참조 삭제 = **지식 손실이 확정**된다(메모리 6건 복원 사례). **선택지**: (a) 포함 + 22종을 '유실됨' 으로 명시 표기 (b) 신규 항목만 검사(날짜 경계) (c) advisory 로 계량만 (d) 현행 유지. **반증 수단**: 22종 중 실제로 복원 가능한 것이 몇 종인가 — 그 수가 크면 (a), 0에 가까우면 (b)/(c) 🔴 **신규 증거 (2026-08-06 세션17)**: dangling 은 메모리 슬러그만이 아니다 — **닫힌 PR 을 구현으로 인용**하는 형태가 있다. `#1295` 는 **머지되지 않고 CLOSED** 됐고 실제 머지본은 `#1296` 인데, `cycle-history.md`(TOC·헤딩·본문) · `STATE.md`(헤드·추적 이력) · `runbooks/owed-verification.md` · `backlog.md`(R60 완료 표기) **4 문서가 `#1295` 를 구현으로 가리키고 있었다**. 원장이 *존재하지 않는 이행*을 가리키는 형태라 메모리 슬러그보다 해롭다(owed 원장은 그 PR 의 운영 검증을 ⏳ 로 추적 중이었다). **관측자 0** — 이번에도 사람이 눈으로 찾았다. 🔴 이 축은 (a)~(d) 어느 선택지와도 무관하게 닫을 수 있다: `#NNNN` 인용의 **PR state 대조**(`gh pr view --json state`)는 복원 불가 문제가 없고 API 한 번이면 판정된다. 후보 (e) 로 추가.  🔴 **2026-08-14 Grok 독립 재실측(`01a00099`) — STILL_TRUE, 위치 정정**: 🔴 *"22종이 live `cycle-history.md` 에 있다"* 는 **2026-08-13 압축 이후 stale** 이다. 재집계 결과: **아카이브(`_archive/cycle-history-folded-2026-08.md`, 390,668B) 에 dangling unique 22** · **live 파일에는 3**(`project-grok-full-review-2026-07-17` · `project-logging-wipe-token-leak-2026-07-19` · `project-premium-readiness-audit-2026-07-18`). 이 머신의 memory 디렉토리 **17곳 전수 대조로 복원 가능 0/22**. 🔴 **둘 다 체커 범위 밖**: `check_memory_refs._DOC_LITERALS`/`_DOC_GLOBS` 에 cycle-history 가 없고 `test_scan_scope_excludes_point_in_time_records` 가 **그 제외를 고정**한다. `check_toc_anchors` 는 목차 앵커만 보고 슬러그를 안 본다 — 다른 축이라 이 사각을 닫지 못한다. ⚠️ 복원 가능이 0이므로 *"체커 범위에 넣고 red"* 안은 **지식 손실을 강요**한다(선택지 판단 입력) |

| **R56** | ✅ 완료 (`#1303` Phase 0+1 — 사용자 결정 '설치'. 잔여 = R67) | **pre-commit 계층이 이 머신에 전면 부재 — 시크릿 훅 포함 13 훅이 19 PR 내내 0회 실행** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r56) |  |

| **R57** | ✅ 완료 (`#1305` — Grok `019fd786` WEAKENED 지적 3건 이행. 잔여 = 프록시 한계 명시) | **`claim-review-not-required` 자기발급 면제가 최근 11 PR 중 6건 — 정책 19 default 를 가드 트리거로 대체했다** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r57) |  |

| **R58** | ✅ 완료 (`#1298` — Grok `71bd2d6c` BROKEN 지적 이행) | **e2e 초록이 공허할 수 있다 — `live_server` 실패 시 121건 전건 skip 후 job exit 0** — 🔴 **잔여**: **잔여**: e2e job 은 여전히 **required check 가 아니라** red 가 머지를 막지 않는다(R64) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r58) |  |

| **R59** | 🟡 착수 가능 (P1) | **`retro_scope.py` 가 정책 8-(5) 의 절반만 구현 — 미머지 세션 산출물이 구조적으로 비가시** | **기전**: 정책 8 진화 (5)는 회고 범위를 *"직전 회고 이후 머지 PR + **본 세션 산출물 전체**"* 로 규정하는데, `retro_scope.py` 는 **머지 PR 만** 열거한다. 이번 회고에서 `#1294`·`#1295`(당시 미머지 — 최종 머지본은 `#1296`. 이 창에서 행동 표면을 가장 크게 바꾼 변경)가 범위에서 빠졌다. 🔴 그리고 `retrospective.mjs` 는 그 스크립트를 **아예 호출하지 않는다**(2026-07-26 회고가 이미 적발·미시정). **반증 수단**: 미머지 브랜치 산출물을 열거하도록 고친 뒤 이번 창을 재실행하면 새 발견이 나오는가  🔴 **2026-08-14 Grok 독립 재실측(`01a00099`) — PARTIALLY_FIXED, 두 조각으로 갈렸다**: **(1) 배선 축 ✅ 해소** — `retrospective.mjs` 가 Scope 에서 오라클을 실행하고 Report 에서 다시 잰다(`scope_drift_during_run`, 2026-08-14). **(2) 세션 산출물 축 STILL_TRUE** — 실증: 미커밋 파일을 만들고 트리를 dirty 로 만든 뒤 `--json` 을 다시 돌렸는데 **바이트 단위로 동일**했다(`prs` 불변). `compute()` 는 `merged_prs(boundary)` 만 담고 `session_commits`·dirty·worktree 키가 **0개**이며 소스에 `git status` 문자열조차 없다. `SCOPE_SCHEMA` 의 required 도 `ok·pr_count·prs·head·raw` 뿐이다. 🔴 종료 재측정이 잡는 것은 *"회고가 도는 동안 머지된 PR"*(`#1347` 클래스)뿐 — **미커밋·미머지·PR 번호 없는 커밋은 두 번 재도 안 보인다**. ⚠️ 미구현 사유: *"세션 산출물"* 의 정의(dirty 트리 / `main..HEAD` / open PR / 번호 없는 커밋)가 **사용자 결정** |

| **R60** | ✅ 완료 (`#1296` — 이번 창 이행) | **6-step ⑤ 의 '서사 축' 이 4 세션 방치 — 수치는 5회 동기화, 이야기는 0회** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r60) |  |

| **R61** | ✅ 완료 (`#1297` — Grok `6580850b` BROKEN 지적 이행) | **Anthropic 응답을 4곳 모두 `content[0].text` 로 인덱싱 — 첫 블록이 text 가 아니면 전부 조용한 `api_error`** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r61) |  |

| **R62** | 🟡 착수 가능 (R60 잔여) | **6-step ⑤ 서사 축에 관측자가 0개다 — 이행은 매번 사람 기억에 의존** | **기전**: R60 을 이번 창에서 손으로 이행했으나 **기계는 여전히 아무것도 보지 않는다**. 수치 축은 `check_docs_sync`(3층 fail-closed)가 강제하는 반면 서사 축은 `check_toc_anchors`(앵커만)·`check_memory_refs`(명시 제외) 뿐이라, 다음 세션이 또 빠뜨려도 초록이다 — 실제로 **4 세션 연속** 그랬다. **처방 후보**: (a) `STATE:최신 블록이 인용한 최대 PR 번호 < 머지된 최신 PR 번호 - N` 이면 advisory loud (b) 세션 종료 trailing sync PR 템플릿에 체크 항목 고정. 🔴 **차단이 아니라 advisory** — 서사는 기계가 품질을 판정할 수 없고, 차단으로 걸면 형식만 채운 가짜 서사를 부른다(정책 17). **반증 수단**: 가드 도입 후 한 세션을 일부러 빠뜨렸을 때 경고가 뜨는가  🔴 **2026-08-14 Grok 독립 재실측(`01a000a3`) — STILL_TRUE**: 구성 실험 — 테스트 파일만 추가하고 cycle-history 를 **안 건드린** 상태에서 `check_toc_anchors`·`check_docs_sync`·ledger headline 테스트를 돌리니 **전부 EXIT 0**. 서사 축 관측자가 0인 것이 재확인됐다. `scripts/` 전수에서 cycle-history 를 언급하는 실행 스크립트는 3개뿐이고 그중 `check_memory_refs:29` 는 **의도적 제외**, `check_claim_review_trace:78` 은 주석 인용, `check_toc_anchors` 는 앵커만 본다. `check_docs_sync`·`check_test_count_sync`·`pre_push_gate` 에는 문자열 **0건**. 기존 `test_cycle_history_headline_pr_count_matches_its_range` 는 **이미 있는 헤드라인의 내부 산술**만 보므로 엔트리를 안 넣으면 그대로 통과한다. ⚠️ 처방은 **advisory 한정**이어야 한다(차단 금지 — 정책 17) |

| **R63** | ✅ 완료 (`#1299` — Grok `32b9a2f9` 2라운드) | **성공 로그가 응답 파싱보다 앞서서, 추출 실패 시 success 와 error 가 둘 다 기록된다** — 🔴 **잔여**: **잔여 = R65** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r63) |  |

| **R64** | ✅ 완료 (2026-08-06 — 사용자 결정 '승격'. required 9→10 라이브 적용) | **e2e job 이 required check 가 아니라 red 여도 머지를 막지 않는다** — 🔴 **잔여**: **잔여 = R68** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r64) |  |

| **R65** | ✅ 완료 (`#1300` — Grok `019fd6e8` BROKEN 지적 이행. 잔여 = R66·백필 미수행) | **비용 로그의 error 경로가 토큰을 0 으로 덮어 비용이 과소 계상된다 + 과거 이중 행은 그대로 남는다** — 🔴 **잔여**: 🔴 **잔여 2**: (a) **백필 없음** — 과거 이중 행·0 토큰 행은 그대로다(신규 기록 규약일 뿐) (b) **R66**(SDK 재시도분 비가시) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r65) |  |
| **R68** | ✅ 완료 (`#1314` — 사용자 확인 '**auto-merge 를 실제로 운용한다**' → P1 확정 후 이행) | **required check 미충족(`blocked`)이 auto-merge 에서 종결 실패라, 체크가 도는 동안 시도된 PR 은 영구 포기된다** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r68) |  |
| **R67** | ✅ 완료 (2026-08-15 — 사용자 승인 · Grok 저술 + Claude 실훅 계측) | **훅 명령 7종이 상대 경로라, 셸 cwd 가 리포를 벗어나면 세션이 자력으로 못 빠져나온다** | **기전**: `.claude/settings.json` 의 훅 명령이 전부 `$PY .claude/hooks/X.py` · `$PY scripts/X.py` 형태다(`:9 :14 :19 :24 :36 :46 :51 :63` — **8종**, `grep -n '"command"'` 실측 2026-08-08). 🔴 초판은 *"`:9 :14 :19 :31 :41 :46` 등 7종"* 이라 적었는데 `:31`·`:41` 은 `{` 한 글자다 — P3(`#1311`)가 `:19` 에 훅을 끼워 넣어 이후 줄이 전부 밀렸고 인용이 따라오지 않았다(정책 6 drift 실사고 — 회고 N-P1-7). Claude Code 는 훅을 **셸의 현재 cwd** 에서 실행하므로, Bash 호출 중 `cd` 로 리포 밖에 나간 상태가 남으면 훅이 파일을 못 찾아 **에러 → Bash·Write·Edit 전 호출 차단**된다. 🔴 **자력 복구 불가**: cwd 를 되돌리려면 `cd` 가 필요한데 그 `cd` 자체가 Bash 라 같은 훅에 먼저 걸린다. 서브에이전트 위임도 **같은 cwd 를 상속**해 동일하게 막힌다(실측). **라이브 실측 (2026-08-06 세션17)**: R56 조사 중 임시 리포(`f:/tmp/pcgen`)에서 pre-commit 훅 생성물을 관측하다 cwd 가 남아 **세션이 두 번 잠겼다**. 탈출 경로는 `Monitor` 툴(별도 셸·훅 미적용)로 훅 사본을 그 경로에 만들어 넣는 것뿐이었다 — 우연히 찾은 경로이고 설계된 복구 수단이 아니다. ⚠️ **fail-closed 라 안전 축은 무사하다** — 아무것도 통과시키지 않고 전부 막는다. 가용성 문제이지 보호 구멍이 아니다. **처방 후보**: `$CLAUDE_PROJECT_DIR/.claude/hooks/X.py` 로 절대화. 🔴 **그런데 이 변경은 이 리포에서 가장 위험한 편집면이다** — `#1243` 이 같은 파일의 command 6종을 재작성했을 때 배선 가드가 그 재작성과 `echo` 를 구별하지 못했다(guards.md §배선 술어). `_wiring_shape` 술어는 **allowlist 밖 호출 형태를 fail-closed 로 거부**하므로 새 형태가 실배선을 거부당할 수 있고, `test_hook_interpreter_liveness.py` 는 명령을 **실제 실행**하므로 `CLAUDE_PROJECT_DIR` 미설정 환경에서 프로브가 깨질 수 있다. **반증 수단**: (a) 훅 실행 시 `CLAUDE_PROJECT_DIR` 이 실제로 설정되는가 — 훅 안에서 덤프해 실측 (b) 절대화 후 `_wiring_shape`·liveness 가드가 여전히 green 인가 (c) cwd 를 리포 밖으로 옮긴 뒤 Bash 가 정상 동작하는가. **결정 필요**: 지금 고칠 것인가(위험 편집면), 아니면 '리포 밖으로 cd 하지 않는다' 는 운영 규율로 둘 것인가  🔴 **2026-08-14 Grok 독립 재실측(`01a00061`) — STILL_TRUE, 수치 정정**: 훅 명령은 **7종이 아니라 9종**이다(이 세션이 `check_open_decisions.py` 를 추가해 늘었다). 전부 `$PY scripts/…` · `$PY .claude/hooks/…` 상대 경로이고 `CLAUDE_PROJECT_DIR` 은 `settings.json` 에 **한 번도 나오지 않는다**. **실측**: 리포 밖 임시 디렉토리에서 `py -3 scripts/check_retro_cadence.py` → `can't open file …` **exit 2**. Claude Code 는 PreToolUse 훅의 비정상 종료를 **차단**으로 취급하므로 세션이 자력 복구 불가 상태가 된다 (메모리 `feedback-hook-relative-path-locks-session` 이 기록한 실사고). 🔴 **결정 대기 5건 중 유일한 운영 사고 차단 축**이며, 이 세션이 9번째 훅을 더해 표면이 넓어졌다   🔴 **2026-08-15 해소 (`#1354` · 사용자 승인 · Grok 저술)**: 9종 전부 `${CLAUDE_PROJECT_DIR:-.}/` 로 바꿨다 — bare `$CLAUDE_PROJECT_DIR/` 는 변수가 비면 `/scripts/…` 가 되어 **오늘보다 나쁘다**. **반증 수단 (a) 는 실훅 계측으로만 닫혔다**: 훅 런타임의 변수 존재는 Claude Code 세션만 잴 수 있어 Grok 이 원리적으로 못 본 축이었고, PostToolUse 훅에 임시 덤프를 심어 `CLAUDE_PROJECT_DIR='f:/DEVELOPMENT/SOURCE/CLAUDE/SCAManager'` 를 읽었다(**이 머신 1회** 관측 — 그래서 폴백을 남긴다). 🔴 **가드가 옳은 수정을 거부했다**: `_wiring_shape._mentions_path` 의 `str.lstrip("./")` 가 `.claude` 의 선행 점까지 먹어 실배선을 미배선으로 오판했다(되돌리면 실배선 단언 3건 red). 🔴 **초판 계약 테스트는 claim-review 가 BROKEN 으로 깼다**(`01a00214`) — 스키마를 나열해 걷는 방식이라 flat `command` · `args` 배열 · `tools/` 경로 · `scripts/../../../` 탈출 4형태가 전부 GREEN 이었다. JSON 전체 재귀 스캔으로 재작성해 4형태 전건 red. ⚠️ **봉인 아님**: 변수가 없고 cwd 도 리포 밖이면 여전히 exit 2 · 변수가 다른 트리를 가리키면 훅이 그 트리에서 돈다 · 명령이 인용돼 있지 않아 경로에 공백이 있으면 워드스플릿된다(인용은 liveness 의 `interpreter_prefix` 를 깨서 **측정 후 기각**). 잔여는 새 항목을 만들지 않고 여기 남긴다 ||
| **R66** | 🟡 착수 가능 (R65 잔여 — Grok `019fd6e8`) | **SDK 내부 재시도분의 토큰이 원리적으로 비가시 — 비용 원장은 항상 '마지막 시도' 만 안다** | **기전**: 3경로 모두 `anthropic.AsyncAnthropic(..., max_retries=2)` 다(`ai_review.py:129` · `dashboard_service.py:1028` · `repo_insight_service.py:429`). SDK 가 내부에서 재시도하면 우리가 보는 `response.usage` 는 **마지막 시도 1건**의 값이다 — 그 앞 시도들의 토큰은 우리 원장에 존재하지 않는다. ⚠️ **과소인지는 단정할 수 없다**: 재시도 트리거는 5xx·connection error·timeout 이라(`ai_review.py:122~128` 주석) 그 시도들이 과금됐는지 우리 쪽에서 알 방법이 없다. 즉 R66 은 *"틀렸다"* 가 아니라 **"모른다"** 를 원장이 "안다"처럼 보이게 하는 축이다. 같은 성격의 잔여 2건: (a) `cost_usd` 는 `estimate_claude_cost_usd` 추정치(청구서 아님) (b) `repo_insight` 는 cache 토큰 필드를 아예 안 넘긴다 — 지금은 그 경로에 prompt caching 이 없어(`build_cached_system_param` 미사용) 실해 없지만, 캐싱을 도입하면 즉시 구멍이 된다. **반증 수단**: 재시도를 강제(429/5xx 목)했을 때 기록된 토큰이 시도 횟수와 무관하게 1회분인가 · Anthropic 이 실패 시도를 과금하는지 공식 문서/청구서로 확인 가능한가  🔴 **2026-08-14 Grok 독립 재실측(`01a000a3`, 라이브 API 미호출 — 정적 + SDK 소스) — STILL_TRUE**: 3경로 모두 `AsyncAnthropic(timeout=60.0, max_retries=2)` 이고(`ai_review.py:129` · `dashboard_service.py:1028` · `repo_insight_service.py:429`) 기록은 `response.usage` → `log_claude_api_call` **1행**뿐이다. `with_raw_response`·`retries_taken` 참조는 `src/` 에 **0건**. 🔴 **SDK 한계가 실재한다**: `Usage` 모델에 시도별 배열이 없고(`input/output/cache_*/server_tool_use/service_tier`), `Message` 에 `retries_taken` 필드가 없다. `_base_client` 재시도 루프는 5xx/timeout 시 `err.response.close()` 후 **body 를 버린다** — httpx 훅으로도 선행 시도 usage 를 못 모은다. `APIResponse.retries_taken` 은 **횟수**만 주고 토큰은 없다. 핀 0.120.2 · 로컬 0.94.0 **동일 한계**. 잔여 2건: (a) `cost_usd` 는 `estimate_claude_cost_usd` **추정치** (b) `repo_insight_service` 가 cache 필드를 `log_claude_api_call` 에 안 넘긴다(`:428`·`:467`). ⚠️ 실패 시도가 **과금되는지**는 청구서 없이 알 수 없다 — 관측 불능만 확정됐다 |

| **R69** | 🟡 착수 가능 | **P1 역-뮤테이션 게이트가 신규 파일 전용 PR 을 원리적으로 통과시킨다** | **기전**: `check_reverse_mutation.py` 는 base 에 없는 파일을 `git rm -f` 로 되돌린다 → import 가 **반드시** 죽는다 → 구조적으로 항상 red → 항상 pass. 등급이 `collection` 이어도 `✅ 테스트가 변경을 관측한다` 를 인쇄한다. 🔴 **이 창의 `#1310`·`#1311`·`#1312`(P1·P3·P4 자신)가 정확히 이 경로로 들어왔다** — 즉 게이트를 도입한 PR 들이 그 게이트의 실질 판정을 한 번도 받지 않았다. 소급 13건 중 **6건(46%)이 `collection` 등급**이었고 전부 `ImportError`/`ModuleNotFoundError` 였다. **반증 수단**: 되돌림을 `git rm` 대신 **빈 파일로 치환**하도록 바꿔 같은 PR 을 재판정했을 때 red 가 사라지면 이 축이 확인된다. 또는 `collection` 등급을 통과로 세지 않게 하고 소급 18건을 재실행해 오탐률을 잰다  🔴 **2026-08-14 Grok 독립 재실측(`01a000a3`) — STILL_TRUE, 실브랜치 구성으로 확정**: base 위에 `src/shared/_r69_verify_probe.py` + 그것을 import 하고 `assert probe_value() == 42` 하는 테스트를 **실제로 커밋**하고 게이트를 돌렸다 → **`GATE_EXIT=0`**, 등급 `collection`, 출력은 *"✅ 역-뮤테이션 red — 이 PR 의 테스트가 변경을 관측한다"*. 기전: 신규 파일은 base 에 없어 `git rm` 되고, 그러면 되돌림이 항상 `ImportError` 라 **단언 내용은 한 줄도 안 돈다**. 게이트는 그것을 통과로 기록한다(`:226-234`). 기존 단위 테스트에 **신규파일-only 케이스가 없다**. 🔴 **R91 과 같은 게이트지만 뿌리가 다르다** — R69 는 `prod>0` → collection red 를 통과로 셈, R91 은 `prod=0` → *"생산 표면 변경 0건"* 으로 **판정 자체를 안 함**. 두 항목을 합치면 안 된다. ⚠️ 처방 후보 2개(`git rm` 대신 빈 파일 치환 / `collection` 을 통과로 안 셈)는 사용자 결정 |
| **R70** | 🟡 착수 가능 | **P3 main red 관측자가 `failure` 아닌 실패 결론을 전부 "초록" 으로 인쇄한다 + 무문서 킬스위치** | **기전**: (a) `check_main_red.py` 의 red 판정이 `conclusion == "failure"` 단일 비교라 `timed_out`·`startup_failure`·`cancelled`·`action_required` 가 전부 "초록" 으로 보고된다 — 🔴 `startup_failure` 는 **워크플로 YAML 파손**의 결론값이다(관측자가 가장 필요한 순간에 침묵). (b) 중간의 `cancelled` 1건이 지속시간 역추적을 끊어 red 지속시간이 붕괴한다(실측 시나리오: 실제 60h → 보고 1.0h). 이 스크립트의 **존재 이유가 "몇 시간 만에 고쳤는가"** 인데 그 값이 틀린다. (c) `SKIP_MAIN_RED_CHECK` 는 설정 시 **출력 0줄 · exit 0** 인데 `docs/reference/env-vars.md` 미등재 · 테스트 0건 — 관측자를 조용히 끄는 스위치가 무문서다. ⚠️ 발현 base rate 는 현재 0(최근 60 run 이 `failure`/`success` 뿐). **반증 수단**: `red_span()` 에 각 conclusion 을 넣어 `is_red` 가 True 면 (a)는 틀렸다 · `cancelled` 를 중간에 끼운 시퀀스로 지속시간을 재면 (b) 확인  🔴 **2026-08-14 Grok 독립 검증 + 수정(`01a000aa`) — 이 창에서 해소**: 합성 payload 로 결론별 판정을 전수 실측했더니 **`failure` 를 뺀 전 결론이 *"최신 run 초록"* 으로 인쇄**됐다 — `cancelled`·`timed_out`·`action_required`·`startup_failure`·`stale`·`neutral`·`skipped` 전부. 🔴 특히 `startup_failure`(워크플로 YAML 자체가 깨진 경우)와 `timed_out` 이 **무음**이었다. 그리고 walk 에서 `cancelled` 를 *복구*로 취급해 red 구간을 **60시간 → 1.0시간으로 잘라 먹었다**(이 스크립트가 존재하는 이유가 그 구간 보고인데). 킬스위치 `SKIP_MAIN_RED_CHECK=1` 은 **stdout 0바이트 + exit 0** 이었고 `env-vars.md` 에 `SKIP_*` 항목이 **0개**, 관련 테스트도 0건이었다 — 초록과 구별 불가. **수정**: red 집합을 리터럴 `("failure","timed_out","startup_failure")` 로, `cancelled` 는 구간을 안 끊게, 나머지 비-success 는 *"초록이 아니다"* 로 인쇄, 킬스위치는 loud + 문서 등재. **뮤테이션 3/3 red 를 Claude 환경에서 독립 재현**(red 집합 축소 → 2 failed · 구간 끊기 복원 → 1 failed · env-vars 행 삭제 → 1 failed). 🔴 Grok 자신이 M1 초판을 **A4 자기참조 공허화**로 잡아 기대값을 리터럴 `_EXPECT_RED` 로 고쳤다. ⚠️ **잔여**: `cancelled`·`action_required`·`stale`·`neutral` 이 **최신** 결론일 때는 red 가 아니라 *"초록이 아님"* 까지만이다(제품 결정) |
| **R71** | 🟡 착수 가능 | **P4 🔴 예산제가 저장소 🔴 의 26.0% 만 잰다 — 나머지는 무료로 늘어난다** | **기전**: `check_red_budget.py` 의 `SURFACE_GLOBS` 는 `CLAUDE.md`·`AGENTS.md`·`.claude/rules/*.md`·`.claude/policies/*.md` 뿐이다. 실측: 표면 🔴 라인 **290** / 전체 tracked `.md` **1115** → **표면 밖 825건(74.0%)**. 즉 새 무집행 🔴 규칙을 `docs/runbooks/` 에 쓰면 예산에 잡히지 않는다. 부수 우회: 🔴 을 **한 줄에 몰아 쓰면** 블록 1건으로 세고(다섯 줄에 쓰면 5건), `.claude/rules/<하위디렉토리>/` 로 옮기면 glob 이 non-recursive 라 예산에서 **영구 이탈**한다. **반증 수단**: `rule_blocks` 에 한 줄 다중 🔴 을 넣어 블록 수가 🔴 개수와 같으면 첫 우회는 틀렸다 · `.claude/rules/sub/x.md` 를 만들어 baseline 이 변하지 않으면 이탈 축 확인  🔴 **2026-08-14 갱신 (R90 통합)**: 표면을 **4 → 6개**로 넓혀 `docs/process/*.md`·`.claude/traps.md` 를 편입했다(회고 P0-A — 그 두 층은 이 리포가 *지시문으로 저술한* 층인데 계수 밖이었고, 그 안에 무집행 27건이 있었다). **잔여**: 활성 문서 전수 기준 계수 밖이 여전히 **약 475건**(`docs/backlog.md` 92 · `docs/STATE.md` 41 · `docs/runbooks/owed-verification.md` 35 …). 🔴 **단순 확대는 답이 아니다** — 원장·시점 기록을 편입하면 이력 한 줄이 규칙으로 둔갑한다. **권장**: `doc_review_gate._IMPORTANT` ∖ `SURFACE_GLOBS` 차집합이 **CLAUDE.md 한계 고지에 열거돼 있는지** 대조하는 테스트 — 범위를 넓히는 대신 **정직하게 고정**한다. **반증 수단**: 그 차집합이 빈 집합이면 이 항목은 거짓이다  🔴 **2026-08-14 Grok 독립 재실측(`01a000c8`) — STILL_TRUE, 수치 정정**: 확대(4→6 표면) 후 tracked `.md` 159개를 `check_red_budget` **자신의 함수**(`_strip_code`+`_RED_MARKERS`)로 재고했다(아카이브 제외). **계측 표면 안 101줄 · 표면 밖 517 마커 줄(그중 🔴 498줄)**. 즉 앞선 *"약 475"* 는 근사치였고 실측은 **498** 이다. 활성 문서 분모의 **약 83%** 가 여전히 무료다. 🔴 파일별 수치도 정정한다 — `backlog.md` 97줄(159 출현) · `STATE.md` 44줄 · **`cycle-history.md` 88줄(3위인데 이 행의 예시에서 빠져 있었다)** · `owed-verification.md` 35줄. ⚠️ 단순 확대 금지 주의는 그대로 — 원장·이력을 넣으면 이력 한 줄이 규칙으로 둔갑한다 |
| **R72** | 🟡 착수 가능 | **정본 로컬 게이트 `pre_push_gate.py` 가 tracked 집행면 0 — "정본" 이 머신 로컬 관습이다** | **기전**: `CLAUDE.md` 가 *"push 전 게이트 = `py -3 scripts/pre_push_gate.py`"* 로 지정했는데, `ci.yml`·`.pre-commit-config.yaml`·`.claude/settings.json`·`Makefile` 어디에도 그 호출이 없다(grep 실측 0건). 유일한 호출자는 **git 이 추적하지 않는** `.git/hooks/pre-push` 이고, 그마저 `--full` 없이 부른다. 즉 새 clone·다른 머신·CI 어디에서도 이 "정본" 은 돌지 않는다. 🔴 R56(pre-commit 미설치)과 **같은 클래스, 다른 대상**이다 — 그때 고친 것은 pre-commit 이고 이건 게이트 자신이다. 부수: `pre_push_gate.py` 가 은퇴한 `--advisory-drift` PR 모드를 아직 미러링하고 주석이 *"PR 에서 advisory"* 라 단언한다(`#1309` 가 CI 에서 제거했다). **반증 수단**: 위 4 파일에서 `pre_push_gate` 가 grep 되면 틀렸다 · 러너 인자 대조 테스트(`test_pre_push_gate.py`)가 스크립트 **이름 집합만** 보고 args 를 안 보는지 확인  🔴 **2026-08-14 Grok 독립 재실측(`01a000aa`) — STILL_TRUE**: tracked 집행면이 여전히 **0**이다. `ci.yml`·`.pre-commit-config.yaml`·`.claude/settings.json`·`Makefile` 네 표면 전수 grep → **0 hit**. `git ls-files -- **/pre-push hooks/**` → **빈 결과**(훅이 추적되지 않는다). `Makefile` 의 `gate` 타깃은 pytest·pylint·bandit 뿐이다(`:115-118`). SessionStart 5종에도 없다. 즉 **새 clone 은 sample hook 만 받고 push 해도 러너가 안 돈다**. `check_precommit_installed`(`#1263`)는 훅 부재를 **관측**할 뿐 집행이 아니다. ⚠️ 처방 제약: `.pre-commit-config.yaml` 에 `stages: [pre-push]` 를 넣는 안은 **이미 기각**됐다(`019fbc8e` — `pre-commit install --hook-type pre-push` 를 안 한 머신에서 한 번도 안 도는 가드가 된다). CI 에서 부르면 `repo-integrity` 와 중복이다. **결정 필요** |
| **R73** | 🟡 착수 가능 | **owed 원장이 이 창에서 0건 등재 — 정체 관측자가 발화했는데 조치로 변환되지 않았다** | **기전**: `check_owed_verification.py`(R0-2 가 신설)가 *"원장이 N PR 동안 손대지지 않았다"* 를 매 세션 인쇄했고 실제로 발화했다. 그런데 이 창의 미결 5건은 **전부 이전 세션분**(`#1296`·`#1289`×2·`#1279`·`#1276`)이고 이번 창 등재는 0건이다. 정작 이 창의 backlog 본문은 운영 검증 부채를 **4건 선언**한다(R68 예산 상한 미관측 · R64 롤백 절차 · R57 · R56 확산). 🔴 즉 *"관측은 하는데 아무도 안 움직인다"* 클래스가 관측자 자신에게 발생했다. **반증 수단**: 원장에 이번 창 4건을 등재한 뒤 다음 세션 훅이 정체 경고를 멈추는가 · 등재된 항목이 실제로 운영 검증으로 이어지는가(다음 회고에서 판정)  🔴 **2026-08-14 Grok 독립 재실측(`01a000aa`) — PARTIALLY_FIXED**: *"이 창에서 0건 등재"* 는 **stale 이 됐다** — `#1351` 이 2행을 등재했고 카운터는 이제 조용하다(`merged_prs_since_ledger()=0` < 임계 10). 🔴 **그러나 패턴 자체는 측정됐다**: 원장 touch 간격 실측 — `#1333`→`#1351` 사이에 **19 PR**(자신 제외 18), `#1324`→`#1333` 8, `#1316`→`#1324` 8, `#1301`→`#1316` **15**. 즉 임계 10 을 **8 PR 초과한 뒤에야** 사람이 조치했고, `#1351` 커밋 본문 자신이 *"정체 18 PR 해소"* 라고 적는다. **관측자 → 조치 변환이 여전히 사람이고 늦는다**. 잔여 3건: (1) 변환이 수동 (2) 운영 ⏳ **9건** 미회신 (3) 원장 ID `R57`·`R64`·`R68` 은 **한 번도 원장에 복사된 적이 없다**(0 hit). ⚠️ 자동 변환은 산문 분류기라 이 리포가 R0-2 축 C 로 **이미 기각**한 형태 |

| **R74** | 🟡 착수 가능 (claim-review 필수 승격의 정직한 잔여 — Grok `019fe089`) | **가드 표면 판정은 열거라 원리적으로 불완전하고, 흔적은 형태만 검사한다** | **기전**: (a) `guard_surfaces()` 는 디렉토리 접두사 **또는** 파일명 규칙(`check_*.py` · `test_*guard*.py`)으로 판정한다. 둘 다 **열거**이므로 둘 다 피해 가는 가드 — 예: `tests/unit/gate/test_retry_matrix.py` 안에 심은 test-as-guard — 는 잡지 못한다. AGENTS.md 가 기록하듯 **test-as-guard 가 이 리포 최다 observer-lie 표면**이라 이 잔여는 무해하지 않다. 완전하게 하려면 *"이 변경이 가드인가"* 를 의미적으로 판정해야 하는데 정적으로 불가하다(AGENTS.md §정적 탐지의 천장). (b) 흔적은 **형태**만 본다 — `session: wf_aaaaaa` 처럼 형식만 맞는 값으로 통과할 수 있다. 이건 승격이 만든 결함이 아니라 가드 자신의 docstring 이 이미 적어 둔 한계(*"위조·의미 진위는 원리적으로 못 잡는다"*)이고, 벤더 중립화가 그 한계를 **넓히지도 좁히지도 않았다**(구 UUID 형식도 `00000000-0000` 로 위조 가능했다). (c) `not claims and not surfaces` 조기 return 이 승격보다 **앞선다** — 코드 표면 밖만 건드리고 seal 어휘도 없으면 흔적 없이 exit 0. **왜 지금 안 고치는가**: (a)를 닫으려면 열거를 넓혀야 하는데 `tests/` 전체를 가드 표면으로 보면 거의 모든 PR 이 외부 검증을 기다리게 된다(오탐>진탐 = 가드 자살, 정책 17). 🔴 **사용자 결정 (2026-08-08): 재량 유지 — *"네 권고사항을 수용합니다"***. 가드 표면 필수 승격을 막 도입해 **아직 한 사이클도 겪지 않았으므로**, 가드가 아닌 PR(`src/` 제품 코드 + seal 주장 없음)의 claim-review 는 당분간 Claude 재량으로 둔다. 근거 없이 두 축을 동시에 조이면 마찰 데이터를 얻기 전에 오탐이 진탐을 넘는다. **다음 정식 회고가 실제 마찰을 보고 재평가한다** — 그때 필요한 지표: 가드 표면 PR 수 / claim-review 소요 / 그 검증이 실제로 잡은 결함 수. **반증 수단**: (a) `tests/unit/gate/` 에 이름 규칙을 피한 가드를 만들고 면제를 적어 exit 0 이면 확인 (b) `session: wf_aaaaaa` 로 exit 0 이면 확인 (c) 코드 표면 밖 파일만 바꾸고 seal 어휘 없이 exit 0 이면 확인. 셋 다 **재현했다**(2026-08-08)  🔴 **2026-08-14 Grok 독립 재실측(`01a000b6`) — STILL_TRUE, 기전 정밀화**: **(a) 열거 구멍** — `guard_surfaces()` 실측: `tools/verify_x.py` **탈출**, `tools/verify_new_guard.py` **탈출**(이름에 guard 가 있어도 안 잡힌다), `tests/unit/gate/test_retry_matrix.py` **탈출**. `tools/check_new_guard.py` 는 파일명 규칙에 걸리지만 🔴 **standalone PR 에서는 거기까지 도달하지 못한다** — `tools/` 가 `_CODE_SURFACES` 밖이라 `if not claims and not surfaces: return 0` 으로 **분류 전에 조기 return** 한다(exit 0 실측). **(b) 형태만 검사** — 위조 흔적 `session: wf_aaaaaa` · `00000000-0000` · `deadbeef-dead-…` 가 `missing_trace_fields` 를 **전부 통과**했고 가드 표면 PR 에서도 exit 0. 빈 값과 `totally-made-up-id` 만 걸린다. ⚠️ 위조는 **정적으로 닫을 수 없고**, `tests/` 전체 확대는 이 행이 이미 기각한 가드 자살이다(2026-08-08 사용자 결정으로 잔여 유지) |

| **R75** | 🟡 착수 가능 | **`api_error` 의 원인이 DB 에 없어 사후 규명이 원리적으로 불가 — 143건이 영구 미상이다** | **기전**: `log_claude_api_call`(`src/shared/claude_metrics.py:139-151`)이 `error_type` 을 **구조화 로그로만** 보내고 `analyses` 어디에도 남기지 않는다. 그래서 빈-모델 400 · 529 overload · timeout · 인증 실패가 Postgres 에서 **전부 같은 `api_error` 한 값**으로 수렴한다. 🔴 **실측 규모 (2026-08-10)**: `api_error` **143건**(SCAManager 74 · xzawed-pais 53 · CustomWebService 8 · KeyCloakSDK 7 · ArcanaInsight 1). 각 건은 `_persisted_score_is_unreliable` 로 score·grade NULL + auto-merge 차단이라 **실피해**인데, 원인별 분포를 아무도 모른다. 실제로 `#1289` owed 검증에서 *"이 143건 중 몇 건이 빈-모델 결함이었나"* 를 물었고 **DB 로는 답할 수 없다**는 것이 확인됐다(적대 검증 `wf_28bb3b63-838`). 그래서 원장은 "피해 0" 대신 "관측 구간에서 이 결함 발현 0" 으로 좁혀 적혔다. **반증 수단**: `analyses` 또는 별도 테이블에 `error_type`(`api_status_code`·`error_class`)을 남긴 뒤, 인위적 400/529 를 각각 1회 발생시켜 두 행이 **서로 다른 값**으로 구분되는가. 구분되지 않으면 컬럼만 늘고 규명력은 그대로다  🔴 **2026-08-14 Grok 독립 재실측(`01a000b6`) — STILL_TRUE, 서술 1건 정정**: `Analysis` 에 `error_type`·`status_code`·`error_message` 컬럼이 **없고**, 실패 경로는 `_default_result("api_error")` 로 **모든 예외를 한 enum 에 뭉갠다**. `ai_review.py` 예외 블록은 `str(exc)` 도 `status_code` 도 응답 body 도 안 남긴다(`getattr(exc` 0 hit). 🔴 **정정**: *"구조화 로그로만"* 은 stale — 마이그레이션 0043(2026-07-08)이 `ClaudeApiCall.error_type String(64)` 을 **이미 저장**한다(예외 **클래스명**). 그러나 **`analysis_id` FK 가 없어** 그 143행을 분류하는 데 쓸 수 없고, status/message 도 없다. `#1275`/`#1276` 은 심의 게이트 작업이라 **무관**(이 행의 인용이 틀렸다). ⚠️ 규명력 있는 수정 = 스키마 + 호출부 변경 → **사용자 결정**. 과거 143건은 **backfill 불가** |

| **R76** | 🟡 착수 가능 (R48 잔여 — 적대 감사 `wf_9a4878aa-eab`) | **본문 수치 축은 "돌렸는가" 가 아니라 "낡지 않았는가" 만 잰다 — 실행 증거로 올리려면 `--junitxml` 이 필요하다** | **기전**: `check_test_count_sync.check_body_claim` 이 본문 수치를 대조하는 상대가 `collect_count()` = `pytest --collect-only` 다. 감사 실측 = 그 명령은 **11초**(collect 4.52s)인데 실제 스위트는 **208~247초**다. 즉 저자는 **19배 싼 경로**로 같은 숫자를 만들 수 있고, 불일치 시 가드가 `vs 실측 수집 N` 으로 **정답까지 알려 준다**. 그래서 이 축이 실제로 닫은 것은 *낡거나 파생되지 않은 수치*(#1305·#1310·#1312 가 전부 그 형태였다)이고, 6-step ② 가 요구하는 *실행* 은 여전히 자기 신고다. 🔴 부수 축 = **우회 유인의 방향**이 나쁘다: *틀린 수치는 red · 없는 수치는 green* 이라 가장 싼 대응이 **수치 라인 삭제**이고, 그러면 이 축이 의존하는 관행 자체가 조용히 침식된다. **반증 수단**: CI `test-and-analyze` job 의 pytest 에 `--junitxml=junit.xml` 을 붙이고 가드가 그 산출물의 `tests`/`failures`/`skipped` 를 읽게 한 뒤, **`--collect-only` 숫자만 붙여넣은 본문**이 red 가 되는가(되지 않으면 대체가 성립하지 않은 것이다). 🔴 **선행 확인 필요**: `ci.yml` 변경이라 봇 PR·재실행 경로 영향 검토가 동반돼야 한다  🔴 **2026-08-14 Grok 독립 재실측(`01a000b6`) — STILL_TRUE, 처방 절반이 stale**: 실증 — `--collect-only` 로 얻은 `7179` 를 **스위트를 한 번도 안 돌리고** 본문에 넣으니 `✅ 본문 수치 축 일치` + **exit 0**. 오라클이 `collect_count()` 이므로 *훔친 정확한 숫자*는 초록이다. 🔴 **처방의 절반이 이미 되어 있다**: CI `test-and-analyze` 가 **같은 job 에서** `--junitxml=pytest-results.xml` 을 **이미 생성**하고 Sonar 가 그것을 소비한다(`sonar.python.xunit.reportPath`). 만드는 쪽은 끝났고 **가드가 읽지 않을 뿐**이다. ⚠️ 읽기 배선이 제품 결정인 이유: 그 XML 은 unit+integration 합본인데 본문 축은 unit 한정이고, `tests`/`failures`/`skipped` 중 무엇을 쓸지와 artifact vs workspace 경로가 갈린다. 그리고 **본문에 수치를 아예 안 적으면 여전히 초록**(*'미실행'* 인쇄)이라는 유인은 별개로 남는다 |

| **R77** | ✅ 완료 (2026-08-10 — 🔴 **내가 등재할 때 적은 처방이 틀렸다**) | **import 시점 `ValidationError` 가 자격증명을 그대로 인쇄한다 — 어떤 로그 필터도 원리적으로 닿지 못한다** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r77) | 보안 |

| **R78** | 🟡 착수 가능 (문서감사 PR-2 산출 — 2026-08-11) | **"증명 없는 이동 금지" 에 집행자가 없다 + 채점 데이터셋 위음성 4종** | **기전**: (a) 🔴 **RRS 순위 발행 금지** — 채점 체계가 검증 2라운드에서 **BROKEN** 판정을 받았다: 편집 불변성 없음(처분 후보 26건의 `days_since_edit` 만 0으로 바꾸면 **26/26 이 소멸** — 공백 커밋 1회가 모든 삭제 후보를 해제) · 관측자 오염(8세션 분모 안에 **감사 세션 자신**이 있고 199 중 58건을 건드림) · 접근축 93%가 부수 등장(실제 Read/Edit 대상은 **9건**). 계측기로만 보존하고 순위 기반 작업지시서는 발행하지 않는다. (b) **`glossary.md` 처분 철회** — 발행됐던 지시를 실행하면 `scripts/i18n_comments/translate_comments.py:48`(`BASE_DIR / "scripts" / "i18n_comments" / "glossary.md"`)이 **런타임 파손**된다. 경로를 문자열로 조립하므로 리터럴 grep 이 못 본다. (c) **소비자 수집 4모드 미구현** — `code_consumers` 는 경로 리터럴 grep 이라 위음성 4종(경로 조립 · 정규식/glob · bare-basename · 디렉토리 glob)이 있고, 그 때문에 *"집행자 0인 8건"* 작업지시서가 **8/8 오탐**이었다. 🔴 **이 항목의 가드는 기각됐다** — 리포 내 실제 처분 지시가 0건이라 red 를 만들 수 없다(계획서가 자기 규율을 자기 PR 에 적용한 결과). 따라서 *"증명 없는 이동 금지"* 는 **발행 규칙으로만 존속**하며 기계 집행자가 없다는 사실을 여기 명시한다. **반증 수단**: 4모드로 재수집한 뒤 `code_consumers` 가 늘어난 문서 수 — 0이면 위음성 주장이 거짓이다  🔴 **2026-08-14 Grok 독립 재실측(`01a000bd`) — STILL_TRUE, 과장 1건 정정**: **(a)** *"증명 없는 이동 금지"* 집행자 여전히 **0** — `disposal`·`code_consumer`·`doc_move` 이름의 수집기가 리포에 **없고**, 이번 달 `#1345`(아카이브 **56파일 삭제**)와 `#1344`(폴더 통째 이동)가 **required check 전건 pass** 로 통과했다. 이것이 공백의 존재 증명이다. `check_red_budget.deleted_not_renamed` 는 `SURFACE_GLOBS` 6개만 보고 소비자 증명을 요구하지 않는다. **(b)** 데이터셋 위음성 4모드 실측 — 경로 조립(`Path / "scripts" / …`) · 정규식 이스케이프(`doc_review_gate.py:69` 이 `glossary\.md` 라 경로 grep 에 안 걸린다) · bare basename · 디렉토리 glob. 4모드 재수집 시 consumer 가 오르는 문서 **≥16건**(glossary + 소비자 0 런북 12 + rules 3). 🔴 **정정**: *"glossary 를 옮기면 런타임이 깨진다"* 는 **거짓**이다 — `GLOSSARY_PATH` 는 `:48` 에서 대입만 되고 **한 번도 읽히지 않는 죽은 상수**다(R85 도 같은 지적). 데이터셋 결함은 참, 런타임 파손 주장은 과장 |

| **R79** | 🟡 착수 가능 (문서감사 PR-3 산출 — Grok claim-review `019ff0c3`) | **문서 단언 가드는 봉인이 아니라 drift 탐지기다 — '다시 쓰면' 통과하는 회피 8종** | **기전**: `tests/unit/test_config.py::test_documented_session_secret_failure_modes` 는 (`SESSION_SECRET` ∧ 기동 언급)을 **한 줄 안에서** 찾고 조건 어휘(`커스텀|기본값|dev-secret`)의 **존재**만 본다. Grok 이 이 형태의 회피를 구성했다: **(H1)** 문장을 두 줄로 쪼개면 1행은 술어가 없고 2행은 변수명이 없어 **양쪽 다 스캔 밖** · **(H2)** *"기본값 사용 시 항상 기동 실패"* 처럼 조건 어휘를 **실패 대상**으로 써도 green(토큰 세탁) · **(H3)** `_PROD_AXIS` 도 `ENVIRONMENT` **출현**만 봐서 *"ENVIRONMENT 와 무관하게 APP_BASE_URL 만 본다"* 는 **의미 반전**이 통과 · **(H4)** anti-vacuity 가 파일당 1행이라 정확한 서술을 전부 지우고 형식만 갖춘 1행을 남기면 green · **(H5)** 영문 서술(`fails to start` · `hard fail`)은 술어 정규식 밖 · **(H6)** 예외 타입 회귀(`SettingsValidationError`→`ValidationError`)는 문서 축이 아예 안 본다 · **(H7)** 인용 면제 마커(`session-secret-claim-quote`)를 활성 규칙 줄에 붙이면 세탁 · **(H10)** 한 줄에 조건 어휘가 **부수적으로** 있으면 의미가 퇴행해도 통과(`security.md:27` 형태). 🔴 **정규식을 더 붙이는 방향은 기각** — 메모리 `feedback-prose-guard-both-ways`(산문 가드는 양방향으로 틀린다)와 같은 함정이고, H3 자체가 그 증거다. 실효 방향은 (i) 문서에 **구조화 표**(분기·조건·관측 예외 3열)를 두고 그 표를 파싱해 **코드 실행 결과와 대조**하거나 (ii) 문서 축을 버리고 코드 축만 남기는 것이다. 🔴 **타이트닝이 새 비용을 만들었다 (Grok 2차 `019ff145`)**: `_PROD_AXIS` 를 `ENVIRONMENT` 리터럴로 좁히자 **정당한 서술이 과차단**된다 — `main.py:118,129` 의 실제 게이트는 `settings.is_production` 이므로 *"기본값이고 `is_production` 이면 `RuntimeError`"* 는 **코드 그대로**인데 red 다. 그 결과 저자는 정확도가 아니라 **정규식을 만족시키는 문장**(=`ENVIRONMENT` 토큰 삽입)으로 유도되고, 그 삽입 경로가 곧 H3(의미 반전) 세탁 경로다. 즉 이 축은 조일수록 **가드가 저자를 나쁜 문장으로 민다**. 영문 표기·표 형식 서술도 같은 이유로 red. **반증 수단**: H1(줄 분할)을 실제 문서에 적용했을 때 스위트가 red 가 되면 이 항목은 거짓이다 · 과차단 축은 `\| RuntimeError \| is_production ∧ 기본값 \| 기동 차단 \|` 형태의 표 행을 넣었을 때 green 이면 거짓이다  🔴 **2026-08-14 Grok 독립 재실측(`01a000bd`) — STILL_TRUE, 6+ 회피를 실파일 구성으로 재현**: H2·H3·H4·H5·H6·H10 + H1b·H7b 를 **실제 문서를 고쳐** 재현했고 가드 2종이 **전부 GREEN** 이었다(`mutated != orig` 단언 후 복원). 즉 거짓 주장을 **보존한 채 문장만 바꾸면** 통과한다. H1·H7 은 유일한 RuntimeError 줄(`security.md:53`)을 지워야만 red 인데 그건 **공허화 탐지**이지 클래스 봉인이 아니다. 🔴 **정규식 타이트닝 금지 유지** — 그것이 애초에 H3 세탁 경로를 만들었다(메모리 `feedback-prose-guard-both-ways`). 진짜 종결은 **구조화 3열 표를 `build_settings`/`is_production` 실행 결과와 대조**하거나 산문 축을 버리고 코드 테스트만 남기는 설계 결정 |
| **R80** | 🔴 결정 대기 (P1 — 작업 가용성) | **문서 심의 게이트는 코드 사실을 원리적으로 대조하지 못하고, 같은 입력에 다른 판정을 낸다** | **기전**: `.claude/hooks/doc_review_gate.py` 의 심의 컨텍스트는 `CLAUDE.md`·`AGENTS.md`·`STATE` 머리 + diff 뿐이라 **`src/**` 를 읽지 못한다**. 그래서 *"코드가 실제로는 이렇다"* 는 정정에 대해 consistency-reviewer 가 *"실제 로직과 일치하는지 확인 필수"* 로 block 을 내는데, **확인 수단이 컨텍스트에 없으므로 구조적으로 통과 불가**다. 실측(2026-08-11 PR-3): 실행으로 거짓이 증명된 `CLAUDE.md:51` 단언의 정정이 **6회 시도 중 5회 차단**됐고, 그중 3·4회차는 **같은 치환 문장**인데 3회차만 approve 였다. ⚠️ 이것을 *"비결정 확정"* 으로 읽지 말 것 — 요청 payload 로그가 없어 **입력 동일성이 증명되지 않았다**. 원인 후보는 샘플링(`temperature` 미고정) · warn 통과 후 디스크/컨텍스트 변화 · `old_string`/STATE 차이다(Grok `019ff3ad` 가 초판의 인과 단언을 반증). 🔴 6회차 impact-analyzer 는 **구멍을 명시하는** 문장을 *"거짓 정보로 보안 결함을 은폐"* 로 판정했다. ⚠️ 이것도 *"부호 역전"* 이 아니라 **예측 가능한 실패 모드**로 적는 것이 정확하다 — 심의자가 `src/` 를 못 보는 상태에서 *조건 완화 = 위험* 기준(`doc-impact-analyzer`)을 적용하면, **참인 정정이 보안 약화로 오분류된다**. 같은 회차에서 quality-reviewer 는 `max_tokens` 절단으로 **미심의**(R35 가 봉인한 축이 정직 보고)라 실제 심의는 2/3 였다. 🔴 부수 = 차단 1회당 3 에이전트 호출이라 재시도 비용이 선형으로 늘고, 저자는 *"게이트를 통과하는 문장"* 을 찾느라 **정확도가 아니라 설득력**을 최적화하게 된다. 🔴 **게이트가 리포 자신의 절차와도 충돌한다** — 6-step ⑤ 는 *STATE 이력 꼬리 한 줄만 손으로 적고 나머지 4지점은 `check_docs_sync.py --fix` 가 파생시킨다* 이므로 **꼬리를 적은 시점에 헤더는 반드시 옛값**인데, consistency 가 바로 그 과도기를 *"헤더와 모순"* 으로 차단한다(2026-08-11 실측). 절차를 지키는 유일한 방법이 게이트를 통과하지 못한다. **결정 필요**: (a) 인용된 `src` 파일 발췌를 심의 컨텍스트에 주입(R38 캐시 프리픽스 예산과 충돌 검토) (b) 코드-사실 정정 PR 에 한해 Grok claim-review 흔적으로 게이트를 대체 (c) 현행 유지 + 차단 시 사용자 직접 편집(2026-08-11 채택 경로). **반증 수단**: 동일 편집을 N회 반복해 approve/block 이 갈리면 비결정성 주장이 참이다(1/5 로 이미 관측)  🔴 **2026-08-14 Grok 독립 재실측(`01a00061`) — STILL_TRUE**: 심의 에이전트 컨텍스트에 `src/` 가 **여전히 없고**(`_CONTEXT_SOURCES` = CLAUDE.md·AGENTS.md·STATE.md·`docs/process/**`·`traps.md` — 전부 문서), `temperature` 는 API 호출에 **설정되지 않았다**. 즉 *"코드 사실을 원리적으로 대조하지 못한다"* 는 참이다. ⚠️ *"같은 입력에 다른 판정"* 축은 **재현 실패** — 리포에 요청 payload 로그가 없어 동일 입력 N회 호출을 못 했다(Grok 자기 보고 §못 본 축). 이 축은 여전히 미검증이지 반증된 것이 아니다 |

| **R81** | 🔴 결정 대기 (P0 — 회고 2026-08-13) | **아카이브 보존 가드가 공동화된 아카이브에서 전건 green — `#1335` 의 봉인 주장이 거짓이었다** | **기전**: `tests/unit/scripts/test_rules_archive_backlink.py` 가 Grok 반례(2000자 `X`)를 막으려고 리터럴 지문 3종 + 인용 다양성 하한 25 를 추가했으나, **그 반례 하나만** 막았다. 실측 재현(2026-08-13 회고 중): 서사를 **100% 제거**하고 지문 3종 + 무의미 백틱 인용 30종 + 필러 문장 + 길이 패딩으로 채우니 **38건 전건 통과**. 그 파일 27줄의 단언 *"채움 문자열은 둘 다 통과하지 못한다"* 는 실측으로 거짓이다. 🔴 메모리 `feedback-mutation-red-is-not-sufficient` 가 규정한 클래스 — *"내 뮤테이션은 내가 상상한 실패 모드만 고른다"*. **결정 필요**: (a) 서사 보존을 기계로 재는 방법 설계(원문 대비 n-gram 겹침률? 압축률 하한?) (b) 이 가드를 도달성 축만 남기고 보존 축은 포기 — 그 경우 *"아카이브가 보존한다"* 는 주장을 문서에서 내린다 (c) 현행 유지 + 한계 명시(현재 상태). 🔴 **(c) 를 고를 경우 `#1335` 본문과 아카이브 헤더의 보존 단언을 함께 약화해야 한다** — 지금은 가드가 못 지키는 것을 문서가 지킨다고 적는다. **반증 수단**: 위 채움 뮤테이션을 적용해 red 가 나오면 이 항목은 해소다 | 2026-08-13 회고 P0 · 라이브 재현  🔴 **2026-08-14 Grok 독립 재실측(`01a00061`) — STILL_TRUE, 재현 성공**: 격리 worktree 에서 아카이브를 **101,380자 → 40,191자로 공동화**(서사 제거 + 지문·인용 유지 + 필러 패딩)했는데 `test_rules_archive_backlink.py` 가 **38/38 green** 이었다. `#1335` 의 봉인 주장이 거짓이라는 원 서술이 **독립 재현으로 확정**됐다(1차는 이 리포의 회고, 2차는 Grok — 서로 다른 방법) |
| **R82** | 🔴 결정 대기 (P0 — 회고 2026-08-13) | **승인 기록은 5파일인데 실행은 7파일 — High tier 결정의 범위가 기록 없이 확대됐다** | **기전**: `#1335` 는 커밋 본문에 *"문서 총량 감축 제안서(#1334) §3-C′ 실행. 사용자 승인 2026-08-12"* 한 줄만 남기고 `.claude/rules/` **7파일**을 압축했다. 그러나 제안서 §3-C 가 정의한 범위는 **5파일**(`i18n`·`deploy`·`ui`·`db`·`testing`, −약 43,836자)이고, 추가된 2파일(`pipeline.md`·`api.md`)은 **제안서 자신의 측정이 실익 없음으로 명시 배제**한 파일이다. 리포 어디에도 범위 확대를 재승인한 흔적이 없다. C 는 제안서가 스스로 `🔴 High tier` 로 분류한 항목이라 정책 15 상 **사전 확인 대상**이었다. **결정 필요**: (a) 사후 승인 + 기록만 남김 (b) `pipeline.md`·`api.md` 압축을 되돌림 (c) 제안서 §3-C 범위를 7파일로 정정(사후 정합). 🔴 되돌리면 그 두 파일의 서사가 아카이브에만 남는 상태도 함께 정리해야 한다. **반증 수단**: 제안서 `docs/runbooks/doc-volume-reduction-plan.md:124` 가 7파일을 지목하면 이 항목은 거짓이다 | 2026-08-13 회고 P0 · `#1334` §3-C 대조  🔴 **2026-08-14 Grok 독립 재실측(`01a00061`) — STILL_TRUE**: 제안서 `:124` 의 §3-C 는 여전히 **5파일**(`i18n`·`deploy`·`ui`·`db`·`testing`)이고, `#1335` 머지 커밋 `5458c5e1` 은 **7파일**을 건드렸다(`pipeline.md`·`api.md` 추가). 🔴 **C′ 는 '방법'(경로 불변 인라인 압축)이지 '범위 확대'가 아니다** — 리포 어디에도 그 2파일을 재승인한 기록이 없다. 반증 조건 미충족 |
| **R83** | ✅ 완료 (2026-08-13 사용자 결정 — A+B 혼합 채택) | **반례 대응 규율이 없다 — 반례 하나를 막고 봉인을 선언하는 패턴이 한 세션에 2회 반복됐다** | **기전**: 외부 적대 검증(Grok)이 준 뮤테이션을 red 로 만든 뒤 *"fail-open 을 닫았다"* 로 보고하는 형태가 2026-08-12~13 세션에 **2회** 나왔다 — (1) `#1335` 아카이브 가드(R81) (2) `#1340` 초판의 표면 삭제 테스트(산문 검사라 보호 장치를 지워도 초록, Grok `019ffadc` C3 BROKEN). 둘 다 *그 반례*는 막았고 *클래스*는 열려 있었다. **결정 필요 — 규율을 어느 쪽으로 세울지**: (a) **반례 일반화 의무** — 외부 반례를 받으면 그 **클래스를 명명**하고 클래스 내 **다른 인스턴스 1건 이상**을 스스로 만들어 red 를 확인해야 봉인 선언 가능 (b) **봉인 선언 금지 default** — 뮤테이션 red 는 *"이 뮤테이션을 막았다"* 로만 보고하고 *"fail-open 을 닫았다"* 는 외부 적대 검증 1회를 더 거친 뒤에만. 🔴 (a)는 저자 비용, (b)는 왕복 비용이다. **반증 수단**: 직전 3 세션에서 봉인 선언 후 후속 회고가 같은 축을 다시 연 사례가 0건이면 이 규율은 불필요하다 🔴 **결정: (a)+(b) 동시 채택** — 둘은 배타적이지 않고 **다른 것을 막는다**(A 는 결함을, B 는 거짓 보고를). 이번 창에 나는 둘 다 했다 — 가드는 뚫린 채 뒀고 닫았다고 보고했다. 반영: `AGENTS.md` §불변식 2-b(번호를 4로 붙이지 않은 이유 명시 — 리포 30여 곳이 `3-불변식` 참조) · `.claude/policies/active.md` §A2-b. 어휘 생존 가드 `tests/unit/scripts/test_agents_md_invariant_survival.py`(11건) 동반, 뮤테이션 2종 red 실증(규율 절 삭제 → 6 failed · 헤딩을 `불변식 4` 로 승격 → 1 failed). 🔴 **기계 집행은 주장하지 않는다** — A 의 *"클래스 내 다른 인스턴스인가"* 는 정적 판정 불가라 write-time 규율 + review-time claim-review 가 방어선이다. | 2026-08-13 회고 · 자유 발언 §수정 제안 |
| **R84** | ✅ 완료 (2026-08-15 — 회고 처방 기각, 결번 주석만) | **`docs/STATE.md:305` 결번 8차의 귀속 재구성이 틀렸다 — 처방대로 복원하면 원장이 오염된다** | **기전**: 회고 gap 라운드가 *"빠진 8차 자리에 `#1335` 를 복원하라"* 고 처방했으나 그 전제가 거짓이다. 8차 ordinal 은 `#1331`(README 충돌 마커 + pylint 진리값)의 **머지 전 브랜치**가 이미 점유했고, 그 PR 이 rebase 되며 11차로 재번호되면서 8이 **비워진** 것이다. `#1335` 는 8차를 가진 적이 없다. 처방대로 삽입하면 7차(→7053)와 9차(7053→) 사이에 **체인이 닫히지 않는 거짓 행**이 생긴다. **조치**: 결번을 메우지 말고 *"8차는 rebase 재번호로 비었다"* 는 1줄 주석만 남긴다. 🔴 이 항목은 **회고 처방 자체가 틀린 사례**라 원장에 남긴다 — 처방을 무비판 이행하면 오염이 발생한다 | 2026-08-13 회고 P0 · cross-verify 가 처방을 반증  🔴 **2026-08-14 Grok 독립 재실측(`01a000bd`) — STILL_TRUE 확정, 회고 처방이 틀렸다**: 차수 결번은 실재한다 — 세션19 순서가 `1,2,3,4,6,5,7,9…20` 이고 **8만 비어 있다**(5/6 뒤바뀜은 별개). 🔴 **그러나 회고가 처방한 *"#1335 를 8차로 복원"* 은 거짓 귀속이다**: `git log -S` 실측으로 **`#1331` 이 8차를 점유**했다가(`d44ebb93`: `세션19 8차 … +21 (7053→7074)`) 리베이스로 **8 → 11차 재번호**(`3021f210`)된 것이 확인됐다. `#1335` 는 `세션19 8차` 를 **한 번도 쓴 적이 없고**(grep 0) 더 **먼저** 머지됐다(11:26 vs 15:53). 그리고 7차가 **7053** 으로 끝나고 9차가 **7053** 으로 시작해 **수치 사슬이 이미 닫혀 있다** — 그 사이에 행을 끼우면 `7053→7053` 이라는 조작된 delta 를 만들거나 사슬을 깬다. **조치 권고: 8차 행을 복원하지 말 것.** 허용되는 것은 결번 사유를 적은 **주석 한 줄**뿐이고, `→ **N** 단위` 형식의 항목은 절대 넣지 말 것(`check_docs_sync` 가 수치 없는 꼬리 항목을 red 로 본다)   🔴 **2026-08-15 해소 (`#1354` · 사용자 승인)**: 회고 처방을 **기각**하고 결번 사유 1줄만 `docs/STATE.md` 7차/9차 사이에 남겼다. 행은 추가하지 않았다 — 7차가 7053 으로 끝나고 9차가 7053 으로 시작해 사슬이 이미 닫혀 있어, 끼우면 `7053→7053` 이라는 조작된 delta 가 된다. **이 항목의 값어치는 결번 자체가 아니라 '회고 처방이 틀릴 수 있다' 는 사례**다 ||
| **R85** | 🟡 착수 가능 (P1 — 문서감사 2026-08-12) | **감사 확정 결함 49건 중 미착수 잔여 — 보고서가 SSOT, 여기는 회신 의무 부착점** | **범위**: [`docs/_archive/reports/2026-08-12-docs-audit.md`](_archive/reports/2026-08-12-docs-audit.md) 의 확정 P0 7 / P1 12 / P2 30 중 `#1331`·`#1339` 로 닫힌 것을 제외한 잔여. 🔴 **원문을 여기 복제하지 않는다** — 요약이 원문을 대체하면 다음 세션이 요약만 읽고 닫는다(감사 자신의 §정직 기준). 미착수 대표 4건: (1) `CONTRIBUTING.md:95`·`ko:93` *"E2E 는 CI 에 없다"* 거짓 — `ci.yml:586` 에 e2e job 실재 (2) `docs/runbooks/branch-protection.md:117-121` 이 auto-merge 종결 판정을 코드와 정반대로 서술(`src/gate/merge_reasons.py:80-82` 대조) (3) **스킬 6종이 로드되지 않는다** — `.claude/skills/*.md` 가 평문이라 `find -name SKILL.md` 0건, 라이브 반증 `Skill({skill:"retrospective"})` → `Unknown skill` (4) `scripts/i18n_comments/glossary.md` 의 유일한 소비자가 그 파일을 안 읽는다(`GLOSSARY_PATH` 죽은 상수). 🔴 (3)은 **구조 이동이라 High tier** — `feedback_doc_reorg_behavior_critical` 이 경고한 행동 임계 파일 이동이다. **반증 수단**: 각 건의 `grep -n` 인용을 재실측해 어긋나면 그 건은 해소·오탐이다 | 2026-08-12 문서감사 128건 채점 · Grok 3세션  🔴 **2026-08-14 Grok 독립 재실측(`01a000c8`) — STILL_TRUE, 분모가 존재하지 않는다**: 🔴 **SSOT 로 지목된 `2026-08-12-docs-audit.md` 본문에 확정 findings 목록이 0건이다** — 루브릭·한계·128건 점수표·디렉토리 평균뿐이다. *"49건"* 은 인계 문서(`session-handoff:129`)가 *"P0 7 · P1 12 · P2 30"* 으로 단언한 수치이고 **P1/P2 원문 목록이 어디에도 없다**. 따라서 **49 분율은 원리적으로 측정 불가**(UNVERIFIABLE). 식별 가능한 P0 표본 **8건 실측 = 해소 2 / 미해소 6**: ✅ README 충돌 마커 · ✅ pylint 배지(둘 다 `#1331`) / ❌ CONTRIBUTING 의 *"e2e 가 CI 에 없다"*(실제로는 `ci.yml:608` 에 있다) · ❌ branch-protection 의 `BRANCH_PROTECTION_BLOCKED` 재시도 서술(`merge_reasons.py:80-82` 은 **포함**한다) · ❌ `SKILL.md` 0건(평문 6종) · ❌ `GLOSSARY_PATH` 죽은 상수(AST: store 1 · load **0**) · ❌ 가드 개수 거짓(R86) · ❌ 고아 `docs/design/brief/` 5파일 미등재. **권고: 제목에서 *"49건"* 을 빼라** — 분모가 목록이 아니다 |
| **R86** | 🟡 착수 가능 (P1 — 회고 2026-08-13) | **가드 개수가 문서 13지점에서 거짓이고 그 거짓이 모든 PR 본문에 주입된다 — 이 축에 집행자 0** | **기전**: `pre_push_gate` 가 돌리는 가드 수를 두고 리포에 **7 · 9 · 13 · 16 네 숫자가 공존**한다. 주장 지점: `CLAUDE.md:32,149` · `AGENTS.md:17,23` · `README.md:441-442` · `README.ko.md` · `CONTRIBUTING.md:112,124,211` · `.claude/rules/guards.md:208-211` · `docs/agents-index.md:49-50` · `docs/runbooks/new-machine-setup.md:95` · `.github/PULL_REQUEST_TEMPLATE.md:18-19`. 자칭 `scripts/pre_push_gate.py:12,20` 은 **"7종"**. 라이브 실측 = `_INTEGRITY` 11 + `_INTEGRITY_WITH_ARGS` 1 = 12, `_DIFF_SCOPED` 3 + flake8 = 4 → **16**. 🔴 `PULL_REQUEST_TEMPLATE.md:19` 가 이 거짓을 **열리는 모든 PR 본문에 복사**한다. 🔴 **13지점을 손으로 고치면 다시 낡는다** — `pre_push_gate.py` 에 `guard_inventory()` 를 노출하고 문서 수치를 그 값과 대조하는 가드를 **같은 PR** 에 묶을 것(정책 4). `test_pre_push_gate.py` 는 집합 포함만 강제하고 개수는 보지 않는다. **반증 수단**: `py -3 scripts/pre_push_gate.py` 출력 행 수가 13이면 이 항목은 거짓이다 | 2026-08-13 회고 P0 + 2026-08-12 갭 감사 교차축  🔴 **2026-08-14 Grok 독립 재실측(`01a000c8`) — STILL_TRUE, 이 행의 수치도 틀렸다**: 라이브 실측 = **17 슬롯**(repo-integrity **13** = `_INTEGRITY` **12** + `_INTEGRITY_WITH_ARGS` 1, PR-diff **4** = flake8 1 + `_DIFF_SCOPED` 3). 🔴 **이 행이 적은 `_INTEGRITY` 11 은 틀렸다(실제 12)** 이고, *"라이브 16"* 도 이제 **17** 이다 — 숫자를 손으로 적는 항목이 자기도 낡았다. 불일치 표(활성 표면만): `pre_push_gate.py:12,20` **7종** · `PULL_REQUEST_TEMPLATE.md:18-19` **13종** · `CLAUDE.md:66,187` · `AGENTS.md:17,23` · `README.md:437` · `README.ko.md:495-496` · `CONTRIBUTING.md:112,124,211`(+ko) · `guards.md:215,218` · `agents-index.md:49-50` · `new-machine-setup.md:95` · `check_precommit_installed.py:166` — **전부 13 또는 9+4 로 거짓**. `test_gate_claim_consistency.py:223` 과 `test_pre_push_gate.py:9` docstring 도 거짓. 🔴 **템플릿 `:19` 가 `CI 강제 가드 13종 — repo-integrity 9 + PR-diff 4` 를 매 PR 본문에 복사**한다. ⚠️ 처방 제약: 대조 대상에 `CLAUDE.md`·`AGENTS.md` 가 들어가 Grok 의 편집 금지 표면과 겹친다 |
| **R87** | 🟡 착수 가능 | **rubocop · golangci-lint 가 운영에서 실제로 분석하는지 한 번도 실증되지 않았다** — R13(slither)과 같은 클래스 | **출처**: 2026-04-27 아카이브 `_archive/user-actions-remaining`(2026-08-13 삭제 — git 이력에 남는다)의 **P4-Gate-2 Railway 실증**이 *"진행 필요"* 인 채 3.5개월 방치됐다. 그 파일은 아무도 참조하지 않았고(전 리포 grep 0건) **읽히지 않는 곳에서 열린 일감이 늙고 있었다** — 아카이브 정리 중 발견해 원장으로 승격했다. **기전**: 두 도구의 단위 테스트는 `subprocess`/`shutil.which` 전량 mock 이라 CI green 이 실동작을 증명하지 않는다. 로컬에 바이너리가 없어 애초에 mock 외 선택지가 없었다. 설치가 깨지면 `railway.toml` 의 실패 흡수와 맞물려 **이슈 0건 = 무음 점수 인플레**가 된다(R14 가 slither 에서 기술한 경로와 동형). **반증 수단**: `railway run bash` → `which rubocop && which golangci-lint`, 그리고 `.rb`/`.go` 픽스처 PR 이 `[rubocop/…]`·`[golangci-lint/…]` ruleId 를 실제로 낸다. **선행**: R13 과 함께 처리하면 실바이너리 통합 테스트 한 벌로 3 도구를 덮는다 | 2026-04-27 미종결 사용자 액션 → 2026-08-13 승격  🔴 **2026-08-14 Grok 독립 재실측(`01a000c8`) — STILL_TRUE, 클래스 확정**: `test_rubocop.py:7` 이 스스로 *"subprocess.run mock 으로 실제 rubocop 바이너리 호출 없이"* 라 선언하고 `shutil.which`/`subprocess.run` 을 전 경로 patch 한다. `test_golangci_lint.py` 동일. `tests/integration/` 에 두 도구 파일 **0건** — 대조군 `test_eslint_analyzer.py` 는 **실바이너리 통합 테스트가 있다**. 🔴 `.github/workflows/` 전수 grep `rubocop|golangci` → **0** — **CI 에 두 바이너리가 설치조차 안 된다**. 운영 설치는 `railway.toml` buildCommand 뿐인데 둘 다 `|| echo 'WARNING: … will be disabled'` 로 **설치 실패를 흡수**한다. 깨져도 이슈 0건 = 점수 인플레 경로가 열린다 — eslint 가 막았던 그 클래스다. 닫으려면 eslint 와 같은 `tests/integration/test_{rubocop,golangci_lint}_analyzer.py` + CI 조달, 또는 Railway 라이브 실측 로그. **R13 과 한 묶음이 맞다** |

| **R88** | 🟡 착수 가능 | **아카이브 이동·cross-file 앵커·잔여 stub 보존을 재는 관측자가 리포에 0** — `#1343` 압축을 검증하며 뮤테이션 3종이 전부 GREEN 이었다 | **기전**: (a) 신설 아카이브 2 파일을 **통째로 삭제**해도 `pytest tests/unit/scripts` 1291 passed · `check_toc_anchors` EXIT 0 (b) `docs/cycle-history.md` 의 아카이브 링크 앵커 **143건을 전부 파괴**해도 `check_toc_anchors` EXIT 0 — 그 가드의 `_TOC_LINK` 는 `](#…)` **문서 내부** 링크만, 그것도 `## 목차` 블록 안에서만 본다. cross-file 앵커(`_archive/…#r38`)는 문법적으로 대상 밖이다 (c) backlog 잔여 stub 18곳을 지워도 1291 passed. 🔴 **`#1343` 본문이 검증 근거로 `check_toc_anchors` 를 인용했으나 그 가드는 이번 압축이 만든 링크를 한 개도 재지 않았다** — 그 인용은 철회됐다. **반증 수단**: 위 뮤테이션 3종 중 하나라도 red 를 내는 가드가 있으면 이 항목은 거짓이다. **선행**: R41(완전성 축 미관측)과 같은 클래스 | `wf_d4c837bb-603` 적대 검증  🔴 **2026-08-14 Grok 독립 재현(`01a000ce`) — STILL_TRUE, 3종 전부 재현**: (1) 압축이 만든 아카이브 2파일을 **삭제** → `check_toc_anchors` EXIT 0 · `pytest tests/unit/scripts` **1317 passed**. (2) `cycle-history.md` 의 cross-file 앵커 **143건**을 `#BROKEN-` 으로 치환 → `check_toc_anchors` **EXIT 0**(그 가드의 `_TOC_LINK` 는 `](#…)` **문서 내부** 링크만, 그것도 목차 블록만 본다). (3) backlog 의 아카이브 전문 링크 **39곳**(등재 당시 18 → 완료 행이 늘어 39) 삭제 → **1319 passed**. 🔴 **`#1347` 부록(155줄)도 관측자를 만들지 않았다** — `.py`/`.yml` 에서 그 두 파일명을 가리키는 테스트가 **0건**이다 |
| **R89** | ✅ 완료 (2026-08-14 — 사용자 확인, 권장안 (c) 채택) | **압축이 목차 다이제스트 25,402자를 재생성 불가 형태로 지웠다** | **기전**: `#1343` 이 `docs/cycle-history.md` 를 접으면서 소실된 136 라인은 전부 **구 목차 항목**이다. 이 리포의 목차는 헤딩의 사본이 아니라 손으로 쓴 사이클 다이제스트였다(`#1102~#1114 — alembic fileConfig 가 앱 로깅 런타임 파괴[#1100 무력화]·…`). 본문 사실은 접힌 아카이브에 생존했으나 **한 화면에서 사이클을 훑던 탐색성**이 사라졌다. ✅ **해소**: 압축 직전 커밋(`c8db1995^`)의 목차 블록에서 **원문 그대로** 뽑아 `docs/_archive/cycle-history-folded-2026-08.md` §부록으로 복원했다(재생성이 아니다). 136행 전건 앵커 해소 — 이 파일의 `<a id=…>` 와 대조해 붙였고 착지 실패 0건. 대조군으로 소실 대표 문구(`alembic fileConfig 가 앱 로깅 런타임 파괴`) 복원을 확인했다 | `wf_d4c837bb-603` 적대 검증 → 사용자 결정 |

| **R90** | ✅ 완료 (2026-08-14 — **R71 로 통합**) | ~~🔴 예산 계수 범위가 리포의 명령형 표면 일부만 덮는다~~ | 🔴 **이 항목은 `R71`(2026-08-08 등재)의 중복이었다.** 같은 축(`SURFACE_GLOBS` 범위)을 말한다 — R71 은 *"저장소 🔴 의 26.0% 만 잰다(표면 밖 825건)"*, R90 은 *"밖에 약 475건"*. **수치 차이는 그 사이 마커 221개를 뗀 결과**이고 축은 동일하다. 2026-08-14 회고 P0-A 를 이행하며 등재할 때 **기존 원장을 grep 하지 않아** 중복을 만들었다 — 정책 16(*같은 값·로직이 2+곳이면 수정 직전 `grep -rn` 전수 열거*)의 원장 판 위반이다. 잔여 축과 최신 실측은 **R71 이 정본** | 중복 정리 |
| **R91** | 🟡 착수 가능 (P1 — 회고 2026-08-14) | **`check_reverse_mutation` 은 docs-only PR 을 원리적으로 판정하지 못한다** | **기전**: 그 게이트는 *생산 파일을 base 로 되돌리고 테스트가 red 가 되는가* 를 본다. 문서만 바꾼 PR 은 '생산 표면 변경 0건' 으로 **exit 0(대상 아님)** 이 된다 — 이 세션 8 PR 중 2건이 그 경로로 판정 없이 통과했다. 규칙·정책 문서는 **행동을 바꾸는 산출물**인데 이 축의 밖이다. **권장**: `.claude/rules/**`·`.claude/policies/**`·`CLAUDE.md`·`AGENTS.md` 를 '생산 표면' 으로 편입하고, 그 문서를 읽는 테스트(`test_policy_structure`·`test_red_budget` 등)를 대응 테스트로 잡는다. **반증 수단**: 규칙 문서를 base 로 되돌렸을 때 red 가 되는 테스트가 하나도 없으면 편입해도 무의미하다 | `wf_2b615d5e-8c5` P1-J  🔴 **2026-08-14 Grok 독립 재현(`01a000ce`) — STILL_TRUE, 배너 서술 1건 정정**: `#1347`(순수 docs-only)의 실제 SHA 범위로 게이트를 돌리니 **EXIT 0** 이고 배너는 *"테스트 변경 0건 — 이 게이트의 대상이 아니다 (생산 표면 0건)"* 였다. `PROD_PREFIXES` 에 `docs/`·`.claude/rules/`·`.claude/policies/`·`CLAUDE.md`·`AGENTS.md` 가 없다. 🔴 **R69 와의 구분 확인**: 같은 게이트, 다른 분기가 맞다 — R69 는 `prod>0 ∧ tests>0` 로 게이트가 **실행**되고 신규 파일 `git rm` → collection red 를 통과로 세는 것, R91 은 **skip**. ⚠️ **정정**: 이 행이 적은 *"생산 표면 변경 0건으로 exit 0"* 배너는 `docs+tests` 인 경우이고, 순수 docs-only 는 `if not tests` 에 **먼저** 걸린다(한 줄 어긋난다). skip 인 점은 같다 |
| **R92** | ✅ 완료 (2026-08-14 — 사용자 결정 **(a)** 채택) | **🔴 결정 대기 항목에 재기(re-raise) 기전이 0 — Claude 가 닫을 수 없는 유일한 클래스인데 자동 부상 장치가 없었다** | **기전**: R81·R82 는 P0 로 등재됐으나 **등재 세션 1회로 끝나고** 이후 6 PR 에서 한 번도 회신 요청에 재등장하지 않았다. SessionStart 훅 2종은 회고 카덴스·owed 원장만 보고 backlog 🔴 행은 보지 않았다. 🟡 는 Claude 가 자율 착수해 자연 소멸하므로 구조적으로 정체하는 것은 🔴 뿐이다. ✅ **해소**: `scripts/check_open_decisions.py` 신설 + SessionStart 배선(advisory, exit 0 고정). 라이브 실측 — 6건(R48·R67·R80·R81·R82·R92) 정확히 적발. 배선 가드 `test_session_start_wiring.py` `_REQUIRED` 확대 + 계약 가드 `test_open_decisions.py` 10건(과교정 방지축 = 본문 산문의 🔴 을 상태로 세지 않는다). 뮤테이션 실증: 배선 제거 시 red. ⚠️ **중요도·의도적 보류는 판정하지 않는다** — 촉구일 뿐이고 그 고지를 출력이 매번 인쇄한다 | `wf_2b615d5e-8c5` P1-H → 사용자 결정 |

| **R93** | 🟡 착수 가능 | **`CLAUDE.md` 의 🔴 예산제 한계 고지 3축이 길다 — 정직성을 지키면서 압축할 방법 필요** | **기전**: 2026-08-14 회고 P0-A 를 이행하며 «100%» 옆에 *"이 가드가 못 재는 축"* 3개(프록시 · 표면삭제 CI 전용 · 계수 범위)를 붙였다. 셋 다 실측 근거가 있고 지우면 게이트가 봉인으로 읽히지만, 진입 문서인 `CLAUDE.md` 에서 20줄을 차지한다. 🔴 **사용자 결정(2026-08-14): 우선 현상 유지, 추후 개선.** 즉 이 항목은 *줄여도 되는가* 가 아니라 *어떻게 줄여야 정직한가* 를 푼다. **선택지**: (a) 3축을 `docs/process/guard-authoring.md` 로 옮기고 CLAUDE.md 는 1줄 포인터 — 단 도달률이 떨어진다(traps: 읽히지 않으면 0이다) (b) 3축을 표로 압축 (c) 한계를 스크립트 `--limits` 출력으로 옮기고 문서는 그 명령을 가리킨다. **제약**: `test_red_budget.py::test_claude_md_discloses_all_three_ceilings` 가 세 축의 CLAUDE.md 생존을 고정하므로, 어느 안이든 그 가드를 **같은 PR 에서** 새 위치로 옮겨야 한다. **반증 수단**: 옮긴 뒤 3축을 지웠을 때 여전히 red 인가 | 2026-08-14 사용자 결정  🔴 **2026-08-14 Grok 독립 실측(`01a000ce`) — STILL_TRUE, 수치 정정 + 선택지 제약 확정**: `CLAUDE.md` 239줄 / 18,253자 기준 — 고지 **본체는 13줄(676자, 줄 5.44% · 자 3.70%)** 이고, 실측 단락까지 포함해야 20줄(1,065자)이다. 이 행의 *"20줄"* 은 후자 기준이었다. 🔴 **선택지별 제약**: **(a) process 로 이동** — 가드를 새 파일로만 옮기면 CLAUDE.md 에서 고지가 사라져도 GREEN 이 되어 **공허해진다**. 피하려면 CLAUDE.md 포인터 **와** 새 위치 리터럴을 **둘 다** 못박아야 하고, 도달률 하락은 남는다. **(b) 표로 압축** — 가드가 고정하는 네 리터럴(`무관한 가드 이름을 적어도 통과한다`·`안 쟀음`·`계수 범위가 리포 전체가 아니다`·`SURFACE_GLOBS`)만 보존하면 **가드를 옮기지 않고** 줄 수를 줄일 수 있다 — **가드 이동이 불필요한 유일한 안**. **(c) `--limits` 출력** — 이중 핀이 필요(문서만 보면 스크립트 삭제가 GREEN, 스크립트만 보면 문서 삭제가 GREEN). **Grok 권장 = (b)** |

| **R94** | 🟡 착수 가능 | **owed 원장의 상태를 읽는 관측자가 둘인데 서로 다른 답을 낼 수 있다 — 스위트가 그 불일치를 못 본다** | **기전**: 같은 상태 칸을 두 곳이 읽는다. (a) `scripts/check_owed_verification.py:80` 은 `⏳ in status` 로 **미결**을 세고 (b) `tests/unit/scripts/test_owed_ledger_consistency.py:57` 의 `status_by_id` 는 `(*_TERMINAL, _PENDING_MARK)` 순서로 substring 검사해 **종결 마커를 미결보다 먼저** 판정한다. 그래서 상태 칸 산문에 `✅` 글자가 들어가면 (b)만 종결로 뒤집힌다 — 🔴 **2026-08-14 실사고**: 내가 *"이 관측으로 상태를 ✅ 로 바꾸지 않는다"* 라고 **부정문으로 쓴 문장의 글리프**가 `#1276` 을 (b)에서 ⏳ → ✅ 로 전환시켰다(Grok `01a0003d` 적발). 정정 기록이 자기 가드에 걸리는 형태이며 메모리 `feedback-prose-guard-both-ways` 의 *"인용 면제 필수"* 축과 동형이다. **스위트는 29 passed 로 침묵**했다 — 두 관측자의 답을 대조하는 테스트가 없다. **처방**: (a)(b) 판정을 대조하는 테스트 1건 — 같은 원장에 대해 두 함수의 미결 집합이 일치해야 한다. **반증 수단**: 상태 칸에 종결 글리프를 넣은 미결 행을 만들었을 때 두 함수가 같은 답을 내면 이 항목은 거짓이다 | `01a0003d` claim-review  🔴 **2026-08-14 Grok 독립 재현 + 수정(`01a000ce`) — 이 창에서 해소**: 합성 이중 글리프 행(`⏳ … ✅ 로 바꾸지 않는다`)으로 두 관측자의 불일치를 재현했다 — `pending_rows` → `{#1276}` · 구 `status_by_id` → `{#1276: ✅}`. **수정**: `status_by_id` 의 마커 탐색 순서를 **⏳ 우선**으로 바꾸고 두 관측자가 같은 답을 내는지 **대조 테스트 2건**을 신설했다(라이브 원장 일치 + 합성 불일치 차단, 기대값은 리터럴). **뮤테이션 red 를 Claude 환경에서 독립 재현** — 순서를 종결 우선으로 되돌리니 `test_dual_glyph_row_is_pending_to_both_observers` 가 **FAILED**. 라이브 원장 실측: 두 관측자 미결 집합 **동일**(9건), 이중 글리프 행 0. ⚠️ **잔여**: 같은 파일의 `_safety_rows` 는 **아직 종결 우선**이다(세 번째 관측자) — 이 수정 범위 밖 |

> **상태 요약 — 이 표(현재 창) 60행 기준**: 🔴 결정 대기 **4** · 🟡 착수 가능 **30** · ✅ 완료 **26** (grep 실측 2026-08-15, R67·R84 해소 반영 — R81~R86 은 2026-08-12 문서감사 128건 + 2026-08-13 5+1 회고 182 findings 의 **회신 의무 부착점**이며, 원문 SSOT 는 각 보고서다).
> 🔴 **역사 섹션의 미해결분은 이 수치에 포함되지 않는다** — R28(R0-2 결정 선행) · R34 ·
> R18(⏸️ 설계 천장) + 2026-07-26 창의 R0-2 · R7 · R1~R15 자율 잔여 · B6-b · B7 · H2~H4 ·
> 감사 P2 잔여 6 은 아래 역사 섹션에 있다. 범위를 밝히지 않은 카운트가 R24 가 지적한
> "요약표 🔴 1건인데 실제 3건" 의 정체였다.
> 회귀 가드: `tests/unit/scripts/test_backlog_shape.py::test_status_summary_matches_the_table`.
>
> 🔴 **R45-(d) 와 R7 은 상호 참조**: R47-(c) 가 e2e 열 공백을 실측해 R7(배지 정직화 여부)의
> 결정 근거를 갱신했다 — 다만 **R7 의 원 근거는 stale** 이다(문제삼은 '거짓 배지' 는 창 이전
> `#1237`(2026-07-29)에 이미 정직화됐는데 원장은 여전히 살아 있는 결함으로 서술한다).
>
> P2 62건은 보고서 본문 참조. 위 R35~R48 에 흡수되지 않는 잔여는 다음 회고에서 재평가.
> 🔴 **인용 미확인 6건**(보고서 🔸 표기)은 착수 전 `grep -n` 재실측 의무.

---

## ▶️ (역사) 2026-08-02 세션14 인수인계

🔴 **2026-08-02 세션14 = 이전 세션 잔여 착수분 이행** — 현재 창 🟡 7건 중 **6건 완료**
(R16·R17 `#1268` / R20 `#1269` / R31 `#1270` / R30·R24 `#1271`). 매 PR Grok claim-review
4세션(`019fbe1f`·`019fbe32`·`019fbe49`·`019fbe61`, 전부 HOLDS-with-caveat → 적발분 같은 PR
fix-up) + test-writer TDD 선행 6라운드 + 실경로 뮤테이션 **21건 red**(생존 뮤테이션 1건은
실측 보고 후 단언 강화로 red 전환). 잔여 = **R28**(🔴 R0-2 결정 선행 필요로 미착수) +
신규 **R34**(`#1263` 재검증의 라이브 반례). 5+1 회고(run `wf_d89db046-274`)는 세션 말미 병행.

**직전 세션13 인수인계**(2026-07-31): 5+1 회고(run `wf_d58ff24d-f4d`·188 에이전트·확정 156·
verdict_coverage 1.0) + Grok claim-review(`019fb7fd`) + 뮤테이션 검증(`wf_30d60394-312`).
사용자 결정 = P0 4뿌리 전량 이행 + P1/P2 등재. 상세:
[`_archive/reports/2026-07-31-retrospective.md`](_archive/reports/2026-07-31-retrospective.md).

| ID | 상태 | 항목 | 근거 요약 |
|----|------|------|-----------|
| **R16** | ✅ 완료 (`#1268`) | **B8 fail-open floor 가 자기 스캔 범위를 관측하지 않는다** — `scripts/check_*.py` 만 glob 하므로 **test-as-guard 표면은 원리적으로 미탐**이고, 범위를 비워도 `✅ … bare-substring fail-open 0` **성공 문구를 출력**한다 — 🔴 **잔여**: `check_guard_fail_open` 은 `tests/**/test_*.py` 를 **원리적으로 미탐**한다 — AGENTS.md 가 최다 재발 사고(`#1136`·`#1156`)로 기록한 바로 그 표면이다. 확대하면 오탐>진탐(가드 자살)이라 **의도적 미확대**이며, 이 표면은 write-time 규율과 review-time claim-review 로만 방어된다 | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r16) |  |
| **R17** | ✅ 완료 (`#1268`) | **lint-js 공허화 차단의 false-justification 우회** — 템플릿 인라인 `<script>` 에 비실행 Jinja 유사 토큰(`// {{`)을 넣으면 "정당한 제외" 로 분류돼, 그 파일을 eslint 무시 목록에 넣어도 가드가 통과 — 🔴 **잔여**: 검사 대상 **파일 수 감소 자체**는 여전히 신호가 아니다 — baseline diff 로 명시 결정을 강제할 뿐, 같은 PR 이 baseline 도 고치면 통과 | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r17) |  |
| **R18** | ⏸️ 보류 (기지 한계) | **claim-review 게이트의 위조·어휘 우회** — (a) Grok 을 돌리지 않고 손으로 채운 흔적으로 통과 (b) 트리거 어휘를 피한 seal 주장("강화했다·틈을 막았다")은 요구가 발동조차 안 함 | 둘 다 `check_claim_review_trace.py` docstring **§한계 1·3 에 이미 명시된 설계 천장**이고, AGENTS.md 가 *"fail-open 은 semantic 이라 정적 판정 불가 — 남은 방어선은 review-time claim-review"* 를 확정했다. 뮤테이션 GROK-10/11 은 그 천장의 **재확인**이지 신규 결함이 아니다. 🔴 **그래도 등재하는 이유**: "알려진 한계" 가 문서에만 있고 원장에 없으면 다음 세션이 신규 발견으로 착각해 같은 검증을 반복한다 |
| **R19** | ✅ 완료 (세션13) | **`tests/unit/verifier/test_openai_client.py` 5건이 의존성 없는 환경에서 red** — `openai==2.50.0` 은 `requirements.txt` 핀이지만 `importorskip` 가드가 없다 | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r19) |  |
| **R20** | ✅ 완료 (`#1269` — 잔여 1축 아래 명시) | **정책 19 집행면 결함 9건** — 면제 마커가 **계량되지 않는다**(창의 post-guard seal PR 10건 중 **5건이 면제로 통과**, 첫 사용은 가드 생성 **66분 후**) · **HTML 주석 안의 흔적·면제도 인정**(가드는 exit 0 인데 리뷰어에게 비가시) · **session id 재사용 무탐지**(#1245 가 #1244 의 세션 인용) · seal 어휘가 이 리포 관용구 `뮤테이션 N건 red`(단수형)를 못 잡음 · **집행면이 정책 SSOT 4곳 어디에도 없음** — 🔴 **잔여**: **잔여 = session id 재사용 무탐지**: cross-PR 상태 조회(gh) 의존이라 CI 가드 결정성을 깨 이번 범위에서 제외(기지 한계 — 다음 회고 재평가) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r20) |  |
| **R21** | ✅ 완료 (`#1261` — 사용자 결정 옵션 C) | **`#1244` 커버리지 승격이 조달 불가 언어의 auto-merge 를 영구 차단** — `unavailable_tools → incomplete` 승격으로 **css/scss·dart·powershell·protobuf** 가 영구 incomplete. `#1245` 가 스스로 *"차단 없이 가시화만"* 이라 적은 것과 정면 모순 | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r21) |  |
| **R22** | ✅ 완료 (세션13 — 설정 메타 메시지 드롭 + 실바이너리 축) | **eslint fail-closed 오탐 4건** — `ruleId:null` 을 전부 '미린트' 로 오판해 **흔한 `eslint-disable` 주석 하나로 PR 전체가 `static_analysis` 오판**. 또 10-룰 최소 config 때문에 **설정에 없는 룰을 가리키는 `eslint-disable` 주석이 severity=ERROR 오탐으로 집계돼 점수를 깎는다**(실측 재현) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r22) |  |
| **R23** | ✅ 완료 (세션13 — SessionStart 관측면, advisory) | **pre-commit 미설치를 관측하는 면이 리포 전체에 없다** — 시크릿 훅 5종이 창 **22 커밋 내내 0회 실행**. CI TruffleHog `--only-verified` 는 이 클래스를 대체하지 못한다(검증된 시크릿만 본다) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r23) |  |
| **R24** | ✅ 완료 (`#1271` + 본 sync) | **backlog 원장 자체의 정확성 6건** — R9 는 창에서 양 축 모두 해소됐는데 여전히 🟡 · ~~R2-b 의 반증 수단이 원리적으로 측정 불가(지정 API 가 영원히 404)~~ · 요약표 🔴 1건인데 실제 3건 · 회귀 가드가 원장 23행 중 5행만 본다 — 🔴 **잔여**: 잔여 = R9 상태 · 요약표 카운트 · 가드 커버리지 3건 🔴 **잔여 해소(2026-08-02)**: R9 ✅ 플립(역사 창) · 요약표 재계산(본 sync — 범위 명시 유지) · 가드 커버리지 = `#1271` 전장(whole-file) R행 legality 백스톱(실측 35행 전수 + 하한 30, 기존 5행→현재 창 18행→전장으로 확장) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r24) |  |
| **R25** | ✅ 완료 (세션13 — `check_test_count_sync` ground-truth 축, PR/main 이원 배선) | **`check_docs_sync` 는 ground truth 를 원리적으로 못 본다** — 문서 사본끼리만 대조하므로 **4지점이 함께 틀리면 항상 GREEN**(뮤테이션 실증). 유일한 수동 backstop 인 `/docs-sync` 스킬은 **통합 카운트를 상수 154 로 하드코딩** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r25) |  |
| **R26** | ✅ 완료 (세션13) | **`docs/architecture.md` 핵심 데이터 흐름 stale 2건** — 창에서 P0 로 정정한 `claude -p` 서술이 **이 문서에만 생존**(수정이 README 에만 적용됨) · `#1247` 이 STATE 최신 블록·cycle-history 어디에도 없음 | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r26) |  |
| **R27** | ✅ 완료 (`#1265`·`#1266`) | **CONTRIBUTING(양 언어)이 존재하지 않는 기계 강제를 약속** — "커버리지·pylint 점수 drift 를 pre-commit 훅이 차단" · "src/ 신규 파일 트리 등재를 훅이 강제" 둘 다 실재하지 않는다. 또 path-scoped rules 본문 sync 의무(사이클 86 Q2 **사용자 명시 결정**)가 창의 코드 PR **7건 중 6건에서 미이행** — 🔴 **잔여**: path-scoped rules 본문 sync 의무(사용자 명시 결정)가 창의 코드 PR **7건 중 6건에서 미이행** → **R43** 이 추적(발화율 100% / 이행률 0%) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r27) |  |
| **R28** | 🟡 착수 가능 | **owed 운영검증 원장이 22 PR·2 세션 종료 동안 0행** — 창의 헤드라인 봉인들(eslint/tsc/분석기 커버리지)의 **라이브 검증이 추적면 밖으로 소실**. 직전 회고 R0-2(빈 원장을 green 으로 읽음)가 **더 나쁜 형태로 재발** | 회고 P1 클러스터 12. R0-2 와 같은 뿌리이나 창이 실증을 추가했다. 🔴 **세션14 미착수 사유(정책 3)**: 완전성 축 설계(gh 의존·advisory 유지 여부)가 🔴 결정 대기 R0-2 와 같은 뿌리라 **사용자 결정 선행 필요** — R0-2 회신 시 함께 착수 |
| **R29** | ✅ 완료 (`#1258`+`#1263`) | 🔴 **`make` 이 이 머신에 없다 — CLAUDE.md 가 처방하는 게이트가 한 번도 실행된 적이 없다** — 🔴 **잔여**: 🔴 **잔여 = (a) 사용자 결정**: make 를 설치할지 | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r29) |  |
| **R30** | ✅ 완료 (`#1271` — 관측면. 잔여 = 로컬 3.12 정렬 여부 사용자 결정) | **로컬 인터프리터(3.14) ↔ CI(3.12) 이원** — 로컬 전건 통과가 CI 통과를 보장하지 않는다 | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r30) |  |
| **R31** | ✅ 완료 (`#1270` — loud fail-open 채택[deny 는 전 편집 차단 = 가드 자살, 정책 17]. 행동 23케이스 + main deny stdout freeze) | **`check_edit_allowed.py`(수정 금지 파일 보호)에 행동 커버리지가 0이고 stdin 실패가 fail-open** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r31) |  |

| **R32** | ✅ 완료 (`#1257`) | 🔴 **문서 심의 게이트가 한 번도 심의한 적 없었다** — `ANTHROPIC_API_KEY` 부재로 3 에이전트 전부 실패 → veto `warn` → exit 0 | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r32) |  |
| **R33** | ✅ 완료 (`#1257`·`#1260` + 사용자 키 설정 2026-08-01) | **(a) `ANTHROPIC_API_KEY` 를 설정할지** + ~~(b) 배너가 보이는지 미확인~~ | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r33) |  |
| **R34** | 🟡 착수 가능 | **`#1263` 본문 편집 재검증이 required check 를 실제로 갱신하지 못한다 — 첫 라이브 사용에서 반례 실측(2026-08-02)** | **기전**: `#1271` 본문 수정 → `claim-review-on-body-edit.yml` 이 같은 job 이름(`Repo integrity guards (stdlib backstop)`)으로 **success check run 을 냈으나**(같은 SHA 에 구 failure + 신 success 공존 실측) `mergeStateStatus = BLOCKED` 유지 — branch protection 이 동일명 최신 run 으로 평가한다는 `#1263` 의 설계 가정이 라이브에서 깨졌다. auto-merge 도 리포 설정 비활성이라 우회 불가 → **빈 커밋으로 새 SHA 전체 CI** 로 우회(현 유일 경로). **반증 수단**: 본문 편집 → 재검증 green 만으로 `gh pr merge` 가 통과하는 라이브 케이스 1건. **조치 후보**: (a) 재검증 워크플로가 check-runs API 로 기존 run 자체를 갱신 (b) 가드 문서에 "본문 편집 후엔 빈 커밋 필요" 명시 (c) 리포 auto-merge 설정 활성(사용자 영역) |

> **상태 요약 — 이 표(현재 창) 19행 기준**: 🟡 착수 가능 **2** · ⏸️ 보류 **1** · ✅ 완료 **16**.
> 🔴 **이 창의 결정 대기는 0건 유지**(세션14 종료 시점) — 🟡 잔여 2건 = R28(R0-2 결정 선행) · R34(신규).
> 잔여 사용자 조치(환경 설정): `make` 설치(R29) · 로컬 3.12 정렬 여부(R30-a) ·
> 🔴 **`ANTHROPIC_API_KEY` 재확인(R33-a 재개방)** — 키 설정 후에도 세션14 내내 문서 심의
> 게이트 3 에이전트가 편집마다 전건 "호출 실패"(실측 8회+) — 만료/크레딧/네트워크 재확인 필요.
> 🔴 **역사 섹션의 미해결분은 이 수치에 포함되지 않는다** — R0-2·R7 은 아래 표에 있다(합산 시 🔴 3).
> 범위를 밝히지 않은 카운트가 R24 가 지적한 "요약표 🔴 1건인데 실제 3건" 의 정체였다.
> 회귀 가드: `tests/unit/scripts/test_backlog_shape.py::test_status_summary_matches_the_table`.
>
> P2 76건(관점 중복 포함)은 보고서 본문 참조. 위 R16~R31 에 흡수되지 않는 잔여는 다음 회고에서 재평가.
> 🔴 **직전 창(2026-07-26)의 R0-2·R7 은 여전히 미해결**이며 아래 역사 섹션에 남아 있다.
> **R2-b 는 세션13(2026-08-01)에 해소** — main 브랜치 보호 적용 실측(위 R2-b 행 참조).

---

## ▶️ (역사) 다음 세션 시작점 (2026-07-26 세션9 인수인계)

**이 파일부터 읽으면 된다.**

🔴 **2026-07-26 세션9 = GitHub 정리 + owed #1072 외부계약 반증(#1218) + 5+1 회고(run `wf_47083cad-e71`·197 에이전트·확정 161).** 회고 P0-1 은 #1219 로 봉인, **P0-2 는 미처리**. P1 59건은 관점 중복 포함이라 **루트 ~15 클러스터**로 묶었다. 상세·근거는 [`_archive/reports/2026-07-26-retrospective.md`](_archive/reports/2026-07-26-retrospective.md).

| ID | 상태 | 항목 | 근거 요약 |
|----|------|------|-----------|
| **R0-2** | ✅ 완료 (`#1304` — 축 A·B. 사용자 결정 '진행'. 축 C[PR 본문 판정]는 기각 — 사유 아래) | **owed 원장 완전성 축 부재 (P0)** — 훅이 **빈 원장을 green 으로** 읽어, 부채를 등재하지 않는 것이 가장 싼 통과 경로. 창 42 PR 중 **22건이 미체크 `- [ ]` 를 단 채 머지**됐고 세는 관측자가 없다 | 지배 주제("옳은 일 하면 red")의 **쌍대**. 처방: `check_owed_verification.py` 에 완전성 축(원장 최종 커밋 이후 머지 PR 중 미체크 항목 보유분 열거 → loud, gh 부재 시 무음). 🔴 `gh` 의존·advisory 유지 설계 결정 필요 🔴 **이행 (2026-08-06)**: 두 축을 넣었다. **A(원장 자체)** — 파일 부재·읽기 실패·데이터 0행을 `판정 불가`/loud 로 인쇄한다. 이전에는 파일이 없으면 **무음 `return 0`** 이었고 비어 있으면 *"미결 0건"* 이라는 **초록 문구**가 나왔다 — 등재하지 않는 것이 가장 싼 통과 경로였던 실체가 이것이다. **B(intake 정체)** — 원장 최종 커밋 이후 머지된 PR 수를 세어 임계(10) 초과 시 loud. `git` 만 쓴다. 🔴 **`gh` 의존은 기각했다**: `gh` 부재 시 무음이면 CI·오프라인에서 **항상 무음**이라 새 fail-open 이 된다(회귀 가드가 git 외 도구 호출을 red 로 만든다). 🔴 **축 C(PR 본문을 읽어 '등재 대상인가' 판정)는 기각**했다 — (a) 산문 판정이라 오탐이 크고 이 리포는 그 형태를 이미 학습했다([[feedback-prose-guard-both-ways]]) (b) `test_pre_push_gate.py:52` 가 **CI 가드를 로컬 러너에 강제 유입**하는데 그 러너는 PR env 없이 돌아, fail-closed 로 두면 **모든 로컬 push 가 영구 red** 가 된다(정책 17 가드 자살) (c) `.github/workflows/claim-review-on-body-edit.yml` 에도 같은 step 을 복제해야 하는 패리티 가드가 있다. 세 제약은 설계 워크플로의 적대 검증·완전성 비평이 실측으로 짚었다. ⚠️ **정직 기준**: B 는 *판정이 아니라 촉구*다 — **무엇이 빠졌는지 모른다**. 정체만 관측한다. 뮤테이션 4종 red(부재 무음 되돌림 · 빈 원장 loud 제거 · 정체 축 제거 · 판정 불가를 0 으로). **advisory(exit 0) 유지** — SessionStart 를 차단하면 새 클론·CI 세션이 막힌다. |
| **R1** | 🟡 착수 가능 | **회고 보고서 아카이브 기계화** — 워크플로가 보고서를 쓰지 않아(`grep -rn '_archive/reports' .claude/workflows/*.mjs` 무결과) 카덴스 기계의 **입력이 인간 기억 의존**. 미아카이브 **3회차** | (a) `retrospective.mjs` 종료 단계가 보고서 직접 기록 (b) STATE·cycle-history 가 회고로 인용한 `wf_[0-9a-f]{8}` 은 전부 아카이브에 존재해야 한다는 가드 |
| **R2** | 🟡 부분 이행 | **정책 19 Grok CLAIM-REVIEW 집행면** — 창 42 커밋 중 **26건이 "봉인/fail-closed" 주장**인데 Grok 흔적 0. 2026-07-29 세션이 **또 재발**(Claude 가 자기 가드를 "봉인/양방향 강제" 로 단언 → Grok 이 BROKEN 판정) | ✅ **CI 게이트 신설**(`scripts/check_claim_review_trace.py` + `ci.yml` `repo-integrity`, 사용자 결정 2026-07-29): seal 어휘 감지 → 구조화된 claim-review 흔적(session/claim/verdict 값) 요구. 자기 적용 검증 = 이번 세션 PR #1228·#1229 **양쪽 차단됨**. 잔여는 아래 R2-b |
| **R2-b** | ✅ 완료 (2026-08-01 사용자 승인 —  required check + enforce_admins) | ~~**정책 19 게이트가 아직 머지를 막지 못한다**~~ — (당시) `main` 브랜치 보호 **없음**(API 실측 `Branch not protected` 404) → required status check 부재 → CI 가 red 여도 머지 가능. 🔴 **2026-08-05 현재 상태**: 보호 활성 + required check **2종**(`Repo integrity guards` + `pip-audit`, `strict: false`). 이 행의 서술은 **해소 전 상태**이며, 잔여(나머지 9 job 승격 여부)는 R45-(a) 가 정본이다 | **기전**: 게이트는 status check 로만 보이고 집행력이 없다(Grok claim-review 적발 — "guard is advisory theater"). **반증 수단**: `gh api repos/xzawed/SCAManager/branches/main/protection` 가 200 + `required_status_checks` 에 `Repo integrity guards` 포함이면 해소. 🔴 **GitHub Settings UI = 사용자 영역**(정책 12) — Claude 가 설정 불가. 부수 항목: `on.pull_request.types` 에 `edited` 미포함이라 **CI 초록 후 본문을 seal 주장으로 고쳐도 재검사 안 됨**(추가 시 본문 편집마다 전체 CI 재실행 = 비용 트레이드오프, 사용자 판단 필요 — **잔여**). 🔴 **2026-08-01 사용자 승인으로 해소**: `required_status_checks.checks = [{context: "Repo integrity guards (stdlib backstop)", app_id: 15368}]` + `enforce_admins: true` + `strict: false` 적용. 반증 수단 실측 = `gh api …/branches/main/protection` **200**. Grok claim-review `019fb94d` 로 payload 사전 검증(context 이름 = check-run name 실측 대조 · 항상 실행 job 이라 docs-only PR 도 보고 · DELETE 롤백 경로 확인). 🔴 첫 실전: #1253 이 required check 초록 후 머지. **`enforce_admins: true` 라 소유자 토큰 머지도 green 을 기다린다** — 그것이 이 설정의 요점이다. 🔴 **잔여**: CI **8 job 중 required 는 이 1개뿐** — 나머지 7 job 은 red 여도 머지 가능하다(요구 범위 확대는 사용자 결정 영역). |
| **R3** | 🟡 착수 가능 | **정책 13 smoke 0/42 발화** — 관측면 0. 🔴 실행 비용 실측 = **~5초·무자격증명**(비싸서가 아니라 아무도 안 봐서 빠졌다) | 6-step ⑤ 또는 SessionStart 에 배선 |
| **R4** | 🟡 착수 가능 | **형제 유추 외부 계약 2건 미실측 잔존** — `#1208` 의 `@` escape 는 **GitHub 에서 완전 no-op**(라이브 프로빙 반증, 멘션 인젝션 봉인은 **존재하지 않는다**) · GraphQL `expectedHeadOid` 를 `PUT /merge` `sha` 와 "동일 의미"로 단정(비동기 머지라 원리적으로 다름) | 🔴 `@` escape 는 **실사용 영향 재평가** 필요. 세션이 발명한 "가정된 계약 → 라이브 프로빙" 을 **클래스로 확장** |
| **R5** | 🟡 착수 가능 | **3-불변식에 "시점 의존 상태를 불변식으로 고정" 클래스 없음** — 한 세션에 3건 발생했는데 규칙도 탐지기도 없다 | `AGENTS.md`·`guards.md`(write-time 자동 로드 표면)에 규칙 등재. 2차 주제는 등재됐는데 지배 주제는 안 됨(비대칭) |
| **R6** | 🟡 착수 가능 | **가드 fail-open 뮤테이션 실증 6건** — repo_detail 차트/bulk i18n 가드 2건(자기 설명 주석이 단언을 만족) · arch-tree-sync · B8 이 `.claude/hooks/**` 미검 · `test_current_tree_is_in_sync` 가 실제 out-of-sync 에서 green 등 | 전부 회고가 뮤테이션으로 red 실증 |
| **R7** | ✅ 배선 종결 (`#1288`, 2026-08-05) — 🔴 **후속 R52 신설** | ~~**e2e 122건이 CI 미배선인데 README/STATE 는 "E2E 122 passing" 단언 — 실행되지 않는 초록 배지**~~ → **e2e 122건이 CI 에 배선돼 있지 않다** — 🔴 **잔여**: **잔여 = R52** 🔴 **후속 확정 (`#1291`, 2026-08-05)**: R52 진단 결과 30건 중 **환경 원인은 0건**이었다 — 로컬 Windows 에서도 같은 이름이 실패했다(CI 28 ⊂ 로컬 29) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r7) |  |
| **R8** | ✅ 완료 (2026-08-10 — 🔴 **내 1차 반증이 틀렸다. 실제 유출 경로가 있었다**) | **로그·예외 경로의 DB 자격증명 유출** | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r8) | 보안 |
| **R9** | ✅ 완료 (세션12~13 — R24 잔여 정정으로 2026-08-02 플립) | **doc_review_gate 가 cp949(Windows)에서 deny 불가** + 심의 대상 집합이 2026-07-21 재구성 이후 stale(AGENTS.md·rules/**·policies/** 전부 skip) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r9) |  |
| **R10** | 🟡 착수 가능 | **backlog/원장 정합 4건** — H2 종결 항목이 ⏸️ 로 생존 · 원장 갱신이 delta-scoped · ⏸️ 행에 반증수단 의무 미적용 · owed 파서가 첫 셀 drift 행을 조용히 버림 | |
| **R11** | 🟡 착수 가능 | **반자동 승인 경로 잔여 2건** — 결정을 GitHub 리뷰보다 **먼저** commit 해 "approve 기록됐는데 승인은 없는" 상태 고착(insert-only claim 이라 재시도 영구 차단) · telegram `HeadMovedError` 분기 **배선 테스트 0건**(삭제해도 499 테스트 green — 뮤테이션 실증) | #1218 신규 회귀 |
| **R12** | 🟡 착수 가능 | **파괴적 작업 복구 매니페스트가 세션 임시 디렉토리에만 존재** — 브랜치 49건 삭제분. 휘발성·위치 미고지 | |
| **R13** | 🟡 착수 가능 | **slither 분석기에 실바이너리 테스트 0건** — 단위 18건이 subprocess/`shutil.which` 전량 mock + 정적 JSON 픽스처. CI green 이 slither 실동작을 전혀 증명하지 않는다 | **기전**: #1226(eslint) 과 동일 클래스 — "설치는 됐는데 아무것도 분석 안 함" 이 관측 불가. eslint 에는 `tests/integration/test_eslint_analyzer.py` 실바이너리 테스트가 있으나 slither 대응물 부재. **반증 수단**: 실 slither 바이너리로 Solidity 픽스처를 분석해 ruleId 있는 진짜 이슈가 나오는지. 관련 이슈 #1238(tsc 동일 축) |
| **R14** | 🟡 착수 가능 | **`_PIP_DISTRIBUTION` 조달 가드가 문자열 존재만 확인** — `test_build_command_deps.py` 의 `{'solc-select':'slither-analyzer'}` 가 requirements.txt 에 문자열이 있는지만 보고, slither-analyzer 가 **실제로** solc-select 를 조달하는지는 검증 안 함 | **기전**: crytic-compile 이 solc-select 의존을 버리면 가드는 green 유지 → `railway.toml:3` 의 `\|\| echo WARNING` 이 실패 흡수 → `slither.py:47` `shutil.which('slither')` 는 True → solc 없이 실행 → `slither.py:101-102` 가 `[]` → Solidity 이슈 0건 무음 = 점수 인플레. 2026-07-30 실측 시점엔 crytic-compile 0.4.2 가 `solc-select>=1.0.4` 를 유지해 미발동. **반증 수단**: crytic-compile 의 solc-select 의존을 제거한 상태에서 가드가 red 가 되는지 |
| **R15** | ✅ 완료 (`#1280` — ground-truth 축 신설) | **fastapi 버전 문서 drift** — `.claude/rules/deploy.md:39` 과 `README.md` 배지가 `0.139` 인데 실제 핀은 `0.140.13`(#1233 머지 후) | 전문 → [아카이브](_archive/backlog-completed-2026-08.md#r15) |  |

> P2 100건(관점 중복 포함)은 보고서 본문 참조. 위 R1~R15 에 흡수되지 않는 잔여는 다음 회고에서 재평가.
> 🔴 **R13~R15 는 2026-07-30 dependabot 검증 워크플로(12 에이전트) 부수 발견** — GitHub 이슈로 승격한 2건은 #1238(tsc 무핀 전역설치)·#1239(trufflehog SHA 핀 실효성).

---

### (역사) 2026-07-24 세션8 인수인계

🔴 **2026-07-24 세션8 = 종합감사(#1186~1194) + P2 5클러스터(#1195~1199) + 5+1 회고(80 confirmed) + 회고 P1 이행(#1200~1201).** 상세 세션 메모리: `project-retro-2026-07-24`. **미이행 잔여**:
- **[감사 P2 — 2026-07-24 라운드 처리]**: 명확버그 6(#1204~#1208) + High-tier 설계결정 5 완료. **2026-07-24 후속**(#1210·#1211): pagination(#1210) + webhook issue-close BackgroundTask(#1211) 완료 · approve 민감경로 가드 = **오탐 정정**(대칭·개념 부재) · cron-conn = defer(서비스품질). **후속2**(#1213~#1215): 가드 self-defect 3/4 완료(B8 escape/alias·arch-tree cross-dir·wiring-coverage path-comment/tautological). **잔여 ~6** = 아래 "🟡 2026-07-23 종합감사 잔여 P2" 섹션 (스냅샷 = [`docs/_archive/reports/2026-07-23-comprehensive-review.md`](_archive/reports/2026-07-23-comprehensive-review.md)): 명확버그 1(cron double-send[dedup schema]) · SQL 집계 4 · 가드 self-defect 1(security.md 로깅 coverage — 모호, 재검증) · UI 5(정책 11 시각검증) — 남은 건 전부 schema/perf/모호가드/시각검증.
- **[회고 P1-G ✅]** = Grok claim-review 완료(#1203) — CAS 주장 REFUTED → "이중 처리 차단" overclaim 을 낙관적 동시성으로 정확화.

---

### (역사) 2026-07-19 세션4 인수인계 처방 오류 3건

🔴 **2026-07-19 세션4 전수 점검 결과 — 이 파일의 처방문 3건이 틀려 있었다.** Grok 교차 검증 완료.
처방문은 산문이 아니라 **실행될 코드**이므로, 틀린 처방은 틀린 코드와 같은 피해를 낸다:

| 항목 | 무엇이 틀렸나 |
|------|---------------|
| **B2** | 처방(`[deploy] numReplicas = 1`)이 **무효 Railway 키** — 그대로 했으면 cron P0 재현 |
| **B2-b** | 블로커 "CLI 로 확인 불가" 가 거짓 — **안 본 곳이 불가능으로 기록**됨 |
| **B6** | 근거의 **귀속이 틀림** — 자동 머지가 아니라 Claude 가 소유자 토큰으로 머지 |

**따라서 갱신 규칙에 (기전·반증수단) 의무를 추가**했다(맨 아래).

| 상태 | 건수 | 성격 |
|------|------|------|
| 🔴 결정 대기 | **1** (B6-b) | AI 자기 머지 거버넌스 (감사 High-tier 6건은 2026-07-24 서비스품질 결정 완료) |
| 🟡 착수 가능 | **1** (B7) + 감사 자율 **~6** | 명확버그 1(cron dedup) + SQL 집계 4 + 가드 self-defect 1(security.md 모호) (pagination #1210·동작1 #1211·가드 3건 #1213~#1215 완료) |
| ⏸️ 보류 | **3** (H2·H3·H4) + 감사 UI **5** | note 등급 + 상류 대기 2종(semgrep×sqlfluff click · typescript×@typescript-eslint/parser peer) + UI 템플릿(정책 11 사용자 시각검증) |

> ✅ **2026-07-24 라운드**: 감사 P2 명확버그 6 + High-tier 설계결정 5(서비스품질 위주) 처리 완료(#1204~#1208). 회고 P1-G(Grok claim-review) 이행(#1203, REFUTED→CAS 정확화). **후속(#1210·#1211)**: security_scan pagination + webhook issue-close BackgroundTask 완료 · approve 민감경로 = 오탐 정정 · cron-conn = defer. 잔여 = SQL 집계·가드 self-defect·UI(정책 11)·cron dedup(schema) — 남은 건 전부 schema/perf/meta-guard/시각검증이라 세션말미 착수 회피(회고 교훈), 다음 세션.

> 🔴 **이 표는 본문 섹션의 행 수와 일치해야 한다** — 회귀 가드
> `tests/unit/scripts/test_backlog_shape.py` 가 CI 에서 강제한다.
> 근거: 요약표 "🔴 1건" 과 본문 "_현재 없음._" 이 **정면 모순**인 채 방치됐고,
> 완료된 항목이 착수 순서 1·2위로 남아 다음 세션을 오도했다(회고 P1 12건).
> 🔴 산문은 검사하지 않는다 — **카운트 대응만** 본다(산문 린터는 그 자체가 observer-lie).

**권장 착수 순서**: **B7**(heartbeat DB, 긴급도 하향) 단독.

✅ **2026-07-20 결정 이행** — B5(회고 보고서 **복구**, 소실 선언 X) · **B6-a**(민감 경로 자동 머지 보류 가드 = 인증/마이그레이션/CI 워크플로 변경 무검토 머지 차단. 🔴 권장안이던 **브랜치 보호는 Grok 적대 검토로 반증**돼 미채택 — 사람 검토를 추가하지 않으면서 `blocked`→비재시도 태그로 자동 머지를 종결 실패시킨다).
🔴 **B6-b 는 부분 해소 — 아래 🔴 결정 대기로 재개방** (세션5 회고 P1): 민감 경로 가드는 B6-a(민감 변경의 무검토 머지)를 막지만, **비민감 PR 은 여전히 점수 60 만으로 `reviews=0` 자동 머지**된다. 'AI 가 소유자 토큰으로 자기 머지를 하는가' 라는 넓은 거버넌스 질문은 미결이다.

✅ **2026-07-19 세션4 완료분** (다음 정리 시 이 줄 제거): B1(#1114) · B2/B2-b(#1121·#1125) ·
B3(#1131) · B4(#1130) · D1(#1116) · D3(#1117) · H1(n8n 정리) ·
회고 P1 클러스터 C(#1140) · B(#1141). · **B8**(write-time fail-open floor 게이트 #1165 + **정적 탐지 천장 확정**: 강한 구문 탐지기는 Grok 2회 검증으로 오탐>진탐·실패표면 미스로 폐기, floor+guards.md write-time+review claim-review 3층이 완성. AGENTS.md 명시).

🔴 **owed 원장은 별도다** — [`runbooks/owed-verification.md`](runbooks/owed-verification.md)
안전등급 **미결 0건** — `#1062`(NULL-owner IDOR 오차단)는 **2026-07-22 회고에서 사용자 명시 결정으로
⏭️ 위험 수용 전환**(노출면[NULL-owner repo·다중 사용자]이 현재 비어 있음 + 앱계층 가드 3곳 + RLS
Phase4 검증. 재개 조건은 원장 참조). 운영등급 `#1072` 1건 (`#1075` 는 2026-07-19 20:00 UTC 실측 종결).

---

## 🟡 2026-07-23 종합감사 잔여 P2 (32건 — 스냅샷 = [아카이브 보고서](_archive/reports/2026-07-23-comprehensive-review.md))

> 회고 P1-C(2026-07-24): 감사 잔여를 메모리-only 에서 정본 원장으로 이관(observer-lie 시정). 각 행 상세·정확 line 은 아카이브 참조. 사용자 "전부 진행" 승인 하 — tier 별 처리 방식 명시.

| tier | 영역 | 항목 (요약) |
|------|------|------------|
| ✅ 완료 (2026-07-24) | **명확 버그 6/8** | ~~security_scan 403 오분류~~(#1204) · ~~secret 캐시 poison~~(#1204) · ~~telegram per-repo chat_id dead~~(#1205) · ~~issue_reg 중복 이슈 무음 폐기~~(#1207) · ~~pre-push max_tokens 절단~~(#1208) · ~~escape_markdown @-멘션~~(#1208) |
| 🟡 자율 (잔여) | **명확 버그 1** | ~~security_scan per_page=100 무페이지네이션~~(#1210 — 마지막 페이지까지 순회 + 상한 WARNING) · **cron weekly/trend 'already-sent' 미기록 double-send** (🔴 manual+scheduler 중복 발송 — dedup 기록[schema/column] 설계 필요, 단순 상수 아님. 세션말미 schema 변경 회피 — 다음 세션) |
| 🟡 자율 | **SQL 집계** (중간 위험) | cost/security-alert/trend/frequent_issues 전량 Python 로드(SQL SUM/AVG/GROUP BY 부재) + claude_api_calls·security_alert_process_logs retention GC 부재 |
| ✅ 3/4 완료 (2026-07-24) / 🟡 잔여 1 | **가드 self-defect** | ~~check_guard_fail_open B8 면제 bare-substring·aliased import 미탐~~(#1213 tokenize+import해소) · ~~check_architecture_tree_sync cross-dir substring fail-open~~(#1214 트리엔트리 경계) · ~~test_guard_wiring_coverage tautological/path-comment 오판~~(#1215 comment-strip+dead-hook 탐지) · 🟡 **잔여**: security.md 로깅 리댁션 coverage guard(`test_logging_config` 가 root/uvicorn/uvicorn.access **하드코딩 3 로거만** 검사 — 신규 propagate=False 로거 자체 핸들러가 필터 우회해도 미탐 가능성. 🔴 **모호·실재성 재검증 필요**[approve 오탐 전례], enumerate-all-propagate-false 강화 검토, 다음 세션) |
| ⏹️ 오탐 정정 / 🟡 defer | **misc** | ~~gate/actions/approve auto-approve 민감경로 가드 누락~~ = **오탐 재검증(2026-07-24)**: `approve.py`/`auto_merge.py` **양쪽 모두 동일 3가드**(static_incomplete·ai_truncated·ai_failed) 보유, **민감경로 가드 개념 자체가 두 액션 어디에도 없음** → 비대칭 아님, 수정 대상 없음(감사 오귀속). · cron notify DB 커넥션 per-repo I/O 보유 = **defer 결정(서비스품질)**: 직렬 background cron·≤6 repo·커넥션 수초 보유라 실영향 미미, 세션 lifetime refactor 는 중간위험(rollback 인터리브) — 세션말미 복잡변경 회피(회고 교훈). 다음 세션 |
| ✅/⏹️ 결정 완료 (2026-07-24, 서비스품질 위주) | **설계/동작 5** | ~~verifier band(고득점 인젝션 우회)~~=**밴드 상한 제거·보안>비용**(#1207) · ~~enqueue find-then-INSERT~~=**first-writer-wins**(#1206) · ~~webhook Closes#N base~~=**default 브랜치만 close**(#1205) · retry `attempts_count` 소진=**바운드 유지**(무한루프 위험>30회 후 abandon, 변경 안 함) · abandon_stale lost-update=**P1-5 CAS 가 이미 커버**(변경 불필요) |
| ✅ 완료 (2026-07-24) | **동작 1** | ~~webhook issue-close 동기 인라인~~(#1211 — `_close_referenced_issues` BackgroundTask 위임, N개 GitHub 왕복이 ~10s webhook 타임아웃 유발하던 것 봉인) |
| ⏸️ 정책 11 (사용자 시각검증) | **UI 템플릿** | analysis_detail·add_repo historyRestore 미배선(뒤로가기 후 버튼 죽음) · overview avg_score 0 Jinja truthiness 숨김 · dashboard chart label `\| e`→`\| tojson` · tweaks.js html data-theme desync |

## 🔴 사용자 결정 대기

| **B6-b** | **AI 자기 머지 거버넌스 (넓은 범위)** (세션5 회고 P1). B6-a(민감 경로)는 #1147 로 봉인된 반면 이 넓은 범위는 미결이다. B6-a(민감 경로)는 #1147 로 봉인됐으나, **비민감 PR 의 점수 기반 무검토 자동 머지**(6 리포 전부 `merge_threshold=60`·`reviews=0`)는 그대로다. 남은 결정 = Claude 소유자 토큰 자기 머지를 (a) 비민감도 사람 검토 요구 / (b) 현행(민감만 차단) 유지 / (c) auto-merge 임계 상향. 권장 = **(b) 현행 유지** — 민감 변경은 봉인됐고 비민감 무검토 머지는 속도 이득이 위험을 상회(사용자가 구축한 워크플로). 이견 시 회신 | 세션5 회고가 오기록(전건 이행 주장) 적발 | 🔴 사용자 결정 |

## 🟡 착수 가능 (Claude 자율)

결정 없이 진행 가능. 우선순위 순.

| # | 일감 | 근거 | 출처 |
|---|------|------|------|
| **B7** | **스케줄러 실행 기록 DB 영속화** (D4 결정) — 🔴 **긴급도 재평가 필요**: 원 근거 "로그로는 스케줄러 생존을 판정 못 한다" 는 `#1102`(로깅 복구) 이후 **약화됐다** — 실제로 `scheduler started — 6 jobs` + 60초 주기 tick 이 로그로 관측된다. DB 는 여전히 내구성·다중 인스턴스·소진성 신호 회피에 유용하나 **P0 가 아니라 durability 편의**다. D4 우선순위 재확인 권장 — job 별 `last_run_at`·`last_status` 1행 upsert 테이블 + 마이그레이션. 스케줄러 생존이 **SELECT 1회**로 판정되고, owed 원장의 소진성 신호(만료 캐시 8행 보존 제약) 없이 상시 검증 가능 | 회고1에서 **3 에이전트 독립 권고**. 로그 의존 검증 → DB 의존 검증이 이번 사고의 진짜 일반화된 교훈 | D4 결정 |

---

## ⏸️ 보류 (의도적)

| # | 일감 | 보류 사유 |
|---|------|----------|
| **H2** | **Code Scanning open alert 2건** — `py/unnecessary-lambda`(note), `tests/unit/scripts/test_guard_git_failclosed.py:26,27` | note 등급·테스트 코드. `#1096` 이 `#1097`(note 게이트)보다 먼저 머지돼 시점상 미포착 |
| **H4** | **typescript `>=6.1.0` dependabot ignore (#1232 실측)** — `package.json` 은 `^5.9.3` 핀, `.github/dependabot.yml` 이 재제안을 억제 | **기전**: `@typescript-eslint/parser` 8.x peer = `typescript >=4.8.4 <6.1.0` → typescript 7 로 올리면 `npm ci` 가 **ERESOLVE 로 실패**(빌드 자체가 깨짐). 이 파서는 런타임 eslint 분석기가 .ts/.tsx 를 파싱하는 수단이라 버리면 **TS 정적분석이 #1226 상태로 회귀**한다. 🔴 dependabot PR #1232 가 실제로 올라와 `lint-js 공허화 차단` job 이 red 로 잡았다(가드 첫 실전 적발). 🔴 **2026-07-30 실측 — 잔여 헤드룸 0**: `6.0.3` 은 통과하나 `6.1.0` 도 이미 peer 범위 밖이라(satisfies=false) #1236 머지 후 dependabot 이 제안 가능한 typescript 업데이트는 **6.0.x 패치뿐**이다. **반증 수단**: `npm view @typescript-eslint/parser peerDependencies.typescript` 가 7 을 포함하면 해제 가능 = 이 행 제거 + ignore 삭제 + `pytest tests/integration/test_eslint_analyzer.py`(.ts/.tsx 축) 통과 확인 |
| **H3** | **semgrep `>=1.171.0` dependabot ignore (#1227 항목 2)** — `requirements.txt` 는 `semgrep==1.170.1` 핀, `.github/dependabot.yml` 이 재제안을 억제 | **기전**: semgrep 1.171.0 → `click~=8.4.2` vs sqlfluff 4.2.2(최신) → `click<8.4.0` = ResolutionImpossible. SQL 정적분석 유지를 우선해 semgrep 을 보류(2026-07-29 사용자 결정). 🔴 **ignore 는 해제 조건 충족을 알려주지 않는다**(silent-disable) — 그래서 여기 등재. **반증 수단**: `pip index versions sqlfluff` 로 신버전 확인 후 `pip install --dry-run -r requirements.txt -r requirements-dev.txt` 가 성공하면 보류 해제 가능 = 이 행 제거 + dependabot ignore 삭제 |

---

## 갱신 규칙

- 일감 완료 시 ✅ 로 표시하고 **다음 정리 시 제거**(이 파일은 append-only 아님 — 현재 상태를 반영한다).
- 신규 회고·감사 종료 시 **잔여 findings 를 여기로 이관**한다. 보고서는 시점 스냅샷으로 아카이브에 남기고, "지금 뭐가 남았나" 는 이 파일이 답한다.
- 🔴 **결정 대기 항목은 다음 사이클 진입 시 사용자 회신 요청 의무**(정책 5/9 페어).
- 🔴 **모든 🟡 행은 (기전 · 반증 수단)을 함께 적는다** (2026-07-19 전수 점검 신설).
  처방문은 **실행될 코드**다 — B2 의 처방은 무효 키였고, B2-b 의 블로커는 거짓이었으며,
  B6 의 근거는 귀속이 틀렸다. 셋 다 *그럴듯한 산문*이라 검토를 통과했다.
  - **기전** = 어느 코드/설정면이 이 문제를 만드는가 (예: `src/gate/engine.py::_run_auto_merge`)
  - **반증 수단** = 이 주장이 틀렸다면 무엇을 측정하면 드러나는가
    (예: *"`mergedBy` 가 봇이 아니라 토큰 소유자면 gate auto-merge 가 아니다"*)
  - 🔴 **"봉인/불가능/전량" 류 단정은 착수 전 반증 측정 1회 의무** — 위 3건 모두 이 한 번을
    안 해서 원장에 거짓이 남았다.
  - ❌ **기계 린터는 만들지 않는다** (Grok 협의 결론) — 자유 산문의 진위는 정적 검사로 판정할
    수 없고, 구조만 검사하는 린터는 통과가 아무것도 보장하지 않아 **이 세션이 계속 다룬
    observer-lie 를 하나 더 만드는 일**이 된다. 진위 판정은 사람/Grok 의 몫으로 둔다.
