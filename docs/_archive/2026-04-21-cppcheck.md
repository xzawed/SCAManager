> ⚠️ **ARCHIVED — 2026-04-27**: 이 문서는 해당 시점에 완료된 작업을 기록한 것으로, 현재 코드베이스와 일치하지 않을 수 있습니다. 현재 상태는 [docs/STATE.md](../STATE.md)를 참조하세요.

# cppcheck (C/C++ 정적분석) Implementation Plan — Phase D.1

> **Status:** ✅ **완료** (2026-04-21). STATE.md 그룹 10 참조.
>
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **실행 환경 제약**: `pytest` + `fastapi` + `sqlalchemy` 가 import 가능하고 `make test` 가 정상 실행되는 환경(로컬 PC 또는 GitHub Codespaces)에서 수행한다. 테스트 실행 시 `.env` 프로덕션 자격증명 오염을 회피하려면 아래 래퍼 사용:
>
> ```bash
> ( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
>   mv .env .env.test-stash
>   unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
>         ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
>         GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
>         DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
>   python -m pytest tests/ -q )
> ```

**Goal:** C/C++ 전용 정적분석 도구 `cppcheck` 를 Analyzer Protocol 로 추가해 기존 Semgrep 과 독립적으로 동작하도록 한다. 신규 파일 1개(`src/analyzer/tools/cppcheck.py`) + `static.py` import 1줄 + `nixpacks.toml` aptPkgs 1줄 + 테스트 1파일로 기능 완성.

**Architecture:** `shellcheck.py` 패턴 그대로 재사용. `_CppCheckAnalyzer` 는 `Analyzer` Protocol 을 구현하고 `register()` 로 전역 REGISTRY 에 등록된다. cppcheck 바이너리가 없으면 `is_enabled()` 가 False 를 반환해 조용히 skip (예: 로컬 테스트 환경). 백엔드 데이터 모델 / API / UI / 점수 계산 로직 전부 불변.

**Tech Stack:** Python 3.12, subprocess, `xml.etree.ElementTree` (표준 라이브러리), cppcheck 2.x (apt 바이너리).

---

## File Map

| 상태 | 파일 | 역할 |
|------|------|------|
| 신규 | `src/analyzer/tools/cppcheck.py` | `_CppCheckAnalyzer` + `_parse_cppcheck_xml` + `_register_cppcheck_analyzers()` |
| 수정 | `src/analyzer/static.py` | `import src.analyzer.tools.cppcheck` 1줄 추가 (모듈 로드 시 자동 등록 트리거) |
| 수정 | `nixpacks.toml` | `aptPkgs` 에 `"cppcheck"` 추가 |
| 신규 | `tests/test_analyzer_tools_cppcheck.py` | 단위 테스트 11개 — 지원 언어 · is_enabled · XML 파싱 · subprocess mock |
| 수정 | `tests/test_analyzer_registry.py` | cppcheck 등록 여부 검증 (1~2개 추가) |
| 수정 | `CLAUDE.md` | "Analyzer tools 자동 등록" 주의사항의 예시 목록에 cppcheck 추가 (간단 1줄) |
| 수정 | `docs/STATE.md` | 지원 언어 (정적분석) 수치 증가 + 그룹 10 항목 추가 |
| 수정 | `docs/reference/language-coverage.md` | C/C++ 행 정적분석 도구 컬럼에 cppcheck 추가 + "Phase D.1 완료" 주석 + Phase D 리스크 요약 테이블 갱신 |
| (불변) | `src/analyzer/registry.py` | Analyzer Protocol · REGISTRY · register() 전부 그대로 |
| (불변) | `src/analyzer/language.py` | C/C++ 감지 규칙 이미 존재 |
| (불변) | `src/scorer/calculator.py` | CQ_WARNING_CAP=25 통합 cap 이 cppcheck 이슈도 동일 처리 |
| (불변) | `src/constants.py` | `STATIC_ANALYSIS_TIMEOUT` 상수 재사용 (30초) |

---

## Task 1: 단위 테스트 선작성 (TDD Red)

**Files:**
- Create: `tests/test_analyzer_tools_cppcheck.py`

백엔드 / API / 점수 계산 / 다른 analyzer 불변. 이 Task 는 cppcheck 모듈 존재를 가정하는 테스트를 미리 작성해 Red 상태를 확보.

- [ ] **Step 1: `tests/test_analyzer_tools_cppcheck.py` 신규 생성**

```python
"""_CppCheckAnalyzer 단위 테스트 — XML 파싱 + subprocess mock."""
from __future__ import annotations

import os
os.environ.setdefault("DATABASE_URL", "sqlite:///:memory:")
os.environ.setdefault("GITHUB_WEBHOOK_SECRET", "test_secret")
os.environ.setdefault("GITHUB_TOKEN", "ghp_test")
os.environ.setdefault("TELEGRAM_BOT_TOKEN", "123:ABC")
os.environ.setdefault("TELEGRAM_CHAT_ID", "-100123")

import subprocess
from unittest.mock import MagicMock, patch

import pytest

from src.analyzer.registry import AnalyzeContext
from src.analyzer.tools.cppcheck import _CppCheckAnalyzer, _parse_cppcheck_xml


_XML_TWO_ERRORS = """<?xml version="1.0" encoding="UTF-8"?>
<results version="2">
  <errors>
    <error id="nullPointer" severity="error" msg="Null pointer dereference">
      <location file="test.c" line="10"/>
    </error>
    <error id="variableScope" severity="style" msg="Variable scope can be reduced">
      <location file="test.c" line="25"/>
    </error>
  </errors>
</results>
"""

_XML_EMPTY = """<?xml version="1.0" encoding="UTF-8"?>
<results version="2"><errors/></results>
"""


def _ctx(language: str = "c") -> AnalyzeContext:
    return AnalyzeContext(
        filename="test.c",
        content="int main(){return 0;}",
        language=language,
        is_test=False,
        tmp_path="/tmp/test.c",
    )


# ── supports / is_enabled ───────────────────────────────────────────────

def test_supports_c_language():
    assert _CppCheckAnalyzer().supports(_ctx("c")) is True


def test_supports_cpp_language():
    assert _CppCheckAnalyzer().supports(_ctx("cpp")) is True


def test_supports_rejects_other_languages():
    a = _CppCheckAnalyzer()
    assert a.supports(_ctx("python")) is False
    assert a.supports(_ctx("shell")) is False
    assert a.supports(_ctx("unknown")) is False


def test_is_enabled_when_binary_missing():
    with patch("src.analyzer.tools.cppcheck.shutil.which", return_value=None):
        assert _CppCheckAnalyzer().is_enabled(_ctx()) is False


def test_is_enabled_when_binary_present():
    with patch("src.analyzer.tools.cppcheck.shutil.which", return_value="/usr/bin/cppcheck"):
        assert _CppCheckAnalyzer().is_enabled(_ctx()) is True


# ── _parse_cppcheck_xml ────────────────────────────────────────────────

def test_parse_xml_extracts_both_severities():
    issues = _parse_cppcheck_xml(_XML_TWO_ERRORS, language="c")
    assert len(issues) == 2
    assert issues[0].tool == "cppcheck"
    assert issues[0].severity == "error"
    assert issues[0].category == "code_quality"
    assert issues[0].language == "c"
    assert issues[0].line == 10
    assert "Null pointer" in issues[0].message


def test_parse_xml_maps_style_to_warning():
    issues = _parse_cppcheck_xml(_XML_TWO_ERRORS, language="c")
    # 두 번째 에러는 severity=style → warning 으로 매핑
    assert issues[1].severity == "warning"
    assert issues[1].line == 25


def test_parse_xml_empty_returns_empty_list():
    assert _parse_cppcheck_xml(_XML_EMPTY, language="c") == []


def test_parse_xml_missing_location_line_defaults_to_zero():
    xml = """<?xml version="1.0"?>
<results version="2"><errors>
  <error id="x" severity="warning" msg="no loc"/>
</errors></results>"""
    issues = _parse_cppcheck_xml(xml, language="cpp")
    assert len(issues) == 1
    assert issues[0].line == 0
    assert issues[0].language == "cpp"


# ── _CppCheckAnalyzer.run (subprocess mock) ─────────────────────────────

def test_run_parses_stderr_xml():
    """cppcheck 는 XML 을 stderr 에 출력 — stdout 이 비어 있어도 파싱 성공."""
    mock_result = MagicMock()
    mock_result.stderr = _XML_TWO_ERRORS
    mock_result.stdout = ""  # 의도적으로 비움
    with patch("src.analyzer.tools.cppcheck.subprocess.run", return_value=mock_result):
        issues = _CppCheckAnalyzer().run(_ctx("c"))
    assert len(issues) == 2
    assert issues[0].tool == "cppcheck"


def test_run_returns_empty_on_timeout():
    with patch(
        "src.analyzer.tools.cppcheck.subprocess.run",
        side_effect=subprocess.TimeoutExpired(cmd="cppcheck", timeout=30),
    ):
        assert _CppCheckAnalyzer().run(_ctx()) == []


def test_run_returns_empty_on_oserror():
    with patch(
        "src.analyzer.tools.cppcheck.subprocess.run",
        side_effect=OSError("not found"),
    ):
        assert _CppCheckAnalyzer().run(_ctx()) == []


def test_run_returns_empty_on_xml_parse_error():
    mock_result = MagicMock()
    mock_result.stderr = "not xml at all <<<"
    mock_result.stdout = ""
    with patch("src.analyzer.tools.cppcheck.subprocess.run", return_value=mock_result):
        assert _CppCheckAnalyzer().run(_ctx()) == []


def test_run_empty_stderr_returns_empty():
    mock_result = MagicMock()
    mock_result.stderr = ""
    mock_result.stdout = ""
    with patch("src.analyzer.tools.cppcheck.subprocess.run", return_value=mock_result):
        assert _CppCheckAnalyzer().run(_ctx()) == []
```

- [ ] **Step 2: 테스트 실행 — ImportError 로 모두 FAIL (Red)**

```bash
( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
  mv .env .env.test-stash
  unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
        ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
        GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
        DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
  python -m pytest tests/test_analyzer_tools_cppcheck.py -v --tb=line )
```

기대: `ModuleNotFoundError: No module named 'src.analyzer.tools.cppcheck'` — 11개 테스트 전체 collection error.

- [ ] **Step 3: 커밋**

```bash
git add tests/test_analyzer_tools_cppcheck.py
git commit -m "test(analyzer): cppcheck 단위 테스트 11개 선작성 (Red)

supports(c/cpp/others) · is_enabled binary presence · XML 파싱 엣지
케이스(severity 매핑·location line 0 fallback·empty) · subprocess
timeout/OSError/ParseError 처리 검증."
```

---

## Task 2: `_CppCheckAnalyzer` 구현 (TDD Green)

**Files:**
- Create: `src/analyzer/tools/cppcheck.py`
- Modify: `src/analyzer/static.py`

기존 Analyzer Protocol · REGISTRY · `analyze_file()` 로직 불변. cppcheck 모듈이 import 될 때 `_register_cppcheck_analyzers()` 가 자동 호출됨.

- [ ] **Step 1: `src/analyzer/tools/cppcheck.py` 신규 생성**

```python
"""cppcheck static analysis tool — C/C++ 전용 정적분석 (Phase D.1).

_CppCheckAnalyzer 는 Analyzer Protocol 을 구현하며 registry.register() 로
등록된다. cppcheck 바이너리가 없으면 is_enabled() 가 False 를 반환해 조용히
skip 된다. cppcheck 는 XML 결과를 stderr 에 출력하는 관례가 있어 stdout 이
아닌 stderr 를 파싱한다.
"""
from __future__ import annotations

import logging
import shutil
import subprocess  # nosec B404
import xml.etree.ElementTree as ET

from src.analyzer.registry import AnalyzeContext, AnalysisIssue, register
from src.constants import STATIC_ANALYSIS_TIMEOUT

logger = logging.getLogger(__name__)


class _CppCheckAnalyzer:
    name = "cppcheck"
    category = "code_quality"

    SUPPORTED_LANGUAGES: frozenset[str] = frozenset({"c", "cpp"})

    def supports(self, ctx: AnalyzeContext) -> bool:
        """C/C++ 파일 여부 확인."""
        return ctx.language in self.SUPPORTED_LANGUAGES

    def is_enabled(self, ctx: AnalyzeContext) -> bool:  # pylint: disable=unused-argument
        """cppcheck 바이너리 설치 여부 확인."""
        return shutil.which("cppcheck") is not None

    def run(self, ctx: AnalyzeContext) -> list[AnalysisIssue]:
        """cppcheck XML 출력을 파싱해 이슈 목록 반환.

        - stderr 가 XML 출력 채널 (cppcheck 관례)
        - --enable=warning,style,performance,portability (information 제외)
        - severity=error 만 error, 나머지(style/performance/portability)는 warning
        """
        try:
            r = subprocess.run(  # nosec B603 B607
                [
                    "cppcheck",
                    "--xml", "--xml-version=2",
                    "--enable=warning,style,performance,portability",
                    "--platform=unix64",
                    "--inline-suppr",   # 파일 내부 // cppcheck-suppress 주석 존중
                    "--quiet",
                    ctx.tmp_path,
                ],
                capture_output=True, text=True, timeout=STATIC_ANALYSIS_TIMEOUT, check=False,
            )
            if not r.stderr.strip():
                return []
            return _parse_cppcheck_xml(r.stderr, ctx.language)
        except subprocess.TimeoutExpired:
            logger.warning("cppcheck timed out for %s", ctx.tmp_path)
            return []
        except (OSError, ET.ParseError) as exc:
            logger.warning("cppcheck failed for %s: %s", ctx.tmp_path, exc)
            return []


def _parse_cppcheck_xml(xml_text: str, language: str) -> list[AnalysisIssue]:
    """cppcheck XML v2 결과를 AnalysisIssue 목록으로 변환한다.

    subprocess mock 없이 XML 픽스처만으로 검증 가능하도록 분리된 모듈 레벨 함수.
    """
    root = ET.fromstring(xml_text)  # nosec B314 — 로컬 cppcheck 가 생성한 XML
    issues: list[AnalysisIssue] = []
    for err in root.findall(".//error"):
        sev = err.get("severity", "warning")
        severity = "error" if sev == "error" else "warning"
        message = err.get("msg", "") or err.get("verbose", "") or err.get("id", "")
        line = 0
        loc = err.find("location")
        if loc is not None:
            try:
                line = int(loc.get("line", "0"))
            except (TypeError, ValueError):
                line = 0
        issues.append(AnalysisIssue(
            tool="cppcheck",
            severity=severity,
            message=message,
            line=line,
            category="code_quality",
            language=language,
        ))
    return issues


def _register_cppcheck_analyzers() -> None:
    register(_CppCheckAnalyzer())


_register_cppcheck_analyzers()
```

- [ ] **Step 2: `src/analyzer/static.py` 에 cppcheck import 추가**

기존 `import src.analyzer.tools.shellcheck` 줄 바로 아래 또는 알파벳 순서 위치에 추가:

```python
import src.analyzer.tools.cppcheck  # noqa: F401 — 모듈 로드 시 자동 등록  # pylint: disable=unused-import
```

알파벳 순서로는 `eslint` 다음, `python` 앞. 실제 배치:

```python
import src.analyzer.tools.cppcheck  # noqa: F401 — 모듈 로드 시 자동 등록  # pylint: disable=unused-import
import src.analyzer.tools.eslint  # noqa: F401 — 모듈 로드 시 자동 등록  # pylint: disable=unused-import
import src.analyzer.tools.python  # noqa: F401 — 모듈 로드 시 자동 등록  # pylint: disable=unused-import
import src.analyzer.tools.semgrep  # noqa: F401 — 모듈 로드 시 자동 등록  # pylint: disable=unused-import
import src.analyzer.tools.shellcheck  # noqa: F401 — 모듈 로드 시 자동 등록  # pylint: disable=unused-import
```

- [ ] **Step 3: 단위 테스트 실행 — 11개 PASS (Green)**

```bash
( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
  mv .env .env.test-stash
  unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
        ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
        GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
        DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
  python -m pytest tests/test_analyzer_tools_cppcheck.py -v )
```

기대: 11개 PASS.

- [ ] **Step 4: 전체 회귀 — 1110 + 11 = 1121 PASS**

```bash
( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
  mv .env .env.test-stash
  unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
        ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
        GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
        DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
  python -m pytest tests/ -q )
```

기대: 1121 passed, 0 failed.

- [ ] **Step 5: 커밋**

```bash
git add src/analyzer/tools/cppcheck.py src/analyzer/static.py
git commit -m "feat(analyzer): cppcheck C/C++ 정적분석 도구 추가 (Phase D.1)

_CppCheckAnalyzer 는 Analyzer Protocol 을 구현하고 모듈 로드 시
register() 로 REGISTRY 자동 등록. cppcheck 바이너리 없으면 is_enabled()
가 False 반환 → 조용히 skip.

- XML v2 파싱 (cppcheck 관례: stderr 출력 채널)
- --enable=warning,style,performance,portability (information 제외)
- severity=error → error, 나머지(style/performance/portability) → warning
- category='code_quality' 고정 (보안 분석은 Semgrep 담당)
- _parse_cppcheck_xml 분리로 subprocess mock 없이도 픽스처로 검증 가능"
```

---

## Task 3: Registry 등록 검증 테스트 추가

**Files:**
- Modify: `tests/test_analyzer_registry.py`

`analyze_file()` 를 한 번이라도 실행하면 cppcheck 모듈이 import 되고 `_register_cppcheck_analyzers()` 가 호출돼 REGISTRY 에 cppcheck 이 포함된다.

- [ ] **Step 1: `tests/test_analyzer_registry.py` 에 cppcheck 등록 검증 추가**

기존 test 파일 끝에 추가:

```python
def test_registry_contains_cppcheck_after_static_import():
    """src.analyzer.static 을 import 하면 REGISTRY 에 cppcheck 이 포함된다."""
    import src.analyzer.static  # noqa: F401 — registry 트리거
    from src.analyzer.registry import REGISTRY
    assert any(a.name == "cppcheck" for a in REGISTRY)


def test_cppcheck_analyzer_has_correct_attributes():
    """_CppCheckAnalyzer 의 name/category/SUPPORTED_LANGUAGES 확인."""
    from src.analyzer.tools.cppcheck import _CppCheckAnalyzer
    a = _CppCheckAnalyzer()
    assert a.name == "cppcheck"
    assert a.category == "code_quality"
    assert a.SUPPORTED_LANGUAGES == frozenset({"c", "cpp"})
```

- [ ] **Step 2: 테스트 실행 + 전체 회귀**

```bash
( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
  mv .env .env.test-stash
  unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
        ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
        GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
        DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
  python -m pytest tests/test_analyzer_registry.py -v )
```

기대: 기존 + 신규 2개 PASS.

```bash
( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
  mv .env .env.test-stash
  unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
        ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
        GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
        DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
  python -m pytest tests/ -q )
```

기대: 1123 passed (1110 + 11 신규 tools + 2 신규 registry).

- [ ] **Step 3: 커밋**

```bash
git add tests/test_analyzer_registry.py
git commit -m "test(analyzer): cppcheck Registry 등록 검증 2개 추가

analyze_file() import 시 REGISTRY 에 cppcheck 자동 포함 + 속성
(name='cppcheck', category='code_quality', SUPPORTED_LANGUAGES={c,cpp})
확인."
```

---

## Task 4: `nixpacks.toml` aptPkgs 갱신

**Files:**
- Modify: `nixpacks.toml`

배포 환경(Railway)에서 cppcheck 바이너리를 설치해 프로덕션에서 `is_enabled()` 가 True 가 되도록 한다.

- [ ] **Step 1: `nixpacks.toml` 의 `aptPkgs` 에 `"cppcheck"` 추가**

```toml
providers = ["python"]

[phases.setup]
aptPkgs = ["shellcheck", "nodejs", "npm", "cppcheck"]
```

- [ ] **Step 2: 로컬 cppcheck 설치 후 실제 바이너리로 한 번 확인**

```bash
sudo apt-get install -y cppcheck
cppcheck --version
```

기대: cppcheck 2.x 설치 성공.

**임시 C 파일로 실제 실행 확인** (선택):

```bash
printf '%s\n' 'int main(){int *p=0;return *p;}' > /tmp/nullptr.c
cppcheck --xml --xml-version=2 --enable=warning,style --platform=unix64 --quiet /tmp/nullptr.c 2>&1 >/dev/null | head -20
```

기대: `<error id="nullPointer" severity="error" ...>` 포함된 XML 출력.

- [ ] **Step 3: 테스트 전체 회귀 — 바이너리 설치 상태에서도 기존 테스트가 깨지지 않는지 확인**

```bash
( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
  mv .env .env.test-stash
  unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
        ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
        GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
        DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
  python -m pytest tests/ -q )
```

기대: 1123 passed 유지.

> **주의**: 로컬에 cppcheck 이 설치되면 `analyze_file()` 통합 테스트가 실제 이슈를 반환할 수 있다 — 기존 `tests/test_static_analysis.py` 가 C/C++ 샘플을 사용한다면 신규 이슈 수 증가로 assertion 깨질 가능성. `pytest -q` 결과에서 C/C++ 관련 테스트 실패 발생 시 Task 5 로 이관해 해당 테스트를 `cppcheck` issue 유무에 둔감하도록 수정.

- [ ] **Step 4: 커밋**

```bash
git add nixpacks.toml
git commit -m "build(nixpacks): cppcheck 를 aptPkgs 에 추가 (+30MB)

Railway 배포 이미지에 cppcheck 바이너리 설치해 프로덕션에서
_CppCheckAnalyzer.is_enabled() 가 True 가 되도록 한다. nixPkgs 배열은
추가하지 않음 (Python provider 의 nix 자동 설치 교체 함정 회피)."
```

---

## Task 5: 기존 정적분석 통합 테스트 호환성 확인

**Files:**
- Modify: `tests/test_static_analysis.py` (필요 시만)

Task 4 의 Step 3 에서 기존 C/C++ 통합 테스트가 새로 추가된 cppcheck 이슈로 깨질 수 있음. 이 Task 에서 확인하고 수정.

- [ ] **Step 1: 기존 C/C++ 통합 테스트 식별**

```bash
grep -n '\.c\|\.cpp\|language="c"\|language="cpp"' tests/test_static_analysis.py | head -20
```

기대: C/C++ 파일을 입력으로 사용하는 테스트 위치 확인.

- [ ] **Step 2: 해당 테스트를 issue 수가 아닌 "특정 tool 존재 여부" 또는 "특정 issue 존재 여부" 중심으로 수정**

예시 (테스트가 존재할 경우):

```python
# BEFORE — cppcheck 추가로 깨질 가능성
assert len(result.issues) == 3

# AFTER — cppcheck 유무와 무관하게 유지
assert any(i.tool == "semgrep" for i in result.issues)
```

- [ ] **Step 3: 해당 테스트 없으면 이 Task 스킵**

없으면 이 Task 는 "변경 없음" 으로 종료하고 커밋 생략.

- [ ] **Step 4: 전체 회귀 재확인 (변경 있었을 경우)**

```bash
( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
  mv .env .env.test-stash
  unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
        ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
        GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
        DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
  python -m pytest tests/test_static_analysis.py -v )
```

기대: 전체 PASS.

- [ ] **Step 5: 커밋 (변경 있었을 경우만)**

```bash
git add tests/test_static_analysis.py
git commit -m "test(analyzer): C/C++ 통합 테스트를 cppcheck 이슈 수 무관하게 수정

cppcheck 추가로 issue 수가 달라져도 깨지지 않도록 특정 tool/issue
존재 여부 중심으로 assertion 변경."
```

---

## Task 6: 문서 갱신 (CLAUDE.md + STATE.md + language-coverage.md)

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/STATE.md`
- Modify: `docs/reference/language-coverage.md`

- [ ] **Step 1: `CLAUDE.md` 의 "Analyzer tools 자동 등록" 항목 예시 갱신**

기존:
```markdown
- **Analyzer tools 자동 등록**: `tools/semgrep.py`, `tools/eslint.py`, `tools/shellcheck.py` 는 `analyze_file()` 에서 해당 모듈을 import할 때 자동으로 `register()` 호출. ...
```

→ 교체:
```markdown
- **Analyzer tools 자동 등록**: `tools/semgrep.py`, `tools/eslint.py`, `tools/shellcheck.py`, `tools/cppcheck.py` 는 `analyze_file()` 에서 해당 모듈을 import할 때 자동으로 `register()` 호출. 새 도구 추가 시 (1) `tools/` 아래 클래스 작성 + `register()` 호출, (2) `analyze_file()` 에서 import, (3) SUPPORTED_LANGUAGES 에 지원 언어 선언 세 단계 필수.
```

- [ ] **Step 2: `CLAUDE.md` 의 "지원 언어 (정적분석)" 표 또는 최하단 배지 줄 갱신 (해당 내용 있을 경우)**

STATE.md 를 먼저 갱신하므로 CLAUDE.md 는 "최신 수치" 한 줄만:

```markdown
최신 수치는 [docs/STATE.md](docs/STATE.md) 참조 — 단위 테스트 1123개 | E2E 49개 | pylint 10.00 | 커버리지 96.2%
```

- [ ] **Step 3: `docs/STATE.md` 수치 + 그룹 10 추가**

상단 수치 테이블:
```markdown
| 단위 테스트 | **1123개** | pytest (0 failed) |
| 지원 언어 (정적분석) | **35개+** | Semgrep 23 + ESLint 2 + ShellCheck 1 + cppcheck 1 + Python 3 도구 |
```

그룹 9 블록 아래에 추가:
```markdown
### 그룹 10 — Phase D.1 cppcheck 도구 추가 (2026-04-21)

| 작업 | 주요 내용 | 테스트 증분 |
|------|----------|-----------|
| 단위 테스트 선작성 (TDD Red) | supports/is_enabled · XML 파싱 엣지 케이스 · subprocess timeout/OSError/ParseError 처리 | +11 |
| `_CppCheckAnalyzer` 구현 | Analyzer Protocol 준수 · XML v2 stderr 파싱 · category='code_quality' 고정 · register() 자동 등록 | — |
| Registry 등록 검증 | REGISTRY 에 cppcheck 자동 포함 + 속성 확인 | +2 |
| nixpacks.toml aptPkgs | cppcheck 바이너리를 배포 이미지에 포함 (+30MB) | — |
| 문서 갱신 | CLAUDE.md · STATE.md · language-coverage.md — Phase D.1 완료 반영 | — |
```

잔여 과제 테이블에서 D.1 을 제거하거나 "완료" 표시:
```markdown
| D.1 | cppcheck | C/C++ | +30MB | ✅ 완료 (그룹 10) | 2026-04-21 |
```

- [ ] **Step 4: `docs/reference/language-coverage.md` C/C++ 행 갱신**

```markdown
| 7 | C | `.c` `.h` | Full | semgrep, **cppcheck** | — (Phase D.1 완료) |
| 8 | C++ | `.cpp` `.cc` `.cxx` `.hpp` `.hxx` | Full | semgrep, **cppcheck** | — (Phase D.1 완료) |
```

"Phase D 리스크 요약" 테이블의 🟢 행 갱신:
```markdown
| 🟢 낮음 | ~~cppcheck (C/C++)~~ ✅, slither (Solidity) | +30~100MB |
```

- [ ] **Step 5: 커밋**

```bash
git add CLAUDE.md docs/STATE.md docs/reference/language-coverage.md
git commit -m "docs: Phase D.1 cppcheck 완료 — CLAUDE.md/STATE.md/language-coverage 갱신

- CLAUDE.md: Analyzer tools 자동 등록 예시에 cppcheck 추가 + 수치 1123
- STATE.md: 단위 테스트 1110 → 1123 (+13) · 정적분석 34+ → 35+ · 그룹 10
  항목 추가 · 잔여 과제 D.1 완료 표시
- language-coverage.md: C/C++ 행 정적분석 도구 컬럼에 cppcheck 추가 ·
  Phase D 리스크 요약의 🟢 행 갱신"
```

---

## Task 7: lint + 품질 게이트 확인

**Files:** (검사만)

- [ ] **Step 1: pylint 전체 실행**

```bash
python -m pylint src/
```

기대: `10.00/10` 유지. 실패 시 수정 후 재실행.

- [ ] **Step 2: flake8 전체 실행**

```bash
python -m flake8 src/
```

기대: 0건.

- [ ] **Step 3: bandit HIGH 검사**

```bash
python -m bandit -r src/ -ll -q
```

기대: HIGH 0건. `# nosec B603 B607` (subprocess) / `# nosec B314` (XML) / `# nosec B404` (import subprocess) 주석이 적절히 적용됐는지 확인.

- [ ] **Step 4: 전체 테스트 최종 재실행**

```bash
( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
  mv .env .env.test-stash
  unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
        ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
        GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
        DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
  python -m pytest tests/ -q )
```

기대: 1123 passed, 0 failed.

- [ ] **Step 5: 변경사항 있었을 경우만 커밋**

```bash
git add -p   # pylint/flake8 수정만 포함
git commit -m "style: cppcheck 모듈 lint 정리"
```

(없으면 스킵)

---

## Task 8: 완료 3-step (push + Railway 빌드 검증)

**Files:** (push + 검증만)

- [ ] **Step 1: git push**

```bash
git push origin main
```

- [ ] **Step 2: Railway 빌드 로그 확인**

Railway 대시보드에서:
1. 새 배포가 트리거됐는지 확인
2. 빌드 로그에서 `apt-get install cppcheck` 성공 확인
3. 런타임 로그에서 `cppcheck --version` 이나 유사 확인 불가 — 배포 후 실제 C/C++ Webhook 수신 시 cppcheck 이슈 포함 여부로 검증

- [ ] **Step 3: 배포 후 실제 C/C++ 파일로 E2E 검증 (선택, 별도 리포 필요)**

테스트 리포에서 C 파일을 수정해 push:
```c
// test.c — null pointer 샘플
int main(void) {
    int *p = 0;
    return *p;
}
```

기대: SCAManager 분석 결과에 `cppcheck` tool 의 `nullPointer` (severity=error) 이슈 포함.

- [ ] **Step 4: 최종 수치 재검증**

```bash
( trap 'mv -f .env.test-stash .env 2>/dev/null' EXIT
  mv .env .env.test-stash
  unset GITHUB_TOKEN TELEGRAM_BOT_TOKEN GITHUB_WEBHOOK_SECRET DATABASE_URL \
        ANTHROPIC_API_KEY TELEGRAM_CHAT_ID API_KEY GITHUB_CLIENT_ID \
        GITHUB_CLIENT_SECRET SESSION_SECRET APP_BASE_URL \
        DATABASE_URL_FALLBACK N8N_WEBHOOK_SECRET
  python -m pytest tests/ -q )
python -m pylint src/
python -m flake8 src/
```

기대: 모든 검사 통과.

---

## Verification (E2E / 통합 체크리스트)

### 단위 수준

1. `tests/test_analyzer_tools_cppcheck.py` — 11개 PASS
2. `tests/test_analyzer_registry.py` — REGISTRY 에 cppcheck 등록 검증 2개 PASS
3. 전체 테스트 1123 passed, 0 failed

### 로컬 바이너리 수동 검증

4. `apt-get install cppcheck` 성공 (로컬 Codespaces/Ubuntu)
5. `cppcheck --version` 2.x 출력
6. `printf '%s\n' 'int main(){int *p=0;return *p;}' > /tmp/nullptr.c` 후 다음 커맨드 실행:
   ```bash
   cppcheck --xml --xml-version=2 --enable=warning,style --platform=unix64 --quiet /tmp/nullptr.c 2>&1 >/dev/null | grep nullPointer
   ```
   기대: `<error id="nullPointer" severity="error" ...>` 한 줄 출력
7. Python REPL:
   ```python
   from src.analyzer.static import analyze_file
   result = analyze_file("test.c", open("/tmp/nullptr.c").read())
   print([(i.tool, i.severity, i.message) for i in result.issues])
   ```
   기대: `('cppcheck', 'error', 'Null pointer dereference')` 포함 + Semgrep 이슈 병렬 출력

### 배포 검증

8. Railway 배포 성공 — 빌드 로그에서 `cppcheck` apt 설치 확인
9. 배포된 SCAManager 에서 C/C++ PR 분석 시 cppcheck tool 이슈 포함 여부 확인

---

## Out of Scope (의도적 제외)

스펙 `docs/superpowers/specs/2026-04-21-cppcheck-design.md` §9 의 "범위 밖" 6개 항목을 그대로 인용:

| 항목 | 제외 사유 | 권고 |
|------|----------|------|
| `--enable=information` 활성화 | 노이즈 (unused function, missing include 등) | Repo 별 설정으로 장기적 opt-in 고려 |
| `--enable=unusedFunction` | 단일 파일 분석이므로 false positive 높음 | 범위 밖 |
| MISRA C 룰셋 | 유료 또는 별도 라이선스 파일 필요 | 엔터프라이즈 티어 고민 |
| 다중 플랫폼 (`unix32`, `win64`) | 분석 시간 2~3 배 증가 | 필요 시 Repo 설정으로 추가 |
| CMake/Make 프로젝트 단위 분석 | `static.py` 는 파일 단위 분석 설계 | Phase E 이후 |
| C# 전용 도구 (Roslyn Analyzers) | Phase D 범위 외 (STATE.md 잔여 표) | 별도 Phase |

이 Task-by-Task 플랜은 단일 analyzer 추가(파일 1개 + import 1줄 + nixpacks 1줄) + 테스트 1파일 + 문서 3파일 갱신으로 **백엔드 데이터 모델 · API · UI 불변 · 다른 analyzer 독립 동작 · 기존 점수 계산 호환** 네 원칙을 모두 준수한다.
