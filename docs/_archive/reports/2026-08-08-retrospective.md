# 5+1 회고 — 세션17 (2026-08-08)

> 정책 8 정식 회고. 카덴스 트리거 발화(직전 정식 회고 이후 머지 PR **18건** ≥ 임계 15)로 진입.
> 이월 승인 기록 없음 → 회고 진입이 default.

## 범위 (기계 산출)

```
$ py -3 scripts/retro_scope.py
직전 정식 회고 : 2026-08-06-retrospective.md
경계 커밋      : e7ef341 → HEAD
머지 PR        : 18건  #1297~#1314
```

🔴 **이 회고는 자기 범위를 처음에 틀리게 알고 시작했다.** 브리프에 `17건` 이라 적었는데,
그것은 세션 시작 훅(`check_retro_cadence.py`)이 세션 **초에** 인쇄한 값이었다. 그 사이
`#1313`·`#1314` 가 머지됐다. 두 도구가 어긋난 게 아니라 **읽은 시점이 달랐고**, 그 시점 차를
없애라고 만들어 둔 `retro_scope.py` 를 진입점이 부르지 않았다(→ N-P0-5).

## 구성

| 축 | 값 |
|---|---|
| 에이전트 | **11** (5 관점 finder → 각 관점 적대 검증 5 → 완전성 비평 1) |
| verdict_coverage | **1.0** (5/5 관점 전건 cross-verify) |
| 관점 | 신규게이트 · 사고근본원인 · 문서/원장정합 · 제품영향 · 협업/프로세스 |
| 추가 검증 | Grok claim-review `019fe026` (봉인 주장 3건 적대 반증) |

---

## 이번 창이 무엇이었나

세션 중반에 사용자가 물었다 — *"반복되는 실수·검증 실패가 문서 전체 규모의 문제인가?"*
9-에이전트 진단 결론은 **총량 가설 3/10 · "규칙이 지켜지지 않는다" 8/10** 이었고,
지배 원인을 이렇게 정리했다:

> **결함을 고친 당사자가 그 결함을 검증할 관측자를 같은 PR 안에서 만들고,
> 그 관측자는 하필 그 PR 이 고친 결함에만 맹목이다.**

그 처방으로 게이트 4개(P1~P4)를 만들었다. **이번 회고의 1순위 질문은 그 게이트들이
정말 작동하는가** 였고, 답은 절반이었다.

🔴 **가장 중요한 결과**: 내가 이번 창에 만든 게이트 4개 중 **3개가 같은 형태로 결함**이었다.
면제 마커 관용구는 복제했는데 그 관용구가 이미 치른 **하드닝은 복제하지 않았다**.
즉 진단이 지목한 그 결함 형태를, 진단의 처방이 재생산했다.

---

## P0 — 5건 (전건 이번 세션 내 처리)

| ID | 결함 | 상태 |
|---|---|---|
| **N-P0-3** | 🔴 테스트 수 계기가 git 인덱스를 읽어 부푼 값을 낸다 — **`6840` 사고의 실기전** | ✅ `#1315` |
| **N-P0-1** | 신규 게이트 3종이 HTML 주석 은닉 면제를 받는다 — R20 결함 1 의 **3중 재발** | ✅ `#1315` |
| **N-P0-2** | 이월 마커가 머지 후 소멸해 "PR 초록 → main red" 를 만든다 | ✅ `#1315` |
| **N-P0-4** | STATE E2E 수치가 같은 파일·baseline·배지 4지점과 모순 | ✅ 본 PR |
| **N-P0-5** | 회고 진입점이 범위를 손 조립한다 — 정책 8 기계 산출 미이행 | ✅ 본 PR |

### N-P0-3 — `6840` 은 오판독이 아니었다

`git ls-files` 는 **머지 충돌 중인 경로를 stage 1/2/3 으로 3번** 출력한다(실측).
`tracked_docs()` 결과가 `parametrize` 2곳에 쓰이므로 충돌 `.md` 1건당 collected 가 **+4**:

```
6824 + 4×4 = 6840          ← 배치-PR 충돌 4건 상태에서 잰 값
```

그 값이 `--fix` 로 4지점에 파생 전파됐고, **사본끼리는 완벽히 일치**해 `check_docs_sync` 가
✅ 를 냈고, 머지 후 충돌 없는 CI 가 6824 를 재서 **main 이 12시간 49분 red** 였다.

🔴 이 사고는 `#1308` 에서 *"사람의 수치 오판독"* 으로 기록됐다. **그 규명이 틀렸다.**
계기가 거짓을 냈고 사람은 그 값을 정확히 읽었다. 원인 규명이 틀리면 처방도 틀린다 —
전파 차단(P2)은 두 번째 방어선이지 원인 제거가 아니었다.

### N-P0-1 — 하드닝은 복제되지 않는다

`check_claim_review_trace` 가 R20 결함 1 · Grok `019fbe32` 를 거치며 닫아 둔 축이,
새 게이트 셋 다에서 재발했다. 실측(수정 전 → 후): 은닉 면제 `True → False`,
가시 면제 `True → True`(과교정 없음).

🔴 **교훈은 "복제하지 말라" 가 아니다** — 관용구는 앞으로도 복제된다.
**"읽는 지점을 하나로 두라"** 가 맞다. `read_pr_body()` 를 부르는 한 다음 하드닝도 따라간다.

### N-P0-4 — 집행자 유무가 정확도를 갈랐다

같은 문서, 같은 세션에서:

| 축 | 집행자 | 정확도 |
|---|---|---|
| 단위/통합 테스트 수 | `check_test_count_sync` · `check_docs_sync` | **100%** |
| E2E 수 · #TBD · 델타 · 날짜 | **없음** | **8곳 오류** |

🔴 이것이 *"문제는 문서의 양이 아니라 집행 여부"* 라는 진단의 가장 깨끗한 대조 실험이다.

---

## Grok claim-review — 봉인 주장 3건 적대 반증 (`019fe026`)

내가 "봉인했다" 고 적은 세 주장을 반증하게 시켰다. **1건 BROKEN · 2건 WEAKENED.**

| claim | verdict | 지적 |
|---|---|---|
| 이월 마커 봉인 | **BROKEN** | 마커를 커밋 **본문**에 적고 **merge commit** 으로 머지하면 tip 에 마커가 없다 → PR 초록 → main red |
| 단일 리더 | WEAKENED | `os.getenv` · `"PR_" + "BODY"` · f-string 이 탐지기를 우회 |
| 인덱스 오염 | WEAKENED | *"인덱스가 아니라 트리"* 는 과장 · `split()` 이 공백 파일명을 쪼갠다 |

🔴 **BROKEN 판정은 실측으로 확인했다** — `gh api repos/xzawed/SCAManager` 가
`allow_merge_commit: true` · `allow_rebase_merge: true` 를 돌려준다. 도달 가능한 경로였다.
그리고 내 테스트는 `_git_text` 를 단일 문자열로 mock 해서 **머지 토폴로지를 한 번도
실행하지 않았다** — Grok 이 그 공백을 정확히 짚었다.

🔴 **이 창의 가장 중요한 사실**: 5+1 회고 11 에이전트가 이 결함을 **못 찾았다.**
회고는 "게이트가 작동하는가" 를 물었고 통과를 확인했다. Grok 은 *"이 봉인을 어떻게 깨는가"*
를 물었고 깼다. **질문의 형태가 결과를 갈랐다.**

---

## P1 — 원장 등재 (13건, `docs/backlog.md`)

핵심만: P1 게이트가 **신규 파일 전용 PR 을 원리적으로 통과**시킨다(되돌림이 `git rm` 이라
import 가 반드시 죽어 항상 red → 항상 pass). 🔴 **이 창의 `#1310`·`#1311`·`#1312` 자신이
그 경로로 들어왔다.** P3 관측자가 `failure` 외 결론(`timed_out`·`startup_failure`)을 전부
"초록" 으로 인쇄한다. P4 는 저장소 🔴 의 **26.0%**(290/1115)만 잰다. 정본 로컬 게이트
`pre_push_gate.py` 는 **tracked 집행면이 0**이다(유일 호출자가 git 미추적 `.git/hooks/pre-push`).

전체 목록·기전·반증 수단은 `docs/backlog.md` 신규 항목 참조.

---

## R48 판정 — 6-step ② 자기 면제

**권고: 질문을 바꿔 종결한다.** *"전체 테스트를 돌렸는가"*(자기 신고, 기계 검증 불가)가
아니라 ***"본문 숫자가 기계값에서 파생됐는가"***(기계 검증 가능)로.

- 실측: 18 PR 중 18건이 본문에 수치를 남겼고 그중 **4건이 기계값과 불일치**
  (`#1310` −1 · `#1312` −16 등).
- 산문 강화((a)안)는 기각 — 이 창이 *"예외 없음"* 산문의 무력함을 실증했다.
- 면제 신설((b)안 원안)도 기각 — 이 창은 면제 마커 신설의 대가를 이미 치렀다.
- **채택 기준**: `#1305`·`#1306`·`#1310`·`#1312` 4/4 소급 red.

🔴 **선행 조건 2건은 이미 닫혔다** — `#1315` 가 (a) 이월 마커의 PR↔push 비대칭과
(b) `_EXEMPT`/`_DEFERRED` 3종의 HTML 주석 스트리핑을 함께 봉인했다.

---

## 자유 발언 (정책 9)

### 바라는 점
게이트를 만든 세션과 그것을 검증하는 세션이 같으면 안 된다는 것이 이번 창의 교훈이다.
같은 창에서 만들고 검증하면 **내가 상상한 실패 모드만** 검증하게 된다.

### 자성
🔴 나는 이번 창에서 **"봉인" 이라는 단어를 4번 썼고 그중 1번은 거짓이었다.**
Grok 이 반증하지 않았다면 그대로 머지됐을 것이고, 다음 사람이 마커를 처음 쓰는 날
main 이 빨개졌을 것이다. AGENTS.md A2(*"새 seal 은 실경로 뮤테이션 red 없이 HOLDS 금지"*)를
지켰는데도 틀렸다 — **내 뮤테이션이 내가 상상한 실패 모드만 골랐기** 때문이다.
뮤테이션 red 는 필요조건이지 충분조건이 아니다.

또한 `6840` 을 *"내 오판독"* 으로 3회 이상 보고했다. 정직하려고 한 자기 귀책이었지만
**결과적으로 진짜 원인을 12시간 넘게 가렸다.** 자책은 정직이 아니다 — 측정이 정직이다.

### 필요한 것
새 가드/봉인 PR 에 대한 **적대적 2차 관점의 상시화**. 이번엔 내가 자발적으로 Grok 을
불렀지만, 그것이 재량이면 바쁜 창에서 가장 먼저 생략된다.

### 수정 제안
`check_claim_review_trace` 가 이미 seal 어휘를 탐지한다. 그 탐지를 **"흔적이 있는가"**
에서 **"BROKEN/WEAKENED 판정을 받고 반영했는가"** 로 한 단계 올리는 것을 제안한다.
(현재는 `verdict: SURVIVES` 한 줄로도 통과한다 — 자기 승인이 가능하다.)

---

## 🔍 회고 질문 (사용자 회신 의무 — 정책 9)

1. **가드/봉인 PR 은 Grok claim-review 를 필수로 승격할까요?** 이번 창에서 4개 중 3개가
   같은 결함이었고, 마지막 한 겹은 회고가 아니라 Grok 이 벗겼습니다.
2. **`allow_merge_commit` / `allow_rebase_merge` 를 끄고 squash 만 남길까요?**
   코드는 셋 다 지원하도록 고쳤지만, 표면이 셋이면 앞으로도 셋을 다 지켜야 합니다.
