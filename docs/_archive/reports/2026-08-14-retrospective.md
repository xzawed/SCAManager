<!-- guard-cue-quote: 이 보고서는 회고 findings 를 인용한다. 지시가 아니라 시점 기록이다. -->

# 5+1 회고 — 세션19 (2026-08-14)

> **한 문장**: 이 세션은 *"규칙을 계측기 사각으로 이주시키고 100% 를 선언했다"*.
> 문서 체계를 3층으로 재편한 것 자체는 성립하지만, **그 재편이 만든 두 층이 자기가
> 강화한 계측기의 분모 밖**이었다. 직전 회고 P0(가드 삭제가 개선으로 채점됨)와 같은
> 클래스가 **한 단계 위에서** 재생산됐다.

## 실행 제원

| 축 | 값 |
|---|---|
| run | `wf_2b615d5e-8c5` |
| 에이전트 | **179** (오류 0 · 빈 결과 0) |
| 토큰 | 14.2M · tool calls 3,513 · 3h07m |
| 라운드 | **3** (loop-until-dry) + completeness critic 표적 라운드 |
| finding | **157** → 확정 **143** · FP 기각 **14** · 심각도 조정 **51** |
| **verdict_coverage** | **1.0** (UNVERIFIED 0건) |
| 분포 | **P0 6 · P1 53 · P2 84** |
| 범위 | 직전 회고(2026-08-13) 이후 머지 PR — 브리프 **#1340~#1346** (아래 P0-B 참조) |

라운드별 신규: 39 → 38 → 35. completeness critic 이 gap 6건을 지목해 표적 라운드를 돌렸다.

---

## P0 — 2개 클래스 (6 finding 이 여기로 수렴)

### P0-A. 🔴 예산제의 분모가 4 glob 뿐 — 이 세션이 계수 밖에 마커 30개를 새로 만들었다

`CLAUDE.md` 가 발행한 **«집행자 동반 비율 28% → 100% · 무집행 0건»** 은
`check_red_budget.SURFACE_GLOBS` 네 개 위에서만 계산된다:

```python
SURFACE_GLOBS = ("CLAUDE.md", "AGENTS.md", ".claude/rules/*.md", ".claude/policies/*.md")
```

같은 마커 집합(`_RED_MARKERS`)으로 활성·비아카이브 문서를 전수 계수한 실측:

| 축 | 값 |
|---|---|
| 계수 **안** | **92건** |
| 계수 **밖** | **505건 (84.6%)** |
| 그중 **#1345 가 스스로 만든 것** | **30건** (`docs/process/**` 22 · `.claude/traps.md` 8) |

즉 *"100% 달성"* 을 선언한 그 커밋이 **계수되지 않는 필수 마커 30개를 함께 만들었다.**
그중 집행자 없는 것이 **27건**이다. `CLAUDE.md` 는 *"새 마커를 붙이려면 집행 가드를 같은
PR 에 — **예외 없음**"* 이라고 무조건문으로 적는데, 집행은 4 glob 한정이다.

🔴 **가장 값싼 우회로는 가드가 자백한 «🔴 줄만 지우기» 가 아니라 «그 줄을 `docs/process/`
로 옮겨 적기» 다.** 규칙은 독자에게 그대로 작동하고, delta 축은 무집행 **감소**를 개선으로
채점한다. 그리고 `#1340` 이 신설한 분모 축(`deleted_not_renamed`)도 같은 glob 만 보므로
**두 층을 통째로 삭제해도 발화하지 않는다**(뮤테이션 실증: 6파일 삭제 → 1463 passed ·
red-budget CI 모드 **EXIT 0**).

**«28% → 100%» 의 정체**: 분자는 **+6**(86 → 92)이고 분모가 **307 → 92** 로 줄었다.
집행이 는 것이 아니라 **분모를 뗀 것**이다. 명령형 표면 전체(597)로 재면 집행률은 **약 11%** 다.

### P0-B. 회고 범위가 손입력이라 `#1347` 이 회고를 통째로 피했다 — 2회 연속

정책 8-(5)의 범위 SSOT 는 `scripts/retro_scope.py` 다. 그 스크립트는 매 실행 말미에
*"🔴 회고 착수 **직전에** 다시 실행할 것 — 그 사이 머지분이 빠지는 것이 P0 의 기전이었다"*
를 인쇄한다. 그런데 **`.claude/workflows/retrospective.mjs` 는 그 스크립트를 한 번도
부르지 않는다** — `spawn|execSync|child_process|exec(` grep **0건**. `scope`/`context` 를
호출자 문자열로 받을 뿐이고, 집행은 `.claude/skills/retrospective.md` 의 **산문 한 줄**이다.

- 브리프 범위: **7건 #1340~#1346** (HEAD `01c58af7`)
- 기계 산출(회고 도중 재실행): **8건 #1340~#1347** (HEAD `d816f259`)

빠진 `#1347` 은 하필 **범위 안 PR(#1343)이 낸 25,402자 소실을 수습한 PR** 이고,
`claim-review-not-required` 로 자기면제했으며, 자기 §한계가 *"이 부록을 통째로 지워도 어떤
가드도 red 가 되지 않는다"* 를 자인한다. **가장 검증이 덜 된 산출물이 회고를 피한다** 는
정책 8 의 시나리오가 그대로 성립했다.

2026-08-08 회고가 **17 vs 18** 로 같은 자리에서 깨졌다. 도구는 두 번 다 옳은 값을 인쇄했고,
**그 값을 워크플로에 넣는 단계만 사람 손에 남아** 두 번 다 깨졌다.

---

## P1 — 12개 클래스 (53 finding)

| # | 클래스 | 건수 | 핵심 |
|---|---|---|---|
| **A** | red-budget 분모 사각 | 7 | P0-A 의 파생 — 우회로가 경로 이동 · 두 층 삭제 GREEN |
| **B** | **배치 이월 산술 불일치** | 6 | `STATE.md:309` 내역 합 **+16** ≠ 선언 **+5**. `#1342` 는 자기 STATE 를 이미 썼는데 이월분으로 **이중 계상**됐다. `#1344` 의 −12 는 측정값이 아니라 **잔차 역산** |
| **C** | **traps.md 클래스 수** | 6 | 5 지점에서 **3가지 값**(16·16·17·17) — **실측 22**. 전부 파일을 만든 그 PR 이 함께 썼다. `guards.md` 는 자기가 만든 **A7 을 목록에서 뺐다** |
| **D** | 죽은 경로·링크 | 4 | `#1344` 본문 *"살아있는 소비자 10파일만 갱신했다"* 가 **거짓** — 활성 문서 4곳이 소멸 폴더를 계속 가리킨다. 아카이브 56 삭제가 **죽은 링크 23개** 생성. 경로 축 관측자 **0** |
| **E** | 새 층의 관측자 0 | 4 | `docs/process/` + `traps.md` 를 통째로 지워도 전 가드 초록 |
| **F** | cycle-history 누락 | 2 | **세션18 전체(#1319~#1341, 23 PR)가 존재한 적이 없다**. 이번 세션도 8 중 4 누락 |
| **G** | 메모리 born-false | 4 | `feedback-traps-index-is-the-ssot` 가 **머지 41분 전 초안** 기준으로 쓰여 정본과 어긋난 채 매 세션 자동 주입. 삭제해도 `check_memory_refs` EXIT 0 |
| **H** | 회신 의무 미이행 | 3 | R81·R82(🔴 P0)가 등재 1회로 끝나고 6 PR 동안 재등장 0. **재기(re-raise) 기전이 0** |
| **I** | R86 확대 | 2 | 가드 개수 거짓을 **등재한 그 세션이** 같은 거짓을 3회 더 발행 — 문서 6지점 · 4개 숫자 |
| **J** | 가드 구조 사각 | 4 | `check_reverse_mutation` 은 **docs-only PR 을 원리적으로 판정 못 한다**(이 세션 8 중 2건) · 규칙 문서가 '생산 표면' 이 아님 · claim-review 가 **`verdict: BROKEN` 을 충족으로 받는다** |
| **K** | 이월 마커 검증 부재 | 2 | 존재만 검사 — 한 커밋이 **서로 모순인 이월 사유 2개**를 달고도 초록 |
| **L** | 격리 규칙 사각 | 1 | 읽기 전용 분석 에이전트가 격리 규칙 밖 — 메인 작업트리에서 `git stash` 가능(**본 회고에서 근접사고**) |

---

## 이 회고가 스스로에 대해 말하는 것

**잘 작동한 것**

- `verdict_coverage 1.0` — 157 finding 전건이 cross-verify verdict 를 받았다. 단일 패스 회고의 13/8 한계가 재발하지 않았다.
- **FP 14건 기각 · 심각도 조정 51건**. 특히 정책 13 smoke 축은 P1 로 올라왔다가 *"이미 backlog R3 에 등재된 열린 항목의 재관측이고, 본 사이클 `src/` 변경은 i18n 링크 문자열 3줄뿐이라 실피해 0"* 이라는 근거로 **P2 로 내려갔다**. 그 조정 자체가 이 리포가 원하는 규율이다.
- 교차 검증이 **초판 판정을 뒤집은 사례**가 여럿이다. `#1343` 은 초판 BROKEN 이었으나 공허성 렌즈가 *"잔여 전문은 아카이브 앵커로 도달 가능 — 소실이 아니라 가독성 결함"* 으로 반박해 WEAKENED 로 내려갔다.

**작동하지 않은 것**

- 🔴 **이 회고 자신이 P0-B 를 저질렀다.** 범위를 손으로 적었고, 그 사실을 **회고가 스스로 적발**했다. 도구가 인쇄한 경고를 저자가 안 읽은 것이 아니라 — **읽고 실행까지 했는데**(디스패치 직전 `retro_scope.py` 실행) 그 사이 `#1347` 이 머지됐다. 즉 *"직전에 실행하라"* 는 처방으로는 부족하고 **배선**이 필요하다.
- 세션 중 돌린 검증 2종(Grok `019ffb93` · 워크플로 `wf_d4c837bb-603`)이 **P0-A 를 둘 다 놓쳤다**. 둘 다 *"이 가드가 이 규칙을 집행하는가"* 를 물었고, 아무도 *"이 가드의 **분모가 어디까지인가**"* 를 묻지 않았다. suspicion 7("그 검증들이 놓친 축은?")의 답이 P0-A 다.

---

## 처리

| 항목 | 처리 |
|---|---|
| **P0-A** (분모 사각) | 이 세션에서 **수정** — `SURFACE_GLOBS` 확대 + 무집행 27건 정리 + 범위 한계 명시 |
| **P0-B** (범위 손입력) | 이 세션에서 **수정** — `retrospective.mjs` 에 `retro_scope.py` 배선 + 불일치 시 red |
| **P1 B·C·D·F·G·I** | 이 세션에서 **수정**(수치·경로·링크·이력·메모리 정합) |
| **P1 A·E·H·J·K·L** | **backlog 등재** — 구조 변경 또는 사용자 결정이 필요하다 |
| **P2 84건** | backlog 등재 없이 본 보고서가 SSOT. 다음 회고 범위에서 재부상하면 승격 |

> 🔴 **fix 는 사용자 결정이다**(정책 7·15). 이 보고서는 판정만 담고, 실제 수정은
> 사용자 사전 승인(2026-08-14 *"이후 내용도 같이 진행해서 PR후 머지 진행 승인합니다"*)에
> 근거해 별도 PR 로 진행했다.

---

## 원자료

- 워크플로 저널: `.../subagents/workflows/wf_2b615d5e-8c5/journal.jsonl` (에이전트별 반환값)
- 확정 143건 전문 + P2 84건은 저널이 SSOT다 — 본 보고서는 클래스 요약이다.
