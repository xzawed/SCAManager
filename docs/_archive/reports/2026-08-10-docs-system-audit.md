## 결론

<!-- guard-cue-quote: 이 문서는 실행 지시 어휘를 **증거로 인용**한다(그 어휘를 가진 문서를 찾아낸 감사 결과). 독자에게 내리는 지시가 아니므로 do-not-execute 표지 대상이 아니다. -->

**부분적으로 무리.** 문서의 *총량*은 문제가 아니다 — 층간 텍스트 중복은 8-gram 실측 2.2% 이하이고, 문서를 0자로 줄여도 이 세션 비용의 97%가 남는다. 무리한 것은 **집행 구조**다: 같은 리터럴 값이 4~12곳에 손유지되고(12개 사실 축 중 ground-truth 가드는 6개), 규칙이 지배한다고 적은 파일에 그 규칙이 도달하지 않으며, 이 감사에 주어진 기준선 자체가 2.8배 과대였다.

## 측정 (정정 포함)

| 축 | 값 | 비고 |
|---|---|---|
| 총량 | 199파일 · 3,563,320자 (≈1M tok) | 78.9%가 기록 문서(로드 안 됨) |
| 매 세션 고정 A | 21,373자 = **15,184 tok** | `count_tokens` 실측 |
| path-scoped rules B | 8세션 평균 5.1파일 61,915자 / 이 세션 **46,932 tok** | docs·guards 8/8, testing 7/8 = 사실상 상주. A의 1.7배 |
| 문서 편집 1회 C | **27,185 tok × 3 = 81,555 tok** | 🔴 **기준선 120,262자·34k×3 은 2.8배 과대** — `doc_review_gate.py:660-670` 이 STATE를 16,000자로 절단. STATE 지분 77%→**33.6%(토큰)/37.2%(자)** |
| 세션 비용 비중 | 게이트 $0.91 : 세션 $188.87 = **0.48%** | 리포 전체 통독해도 2.6% |
| 도달성 | 링크 76/201(38%) · 활성 미도달 59 = 죽은 6 / 타경로 22 / **진짜 고아 31** | |

## P0

1. **pylint 배지 4지점이 CI 실측과 다르다.** CI run 31395349428 = **9.99**(`pipeline.py` 1089줄 C0302), 문서는 10.00 (README.md:23·README.ko.md:23·STATE.md:23,35). 집행자 0 — `check_docs_sync.py` 는 Tests·FastAPI 배지만 파싱. `cycle-history.md:566` 이 "CI 게이트가 9.90 이라 이 drift 를 영영 못 잡는다"고 이미 2회 예언. **영향**: 다음 세션이 Phase 완료 선언에 10.00을 복사한다. **권고**: 배지 즉시 9.99 정정 + CI 로그 파싱→대조 가드. **효과**: 동일 축 3회 재발 종료.
2. **예산 가드가 거짓 집행자다.** `test_doc_review_gate.py:1436` = `"pylint" in state_section`. 오늘 실측: 창 안 pylint 7건 중 **값 행(7,773)보다 앞선 산문 1건(1,906)** → 예산을 8,000으로 낮춰도 GREEN. 2026-08-04~08-06 실제 은폐 확인. **권고**: 값 행 정규식(`\|\s*pylint\s*\|`)으로 교체 + 뮤테이션 red 실증. 🔴 **P0-1 정정 후에 할 것** — 순서를 뒤집으면 9.99를 10.00으로 기계 봉인한다.
3. **보안 규칙이 `src/config.py` 에 도달하지 않는다.** `security.md:27,50` 이 그 파일을 지배한다고 적으면서 frontmatter `paths:` 에 없다. 이번 세션 실비용: config.py 3커밋 + Grok 반증 1회, 최종 결론이 `security.md:36`(exc_text 리댁션)의 재발견. 도달성 가드 `test_rule_reachability.py:41` 은 심볼 1-핀(`WorkerSessionLocal`)이라 부류를 못 본다. **권고**: paths 1줄 추가 + 규칙 본문이 인용한 `src/**.py` 경로 역방향 유도 검사.
4. **6-step ②와 메모리가 정면 충돌.** CLAUDE.md:147 "tests/unit 전체, 예외 없음" vs 자동 로드 메모리 "전체 테스트 실행 금지(사용자 명시)". 중재 문장 리포 전체 grep **0건**. 메모리가 제시한 탈출구(`make test-fast`)는 make 부재로 실행 불가. **권고**: ②에 "금지는 `tests/` 대상, `tests/unit` 은 예외" 1줄 + 페어 테스트.
5. **claim-review 형식 계약의 도달 범위가 집행 범위보다 좁다.** required check 는 전 PR을 막는데, 형식은 `guards.md:242-262`(= `scripts/**` 편집자에게만 로드)에만 있다. `PULL_REQUEST_TEMPLATE.md:55-59` 슬롯은 활성 규범 문서에서 참조 **0건**이고, 정책 10의 `--body-file` 이 템플릿을 대체한다. 실제 피해: 이번 세션 형식 사유 재푸시 2건, 그중 1건은 **없는 session id를 형식만 맞춰 지어냄**. **권고**: CLAUDE.md 정책 19에 형식 4줄 리터럴 인라인 + `session: none (headless)` 정직한 출구 + 정책 10에 "템플릿 복사로 시작" 명시.
6. **실행형 계획 가드가 4개 배치 중 2개만 스캔.** `test_plans_are_not_executable.py:48` 베이스 하드코딩. 미커버 13파일에 "REQUIRED: task-by-task 구현" 배너 + **미체크 495스텝**(가드 계기였던 359건보다 많다). 대조군은 "9개 찾음"으로 초록. **권고**: 스코프 확대 + allowlist + 선례 형식 배너.

## P1

- **smoke 기대값 사본 stale**: `history.md:192` = `/auth/logout → /login`, 실제는 POST 전용 → `/`(14개월 전 값). CLAUDE.md:86이 "리터럴 복제 금지"라 적은 그 축. 리터럴 사본 5곳, 집행자는 "규칙 문장이 CLAUDE.md에 있는가"만 검사.
- **`workflow.md:54` 이 pylint 8.0+ 처방** — CI는 9.90. CLAUDE.md:164 에서 1-hop 목적지. 수치 제거 후 ci.yml 참조로.
- **`docs/README.md` 인바운드 0**: CI가 등재를 강제(`test_rules_and_index_coverage.py:187`)하는 허브인데 CLAUDE.md·AGENTS.md 어디에도 링크가 없다. 링크 1줄로 활성 고아 59→31.
- **pylint floor `9.90` 이 실행 4지점 리터럴 복제**, 값 일치 가드 0(`test_lint_gate_wiring.py:70` 은 플래그 존재만 확인).
- **설정 5-way 동기화가 3가지 arity**로 존재(api.md:29 "4곳" vs pipeline/ui/services "5-way"). `src/notifier/**` 편집 시 로드되는 것은 api.md 뿐 → PRESETS 누락 경로.
- **거버넌스 표면 무규칙**: `.claude/{rules,policies,agents,skills}/**`·`.github/workflows/**` 를 매칭하는 rules 0개. frontmatter 5줄로 해소.
- **활성 깨진 링크 8건**, 6건이 상대경로 버그. `api.md:80`↔`pipeline.md:79` 상호 참조가 양방향 사망.

## P2

- 🔴 마커: 집행자 동반율 3.5배 변별하나 CLAUDE.md:194가 선언한 "사고 검증" 축은 1.4배. 정의를 실측(집행자 유무)에 맞추는 편이 낫다. 예산제는 저장소 🔴의 25.2%만 재고 표면 안에서도 14.1% 무료(줄 단위 계수).
- `check_docs_sync` 초록이 보증하는 범위는 테스트 카운트뿐 — E2E·pylint·bandit·Coverage 4배지 무집행. Coverage 97%는 STATE 자체 기재로 2026-06-12 스냅샷.
- 가드 개수 표기 4판 불일치(13 / 9+4 / 7+4 / 실측 12+4). 산문에서 숫자 제거 권고.
- 회고 산출물 크기: 파일당 5,017자(6월)→91,333자(8월), 상한 규율 0. 요약/전문 분리 + `_RETRO_NAME` 정규식 동반 수정.

## 🔴 하지 말아야 할 것

1. **문서 총량 축소·아카이브 정리를 비용 근거로 추진하지 말 것.** 측정: 게이트는 세션 비용의 0.48%, 리포 전체 통독도 2.6%. `_archive` 1.88MB는 로드되지 않는다. backlog R54가 이미 총량 가설을 기각했고 독립 측정도 동일.
2. **경로 이동 금지 — 소비자 증명 미완.** `.claude/skills/*.md → <name>/SKILL.md` 제안은 소비자 3곳 중 2곳(`test_doc_review_gate.py:379`·`test_retro_scope_is_machine_derived.py:10`)이 미지목이었고, 이동 시 :379는 **초록인 채 공허해진다**. `STATE.md`·`architecture.md`·`_archive/reports/`·`docs/design/` 도 훅 하드코딩 대상(소비자 7~9 file:line).
3. **STATE.md 22불릿 삭제 제안 — 근거 부족으로 강등.** 소비자 증명이 없고, offset 이동이 16,000 예산 창 내용을 바꿔 P0-2 가드 지문 위치를 흔든다.
4. **6-step 6줄 분해 / 정책표 🔴 제거 — 보류.** 측정된 것은 길이(821자)뿐이고 이행률 개선 근거 0. 반대 방향(문장 추가)을 요구하는 P0 2건과 충돌. 정책 17 대상이므로 임의 실행 금지.
5. **impact·quality 에이전트에서 STATE 제거(절감 $0.153/세션) 및 절단 파편 제거($0.024) — 기각.** 회귀 가드 6건 재저술 대비 ROI 음수.
6. **rules 파일 축소·재배치 금지.** 층간 중복 실측 0.4%, 겹치는 것의 실체는 13줄 포인터 블록이다. 잉여가 없으므로 자르면 순손실.

## 사용자 결정 (3)

1. **P0-1 → P0-2 순서 확정 승인.** pylint 배지 9.99 정정을 먼저 머지하고, 그 다음 예산 가드를 값 행 정규식으로 교체. (순서 역전 시 거짓 값 기계 봉인)
2. **`/lint` 1회 실행으로 스킬 등록 여부 확정.** 미등록이면 `/retrospective`·`/integrity-audit` 등 6종 진입점이 끊긴 상태이며 정책 8 강제 트리거가 무력. 등록돼 있으면 이 항목 종료. *현재 확신도: 미확정 — 서브에이전트 로스터 필터링 가능성 배제 불가.*
3. **`_archive` 문자열로 제외하는 5개 가드의 처리.** `docs/_archive/reports/` 는 SessionStart 회고 카덴스를 구동하는 라이브 원장인데 이름 때문에 관측이 가장 적다. 라벨만 정정(저비용) / 원장 파일을 가드 대상으로 복귀(High tier) 중 택1.