# Phase 1 (Insight Dashboard 재설계 → MVP-B 출시) 5 PR 시리즈 회고

**기간**: 2026-05-02 (단일 작업일)
**범위**: PR #188, #189, #190, #191/192, #193 (5 PR)
**결과**: `/insights` 폐기 4종 + 신규 `/dashboard` MVP-B 출시 + 운영 sucesss
**다음 사이클**: Phase 2 (차별 KPI — Auto-merge 성공률 + AI 정합도)

본 문서는 정책 8 (다중 에이전트 회고 의무) + 정책 9 (Claude 자유 발언 의무) 적용 결과 + 사용자 ↔ Claude 합의 정리.

---

## 1. 정량 결과

| 지표 | 값 |
|------|-----|
| PR 수 | 5 (#188 ~ #193) |
| 작업 시간 | ~3 시간 (commit timestamps 13:38 → 15:33 UTC) |
| 누적 LOC | 폐기 -2175 + 신규 +1119 + redirect/nav +60 = **순 -996** |
| 단위 테스트 변동 | -3 + -6 + -12 + +19 + +3 = **+1 (1986 → 1987)** |
| 회귀 가드 신규 | +13 (`test_analytics_service_deprecations.py` 단일 모듈 누적) |
| 운영 사고 | 0건 (Railway CI 검증으로 빌드 실패 시 머지 차단) |
| pre-existing 7 fail | 5 PR 동안 미해결 보존 — Phase 2 진입 전 분류 의무 |

---

## 2. 5-에이전트 병렬 회고 결과 (정책 8)

### P0 — 반드시 다음 사이클 적용

1. **PR 4 + PR 5 통합 의무** — nav 깨진 링크 일시 노출 (PR 3 머지 ~ PR 5 머지). 신규 라우트 + redirect + nav 갱신 = 동일 PR 강제 → **정책 7 강화 (응집 단위)** 로 정책 본문 진화.

2. **누적 회귀 가드 모듈 default 화** — `tests/unit/services/test_analytics_service_deprecations.py` 가 4회 갱신 (PR 1: 2 가드 → PR 5: 13 가드 / 191 LOC). assertion 의미 분할 패턴 (`function removal` vs `route 200 차단` vs `redirect 동작`) → 가드 수명 ↑.

3. **Stripe-style sparkline 누락** — 목업 v2 의 핵심 시각 차별화 요소 (KPI 카드 4 × canvas) 가 운영 dashboard 에 부재. 사용자 발화 ("디자인 고도화") 충족 미달 → Phase 2 또는 별도 PR 에서 sparkline 4 canvas 추가 의무.

4. **검증 환류 메커니즘 부재** — 5 PR × 30+ 검증 항목 모두 사용자 응답 0건. **사용자 응답 (2026-05-02): "PR본문 검증의 경우 디테일한 검증은 없었습니다."** → **정책 2 진화 (Phase 종료 시 일괄 회신 묶음)** 로 정책 본문 진화.

5. **권장안 수용률 100% (8/8) — 사용자 응답으로 일부 무효** — 회고 시점 "검토 부담을 권장 신뢰가 가렸을 가능성" 우려 제기. 사용자 응답: *"실제 권장안이 좋은 내용이었습니다."* → 100% 수용은 권장 품질 결과로 확인. **단 정책 1 의 "옵션 표 + 장단점" 형식은 유지 의무** (다음 사이클 권장 품질 보장 안 됨).

### P1 — 권장 (사용자 전부 승인)

1. **3-PR 분할 default** (5 PR → 3 PR): 폐기 1 PR + 신규 1 PR + 전환 1 PR (정책 7 강화에 반영)
2. **저위험/고위험 결정 분리 default**: redirect 위치 = 자율, KPI 의미 = 옵션 표
3. **사용자 패턴 (`importlib + AttributeError`) default 화**: lint 우회 디렉티브 5개 → 0개
4. **Railway 빌드 stack 머지** (PR 시리즈 5회 빌드 → Phase 종료 시 1회) — Railway CI 가 보장하므로 우선순위 낮음
5. **자율 판단 보고 PR 본문 최상단 배치** → **정책 3 진화** 로 정책 본문 진화

### P2 — 참고

- PR #191/#192 중복 머지 사고 (PR 번호 확정 가드 필요)
- pre-existing 7 fail 누적 — Phase 2 진입 전 단일 PR 분류 의무
- glass 테마 + scoped 토큰 backdrop-filter 충돌 가능성 — 사용자 시각 검증 필요
- Crimson Pro fallback 순서 역전 (`Charter` 우선 — base 와 정렬 필요)
- 모드 토글 (📊/💬) Phase 3 까지 미구현

### 정책 효과 정량

| 정책 | 적용 횟수 | 사용자 검토 명시 | 효과 등급 |
|------|----------|----------------|----------|
| 정책 1 (옵션 + 장단점) | 8 | 0 (모두 🅐 일괄 승인) | **A** (사용자 응답 — 권장 품질 OK) |
| 정책 3 (자율 판단 보고) | 18 | 0 (개별 언급 0) | **C → 진화** (정책 3 진화로 PR 최상단 배치) |
| 정책 5 (사이클 종료 신호) | 5 | 5 | **A** |
| 정책 7 (PR 단위) | 5/5 | 5 | **B → 진화** (응집 단위 강화) |

---

## 3. Claude 자유 발언 결과 (정책 9) — 사용자 응답 반영

### 자성 항목 (회고 시 Claude 가 자성)

1. **PR 4+5 분리 결정 = 잘못된 판단** ✅ 정책 7 강화로 시정
2. **자율 판단 보고를 PR 본문 묻음** ✅ 정책 3 진화로 시정
3. **시각 검증 불가 명시 누락** ✅ **신규 정책 11** 으로 시정

### 사용자 응답 — 정보 비대칭 해소

1. **Railway 트래픽 / 운영 데이터** — *"Railway는 모두 성공기록만 있습니다. 한 예로 현재 Railway는 CI 검증이 있어서 실패시 빌드 수행이 되지 않습니다."* → Railway CI 가 빌드 실패 차단. 운영 사고 0건의 신뢰도 ↑.
2. **나머지 정보 비대칭 (pre-existing 7 fail / Phase 3 시점) 모두 승인** → Claude 가 다음 사이클에서 진행 결정.
3. **수정 필요 항목 5건 (정책 진화 + 신규 정책 11) 모두 승인** → 본 PR 에 반영.

---

## 4. 정책 본문 진화 (CLAUDE.md 반영)

| 정책 | 변경 내용 | 적용 위치 |
|------|----------|----------|
| **정책 11 (신규)** | UI/시각 변경 PR 본문 최상단 "🚨 Claude 시각 검증 불가" 의무 | CLAUDE.md L555+ |
| **정책 2 진화** | Phase 종료 시 일괄 회신 묶음 의무 | CLAUDE.md L (정책 11 후) |
| **정책 3 진화** | 자율 판단 보고 PR 본문 Summary 직후 배치 + 이의 가능성 ≥ 중 강조 마커 | CLAUDE.md |
| **정책 7 강화** | "PR 단위 = 응집 단위 분할" 명시 + nav/redirect/라우트 동일 PR 의무 | CLAUDE.md |

---

## 5. Phase 2 진입 전 의무 (사용자 승인)

| # | 작업 | 진행 주체 | 선행 |
|---|------|----------|-----|
| A | 정책 11 + 진화 + 회고 문서 PR | Claude (본 PR) | — |
| B | pre-existing 7 fail 분류 PR | Claude | A 머지 후 |
| C | `analysis_feedbacks` row 수 ≥10 검증 | Claude (DB 접근) 또는 사용자 (운영 SQL) | B 머지 후 또는 병렬 |
| D | Phase 2 착수 (Auto-merge 성공률 + AI 정합도 카드) | Claude + 사용자 | A/B/C 완료 후 |

---

## 6. 다음 사이클 default 패턴 (정책 진화 적용)

- 모든 PR 본문 Summary 직후에 "자율 판단 보고" 섹션 (정책 3 진화)
- UI/시각 변경 PR 은 정책 11 의 8 조합 체크리스트 의무
- PR 분할은 응집 단위 (정책 7 강화) — 사용자 인지 깨지는 변경 동일 PR
- Phase 종료 시 누적 검증 항목 단일 회신 표로 묶음 (정책 2 진화)
- 회귀 가드 누적 모듈 패턴 (`test_*_deprecations.py`) 모든 폐기 사이클 default
- 신규 service 모듈은 in-memory SQLite + 자체 fixture 패턴 (`redefined-outer-name` 허용)

---

## 부록 — 사용자 발화 인용 (정책 변경 근거 보존)

- 정책 11 신설: *"PR본문 검증의 경우 디테일한 검증은 없었습니다."*
- 정책 1 무효 항목 정정: *"실제 권장안이 좋은 내용이었습니다."*
- Railway 신뢰: *"현재 Railway는 CI 검증이 있어서 실패시 빌드 수행이 되지 않습니다."*
- 정책 진화 + Phase 2 의무 승인: *"필요한 부분은 전부 승인을 합니다. 그리고 나머지 필요한 부분도 승인합니다."*
- 회고 종료 + 다음 작업 진행: *"회고는 잘봤고 이후 다음 내용을 수행해주세요"*

(Phase 1 종료. Phase 2 사전 의무 진입.)
