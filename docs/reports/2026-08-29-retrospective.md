# 회고 2026-08-29 — `#608~#1563` (PR 6건)

범위는 기계 산출: `py -3 scripts/retro_scope.py --json --since 8d18473` → boundary `8d18473` · head `4ab5a2a0`.
5+1 다관점 · loop-until-dry 3라운드 · 전건 cross-verify(141 에이전트).

조사 질문: **실측 기반 수행 + 실측 기반 검토를 매 단계 했는데도 오류가 반복된 이유.**

## ROI

| 지표 | 값 |
|---|---:|
| 라운드 | 3 |
| 총 finding | 119 |
| verdict 커버리지 | 1.00 |
| 확정 | 103 |
| 오탐 차단 | 16 |
| 심각도 하향 | 38 |
| P0 / P1 / P2 | 0 / 34 / 69 |

심각도 하향 38건이 확정 103건의 37%다 — 1차 finder 의 심각도 판정은 3건 중 1건이 과대였다.

## P0

없음.

## P1 — 이 사이클이 만든 것

### 1. slither `success=false` → raise 가 Solidity 를 영구 차단할 수 있다

`src/analyzer/io/tools/slither.py::slither reported success=false` 로 `ValueError` 를 올리게 바꿨다(#1564, 머지됨).
그런데 `success=false` 의 주된 원인은 크래시가 아니라 **solc 부재/버전 불일치**다.

```
railway.toml   "solc-select failed — slither analyzer will be disabled"
실제 is_enabled  return shutil.which("slither") is not None      ← solc 를 안 본다
```

slither 는 pip 설치라 solc 가 없어도 `which` 를 통과한다. 즉 조달이 실패해도 **비활성화되지 않고
실행되어 전량 `incomplete`** 가 된다. 조달 실패가 「게이트가 아니라 벽」이 되는 형태이고,
`PROVISIONED_ANALYZERS` 가 없애려던 상태의 재발이다.

처방: 「도구가 크래시했다」와 「툴체인이 없다」를 가른다 — `is_enabled` 에 solc 확인을 넣거나
`error` 를 조달 축(`unavailable_tools`)으로 보낸다.

### 2. clippy 는 fail-closed 로 집계됐으나 실패한 빌드는 여전히 «이슈 0건»

새 판별식이 「빈 stdout」 하나뿐이라, stdout 이 비지 않고 compiler-message 가 0줄인 빌드 실패는
그대로 `[]` 다. 모듈에 `raise` 가 생겼다는 이유로 재고 탐지기는 clippy 를 전환 완료로 분류한다 —
**부채가 아니라 계기가 사라졌다.**

### 3. 재고 탐지기가 `raise` 하나로 「조용한 누산기」에 눈먼다

형태 4(파싱 실패를 `continue` 로 삼킴)는 축 A(모듈에 `raise` 부재)로만 잡힌다. 어댑터가 다른
이유로 `raise` 를 얻으면 그 형태가 통째로 투명해진다. `ba1e0955` 가 clippy 에서 정확히 그 일을
했다 — 픽스처 docstring 이 clippy 를 그 형태의 실물 좌표로 지목하는데 탐지기는 결함 0건이라 한다.

### 4. 잔여 우선순위가 조달과 대조된 적이 없다

| | 개수 | 목록 |
|---|---:|---|
| 이번에 닫은 것 중 **프로덕션 미실행** | 5 | buf_lint · clippy · dart_analyze · psscriptanalyzer · stylelint |
| 잔여 9개 중 **프로덕션 실행** | 5 | cppcheck · golangci_lint · hadolint · ktlint · shellcheck |

투입의 약 45%가 배포본에서 도달 불가능한 위험에 갔고, C/C++·Go·Dockerfile·Kotlin·shell 이
크래시를 «이슈 0건 · 완전» 으로 기록하는 경로는 그대로다.

### 5. W1 범위 산정이 존재하지 않는 언어 이름 위에 세워졌다

`scss`·`hcl` 은 `detect_language` 가 한 번도 내지 않는다(실제 `css`·`terraform`). `terraform` 은
semgrep 이 선언하므로 **tflint 는 「유일한 관측면」이 아니었다.** 「실측」 표기가 붙은 판정이
거짓이었고, 같은 오해가 발화 불가능한 단위 테스트 2건을 낳았다.

## P1 — 이전부터 있던 것 (요약)

- 회고 앵커가 도입 이래 성립한 적 없음 — `docs/reports/` 가 비어 있었다(**이 파일이 그것을 되살린다**)
- `pre_push_gate` 가 안 잰 축을 OK 로 인쇄 · CI 최다 실패축(본문 수치)이 로컬에서 advisory
- `pipeline.md` 가 이번 사이클이 11개 어댑터에서 틀렸다고 실측한 관용구를 아직 처방
- 이번 사이클이 켠 차단 게이트(witness-corpus)의 요구 표기가 어느 문서에도 없음
- 삭제된 문서를 가리키는 인용 48건, 2건은 개발자에게 출력됨
- `check_env_vars_sync` 가 Settings 클래스명이 어긋나면 필드 0건 위에서 영구 초록
- worktree 정리 기전이 계약 미이행 — 4.5GB, 대상의 30%가 구조적으로 도달 불가
- 메모리 층의 새 지식이 미커밋 — 한 번 유실된 층에서

## 오탐 16건 — 어떤 근거에서 나왔나

| # | 주장 | 오탐의 기전 |
|---|---|---|
| FP-7 | 「사람이 눈으로 확인할 항목」이 40건 중 37건 누락 | **부분문자열 판정.** 규칙 원문 표기만 매칭했는데 리포는 3가지 표현을 쓴다. 전수 재측정 = **56/56 (100%)** |
| FP-11 | verify.md 에 「가드 신설」 수용 기준 부재 | **grep 0건 → 부재 결론.** 조항은 다른 이름(「판정식을 쓸 때」)으로 같은 파일에 실재 |
| FP-2 | 공유 판별식에 테스트 0건 | **안 돌려보고 단언.** 뮤테이션을 실제로 걸자 2건이 죽었다 |
| FP-1·5·6·9·10·12 (6건) | 회고 앵커가 죽은 원인 | 사실은 맞고 **인과가 각각 다르게 지어졌다.** 실제 기전은 「쓰는 경로 ≠ 읽는 경로」 |
| FP-13 | 게이트 경고 후 13분 만에 머지 | **존재하지 않는 경로 인용**(`src/gate/auto_merge.py`) + 대비군이 관측이 아니라 해석 |
| FP-16 | 종료코드 4갈래라 스윕 불가 | **피해 부재.** 소비자 전원이 `== 0` 불리언이라 관측 불가능한 분기 |
| FP-8·4·3·14·15 | 각종 | 1차 사실은 맞고 **하중을 지는 추론**이 반증 |

전 16건의 공통 구조: **인용 좌표는 거의 항상 실재했다**(citation_verified). 무너진 것은
좌표가 아니라 **그 위에 세운 인과·피해·범위 서술**이다.

## 왜 「실측했는데도」 틀렸나

측정은 **그 순간 그 호스트에서 관측된 값**만 보증한다. 반복된 실패는 셋이다.

1. **우연값을 불변식으로 승격** — `exit=127`(다른 호스트 4294967295) · 「3줄」(다른 측정 2줄) ·
   「존재하지 않는 언어 이름」. 측정값을 적을 때 **무엇이 재현되는 성질이고 무엇이 이번 값인지**
   를 가르지 않았다.
2. **부재를 측정으로 오인** — grep 0건, `which` 실패를 「없다」로 읽었다. B-9(「잴 수 없다」)는
   `cargo`·`dotnet` 이 로컬에 있는데도 확인 없이 사용자 보고까지 갔다.
3. **계약을 사실로 서술** — 부분 전환 중에 「이제 이렇다」를 트리 전체에 대해 적었다.

## 다음 회고를 위한 규율

- 측정값을 코드·문서에 적을 때 **재현 조건을 함께 적거나, 값 대신 불변식을 적는다.**
- 「없다」를 결론하기 전에 **탐지기 자신을 대조군으로 검증한다**(찾을 수 있는 것을 실제로 찾는가).
- 부분 전환 중의 서술은 **계약과 사실을 명시적으로 가른다**.
- 잔여 우선순위는 **조달 축과 대조한 뒤** 정한다.

## 후속 (fix 는 사용자 결정 — 자동 수정 없음)

P1 5건이 이 사이클 산출물이고 그중 1번은 머지된 상태다. 착수 순서는 사용자 판단.
