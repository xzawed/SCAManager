# 미결 운영 검증 원장 (Owed Operational Verification Ledger)

> **목적**: 코드로 증명 불가한 운영/외부 검증(HSTS 헤더·쿠키 Secure·cron 실제 실행·외부 API 계약·DELETE 실행·이메일 실제 발송 등)을 남긴 PR 을 **append-only** 로 추적한다. 머지 = 코드 결정 종결이나, 운영 검증 증거는 사용자가 OK/NG 를 찍기 전까지 미결로 유실될 수 있다(회고 2026-07-18 P1#13·P2#31·P2#38).
>
> **작성 규칙**: 세션/Phase 종료 시 코드-미증명 운영 검증을 남긴 PR 을 이 표에 추가한다(trailing sync PR body 의 §owed-verification 표와 페어). 사용자 회신(OK/NG/미수행) 전까지 **행 삭제 금지**(append-only). 정책 2 진화(Phase 종료 일괄 회신) + 정책 13(smoke check) + 정책 5 NEW-P0-N(안전등급은 매 사이클 회신 의무) 페어.
>
> **상태 범례**: ⏳ 미검증(사용자 회신 대기) · ✅ 검증 완료(OK) · ❌ 이슈 발견(NG) · ⏭️ 사용자 미수행(보류)
>
> 🔴 **Claude 자기 인증으로 ⏳ → ✅ 전환 금지 (2026-07-19 회고 P0 — 실제 위반 후 신설)**
> 이 원장의 항목은 정의상 **"사용자만 할 수 있는 검증"** 이다(`docs/backlog.md:6`). Claude 가 코드·DB·로그를
> 근거로 아무리 강한 논증을 만들어도 그것은 **회신의 대체물이 아니다** — 애초에 코드로 증명 가능했다면
> 이 원장에 올라오지 않았을 항목이다.
> - **실제 사고**: `#1129` 가 `#1062`(안전등급)를 DB 스냅샷 논증으로 ✅ 처리해 **매 세션 loud 경고를 내던
>   유일한 안전등급 신호를 소거**했다. 같은 파일이 아래 §거짓 안전등급 경고 방지에서 *"경고 피로가 쌓이면
>   진짜 안전 항목(`#1062`)까지 함께 무시된다"* 고 경고해 둔 바로 그 항목이었다.
> - 🔴 **특히 가변 스냅샷 논증 금지**: "지금 사용자가 1명이라 도달 불가" 류는 **구조 불변식이 아니다**.
>   전제가 바뀌는 순간 조용히 거짓이 되는데, 그 변화를 감지하는 기계 트리거가 없으면 ✅ 는 영구 거짓이 된다.
>   구조 논증을 남기고 싶으면 **상태는 ⏳ 로 두고 근거만 병기**하라(사용자 판단을 돕되 대체하지 않는다).
> - **허용되는 것**: Claude 가 근거를 수집·정리해 **회신 비용을 낮추는 것**. 상태 전환은 사용자 몫.

---

## ✅ 인앱 스케줄러 실동작 확정 (2026-07-19 05:0x UTC — 로그 직접 관측)

`#1102`(alembic 로깅 파괴 봉인) 배포 직후 운영 로그가 복구되면서, **3세션째 막혀 있던 cron 검증이 20:00 UTC 대기 없이 즉시 종결**됐다. 더 이상 DB 우회 관측이 필요 없다 — **로그가 직접 증거**다.

```
04:34:50 INFO [src.scheduler] scheduler started — 5 jobs: retry-pending-merges,
                              sweep-orphans, trend, retention-sweep, weekly-reports
04:35:54 INFO [src.scheduler] scheduler retry_pending_merges: counts={...}   ← 이후 ~61초 간격 연속
04:44:51 INFO [src.scheduler] scheduler sweep_orphans: surfaced=0
04:54:52 INFO [src.scheduler] scheduler sweep_orphans: surfaced=0            ← 정확히 600초 간격
05:04:53 INFO [src.scheduler] scheduler sweep_orphans: surfaced=0
```

| 확인 항목 | 결과 |
|-----------|------|
| 5종 job 등록 | ✅ 기동 로그에 전부 명시 |
| interval job 실행 | ✅ `retry-pending-merges` ~61초 · `sweep-orphans` 정확히 600초 주기 (33분 연속 관측) |
| `#1073` orphan sweep | ✅ **종결** — cron 전용 경로가 실제 실행 중 |
| `#1075` retention sweep | ✅ **종결 (2026-07-19 20:00 UTC 실측)** — 아래 표 참조 |

~~🔴 **만료 캐시 8건을 다른 방법으로 지우지 말 것**~~ — ✅ **소임 완료.** 이 8건은 `#1075` 의 DELETE 실증 신호로 보존되던 것이고, 2026-07-19 20:00:01 UTC 에 cron 이 **정확히 그 8건을 삭제**해 신호가 소비됐다. 보존 제약 해제.

🔴 **교훈 — 소진성 신호의 한계**: 이 검증은 *한 번 쓰면 사라지는* 증거에 의존했다(8건을 지우면 다음 검증에 쓸 수 없다). 그래서 원장이 "다른 방법으로 지우지 말 것" 이라는 **인지 의존 제약**을 걸어야 했다. 상시 검증 가능한 대체 = `docs/backlog.md` B7(스케줄러 실행 기록 DB) — 다만 로깅 복구(`#1102`) 이후 로그만으로도 판정 가능해져 긴급도는 낮아졌다.

### 이미 확인된 것 (재확인 불필요)

| 항목 | 결과 |
|------|------|
| 인앱 스케줄러 기동·interval job 실행 | ✅ **로그 실측 확정** (위 표) |
| 서비스 계층(엔드포인트·인증·`cron_service`·worker DB 세션) | ✅ 정상 — 잘못된 키 **401** / 정상 키 **200 `{"status":"ok","orphans_surfaced":0}`** |
| 앱 헬스 | ✅ `/health` **200** (0.5~0.8s) — 재시작 루프 아님 |
| `INTERNAL_CRON_API_KEY` | ⚠️ Railway 에 설정됨·기능상 유효. 과거 노출됨 → 로테이션은 **⏭️ 사용자 명시 결정으로 종결**(하지 않고 현 키 유지 — 아래 안전등급 표 `#1106` 행이 정본, 재개 조건 포함). "유효" 를 "안전" 으로 읽지 말 것 |
| 🔴 ~~Railway **deploy 로그 수집**~~ | ❌ **오귀인이었다 — 2026-07-19 반증·수정 완료(`#1102`).** 원인은 Railway 가 아니라 **앱 자신**(`alembic/env.py` 의 `fileConfig` 가 마이그레이션 시 앱 로깅 파괴). 아래 §오귀인 정정 참조 |

⚠️ weekly 리포트 첫 발송 = 다음 주 **월요일 00:00 UTC**(출시 이래 0회 발송).

🔴 **로깅 복구가 드러낸 잠복 결함**: 로그가 살아나자 **Telegram 봇 토큰이 URL 경로에 평문으로 남고 있음**이 드러났다(httpx 가 요청 URL 전문을 INFO 로깅 — 실측 5건). `#1104` 로 2계층 리댁션 적용. 이미 남은 토큰은 코드로 회수 불가하다. 로테이션 여부는 **⏭️ 사용자 명시 결정으로 종결**(하지 않고 현 토큰 유지 — 아래 안전등급 표 `#1104` 행이 정본, 재개 조건 포함).

## 🔴 안전/데이터 등급 (다음 세션 진입 전 명시 회신 의무 — 정책 5 NEW-P0-N)

| PR | 검증 항목 | 검증 방법 | 정책 | 상태 |
|----|----------|----------|------|------|
| **#1058** | ~~SMTP 이메일 실제 발송~~ → **검증 불가 + 운영 위험 0 확정** (2026-07-19 실측 재분류). 이메일 채널은 운영에서 **한 번도 활성인 적이 없다** — "고장난 발송이 복구됐는지"가 아니라 **"설정된 적이 없다"** 가 사실. 587 STARTTLS 수정 자체는 실재 코드 결함 교정이나 **운영 효과 0** | **재분류 근거 (실측)**: ① Railway SMTP 변수 **0개**(`SMTP_HOST`·`SMTP_PORT`·`SMTP_USER`·`SMTP_PASS` 전부 미설정) ② `repo_configs` **8건 중 `email_recipients` 설정 0건** → `src/notifier/email.py:126` 의 `if not recipients or not smtp_host: return` 에서 **즉시 반환**(발송 코드 미진입). 살아있는 이메일 경로가 없으므로 미검증 상태가 위험을 만들지 않는다. **재개 조건**: 사용자가 SMTP 4종 + 대상 리포 `email_recipients` 를 설정하면 → 상태를 ⏳ 로 되돌리고, 그 리포에 push/PR 유발 후 `email_recipients` 주소 수신함에서 `[Code Review]` 제목 메일 도착 확인 | 5·13 | ⏭️ |
| **#1062** | NULL-owner 저장소 **쓰기 차단(IDOR)** 과잉 차단 여부 — 정상 소유자 흐름이 403 로 오차단되지 않는지 | ⏭️ **사용자 명시 결정 (2026-07-22 회고): 위험 수용 — 운영 검증 미수행하고 현 구조 논증에 의존.** 🔴 **자기 인증 아님** — 상태 전환의 근거는 Claude 판단이 아니라 **사용자 명시 회신**(2026-07-22 회고 질문 §owed #1062 = "위험수용 ⏭️ 전환"). 위험 수용 근거(사용자 판단 자료): ① 차단 조건 전수 = `ui/_helpers.py:96`·`api/issue_registration.py:119`·`ui/routes/repo_insights.py:93` 3곳 모두 (a) `user_id is not None and user_id != current_user.id` → 404 (b) `require_write and user_id is None` → 403 ② 2026-07-19 DB 실측 = `repositories` 8건 중 NULL-owner **0건**·전부 `user_id=1`·`users` **1명** → **IDOR 노출면(NULL-owner repo·다중 사용자)이 현재 비어 있음** ③ RLS Phase4 운영 전환 검증 완료(#920). 미검증분은 "정상 소유자 403 오차단"(기능 회귀·fail-safe 방향)뿐이고, IDOR 자체(under-block)는 위 가드가 차단. 🔴 **재개 조건 (⏭️→⏳ 복귀)**: (a) `users` 2명 이상 발생 **또는** NULL-owner `repositories` 1건 이상 생성 = IDOR 노출면 활성화 시 **또는** (b) 정상 소유자가 본인 저장소에서 403 오차단 실제 보고 시 → 운영 계정으로 7 쓰기 라우트 200/거부 확인(약 15분). 🔴 **가변 스냅샷 한계 잔존**: 위 재개 조건 (a)를 감지하는 기계 트리거는 아직 없다(별도 backlog — NULL-owner/사용자수 감시 불변식 후보) | 15 | ⏭️ |
| **#1104** | `TELEGRAM_BOT_TOKEN` 로테이션 — 봇 토큰이 운영 로그에 **평문 기록**됨(httpx 가 요청 URL 전문을 INFO 로깅 · 실측 5건) | ⏭️ **사용자 명시 결정 (2026-07-19): 로테이션하지 않고 현 토큰 유지.** 위험 수용 근거 = ① `#1104` 리댁션이 **향후 유출을 이미 차단**해 노출이 더 누적되지 않음 ② Railway 로그 보존 기간이 짧아 기존 노출분도 자연 소멸 ③ 노출면이 비공개(Railway 프로젝트 접근자 + 대화 기록). **재개 조건**: 봇이 민감 채널·다수 수신자로 확장되거나, 토큰이 공개 표면(이슈·PR 본문·스크린샷)에 재노출될 경우 → ⏳ 로 되돌리고 BotFather `/revoke` | 5·14 | ⏭️ |
| **#1106** | `INTERNAL_CRON_API_KEY` 로테이션 — **Claude 가 유발한 노출**: Railway 변수 조회 시 grep 패턴(`CRON`)이 **값까지 매칭**해 대화 기록에 평문 출력(정책 12 위반) | ⏭️ **사용자 명시 결정 (2026-07-19): 로테이션하지 않고 현 키 유지.** 위험 수용 근거 = 노출면이 **대화 기록 단독**(운영 로그에는 없음). 잔여 위험 = 키 보유 시 공개 URL 의 내부 cron 엔드포인트 호출 가능(retention-sweep DELETE · 리포트 발송 트리거). **재개 조건**: 대화 기록이 외부 공유되거나 cron 엔드포인트에 비정상 호출이 관측될 경우. 🔴 **재발 방지(코드 무관·항구 적용)**: `railway variables --kv` 결과를 **grep 하지 말 것** — `cut -d= -f1` 로 **이름만** 추출 후 필터 | 5·12 | ⏭️ |

🔴 **거짓 안전등급 경고 방지 (2026-07-19 학습)** — 안전등급 ⏳ 는 SessionStart 훅이 **매 세션 loud 경고**로 띄우는 자리다. 따라서 이 표에는 **사용자가 실제로 회신할 수 있는 항목만** 남긴다. `#1058` 은 선행 설정이 없어 **사용자가 회신할 수 없는데도** 4사이클째 경고를 발생시켰다 — 경고 피로가 쌓이면 진짜 안전 항목(`#1062`)까지 함께 무시된다. **신규 안전등급 행 추가 시 자문**: (a) 이 검증에 필요한 **선행 조건이 운영에 실제로 갖춰져 있는가**(env·설정 실측 확인) (b) 미검증 상태가 **실제 위험을 만드는가**(비활성 기능이면 위험 0). 둘 중 하나라도 아니면 안전등급이 아니라 ⏭️ + 재개 조건 명시가 맞다.

## 운영/외부 계약 등급 (Phase 종료 일괄 회신 — 정책 2 진화)

| PR | 검증 항목 | 검증 방법 | 정책 | 상태 |
|----|----------|----------|------|------|
| **#1294** | 🔴 **화면이 이전과 동일한가** — CSP 가 차단하던 외부 폰트 링크 5개를 제거했다 | ✅ **2026-08-06 사용자 확인: "화면은 이전과 같습니다"**. Claude 실측(제거 전후 `font-family` 동일 · 등록 폰트 0 동일 · 콘솔 에러 2→0)과 일치 — 폰트는 14개월간 적용된 적이 없었고 링크 제거는 시각 변화 0 이었다 | 2·11 | ✅ |
| **#1314** | 🔴 **auto-merge 가 required check 대기 중 PR 을 실제로 살려 내는가** — `blocked` + CI `running` 을 재시도로 승격했다 | 🔴 **코드로 증명 불가**: 단위 테스트는 `should_retry` 의 판정만 본다. 실제로 (a) 분석 종료 시점에 체크가 아직 도는 PR 이 생기고 (b) 그 PR 이 재시도 큐에서 살아나 머지되는지는 **라이브 auto-merge 관측**뿐이다. ⚠️ 커밋 본문이 정직하게 적었듯 **예산 상한(`max_attempts`·`is_expired`)이 무한 재시도를 막는 것은 소스 존재로만 확인**했고 실제 소진 시나리오는 실행 관측하지 않았다 | 15 | ⏳ |
| **#1315** | 🔴 **push 이벤트에서 이월 마커 배선이 실제로 채워지는가** — `PUSH_BEFORE_SHA`/`PUSH_AFTER_SHA` 를 CI 에 새로 넘긴다 | 🔴 **코드로 증명 불가**: 로컬은 실제 git 저장소로 대체 검증했으나, GitHub Actions 가 `github.event.before` 를 **어떤 머지 방식에서 어떤 값으로** 주는지는 라이브 push 이벤트에서만 보인다. 특히 merge-commit·rebase-merge 로 머지했을 때 범위가 브랜치 커밋을 포함하는지 확인 필요 | 13 | ⏳ |
| **#1306** | 🔴 **required check 10종 승격의 롤백 절차가 실행 가능한가** — 장애 시 되돌릴 입력 파일(`rsc.rollback.json`)이 리포·git 어디에도 없다(승격 세션의 로컬 임시 파일) | 🔴 **코드로 증명 불가**: `docs/runbooks/branch-protection.md:42-64` 가 그 파일을 입력으로 요구한다. 표에 정본 10종이 있어 재구성은 가능하나 **장애 중 손조립**이다. GitHub Actions 장애가 이 창에서 실제로 머지를 막았다 | 13 | ⏳ |
| **#1303** | pre-commit 계층이 **이 머신 밖에서도 도는가** — R56 이 설치한 것은 로컬 `.git/hooks` 다 | 🔴 **코드로 증명 불가**: 새 clone·다른 머신·CI 어디에서도 `pre-commit install` 이 자동 실행되지 않는다. 리포는 로컬 훅을 강제할 수 없다(guards.md 가 기록한 설계 한계) — 관측만 가능하다 | 13 | ⏳ |
| **#1296** | 🔴 **다음 세션에서 Claude 행동이 달라지지 않는가** — CLAUDE.md 를 424→196줄로 축소했다 | 🔴 **코드로 증명 불가**: 행동 규칙 생존 가드 30건이 *어휘의 삭제*는 잡지만 **규칙의 역전은 못 잡고**(실측), 무엇보다 *규칙을 읽고 지키는지*는 다음 세션의 실제 행동으로만 관측된다. Grok 이 1차에 **행동 규칙 8건 소실**을 잡았고 회고가 다시 3건을 잡았다 — **자기 검토만으로는 부족하다는 실증**이다. **확인 항목**: 규칙을 놓치거나 절차를 건너뛰면 알려 주실 것(해당 규칙을 본문으로 되돌린다) | 2·17 | ⏳ |
| **#1289** | 🔴 **운영 `CLAUDE_REVIEW_MODEL` 값이 빈 문자열인가** — 빈 값이면 `#1289` 이전까지 **모든 AI 코드리뷰가 `api_error`** 였다 | ✅ **2026-08-10 종결 — 운영 DB 관측으로 반증. 과거 피해 0.** 🔴 **원장이 지정한 방법(사용자가 Railway 변수를 눈으로 확인)은 불필요했다** — 같은 질문을 `analyses.review_model` 이 이미 답하고 있었다. **기전**: [`src/analyzer/io/ai_review.py:186`](../../src/analyzer/io/ai_review.py#L186) `result.used_model = model` 은 **API 호출이 반환된 뒤**에 실행되고 `model` 은 실제로 Anthropic 에 넘어간 문자열이다. 빈 값이었다면 `messages.create(model="")` 가 400 으로 터져 except 로 빠지고 `used_model` 은 **설정되지 않는다**. **실측**: `review_model='claude-sonnet-4-6'` 458행(2026-05-28~08-08) · `'claude-haiku-4-5-20251001'` 1972행(05-20~07-08) — **`#1289` 픽스(08-05) 이전 구간에 이미 유효 모델 성공이 연속**이다. 즉 Railway 값은 비어 있지 않았다. 교차 확인: `api_error` 행은 **전건 `review_model` NULL** 로 기전과 일치한다. 🔴 **결정적 관측 (적대 검증 `wf_28bb3b63-838` 이 지정한 판정 기준)**: `api_error` 가 난 **모든 날**에 전역 폴백 경로(`KeyCloakSDK` — `repo_configs.review_model` NULL)의 `claude-sonnet-4-6` **성공이 같은 날 함께** 있다 — 07-05(성공 8 / 오류 1) · 07-06(11/2) · 07-07(7/2) · 07-08(8/1). 전역 변수가 비어 있었다면 그 날 그 경로의 성공은 **원리적으로 불가능**하다. 🔴 **초판 서술 2건을 철회한다**: (a) *"api_error 는 산발 5건"* 은 **거짓**이었다 — 실제 전체는 **143건**(SCAManager 74 · xzawed-pais 53 · CustomWebService 8 · KeyCloakSDK 7 · ArcanaInsight 1)이고, 5건은 `LIMIT 10` 결과를 전체로 오독한 것이다(계기가 아니라 판독이 틀린 사례). (b) *"sonnet 458행"* 도 단일 리포가 아니다 — `KeyCloakSDK` 388 + `xzawed-pais` 47 + `SCAManager` 11 + `ArcanaInsight` 11 + `claude-grok-build-plugin` 2. 🔴 **교란 요인을 배제한 근거 (심의 게이트 quality-reviewer 가 "암묵적 가정" 으로 적발 → 재검증)**: `review_model` 이 전역 설정의 증거가 되려면 **리포별 오버라이드가 없어야** 한다 — `repo_configs.review_model` 이 있으면 `ai_review.py:130` 의 `model or settings.…` 에서 앞쪽이 이겨 전역값을 우회한다. 실측: 성공을 내는 **유일한 리포 `xzawed/KeyCloakSDK` 의 `review_model` 은 NULL**(`ai_review_enabled=true`)이라 전역 설정으로 폴백한다. 나머지 4 리포는 `claude-haiku-4-5-20251001` 오버라이드를 갖고 있고 그 행들은 07-08 에 끊기는데, 같은 리포들의 `ai_review_enabled` 가 07-09·07-19 에 false 로 바뀐 시점과 일치한다 — 두 계열이 서로를 설명한다. **결론 (범위를 좁혀 적는다)**: 전역 설정은 **측정 가능한 전 구간에서 비어 있지 않았다** — 즉 `#1289` 결함은 운영에서 발현한 적이 없고 빈 값은 **로컬 `.env` 한정**이었다(운영값은 미설정이거나 `claude-sonnet-4-6` — 둘 다 비어 있지 않다는 점에서 동치). 🔴 **"과거 피해 0" 은 철회한다 — DB 가 원리적으로 답할 수 없는 주장이다**: `log_claude_api_call`(`src/shared/claude_metrics.py:139-151`)이 `error_type` 을 **구조화 로그로만** 보내고 테이블에 남기지 않아, 빈-모델 400 과 529/timeout 이 Postgres 에서 **영구히 구별 불가**하다. 그러니 정확히 말하면 *"이 결함으로 인한 피해는 관측 구간에서 0"* 이고, `api_error` 143건 자체는 그 자체로 피해다(`_persisted_score_is_unreliable` → score·grade NULL + auto-merge 차단). 🔴 **증거가 닿지 않는 구간**: `review_model` 컬럼은 `alembic/versions/0032_...`(2026-05-19)에서 **backfill 없이** 추가됐으므로 그 이전(SCAManager 74건 중 다수가 여기 있다)은 이 축으로 판정 불가다. 또 `repo_configs` 는 이력이 없고 `updated_at` 만 있는데 그 값이 07-09~07-19 라 **현재의 NULL 오버라이드를 과거로 투영할 수 없다** — 위 결정적 관측이 07-05~07-08 창에 한정되는 이유다. 픽스는 그대로 유효하다(로컬 `.env` 는 지금도 빈 값이고 validator 가 `claude-sonnet-4-6` 으로 흡수함을 실측). ⚠️ 변수 조회가 필요해지면 `railway variables --kv` 를 grep 하지 말 것 — 값까지 매칭돼 평문 노출된다(`#1106` 학습), `cut -d= -f1` 로 이름만 | 13 | ✅ |
| **#1289** | AI 리뷰 **품질 회귀 없음** — `output_config` json_schema 강제 후 리뷰 문장이 짧아지거나 어투가 바뀌지 않는가 | 🟡 **2026-08-10 정량 축 부분 해소 — 회귀 신호 0, 그러나 '품질' 자체는 여전히 사용자 판정.** 운영 DB 로 `status='success'` 를 픽스 전후로 갈라 4 지표 대조(2026-07-20 이후, 전후 모두 동일 리포 `KeyCloakSDK` 라 리포 교란 없음): 출력 토큰 avg **1997→1943**(−2.7%) · **median 2016→2022**(+0.3%) · `ai_summary` 길이 **221→230자**(+4%) · 제안 수 **3.39→3.51**(+3.5%). 중앙값·길이·제안 수가 모두 **미세 상승**이라 "짧아졌다" 는 회귀 신호는 없다. 🔴 **과대 해석 금지 — 이 관측이 증명하지 않는 것 2가지**: (a) 🔴 **`parse_error` 0건을 "해소" 로 읽지 말 것** — 같은 창(2026-07-20~08-05 11:00)의 사전 기저율은 **4/163 = 2.45%** 이고, 픽스 후 57건에서 0건이 나올 확률은 **0.24**, 기대 건수는 **1.40건**이다. 즉 *애초에 안 나올 만했다* — 우연과 구별되지 않으므로 json_schema 가 파싱을 고쳤다는 주장은 이 표본으로 **성립하지 않는다**. (초판은 기저율을 3.05%/p=0.17 로 적었는데 성공은 07-20 이후로 세고 `parse_error` 는 전 기간에서 세어 **창이 어긋났다** — 심의 게이트 consistency-reviewer 가 적발. 방향은 같으나 수치는 이 줄이 정본이다.) 판정을 뒤집으려면 **≥121건** 무결 관측이 필요하다(새 기준을 세우는 것이 아니라 위 기저율에서 파생된 값 — `ln(0.05)/ln(1−0.0245)`). 덧붙여 픽스 후 창에도 `api_error` 가 **1건**(08-05 17:25) 있다 — 산발적 API 오류는 계속 존재한다. (b) **토큰 수는 품질의 대리지표일 뿐이다** — 문장이 같은 길이로 더 공허해지는 회귀는 이 지표가 원리적으로 못 본다. 🔴 **적대 검증(`wf_28bb3b63-838`)이 이 비교를 BROKEN 으로 판정했다 — 결함 3건**: (c) **경계가 시간대 때문에 틀렸다** — `analyses.created_at` 은 UTC 인데(`src/models/analysis.py:60`) 픽스 머지는 2026-08-05 07:53 **+0900** = 08-04 22:53 UTC 다. 내가 그은 `08-05 11:00` 경계는 픽스 후 행 최대 ~12시간을 *before* 쪽에 넣었고, 그 오배치는 **하필 내 결론(차이 없음) 쪽으로** 편향된다. (d) **after 창은 단일 코드 상태가 아니다** — `#1297`(`61be16ae`, 08-06)이 `first_text_block` 을 재작성했고 그건 `ai_review.py:181` 파싱 경로에 직접 놓여 있다. 따라서 *"이후 `parse_error` 0"* 은 json_schema 가 아니라 `#1297` 의 효과일 수 있다. (e) **아무도 안 잰 기계적 변화가 있다** — 스키마가 점수를 `"type": "integer"` 로 강제하고 11키를 전부 required 로 만들어 `_coerce_score` 절단 경로와 `score_keys_present` parse_error 트리거(`ai_review.py:331`)를 **우회**한다. 즉 **점수·등급 분포가 바뀔 수 있는데 위 4 지표는 점수를 아예 안 본다**. 🔴 **방어 가능한 재서술**: *"출력 분량·요약 길이·제안 수에서 감지되는 변화 없음. 품질 자체는 측정하지 않았다."* 정책 16 §명시 제외 영역이라 그 판정은 사용자 몫으로 남는다. **잔여 확인 항목**: 실제 리뷰 1~2건을 읽고 *구체성*(파일·라인 지목, 실행 가능한 제안)이 이전만 한지. 나빠졌으면 `output_config` 한 줄 제거로 즉시 되돌릴 수 있다(스키마 정의는 남겨도 무해) | 2·13·16 | ⏳ |
| **#1071** | prod 하드닝 — HSTS 헤더·쿠키 Secure·`/docs` 비노출이 Railway(https)에서 적용되는지 | ✅ **2026-07-19 실측 3/3 충족**: ① `strict-transport-security: max-age=31536000; includeSubDomains` 존재 ② `/auth/github` 세션 쿠키 = `path=/; Max-Age=604800; httponly; samesite=lax; **secure**` ③ `GET /docs` → **404** | 13 | ✅ |
| **#1072** | approve SHA 결속 — head 이동 PR 에 approve 시도 시 GitHub **422 fail-closed** | ❌ **2026-07-26 격리 리포 실측 — 전제 반증(NG). 검증은 종결, 결과는 이슈 발견이며 #1218 로 봉인.** 🔴 **원장이 지정한 "운영 로그 422 관측" 은 결론을 낼 수 없는 방법이었다** — 이 계정은 PR 작성자와 토큰이 동일(`xzawed`)해 APPROVE 가 self-approval 로도 422 를 내므로 두 원인이 구분 불가. 그래서 webhook 없는 private 스크래치 리포로 외부 계약을 직접 프로빙: **구 SHA → 200**(거부 안 함, 리뷰가 그 SHA 로 기록됨) · **force-push 로 PR 에서 사라진 SHA → 200** · **존재하지 않는 SHA → 422** `The commitOID is not part of the pull request`. 즉 GitHub 의 `commit_id` 검증 기준은 "head 인가"도 "PR 에 포함됐는가"도 아니라 **"저장소에 오브젝트가 존재하는가"** 뿐 — 분석된 SHA 는 정의상 존재하므로 **이 가드는 원리적으로 발화 불가**였고 운영 auto-approve 104 건 중 한 번도 차단하지 않았다. 🔴 **형제 대조**: `merge_pr(expected_sha=)`(#1057)는 틀린 sha 에 **409 `Head branch was modified` + 머지 차단**으로 계약이 **성립** — merge 형제가 실제로 작동하니 approve 도 대칭이라 **가정**했고 그 가정이 검증된 적 없던 것이 근본. **조치**: 결속을 GitHub 이 아니라 **우리 코드**가 강제(`post_github_review` 가 POST 전 head 조회 → 불일치 시 `HeadMovedError`, POST 안 함) + 422 를 'head 이동'으로 단정하던 로그 정정. 잔여 한계 = GET→POST 레이스(서버측 원자성 수단 부재 실측 확인) | 13 | ❌ |
| **#1073** | orphan sweep cron **실제 실행** — `analysis_attempts` 소실 탐지·표면화·정리 | ✅ **2026-07-19 로그 직접 관측으로 종결**. `#1102` 로 로깅 복구 후 `[src.scheduler] scheduler sweep_orphans: surfaced=0` 이 **정확히 600초 간격**으로 반복 확인(04:44:51 · 04:54:52 · 05:04:53). cron 전용 경로가 실제 실행 중임이 확정 — DB 우회 관측 불필요 | 13 | ✅ |
| **#1075** | retention sweep cron **실제 DELETE** — 만료 캐시·종결 큐 GC | ✅ **2026-07-19 종결 — 원장이 지정한 2축 모두 일치.** ① 로그: `2026-07-19 20:00:01 INFO [src.scheduler] scheduler retention_sweep: counts={'expired_cache': **8**, 'terminal_queue': 1407}` — 예정 시각 20:00 에 발화하고 **보존해 둔 8건을 정확히** 삭제 ② DB 교차확인: `insight_narrative_cache` 만료 행 **8 → 0**(현재 total_rows=0). 예측값과 실측값이 일치했다 | 13 | ✅ |
| **#1350** | 🔴 **심의 게이트가 예산 상향 후에도 정상 동작하는가** — `AGENTS.md` 컨텍스트 예산을 12,000 → 16,000 으로 올렸다. 심의 에이전트의 **입력 변경**이고 그 에이전트는 모든 Write/Edit 를 차단할 수 있는 경로다 | 상향 후 문서 편집에서 심의 배너가 뜨고 판정이 이상하지 않은지 사용자 관찰 → **2026-08-14 사용자 확인: "특별히 이상점은 없어 보입니다"**. ⚠️ **덮는 범위** = 관찰 구간의 판정 정상성. 덮지 **않는** 것 = 장기 비용 변화(캐시 프리픽스 +4,000자) · 다른 등급 문서에서의 거동 | 정책 2 · 15(High tier) | ✅ |
| **#1349** | 🔴 **SessionStart 배너가 실제로 사용자에게 도달하는가** — backlog 결정 대기 카운터를 신설·배선했다 | 다음 세션 첫 화면에 *"사용자 결정 대기 N건"* 이 뜨는가. 안 뜨면 배선은 있는데 채널이 죽은 것이다(메모리 `feedback-hook-output-channel` 클래스) → **코드로 증명 불가**: `test_session_start_wiring.py` 는 `settings.json` 에 인터프리터 호출이 있는지(**구조**)만 본다 — 실제 실행·출력 도달은 못 잰다(그 한계를 테스트 docstring 이 명시) | 정책 2 | ⏳ |
| **#1279** | 심의 게이트 **한글 심의 품질** — stdin UTF-8 봉인 후 심의 코멘트가 *인코딩* 불평이 아니라 **문서 내용**에 대한 것인지 · 정당한 편집이 과잉 차단되지 않는지 | 🔴 **코드로 증명 불가**: 픽스 전후 대조 1쌍(같은 `guards.md` 편집 = 픽스 전 3/3 "인코딩 오류" 차단 → 픽스 후 "섹션 중복" 지적 후 통과)은 관측했으나, **반복 편집에서의 판정 품질·차단 빈도는 사용자만 관측할 수 있다**. 🔴 **이미 관측된 잔여 3축(backlog R49)**: (a) 차단 사유가 비구체적(*"모순"·"모호성"* 만 근거로 3회 연속 block → 무엇을 고칠지 알 수 없음) (b) prepend 편집을 심의자 2/3 가 "기존 규칙 삭제" 로 오독 (c) 편집당 ~85k 입력 토큰(R38 미이행)이라 한 파일에 5회 호출 = 비용 급증. **확인 항목**: 정당한 편집이 반복 차단되면 `DOC_REVIEW_GATE_DISABLED=1` 로 명시 종료할지 결정 필요 🟡 **2026-08-12 세션 관측 (⏳ 유지)**: **코드로 받쳐지는 것**과 **관측**을 구분해 적는다. 🔴 **구조(코드 실측)**: 반복 차단 사유가 *"실제 로직과 일치하는지 확인 필수"* 였는데 심의 컨텍스트에 `src/` 가 없어 **저자가 해소할 수단이 원리적으로 없다** — 고칠 방법이 없는 사유를 받는다. 🔴 **관측(재현 로그 없음)**: 정당한 정정이 반복 차단됐고, 한 회차는 보안 구멍을 *명시하는* 문장을 *"결함 은폐"* 로 판정했다. ⚠️ 이것을 *"판정 부호가 역전됐다"* 로 쓰지 말 것 — Grok `019ff3ad` 는 **랜덤 역전이 아니라 예측 가능한 실패 모드**로 설명한다(코드를 못 보는 impact 가 *조건 완화 = 위험* 기준으로 오분류). 🔴 이번엔 kill-switch 대신 **사용자 승인 하 심의 밖 경로 1회**로 우회했다(`#1331` 본문 §우회 경로). 🔴 반복 차단의 실제 비용은 토큰이 아니라 **정확도 왜곡**이다 — 저자가 *"옳은 문장"* 이 아니라 *"게이트를 통과하는 문장"* 을 찾게 된다(이번에 계획서가 요구한 결합 서술을 실제로 포기했다). 구조 축 = backlog **R80** | 2·13 | ⏳ |
| **#1276** | 문서 심의 게이트 **실사용 동작** — 봉인 후 CRITICAL 문서 편집이 실제로 3-에이전트 심의를 받는가 (과잉 차단 없이) + 편집당 비용이 감당 가능한가 | 🔴 **코드로 증명 불가**: 라이브 실측은 격리 payload **1회**뿐이고 (impact=approve · consistency=warn · quality=approve, RC=0), 실제 세션에서 반복 편집 시의 판정 품질·차단 빈도· 토큰 비용은 사용자만 관측할 수 있다. **확인 항목**: (a) CRITICAL 문서 편집마다 `[문서 심의] … 경고 후 진행` 또는 `deny` 가 실제로 뜨는가 (b) 정당한 편집이 부당하게 막히지 않는가 (c) 편집당 ~85k 입력 토큰(R38 미이행)이 감당 가능한가 — 아니면 `DOC_REVIEW_GATE_DISABLED=1` 로 명시 종료. 🔴 **재개방 조건**: 게이트가 다시 무동작으로 보이면 `INOPERATIVE - REVIEWED NOTHING` 배너의 `detail:` 원문을 확인할 것 — 그 문자열이 원인을 말한다(R36) 🟡 **2026-08-12 세션 관측 (⏳ 유지 — 자기 인증 금지)**: CRITICAL 문서 편집 다수에서 게이트가 **매번 발화**했다(무동작 0) → (a) 확인. 🔴 **(b) 는 코드로 받쳐지는 구조 근거와 재현 불가 관측을 나눠 적는다.** **구조(코드 실측)**: 심의 컨텍스트는 `_CONTEXT_SOURCES`(`CLAUDE.md`·`AGENTS.md`·STATE 앞 16k) + diff 뿐이라 **`src/` 를 못 본다**. 게다가 impact 의 `block` 은 전 grade 에서 deny 다(veto matrix). 따라서 **보안 서술을 약화시키는 것처럼 보이는 정정은 코드 대조 없이 차단될 수 있다** — 실제로 `CLAUDE.md:51` 의 거짓 단언(3분기 실측 = `config.py:248-254`) 정정이 반복 차단됐다. **관측(재현 로그 없음)**: 같은 치환 문장에 warn 후 block 1쌍. 원인 후보 = 샘플링(`temperature` 미고정) · warn 통과 후 디스크/컨텍스트 변화 · `old_string`/STATE 차이. ⚠️ **입력 동일성은 payload 로그가 없어 증명되지 않았다 — "비결정" 을 코드 사실로 쓰지 말 것** (Grok `019ff3ad` 가 초판의 과잉 주장을 적발). (c) 비용축 미측정. **잔여 = 사용자 결정** — R80 의 (a) src 발췌 주입 / (b) claim-review 대체 / (c) 현행 유지 🔴 **2026-08-14 세션19 관측 (근거 — 판정 아님)**: CRITICAL 문서 편집이 3-에이전트 심의를 실제로 받았고(impact·consistency·quality 판정이 매 편집 인쇄) **3회 차단**됐다. 차단 사유는 전부 **문서 내용**에 대한 것이었다(*"의무 규칙이 선택적 제약으로 약화"* 등) — 인코딩 불평이 아니다. 🔴 **과잉 차단 여부·비용 축은 여기 적지 않는다** — 전자는 차단당한 당사자인 내가 판정할 수 없고 후자는 미측정이다. 상태 칸은 손대지 않았다 | 2·13 | ⏳ |

---

## 회신 방법

사용자는 각 행의 상태를 `✅/❌/⏭️` 로 갱신하거나, Claude 에게 결과를 전달하면 Claude 가 갱신한다. `❌`(NG) 발견 시 = 즉시 fix PR 착수(정책 7). 전 행 `✅`/`⏭️` 확정 시 = 원장에서 **아카이브 섹션으로 이동**(본 상단 표는 미결만 유지).

## 🔴🔴 근본 원인 확정 — Railway cron 은 **한 번도 실행된 적이 없다** (2026-07-19 P0)

`#1073`·`#1075` 가 검증되지 않던 진짜 이유. `railway.toml` 의 `[[deploy.cronJobs]]` 는 **Railway 스키마에 존재하지 않는 키**라 조용히 무시됐다.

| 실측 | 결과 |
|------|------|
| SCAManager 서비스 `cronSchedule` / `nextCronRunAt` | **둘 다 null** |
| Railway cron 설정 방식 (공식 문서) | 서비스당 **단일** `deploy.cronSchedule` 또는 대시보드 — **배열 미지원** |
| 20:00 UTC 스윕 3.5h 경과 후 만료 캐시 | **8건 잔존** (실행됐다면 `purge_expired` 가 0으로 만들었어야 함) |

→ weekly · trend · retry-pending-merges · sweep-orphans · retention-sweep **5종 전부 미실행**.
(직전 #1095 의 따옴표·`-f` 수정은 실재 결함이었으나 **명령 자체가 실행되지 않아 무의미**했다.)

✅ **대체 = 인앱 스케줄러 `src/scheduler.py`** (lifespan 기동·운영 전용). 배선을 `tests/unit/test_scheduler.py` 가 단언한다 — 저장소 밖 설정이 조용히 어긋나던 실패 모드를 코드로 옮긴 것이 이 사고의 교훈이다.

~~🔴 따라서 #1073·#1075 는 스케줄러 배포 후에야 검증 가능하다.~~ ✅ **둘 다 2026-07-19 종결됐다** — `#1073` 은 로그 실측(600초 간격 3회), `#1075` 는 로그 + DB 교차확인(만료 캐시 8→0). 정본은 **위** 검증 상태 표의 해당 행(둘 다 `✅`)이다.
🔴 원문의 *"아래 DB 관측"* 은 현재 **⛔ 폐기된 결론 섹션**을 가리키므로 검증 기준으로 삼지 말 것 — 실제 기준값은 위 `#1075` 행에 있다.

## 🔴🔴 오귀인 정정 — 로그 소실은 Railway 가 아니라 **앱 결함**이었다 (2026-07-19 반증)

직전 세션이 *"Railway 로그 수집이 초기 출력 후 끊김 — **앱 문제 아님**"* 으로 기록한 진단은 **틀렸다**. 이 오귀인이 원장에 사실로 남아 다음 세션의 조사 방향을 잘못 유도했다(로그 경로 포기 → DB 관측만 남김).

**진짜 원인**: `alembic/env.py` 의 `fileConfig(config.config_file_name)` 가 **앱 lifespan 안에서** 실행된다 (`src/main.py:215` `_run_migrations` → `command.upgrade`). `logging.config.fileConfig()` 의 기본값이 `disable_existing_loggers=True` 이고 `alembic.ini` 가 `[logger_root] level = WARN` + stderr 핸들러이므로, 마이그레이션이 앱 로깅 설정을 **통째로 덮어쓴다**.

| 항목 | 마이그레이션 이전 | 이후 |
|------|-----------------|------|
| root level | INFO | **WARNING** |
| root handler | `configure_logging()` 의 stdout 핸들러 | **alembic stderr 핸들러** (우리 것 제거) |
| `uvicorn.access` 로거 | 정상 | **`disabled=True`** ← access log 24h 0건의 정체 |
| `src.*` 로거 INFO | 활성 | **전부 비활성** ← scheduler·cron 로그 소실 |

**운영 로그가 이 기전을 그대로 증언한다** — `[src.main] startup: production hardening = ON`(마이그레이션 **이전**, main.py:213)은 보이고, 바로 다음 줄인 `DB migration completed`(마이그레이션 **직후**, main.py:216)부터 전부 사라진다. 모든 배포에서 동일한 12줄 컷오프가 재현됐다.

**따라서 #1100(로깅 설정 신설)은 운영에서 무력(inert)이었다** — `configure_logging()` 직후 마이그레이션이 즉시 되돌렸기 때문. 단위 테스트는 alembic 상호작용을 재현하지 않아 통과했다.

✅ **수정**: `alembic/env.py` 가 `is_configured()` 로 인프로세스 마이그레이션을 식별해 fileConfig 를 건너뛴다 (alembic CLI 단독 실행은 기존대로). 회귀 가드 = `tests/unit/migrations/test_alembic_env_logging_guard.py` (뮤테이션 실증 — 가드 제거 시 5건 FAIL).

🔴 **교훈**: 관측 부재를 외부 인프라 탓으로 돌리기 전에 **앱 자신이 관측을 끄고 있는지** 먼저 배제하라. "앱 문제 아님" 같은 단정은 반증 실험 없이 원장에 기록하지 말 것.

## ⛔ 폐기된 결론 — "cron 은 로그로 판별 불가" (2026-07-18 기재 → 2026-07-19 반증)

> 🔴 **이 섹션의 결론은 틀렸다. 실행 지침으로 삼지 말 것.** 아래는 폐기 사유와 함께 남긴 역사 기록이다.
> `#1073`·`#1075` 는 결국 **로그 직접 관측으로 종결**됐다.

### 무엇이 틀렸나 — 관측은 옳았고 **추론이 틀렸다**

2026-07-18 의 측정 자체는 정확했다(로그가 실제로 11줄이었고 access log 가 실제로 0줄이었다).
틀린 것은 거기서 끌어낸 일반화다 — *"그러므로 cron 은 로그로 판별 불가"*.

**진짜 원인은 cron 이 아니라 앱이었다**: `alembic/env.py` 의 `fileConfig` 가 lifespan 마이그레이션에서
앱 로깅을 런타임에 파괴하고 있었다(`#1102`). 즉 **관측 수단이 없던 게 아니라 관측기를 우리가 끄고 있었다.**

| 항목 | 2026-07-18 (당시) | 2026-07-19 (`#1102` 이후 실측) |
|------|------------------|--------------------------------|
| 총 로그 줄수 | 11줄 | **208줄** |
| access log(`"GET`/`"POST`) | 0줄 | **12줄** |
| `[src.scheduler]` 라인 | 0 | **155줄** |
| cron job 실행 관측 | 불가 판정 | `retry_pending_merges` **139회** · `sweep_orphans` **14회** · `retention_sweep` **1회** |

### 🔴 이 항목이 남긴 교훈 (원장 자신에 대한)

이 섹션은 **반증된 뒤에도 🔴 마커를 단 채 원장에 남아** 있었다. 표 셀은 `✅ 종결` 로 갱신됐지만
그 아래 산문은 반증 처리되지 않아, 원장이 **정정된 사실과 폐기된 결론을 동시에** 들고 있었다.
다음 세션이 이 산문을 읽으면 로그 경로를 다시 포기한다 — 원장 스스로가 경고한 "낡은 결론 고착" 의 재생산이다.

**규칙**: 표 셀 상태를 바꿀 때 그 근거가 된 **산문 섹션도 같은 커밋에서** 폐기/정정한다.
(2026-07-19 backlog B4 로 식별 → 본 정정)

<details>
<summary>폐기된 원문 (역사 보존)</summary>

`#1073`·`#1075` 는 검증 방법으로 *"Railway cron 로그에서 sweep 실행 확인"* 을 명시했으나, **그 관측 수단이 존재하지 않는다**.

| 실측 (2026-07-18, Railway CLI) | 결과 |
|--------------------------------|------|
| `railway logs --deployment --lines 500 --since 24h` | **총 11줄** — 컨테이너 시작 + alembic 뿐 |
| 같은 로그에서 access log(`"GET`/`"POST`) 건수 | **0줄** (uvicorn `--no-access-log` 아닌데도 미유입) |
| `railway logs --http` 에서 cron 경로 | **0건** — cron 은 `http://localhost:$PORT` 내부 호출이라 엣지 프록시 미경유 |
| cron 실행이 배포 항목으로 기록되는가 | ❌ — 배포 목록은 커밋당 1건뿐 |

→ ~~**cron 실행 여부는 로그로 판별 불가.**~~ ⛔ **반증됨** — 위 표 참조. 대체 수단으로 제시했던 DB 부작용 관측은 여전히 **유효한 교차 확인**이나(실제로 `#1075` 가 로그+DB 2축으로 종결됐다), **유일 수단은 아니다**. `retry-pending-merges` 가 webhook 으로도 트리거되어 cron 단독 증거가 못 된다는 지적은 지금도 옳다 — 다만 이제 `[src.scheduler] scheduler retry_pending_merges` 로그 라인이 **스케줄러 경유임을 직접 명시**하므로 그 한계도 해소됐다.

</details>

관련 실측(같은 날): 수정 전 cron 형태(리터럴 `$INTERNAL_CRON_API_KEY` 전송)를 운영에 재현 → **HTTP 401** 확인(`/health` 200 통제군 대비). `INTERNAL_CRON_API_KEY` 는 Railway 에 **설정돼 있음** → 키가 아니라 셸 미확장이 원인이라는 진단이 운영에서 확정(#1095 로 수정).

## 🔴 집행 기전 (2026-07-19 P0 — 원장이 write-only 였던 자기위반 봉인)

이 원장은 **기계 배선**돼 있다. `scripts/check_owed_verification.py` 가 위 표를 파싱해 **안전등급 ⏳ 미결 건이 있으면 세션 시작 시 loud 경고**하고, 훅 stdout 이 Claude 컨텍스트에 주입된다.

| 요소 | 값 |
|------|-----|
| 실행 주체 | `.claude/settings.json` `hooks.SessionStart` (matcher `startup\|resume`) |
| 판정 | 안전등급 ⏳ ≥1건 → breached (운영등급 미결은 카운트만 보고 — 정책 2 진화 Phase 종료 일괄) |
| 성격 | advisory (비차단·항상 exit 0) — 세션/커밋/PR 미간섭 (정책 17) |
| 회귀 가드 | `tests/unit/scripts/test_check_owed_verification.py` · `test_session_start_wiring.py` |

🔴 **표 형식 변경 시 파서 동반 확인 의무** — 상태 컬럼이 **마지막 셀**, PR 셀이 `**#NNNN**` 형태라는 두 계약에 파서가 의존한다. 형식이 바뀌면 파싱 0행 = 카운터가 무음으로 눈이 먼다(`test_live_ledger_parses_nonempty` 가 이 무음 실패를 차단).

> 🔴 이 원장은 회고 2026-07-18 P1#13(8-PR Wave 종료 시 owed 운영검증 미취합) 조치로 신설됐다. 세션 종료 시 이 원장 갱신이 정책 5 Phase-종료 cross-reference 자가 검토(정책 2/5/8/11)의 하위 체크다.
>
> 🔴 **2026-07-19 회고 P0 — 신설 당시 이 원장은 어떤 집행면에도 배선되지 않은 기록 전용 장치였다.** 회고가 P0 로 규정한 '문서-only 시정은 행동을 못 바꾼다'를 같은 세션에 재생산(3회차)했고, 그 결과 안전등급 2건이 4세션째 ⏳ 로 누적됐다. 위 §집행 기전이 그 시정이다.
