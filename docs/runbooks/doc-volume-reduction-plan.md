<!-- guard-cue-quote: 아래 "착수·단계" 어휘는 사용자 결정을 받기 위한 선택지 서술이며 이 문서 자체의 실행 지시가 아니다. -->

> 🟢 **이 문서는 결정 대기 중인 제안서입니다 — 아무것도 실행하지 않습니다.**
> 측정과 선택지만 담습니다. **어떤 파일도 삭제·이동하지 않으며**, 각 단계는 사용자 결정 후에만
> 별도 PR 로 착수합니다. 🔴 결정 전 이 문서를 근거로 문서를 줄이지 마세요.

# 문서 총량 감축 제안 — 기록성 ↔ 집행성 분리

> **요청 배경 (2026-08-12 사용자)**: *"수행 결과가 계속 틀린 것은 확인하는 전체 문서 총량이
> 많다는 증거다. 기록성 내용 이외, 운영·개발에 실질 반영되는 부분 이외 정리를 요청한다."*

## 0. 기존 감사 전제와의 관계

2026-08-10 감사는 *"용량 절감 근거 정리안 기각"* 을 전제 1로 두었고 그 근거는 **토큰 비용**이었다.
이번 요청은 **인지 부하** 축이라 그 반박 대상이 아니다. 🔴 나머지 전제는 이 제안도 지킨다:

| 전제 | 이 제안 |
|---|---|
| 파일 삭제·경로 이동 **0건** | 지킴 — 파일 제자리, 내용만 분리 |
| `CLAUDE.md`·`AGENTS.md` 축소 금지(`#1296` 순손실) | 지킴 — **대상 제외**(서사 20%·10%) |
| 정책 17 본문 보존 default | 지킴 — 본문 보존이 깨지는 C 는 ★보류 |

## 1. 실측

### 1-1. 집계 규칙 (재현 가능하도록 명시)

🔴 **backlog ✅ 계수 규칙** = `^\| \*\*R\d+\*\* \|` 로 시작하는 **R행**의 **상태 셀(2번째)** 이 `✅` 를
포함하는 행. 이 규칙으로 **39행 / 40,553자 / 47%** 이며, 두 판정식(`startswith` · `in`)이 동일값을 낸다.
⚠️ 리포 전체의 `✅` 를 세면 **41개** 가 나온다 — R행이 아닌 표(감사 P2·사용자 결정 대기)가 포함되기
때문이다. **두 수치는 서로 다른 것을 세며, 이 제안은 R행 기준을 쓴다.**

### 1-2. 표면별 기록성 비중

| 표면 | 전체 | 기록성 | 게이트 16k 창 |
|---|---:|---:|---|
| `docs/backlog.md` | 86,245자 | ✅ R행 39 = 40,553자 (47%) | — |
| `docs/STATE.md` §**작업 이력** | 38,449자 | 대부분 | 🔴 **창 안** (offset 14,146) |
| `docs/STATE.md` §**테스트 수 추적 이력** | 45,129자 (160항목) | 100% | ✅ **창 밖** (offset 52,596) |
| `.claude/rules/**` 11파일 | 139,340자 | 서사 68,607자 (49%) | 편집 경로 매칭 1파일 |
| `CLAUDE.md` / `AGENTS.md` | 16,131 / 10,597 | 20% / 10% | 전문 |
| `docs/cycle-history.md` | 320,800자 | 100% | 자동로드 아님 — 부하 0 |

*(서사 지표 = `#NNNN` · `20NN-MM-DD` · `사이클 N` 포함 줄. 규칙 문장이 근거로 PR 을 인용해도
서사로 계수되므로 이 수치는 **상한**이다.)*

### 1-3. 🔴 rules 서사 비중은 매우 불균등

| 파일 | 크기 | 서사 | 비중 |
|---|---:|---:|---:|
| `i18n.md` | 8,942 | 6,749 | **75%** |
| `deploy.md` | 10,498 | 7,525 | **72%** |
| `ui.md` | 18,751 | 12,139 | **65%** |
| `db.md` | 14,630 | 9,163 | **63%** |
| `testing.md` | 13,314 | 8,260 | **62%** |
| `pipeline.md` | 18,794 | 9,990 | 53% |
| `api.md` · `security.md` · `services.md` | 34,068 | 12,519 | 23~41% |
| **`guards.md`** | **17,586** | **2,013** | 🔴 **11%** |
| `docs.md` | 2,757 | 249 | 9% |

🔴 **최대 파일이 가장 조밀하다.** *"서사를 걷어내면 guards.md 가 3분의 1"* 은 **틀렸다** — 이 제안의
저자가 그렇게 추정했고 측정이 뒤집었다. 실익은 **i18n·deploy·ui·db·testing 5파일**에 집중된다.

## 2. 소비자 grep

```
docs/backlog.md            → 7 파일  (doc_review_gate · check_claim_review_trace ·
                                      check_memory_refs · test_backlog_shape ·
                                      test_plans_are_not_executable ·
                                      test_railway_scaling_guard · dependabot.yml)
docs/STATE.md              → 13 파일 (check_docs_sync · check_test_count_sync ·
                                      check_e2e_scope · doc_review_gate ·
                                      PULL_REQUEST_TEMPLATE + 테스트 8)
docs/cycle-history.md      → 4 파일 · .claude/policies/active.md → 3 · history.md → 0
```

🔴 **`docs/STATE.md` 는 최다 소비 문서(13)** 다. **파일·절 제목·형식은 불변**이어야 한다.

🔴 **"기계는 마지막 1항목만 읽는다" 는 §테스트 수 추적 이력에만 참이다.** STATE **파일 전체**로는
헤더 종합 수치(`check_test_count_sync`)·추적셀·E2E 성분(`check_e2e_scope`)·게이트 앞 16k 등
**여러 축**이 읽힌다. 이 구분을 흐리면 안전성 논증이 과대해진다.

## 3. 단계 선택지

### A. `backlog.md` ✅ R행 이전 — **−40,553자 (47%)**

파일 자신의 §갱신 규칙이 이미 명령한다: *"완료 시 ✅ 표시하고 **다음 정리 시 제거**"*.

🔴 **그러나 맹목 이전은 금지다 — 실측 반례**: ✅ 39행 중 **20행이 `잔여`/`GROK-N`/미해결을 담고
있다**(R37·R38·R49·R51·R52·R56 …). 완료 표시된 행 안에 **열린 일감**이 있다.

→ **선행 작업 필수**: 20행의 잔여를 **신규 R행으로 승격**한 뒤에야 나머지를 아카이브한다.
이 분리 없이 진행하면 *"완료로 표시된 미결"* 이 통째로 사라진다(R41 완전성 축의 재발).

- **위험도**: Low→**Medium**(잔여 승격 판단이 들어가므로)
- **동반 의무**: 상태 요약(`🔴 N · 🟡 M · ✅ K`) + 하단 섹션 표를 **같은 PR** 에서 갱신(`test_backlog_shape` bijection)
- **롤백**: 아카이브에서 행 복원

### B. `STATE.md` §테스트 수 추적 이력 아카이브 — **−약 44,000자**

🔴 **게이트 이득은 0 이다.** 이 절은 offset **52,596** 에서 시작하는데 심의 컨텍스트 예산은
**16,000자**다 — **심의자는 지금도 이 절을 한 줄도 보지 않는다.** 이 단계를 *"게이트 부담 감소"* 로
팔면 거짓이다.

**정당한 명분은 다른 것이다**: 파일 비대(97,725자) 완화 · `grep` 소음 감소 · SSOT 가독.

- 기계 안전성: `check_docs_sync._history_tail` 은 절 존재·**단일성**·`bullets[-1]` 형식만 본다.
  `check_test_count_sync`·`check_e2e_scope`·`test_docs_ledger_shape` 는 항목 수를 보지 않는다.
- 🔴 절 제목·형식 계약 **유지**(절이 2개면 red).
- **보존 N 결정 필요** — 🔴 *"30 = 회고 창 2배"* 는 **불릿과 PR 이 1:1 이 아니므로 미검증**이다.
  날짜 창(예: 최근 N일) 기준이 더 정직할 수 있다.
- ⚠️ 잃는 것: 장기 추세(4718 → 7076)를 본문에서 한눈에 못 본다 → 아카이브 진입 링크로 보완.

### B′. 🔴 `STATE.md` §작업 이력 — **게이트가 실제로 읽는 비대 축** (신규)

**offset 14,146 · 38,449자 → 16k 창 *안*.** 심의 3 에이전트가 매 CRITICAL 편집마다 이 구간을
읽는다. **게이트 토큰을 실제로 줄이는 것은 B 가 아니라 B′ 다.**

- 🔴 이 제안의 초판은 이 축을 **통째로 놓쳤다**(Grok `019ff3d1` 적발).
- 위험도: **Medium~High** — 창 안 내용이 바뀌면 심의자가 보는 문맥이 바뀐다(R37 이 되돌린 축과 인접).
- 별도 측정·별도 결정 필요. **이 제안은 B′ 를 착수 대상으로 올리지 않고 후보로만 등재한다.**
- ✅ **2026-08-15 실행됨 (옵션 B, 사용자 승인)** — 본문 46항목 전량을
  `docs/cycle-history.md` §STATE.md 이관으로 옮기고 STATE 에는 헤딩+포인터만 남겼다.
  한 줄도 접지 않았다. 아카이브 `STATE.md:N`(N≥128) 은 **−32줄** 밀린다(고치지 않음).

### C. rules 5파일 서사 분리 (🔴 High tier · ★보류) / C′ 변형

- **C**: `i18n`·`deploy`·`ui`·`db`·`testing` 의 사고 로그를 external. **−약 43,836자**
- **C′ (Grok 제안 변형)**: **파일 분리 없이** *규칙 1줄 + 왜 1줄 + 아카이브 앵커 링크* 로 **인라인 압축**.
  경로 불변이라 path-scoped silent 회귀 위험이 없다.
- 🔴 역효과: 서사가 그 규칙의 **존재 이유**다. 빼면 다음 세션이 규칙을 *과하다* 고 판단해 완화할 수 있다.
- 🔴 `check_red_budget` 주의: 🔴 블록에서 집행자 파일명이 서사와 함께 빠지면 **무집행 🔴 증가 → red**.

## 4. 🔴 이 제안이 고치지 못하는 것

2026-08-12 세션 오류 3건 귀속:

| 오류 | 원인 | 정리로 예방되나 |
|---|---|---|
| 뮤테이션 전 `git add` 누락 (2회) | 규칙 본문이 **`guards.md` 에만** 있다(`CLAUDE.md`·`AGENTS.md` 0 hit) | 🟡 **부분** — 🔴 guards.md 는 **path-scoped**(`scripts/**`·`hooks/**`·`workflows/**`·`tests/unit/{scripts,hooks}/**`)라 **그 밖 표면을 뮤테이션하면 로드되지 않는다**. *"이미 로드돼 있었다"* 는 내 서술은 맥락 의존이었다(Grok 적발). **총량 증가 0 인 해법 = 진입점 1줄 포인터 + 헬퍼 스크립트** |
| 6-step ⑤ 이월 범위 오독 | 배치-PR 분기를 *차단 게이트까지* 확대 해석 | 🟡 **부분** — 문장 정밀화로 예방(총량 증가 0) |
| 증명 초과 주장 | 규율 오류 | 🔴 **아니오** |

→ 함께 가야 하는 것: **① 뮤테이션 헬퍼(더러운 트리 거부 + 자동 stage/복원) ② `AGENTS.md`/`CLAUDE.md`
1줄 포인터 ③ 배치-PR 분기 문장 정밀화.** 셋 다 총량을 늘리지 않는다.

🔴 **정리만 하고 이 셋을 빠뜨리면 총량은 줄었는데 같은 실수가 반복된다.**

## 5. 검증 방법 (각 단계 공통)

1. §2·§3 의 해당 가드 전건 green · `pre_push_gate` 전건 통과
2. **뮤테이션**: 아카이브로 옮긴 내용 1건을 되살렸을 때 원장/가드가 **중복을 탐지**하는가
   (탐지 못 하면 "옮겼다" 가 아니라 "복제했다" 이다) — 🔴 이 단언의 **기계 구현이 아직 없다**
3. 아카이브 파일의 계약 명시: 경로 고정 · append-only 여부 · `check_docs_sync` 비대상 · 색인 등재
4. 정책 19 — 각 단계 PR 에 Grok claim-review 흔적

## 6. claim-review

- session: `019ff3d1-7b1b-76e0-87a4-43cf68a7afc6` (`owner-interrupt: claim-review`)
- claim: 이 제안서의 측정·안전성·한계 귀속·권고가 옳다
- verdict: WEAKENED — 🔴 **초판이 4축 모두 약화 판정을 받았고 이 문서는 그 반영본이다.**

| Grok 지적 | 처리 |
|---|---|
| ✅ 39행 안에 **잔여 20행** — 맹목 이전 시 열린 일감 소실 | §3-A 에 **선행 승격 의무**로 반영 |
| §테스트 이력은 게이트 창 **밖**(52,596 > 16,000) → B 의 게이트 이득 0 | §3-B 에 명시, 명분을 SSOT 가독으로 교체 |
| 게이트 창 안의 진짜 비대는 **§작업 이력(38,449자)** | **B′ 로 신규 등재**(초판이 통째로 놓침) |
| backlog ✅ 집계 규칙이 재현 불가 | §1-1 에 규칙 명시 + 41 과의 차이 설명 |
| `guards.md` 는 path-scoped 라 "이미 로드" 는 맥락 의존 | §4 에 반영 + 진입점 포인터 추가 |
| C 전면 보류만 제시 = 과잉 신중 | **C′(분리 없는 인라인 압축)** 추가 |
| "N=30 이 회고 창 2배" 미검증 | §3-B 에 미검증 표기 + 날짜 창 대안 |

⚠️ **Grok 지적 중 채택하지 않은 것**: *"backlog ✅ 는 41행/50%"* — R행 기준 재측정에서 **39/47%**
가 두 방법으로 재현됐다. 41 은 R행 밖 표까지 포함한 수로 보이며, **두 수치는 다른 것을 센다**(§1-1).

## 7. 사용자 결정 항목

| # | 결정 | 옵션 | ★권장 |
|---|---|---|---|
| 1 | A (backlog) | (a) 잔여 20행 승격 후 진행 (b) 보류 | ★ **(a)** — 단 승격이 **선행 조건** |
| 2 | B 보존 N | (a) 최근 30항목 (b) 날짜 창 (c) 보류 | ★ **(b)** — 불릿≠PR 이라 개수 기준은 미검증 |
| 3 | B′ (§작업 이력) | (a) 별도 측정 후 결정 (b) 대상 제외 | ★ **(a)** — 게이트 토큰을 실제로 줄이는 유일한 축 |
| 4 | C / C′ | (a) C′ 인라인 압축 (b) C 전면 분리 (c) 보류 | ★ **(c) 보류** — 정책 17 + 역효과 위험 |
| 5 | §4 의 3종(헬퍼·포인터·문장 정밀화) | (a) 같이 진행 (b) 별도 | ★ **(a)** — 없으면 정리가 오진의 근거가 된다 |

**고려했으나 제시하지 않은 안**: `cycle-history.md`(320,800자, 최대 파일) 분할 — 자동로드가
아니라 인지 부하에 기여하지 않는다. 크기만 보고 손대면 노력 대비 효과가 0이다.
