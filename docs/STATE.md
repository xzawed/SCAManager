# SCAManager 프로젝트 상태

> 이 파일이 단일 진실 소스(Single Source of Truth)다. Phase 완료·주요 변경 시 여기를 먼저 갱신한다.

## 현재 수치 (2026-06-23 기준)

> 📌 **이전 세션·PR별 누적 작업 서사는 [`docs/cycle-history.md`](cycle-history.md) 단일 출처** (사이클 60~166, 최신순). 본 헤더는 **최신 1건 + 종합 수치만** 유지 — 32KB 단일 라인 SSOT 가독성 복원 (품질감사 docclr-1, 2026-06-17, cycle-history.md 에 서사 전량 보존[append-only] 확인 후 트리밍). 🔴 **다음 세션 갱신 규칙**: 신규 작업 완료 시 (1) 본 "최신" 블록을 새 작업으로 교체 + 종합 수치 갱신, (2) 직전 작업의 전체 서사는 `docs/cycle-history.md` 최신순 맨 앞에 본문 섹션으로 이관 (헤더에 "직전" 체인 누적 금지 — 본 정리의 회귀 방지).

**최신 (정밀 감사 보안/게이트 #2 — native auto-merge SHA-atomicity fail-closed, 2026-06-23)** — 69-에이전트 다차원 감사(`wf_bb8affb6`, 상세 [`cycle-history.md`](cycle-history.md)) 보안/게이트 4건 중 #2 처리. **#962** — native auto-merge 시 `head_sha` 미확보(GitHub fetch 실패 등)면 SHA를 빈 값으로 둔 채 머지하던 경로 차단: `effective_sha` 미확보 시 terminal 상태(NETWORK_ERROR, 사용자 결정 A) 반환 → guardless merge 봉인(원자성 보존). 🔴 Codex 1차 NG **valid** 적발 — 초안의 retriable(`UNKNOWN_STATE_TIMEOUT`)이 `should_retry`(ci="unknown"→False)·빈 `commit_sha` 가드로 실제 미작동 → terminal 정직 전환, 2차 OK 5/5 (정책 18 mutual ROI 양성). TDD +1(retry path fail-closed→terminal 가드 + native_automerge fail-open→fail-closed 2건 rename). 🔴 잔여 = 감사 보안/게이트 **3건**(auth dev-mode·retry verifier-staleness·static fail-open — High tier 항목별 확인) + 테스트/단일출처 5건 + markdown 인젝션 4채널 + ops(#2 RLS·2nd-LLM 활성화). [[project-deep-audit-2026-06-23]]

**종합 수치**: 전체 **5177** 수집 (단위 **5023** + 통합 154) / E2E 121 (109 표준 + 12 perf) / pylint **10.00/10**.

| 지표 | 값 | 비고 |
|------|-----|------|
| 전체 테스트 | **5177 수집** *(헤더 = 최신값, 이 셀 = pytest 누적 추적)* | `pytest tests/` — 단위 5023 + 통합 154 (현재). 추적 이력 (단위 baseline 4718): (이전 사이클 73~128 누적 2948) + **사이클 129 #614 AI Issue 등록 +59** (2948→3007). + **사이클 130 #617 +2** (3007→3009). + **사이클 131 Claude Design 재설계 #625~#633 +13** (3009→3022 단위). + **사이클 139 #660~#664 +13** (rate_limiter 5 + notifier 5 + dashboard 3, 3022→3035 단위). + **사이클 140 #666~#667 +17** (GateAction 17개, 3035→3052 단위). + **사이클 141 #669 +9** (Rate Limiting 보강 9개, 3052→3061 단위). + **사이클 142 P0 수정 — 실측 기반 재집계** (3061→3213 단위, 이전 과소집계 152건 보정). + **사이클 142 Phase B-D #674~#676 +8** (S2/S4 test_main +3, conftest autouse +5, 3213→3221 단위). + **사이클 142 회고 Tier B #679 +3** (LimitBodySizeMiddleware 413/400/200 회귀 가드, 3221→3224 단위). + **사이클 142 회고 Tier B P1-4 #682 +105** (dashboard repos i18n 키 존재 105케이스, 3224→3329 단위). + **사이클 143 #686+#689+#690 +150** (analysis_detail i18n +18, repo_detail 일반 텍스트 +66, 이슈 UI +66, 3329→3479 단위). + **사이클 143 smoke +2** (151→153 통합). + **사이클 144 #694+#695+#696 +55** (analysis_detail issue_panel +36, repo_detail bulk +12, 렌더 정합 가드 +7, 3479→3534 단위). + **사이클 145 #698+#699 +153** (analysis_detail js_msg +66, repo_detail js_msg +87, 3534→3687 단위). + **사이클 146 #702~#705 +571** (base theme +48, repo_insights +186, settings +120, landing +217, 3687→4258 단위). + **사이클 147 #707~#708 +40** (settings 토스트/hint +18, render-parity 가드 +22, 4258→4298 단위). + **사이클 149 #712~#715 +109** (gate 메시지 i18n + dead code 제거 — gate_messages/merge_advice 키 테스트, 4298→4407 단위). + **사이클 150 #717 +19** (웹 UI 에러 메시지 i18n — web_errors 키 테스트, 4407→4426 단위). + **사이클 151 #724 +16** (hook.py CLI 에러 i18n — hook_errors 키 테스트, 4426→4442 단위). + **사이클 152 #726~#728 +51** (통합 회고 P0 수정 — format_ref +7, engine merge i18n, validator+seam 테스트, 4442→4493 단위). + **사이클 153 #730~#731 +34** (railway Issue i18n +11, cron 주간/트렌드 i18n, 4493→4527 단위). + **사이클 154 +6** (telegram 반자동 콜백 seam +2, cron escape 가드 +2, railway 핸들러 seam +2 — 사이클 153 회고 발견 P0 telegram.py:120 + P2 6건 수정, 4527→4533 단위). + **사이클 155 +11** (발신 모듈 한국어 AST 소스 스캔 자동 가드 +3 + telegram ko-default seam +1, 4533→4537 + 가드 검증 식별 P1 수정 — resolve_ai_summary 현지화 테스트 +7 [ai_unavailable 키 3 + success/en/ja/none 4], 4537→4544 단위). + **사이클 156 Theme B S1 +2** (SSRF _http.py fail-closed 봉인 — 빈 host·DNS gaierror 분기 회귀가드, mutation KILLED, src 무변경, 4544→4546 단위). + **S2 +4** (4채널 SSRF 차단 early-return 봉인 — discord/slack/webhook/n8n validate=False 시 build_safe_client 미호출, mutation 전부 KILLED, 4546→4550 단위). + **S4 +9** (coverage-tail — checks.py `_legacy_state_to_ci_status` parametrize 6 + 레거시 fallback e2e 1 [auto-merge gate 정확성] + security_scan 처리 본체 happy/rollback 2 [kwargs 값 단언 PR-5C 봉인], mutation KILLED, 4550→4559 단위). + **S3 +0** (PG SKIP LOCKED 동시성 CI container — test_retry_concurrency_postgres 3건 CI 영구 skip→pass 상태전환 [new job `pg-concurrency` + postgres:16 service + barrier 결정성], 단위 불변 4559). + **사이클 159 PR-A +1** (security_scan rollback 후 secret-scanning 지속 회귀가드 — except 절 break/return 회귀 차단, 4559→4560 단위). + **사이클 159 PR-B +3** (`_http.py` DNS `except OSError` 확장 — timeout/OSError fail-closed 가드 2 + `scan_all_repos` 외부 루프 rollback 세션 격리 1, 4560→4563 단위). + **사이클 160~161 실측 재집계** (이전 추적 누락분 #760~763 포함 — 단위 4563→4615, E2E 112→113): #764 클램프 회귀가드 단위 +2 (범위초과→cap / 음수→0) · #766 hx-boost 핸들러 누적 가드 nav E2E +1 · 나머지 단위 증가분은 #760~763(integrity-audit 세션) 미추적분 실측 반영 (단위 4615 + 통합 153 = 4768 수집). + **사이클 162 #775~#781 +13** (RLS 자동탐지 가드 5 [test_rls_matrix_completeness 4 + issue_registrations 1] + P1-1 정적분석 타임아웃 가드 3 [AutoMergeAction skip 1 + 파이프라인 wiring 2] + P1-2 동시 insert race 1 + 기타 실측, 4615→4628 단위; #776/#781 은 기존 테스트 정정·#777 JS/#778 워크플로우는 Python 테스트 무증가). + **사이클 163 #783~#787 +20** (area=gate P2 백로그 — #783 ApproveAction 정적분석 가드 +1 · #784 hook 점수 안전변환 +3 [비숫자/None/Infinity 직접 단위] · #785 merge_retry validator +6 · #786 zero-SHA 조기종료 +8 [skip 2 + `_is_blank_sha` parametrize 6] · #787 `_ensure_repo` race 복구 +2, 4628→4648 단위). + **사이클 164 #794~#796 +2** (area=gate 잔여 6 결함 사용자 Q1~Q4 — #795 정적분석 재구성 +3 [타임아웃 부분결과 보존·파일 단위 격리·전량실패 안전망] · #796 telegram 대칭화 −2 [Block2 log_merge_attempt 관측 테스트 engine 이관] · #794 regate first-writer-wins +1, 4648→4650 단위). + **사이클 164 회고 follow-up #798/#799 +5** (#798 PR 코멘트 incomplete 경고 배너 +2 · #799 회귀 가드 3건 [incomplete 종단·_race_recover 대칭·semi-auto 임계 layer], 4650→4655 단위). + **사이클 165 Task9 리메디에이션 #802~814 +58** (P1 #802~810: telegram authz·secure_str_compare·incomplete fail-open 봉인·hook SHA race·check_suite force-due·RLS 가시화 + P2 클러스터 #811~814: #11 게이트 claim 원자화 [gate_decision_repo claim 2 + telegram replay 2] · #13 webhook 본문 파싱 [malformed/array/scalar/dict 4] · #24 ai_review per-field [parity 배터리 + feedback 보존 + Infinity + 비-dict] · #12 SSRF docstring 회귀 가드 2 · #25 hook NULL 저장 [parse_error/빈/응답/result 보존 + 정상 회귀 5] + overview F-오분류 회귀 1, 4655→4713 단위). + **사이클 165 회고 follow-up #816/#817 +3** (#816 docs/rule 정합·정책18 §3 2-tier = docstring/룰만 단위 무증가 · #817 회고 P1 테스트 갭 = claim_decision NOT NULL 흡수 +1 · seam 실DB +1 단위 + PG first-writer-wins invariant +1 통합[pg-concurrency CI job], 단위 4713→4715 · 통합 153→154). + **사이클 165 회고 P2-② 봉인 #819 +3** (CLI-hook parse_error 인플레 점수 → _regate→gate fail-open 봉인 — run_gate_check orchestrator parse_error 차단/success 허용 +2 · _regate→gate result pass-through 배선 +1, 4715→4718 단위). + **사이클 166 Task9 full 감사 P2 백로그 #820~#824 +5** (#821 insight_cache 유령 인덱스 회귀 가드 +1 [Red 2→Green 1] · #823 hx-boost 리스너 누적 정적 가드 +2 [add_repo pagehide·tweaks keydown] · #824 i18n 이중이스케이프 +2 [render no-double-escape·safe-contract allowlist]; #820 docs·#822 effects.js dead-code·#17 ORM 정합·#31 test dead-branch 은 단위 무증가, 4718→4723 단위). + **사이클 166 잔여 DB/정리 #826~#828 +3** (#826 ORM↔alembic 인덱스 가드 +2 [test_0023 state_repo·partial unique] · #828 analyses FK CASCADE 가드 +1 [test_0038]; #827 고아 CSS 단위 무증가, 4723→4726 단위). + **사이클 166 결정 영역 #830/#831 +1** (#831 #23 gate retry 'passed' 의도 회귀 가드 +1; #830 #22 Python SSOT docs-only 무증가, 4726→4727 단위). + **사이클 166 회고/후속 #833/#834 +1** (#834 _coerce_score int() 절삭 의미 가드 +1; #833 Railway 핀 정정 docs-only 무증가, 4727→4728 단위). + **사이클 166 #836 #18 +2** (전역 ORM↔alembic compare_metadata 가드 [PG-only] + 필터 로직 로컬 가드, 4728→4730 단위). + **2026-06-09 적대 재검증 #839~#841 +3** (#32 settings JS-리터럴 tojson 정적 가드 +1 · #840 test_0039 repositories.user_id FK +1 · #841 test_0040 users 인덱스명 rename +1, 4730→4733 단위). + **2026-06-09~10 잔여작업 #843 +2** (drift ③④' analyses/insight_cache 부분 인덱스 ORM 선언 가드 test_orm_partial_indexes 2건, 4733→4735 단위; #844 #2 RLS runbook docs-only 무증가). + **2026-06-10 RLS Phase 2 +52** (test_worker_session_routing — config 변환/factory identity·독립 인스턴스/RLS listener 범위/ast 정적 라우팅 가드 16모듈×2/웹 negative 가드 + Codex R1/R2 강화 [전수 inventory 양방향 2 + 재바인딩 금지 1 + 모듈 객체 import 금지 1], 4735→4787 단위). + **2026-06-10 RLS Phase 3 +22** (test_0041 가드 7 [bijection·NO FORCE 오염 포함] + saas force/bypass 실측 8 + 라우트 db-전달 2 + 렌더 bypass 배너 2 + routing 가드 scripts parametrize 2 + PG-live round-trip 1 [pg-concurrency CI, 로컬 skip], 4787→4809 단위). + **2026-06-10 RLS #2 Phase 4 OAuth blocker +8** (auth_callback worker 세션 전환 [옵션 ②] hybrid 계약 가드 +2 [github.py bare+worker import 존재 + alias 금지 parametrize] + Codex mutual 발견 시스템 API 라우트 3종 worker 재라우팅 가드 +6 [test 5 alias/no-bare parametrize 가 _WORKER_ALIAS_MODULES 17→20 확장, repos·stats·repo_report × 2], 4809→4817 단위; src/auth/github.py·api 3종·test_github.py patch 전환은 무증가). + **2026-06-10 RLS #2 Phase 4 admin hybrid +7** (#849 후속 — admin 2 모듈 `_HYBRID_DB_MODULES` 편입 → test 10 hybrid 계약 parametrize 확장 +4 [api/admin·ui/routes/admin × 2] + 엔드포인트별 라우팅 sentinel 가드 +3 [tenants/operations=worker·rls-audit=web distinct sentinel] + `_get_worker_db` 제너레이터 커버 가드 +2 [api/ui yield+close, CI codecov/SonarCloud coverage-on-new-code fix-up — test_failover::TestGetDb 패턴], 4817→4826 단위; api/admin·ui/routes/admin `_get_worker_db` 분기는 무증가). + **2026-06-11 정합성 감사 follow-up — pipeline AI-fail NULL-persist +4** (P1: AI 리뷰 genuine 실패[api_error/parse_error] 시 인플레 점수 NULL 저장 — hook #25/#814 대칭, _save_and_gate `ai_review_failed` 게이트; api_error/parse_error→NULL·success/no_api_key/empty_diff→점수 유지 4 케이스; P2 ai_review.py literal 17/17/7→`AI_DEFAULT_*_RAW` 상수는 무증가, 4826→4830 단위). + **2026-06-11 정합성 감사 follow-up 후속 4 PR (#855·#854·#856·#852) +8** (#855 api SSRF storage 단일출처화[`shared/ssrf.py::is_safe_webhook_url`, settings+repos field_validator 공유]+email To CRLF strip +3 [test_repos/test_email 추가·test_settings_ssrf refactor] · #854 webhook pre-auth secret 캐시 상한[`WEBHOOK_SECRET_CACHE_MAX=2048`, forged full_name DoS]+telegram/railway 로그 sanitize +3 [test_secret_cache_bound 신규] · #856 ui base.html `_kpiCountupHandler` htmx:historyRestore 등록[#473 대칭] +2 [test_base_kpi_handler 신규] · #852 docs 정합 정정[architecture WorkerSessionLocal·STATE 60~166·i18n 쿠키명·runbook §4 pooler 검증] 무증가, 4830→4838 단위, 각 머지 커밋 collect-only 실측). + **2026-06-11 2nd-LLM 머지 검증자 (cross-vendor 거버넌스, feat/merge-verifier) +26** (merge_verifier 순수+오케스트레이션 15[band 경계/verdict 해석/prompt 인젝션방어/verify_merge_safety fail-closed + Codex-fix interpret 무예외·band=0 거부 2] + openai_client 5[계측 3 + SDK/fallback client 2] + post_plain_pr_comment 1 + AutoMergeAction 검증 가드 5[unsafe 차단+코멘트/safe 머지/밴드밖 skip/api_error fail-closed/비용불변 종단], 4838→4864 단위). + **2026-06-11 잔여/후속 #861 +16** (verifier `interpret_verdict` fail-closed 엄격 파싱 회귀 가드 — 비-bool safe 7 [parametrize] · 비-False manipulation 6 · real-bool 보존 1 · httpx `_call_via_http` fallback 2, 4864→4880 단위). + **2026-06-12 #863 +6** (verifier diff/token cap fail-closed 봉인 회귀 — merge_verifier 47라인 + openai_client 49라인, 4880→4886 단위). + **2026-06-12 #865 +9** (verifier 봉인 P1-1 반자동 parity 단일출처화 회귀 — verifier_blocks_merge 7 + engine 가드 6 + telegram parity 1, 기존 auto_merge_verifier 5 교체, 4886→4895 단위). + **2026-06-12 정합성 감사 #868~#871 +12** (P0 hook auth +3 · U0 dashboard 격리 +1 · C6+C2 AI-fail fail-open +6 · C3 retry 격리 +2, 4895→4907 단위). + **2026-06-12 정합성 감사 P2 백로그 #874~#879 +8** (dead-code 제거 −6[#874] · C27 telegram escape +1[#875] · C10 pylint/bandit 방어 +5·C18 shebang +1[#876] · C14/C28/C11 +4[#877] · U3 render 가드 +1·C30 modify 0[#878] · CodeQL fix 0[#879], 4907→4915 단위). + **2026-06-12 CodeQL #515 fix +1** (migration-completeness `_REGISTERED_MODELS` read 등록 검증 테스트, 4915→4916 단위). + **2026-06-13 U2 effects.js 재초기화 +1** (test_hx_boost_listener_guards `test_effects_animations_reinit_on_hx_boost` 정적 가드, 4916→4917 단위). + **2026-06-13 C1 save_gate_decision dead wrapper 제거 −2** (죽은-래퍼 전용 테스트 2개 제거: `test_save_gate_decision_updates_existing_record`=upsert UPDATE 분기 `test_gate_decision_repo::test_upsert_updates_existing` 중복 + `test_save_gate_decision_db_failure`=래퍼 미호출 side_effect 미발화 false-confidence; 35 inert patch de-indent·multi-patch 4·unused import 정리는 무증감, 4917→4915 단위). + **2026-06-13 C12·C22·U1 머지 #884~#886 +23** (#884 C12 OTP rate-limit +11 [`_OtpAttemptLimiter` 단위 7 + telegram `_handle_connect` 통합 4] · #885 C22 diff 절단 마커 +10 [review_code 4 + result_dict 2 + gate 3 + telegram 반자동 1] · #886 U1 0027 RLS 의도적 divergence 구조단언 가드 +2, 4915→**4938** 단위). + **2026-06-14 회고(5+1) P1 follow-up +1** (#888 회귀 가드 — 디스패처가 `_NOT_CONNECTED_MSG` 상수 직접 반환 ast 단언 `test_dispatcher_returns_not_connected_constant_directly`, 4938→**4939** 단위; README.ko 배지·db.md U1 노트는 무증감). + **2026-06-14 회고 P2 #893~#895 +1** (#894 C22 절단 점수 NULL 회귀 가드 `test_save_and_gate_nulls_score_on_ai_truncated` +1; #893 P2-a 테스트 i18n 키 고정·#895 C12 OTP 6→8 은 단언/생성 변경이라 무증감, 4939→**4940** 단위). + **2026-06-15 starlette 1.0 마이그레이션 #902 +2** (`test_route_helpers.py` registered_paths/route_name_count 견고성 가드 2 [prefix/Mount/중첩 6케이스 + count unique/missing/duplicate]; requirements 핀·라우트 등록 테스트 6 적응은 무증감, 4940→**4942** 단위). + **2026-06-15 마이그레이션 0039/0040 멱등화 #904 +3** (`test_0020_round_trip` PG drift 행동 테스트 1 [운영 사고 재현·pg-concurrency 핀] + test_0039/0040 멱등 가드 정적 2, 4942→**4945** 단위). + **2026-06-16 잔여/후속 #906~#908 +7** (#908 MIGRATION_DATABASE_URL — config 5 [migration_database_url default empty/postgres 정규화/supabase ssl/effective_migration_url fallback/precedence] + env.py ast 회귀 가드 2 [effective_migration_url 사용 강제·database_url 직접 사용 금지]; #906 railway.toml·#907 docs 무증가, 4945→**4952** 단위). + **2026-06-16 회고 P2 #915 +3** (CODE-2 config Supabase SSL host/query-param 파싱 회귀 가드 — pooler SSL / credential `supabase.com` SSL 미강제 / 기존 query `&` 병합·sslmode 중복 방지, 4952→**4955** 단위; #916 docs-only 무증가). (`pytest --co` 단위 4955 + 통합 154 = 5109 수집). + **2026-06-16 회고 P2 #919 +2** (CODE-3 env online connect_args URL 흐름 AST 가드 +1 · TEST-2 effective_migration_url 정규화 결합 +1, 4955→**4957** 단위). (`pytest --co` 단위 4957 + 통합 154 = 5111 수집). + **2026-06-17 #921 차트 hx-boost race 가드 +8** (4 차트 템플릿[dashboard/analysis_detail/repo_insights/repo_detail] `typeof Chart` undefined early-return + vendor `<script>` onload no-anim 재빌드 + `_<scope>ChartReady` 노출 회귀 가드 `test_chart_race_guards.py` 8, 4957→**4965** 단위). (`pytest --co` 단위 4965 + 통합 154 = 5119 수집). + **2026-06-17 품질감사 P2 #926 +6** (resilience logs JSONDecodeError 가드 1 + openai fallback 메트릭 대칭 1 + config validator 멀티필드 통합 회귀 가드 4 [fallback/worker postgres 정규화·빈-값 passthrough], 4965→**4971** 단위). (`pytest --co` 단위 4971 + 통합 154 = 5125 수집). + **2026-06-17 repos 점수추이 차트 #929 +3** (`test_dashboard_loads_chartjs_in_repos_mode` 정적 가드 1 + repos 모드 Chart.js 로드 렌더 단위 2 [score_trend>1 로드·1점 경계 미로드], 4971→**4974** 단위; E2E `test_repos_mode_score_trend_chart_renders` +1 별도). (`pytest --co` 단위 4974 + 통합 154 = 5128 수집). + **2026-06-18 AI 리뷰 max_tokens parse_error 근본 수정 #931 +4** (max_tokens 충분성 회귀가드 +1 + stop_reason 절단 마커 +1 + 정상 케이스 보존 +1 + settings configurable +1, 4974→**4978** 단위). (`pytest --co` 단위 4978 + 통합 154 = 5132 수집). + **2026-06-18 repo_detail I18N 스코프 차트 fix #933 +1** (I18N 전역 스코프 정적 가드 +1 [window._repoChartI18N 고유 전역 노출 + 범용 window.I18N 할당 회귀 차단 + buildChart 지역 참조]; E2E repo_detail scoreChart +1 별도, 4978→**4979** 단위). (`pytest --co` 단위 4979 + 통합 154 = 5133 수집). + **2026-06-18 개요 count-up 0/100 고착 안전망 + 이중 init P1 (commit 1c0a483/75f942e) +1** (`onceInView` IO-miss rAF 안전망 정적 가드 +1 [`test_oncein_view_has_io_miss_safety_net` — scroll/resize 지속 sweep·`!el.isConnected` 자가정리]; 이중 init dispose 회귀 수정[75f942e]은 가드 단언 교체[`_disposers.pop/push`·`while (_disposers` 부재 단언 — 기존 `_disposers in src` false-pass 교정]라 단위 무증가, E2E +3 별도, 4979→**4980** 단위). (`pytest --co` 단위 4980 + 통합 154 = 5134 수집). + **2026-06-18 정적 자산 immutable 캐시 stale 사고 수정 +1** (`CachedStaticFiles` `Cache-Control` immutable+1년 → `no-cache` ETag 재검증 — 무버전 `/static` 자산 배포 미전파 stale 차단; `test_static_file_cache_control_revalidates`(no-cache 존재 + immutable/31536000 부재 단언) + `test_static_file_304_on_matching_etag`(재검증 304 가드) + `test_static_404_no_long_cache`, 기존 immutable 가드 교체, 4980→**4981** 단위). (`pytest --co` 단위 4981 + 통합 154 = 5135 수집). + **2026-06-19 2nd-LLM 검증자 OpenAI-호환 base_url 일반화 +7** (`verifier_base_url` config default/env 2 + openai_client base_url SDK·httpx 양 경로 전달 + 기본값 회귀 가드 합계 4 + merge_verifier 전파 1 — OpenAI 비구독자도 무료 OpenAI-호환 공급자[GitHub Models 등]로 추가 비용 0 활성화, 4981→**4988** 단위). + **2026-06-22 폼 입력 a11y aria-label #946 +23** (test_input_aria_labels 20 컨트롤 raw 템플릿 aria-label 정적 가드 parametrize + count-lock 1 + test_detail render-parity 신규 4키 ko/en 2, 4988→**5011** 단위). (`pytest --co` 단위 5011 + 통합 154 = 5165 수집). + **2026-06-22 SonarCloud BLOCKER+CRITICAL 정리 #948~#950 +2** (CORS 미들웨어 순서 정적 가드 + 조건부 CORS 블록 reload 커버 테스트 2 [#948]; S1192 #949·S3776 #950 은 순수 refactor 무증가, 5011→**5013** 단위). + **2026-06-22 후속 S6853 폼 라벨 #957 +9** (`test_label_associated_control.py` — for/id 연결 6 parametrize + dangling 부재 + telegram_link div 전환 + count-lock, 5013→**5022** 단위; #956 hook·#958 docs·#944/#945 deps 무증가). + **2026-06-22 점수 NULL 절단 분리 #960 무증감** (입력 diff 절단을 `_persisted_score_is_unreliable` NULL 트리거에서 제거 → 절단 시 점수 유지; 회귀 가드 `test_save_and_gate_nulls_score_on_ai_truncated`→`test_save_and_gate_persists_score_on_ai_truncated` rename, 단위 불변 5022). (`pytest --co` 단위 5022 + 통합 154 = 5176 수집). + **2026-06-23 native auto-merge SHA-atomicity fail-closed #962 +1** (`head_sha` 미확보 시 terminal[NETWORK_ERROR] 반환 → guardless merge 차단 — 신규 가드 `test_retry_path_fail_closed_network_error_goes_terminal_no_enqueue`[retry path effective_sha 빈 값 시 terminal·enqueue 미발생] + `test_native_automerge` 2건 fail-open→fail-closed rename[`test_get_pr_state_failure_blocks_enable_fail_closed`·`test_enable_with_path_get_pr_state_failure_fail_closed`], 5022→**5023** 단위). (`pytest --co` 단위 5023 + 통합 154 = 5177 수집). |
| 통합 테스트 | **154개** | tests/integration/ — 사이클 81 영역 🅑 모바일 Phase 1 MVP +34 + **사이클 84 i18n PR-18 smoke +11** + **PR #400 repo insights +22** + **사이클 99 #441 +3** (repo_report_api auth·list·404) + **사이클 100~101 누적** (test_retry_concurrency_postgres assertion 강화). 기존 test_settings_mobile.py 일부 Windows cp949 인코딩 오류 제외. + **사이클 143 #690 repo_detail i18n smoke +2** (151→153). + **사이클 165 #817 +1** (claim_decision PG first-writer-wins invariant — 153→154, pg-concurrency CI job 실행 / 로컬 PG 미설정 시 skip). **= 154 collected (PG 동시성 테스트는 pg-concurrency CI job 에서 통과, 로컬 skip)** |
| E2E 테스트 | **121개** | `make test-e2e` (Chromium Playwright) — Phase 3 PR 6 +7 + **사이클 84 i18n PR-16 +14** (3 언어 × 4 페이지 — login/overview/dashboard/settings + Cookie fallback + locale switch). 사이클 94 #372: test_save_success ordering 트랩 차단 (`_reset_repo_config()` 헬퍼) + test_two_column 보류 (`@pytest.mark.skip`). **+ 사이클 106 #500 `@pytest.mark.perf` 12개** (TTFB/FCP/LCP/DCL/Load — 3회 avg/min/max). **+ 사이클 127 #605 `test_nav_handler_survives_hx_boost_renavigation` +1** (hx-boost 3회 재방문 회귀 가드). **+ 사이클 127 #609 사전 실패 8건 해소** (5 TIMEOUT: test_theme.py glass/claude-dark 셀렉터 → pastel/catppuccin 갱신 + 3 ERROR: test_repos_mode.py auth_cookies 픽스처 제거). **+ 사이클 161 #766 `test_reveal_progress_handlers_use_remove_before_add` +1** (base.html _initReveal/_finishProgress hx-boost 핸들러 누적 가드 — 3회 재방문 후 document 슬롯 단일 저장 검증). **+ 2026-06-13 U2 +2** (`test_effects_init_reruns_on_hx_boost` — effects.js init hx-boost 재실행, 3회 재방문 후 `document._fxEffectsHandler` function + `body.fx-ready` 검증 · `test_magnetic_hover_registers_on_overlapping_cards` — Codex mutual 발견 회귀 가드, effect별 독립 추적으로 magnetic mousemove `--mx` 등록 검증). **+ 2026-06-17 repos 점수추이 차트 #929 +1** (`test_repos_mode_score_trend_chart_renders` — repo 선택 + score_trend≥2[서로 다른 날짜] seed → `Chart.getChart('repoTrendChart')` 인스턴스 부착 검증 + pageerror trap, repos 모드 Chart.js 미로드 회귀 봉인). **+ 2026-06-18 repo_detail 차트 #933 +1** (`test_repo_detail_score_chart_renders` — repo_detail(`/repos/{name}`) scoreChart 렌더 + pageerror trap, I18N 스코프 회귀 런타임 봉인). **+ 2026-06-18 개요 count-up 0/100 고착 (commit 1c0a483/75f942e) +3** (`test_overview_score_survives_io_miss`·`test_overview_score_renders_full_load`·`test_overview_score_survives_double_init` — IntersectionObserver no-op 주입 시 `.repo-card__score` "0/100" pre-fill 고착 해제[rAF 안전망] + below-fold 이중 init[IIFE 재실행 + htmx:afterSettle] dispose 회귀 복구[작은 뷰포트 + `document._fxEffectsHandler()` 직접 호출 결정론 재현], pageerror trap). **+ 2026-06-19 개요 점수 실제 네비게이션 회귀 가드 +1** (`test_overview_score_survives_repo_to_overview_nav` — repo 상세 `/repos/{name}` full load → repo↔개요 hx-boost 왕복 3회 후 `.repo-card__score` "0/100" 미고착 검증. 사용자 보고 경로[repo 화면 거친 뒤 개요 이동 시 0/100 고착]를 그대로 재현 — 기존 테스트가 IO no-op + `document._fxEffectsHandler()` 직접 호출로 이중 init 을 인위 시뮬한 coverage gap 보완). **= 121 collected (109 표준 + 12 perf)**. `make test-perf` 로 perf 마커만 선택 실행. ⚠️ e2e ↔ tests/integration 동시 실행 금지 — `e2e/pytest.ini` 의도적 asyncio_mode 미설정, 분리 실행 default (`make test-e2e` vs CI command `pytest tests/`) |
| SonarCloud Quality Gate | **OK** | #658 ReDoS S5852 hotspot 해소 — **+ #946 폼 입력 20건 aria-label 부여로 `Web:InputWithoutLabelCheck` 해소 → QG ERROR→OK 복구 (2026-06-22, main 재스캔 실측 alert_status=OK)** |
| SonarCloud Security Rating | **A** | Vuln 0, Hotspots 0 (dotnet_format·tsc ReDoS 패턴 #658 수정) |
| SonarCloud Reliability Rating | **A** | Bugs **0** — #946 `Web:InputWithoutLabelCheck` 20건(reliability C) → 폼 컨트롤 aria-label 부여로 **A 복구** (2026-06-22, main bugs=0 실측) |
| SonarCloud Maintainability Rating | **A** | Code Smells **109** (BLOCKER 0·CRITICAL 0·MAJOR 87·MINOR 22 — #948~#957 로 131→109[#957 S6853 폼 라벨 7건 추가 해소], 2026-06-22 재스캔 실측. 잔여 MAJOR/MINOR = maintainability A 유지·게이트 무영향) |
| SonarCloud BLOCKER / CRITICAL | **0 / 0** | #948~#954 로 전부 해소 — BLOCKER `S8414` 1건(#948) + CRITICAL 10건(`S1192` 3 + `S3776` 7, #949~#954). 2026-06-22 재스캔 실측 |
| pylint | **10.00/10** | `python -m pylint src/` — #415 잔여 21건 전체 해소 (C0415 17 lazy import inline disable + C0301 1 + R0913/R0917 3 + W0718/W0613 2 + E0401 1). **10.00 달성** |
| 커버리지 | **Python 97% / JS: E2E 커버** | Python: `make test-cov` (`--cov=src` — 8497줄 중 291 미커버, 97% — 2026-06-12 실측). JS: E2E pageerror 트랩(PR #605) + 정적 스캐너(PR #606) + ESLint(PR #607)으로 보완. `src/templates/*.html` 인라인 JS는 Python 커버리지 미측정 — 언어별 분리 보고 의무 (사이클 127 P0 학습). |
| bandit HIGH | **0개** | bandit 1.9.4 (Python 3.12+ 대응) |
| flake8 | **0건** (E501 16건 soft-pass — `|| true` 가드, CI 차단 없음) | `flake8 src/` |
| 지원 언어 (AI 리뷰) | **50개** | language.py — Tier1/2/3 가이드 |
| 지원 언어 (정적분석) | **52개+** | Semgrep 22 + ESLint/tsc 2 + ShellCheck 1 + cppcheck 1 + slither 1 + rubocop 1 + golangci-lint 1 + Python 3 + **신규 15**: Dockerfile(hadolint)·Kotlin(ktlint)·HCL(tflint)·SQL(sqlfluff)·YAML(yamllint)·PHP(phpstan)·Swift(swiftlint)·CSS(stylelint)·SCSS(stylelint)·HTML(htmlhint)·Protobuf(buf_lint)·Dart(dart_analyze)·PowerShell(psscriptanalyzer)·C#(dotnet_format)·Rust(clippy) |
| Tier1 정적분석 도구 | **25종** | pylint·flake8·bandit·semgrep·eslint·shellcheck·cppcheck·slither·rubocop·golangci-lint·**hadolint**·**ktlint**·**tflint**·**tsc**·**sqlfluff**·**yamllint**·**phpstan**·**swiftlint**·**stylelint**·**htmlhint**·**buf_lint**·**dart_analyze**·**psscriptanalyzer**·**dotnet_format**·**clippy** |
| pytest-asyncio | **1.4.0** | Python 3.12+ DeprecationWarning 제거 완료 · **SSOT=Python 3.12** — CI(`.github/workflows/ci.yml` python-version) + Railway(`.python-version`=3.12, nixpacks 핀; default 3.11) + docs 동일 (로컬 dev 3.13 = 3.12+ 호환). 3.14 미검증 선언 정정 (#22) |
| CodeQL | **✅ pass** | `.github/workflows/codeql.yml` — 주 1회 실행. 본 README 배지 (L15) 와 페어 |

## 주요 파일 역할 (빠른 참조)

| 파일 | 역할 |
|------|------|
| `src/constants.py` | 전역 상수 단일 출처 — 점수배점·감점·AI기본값·등급·알림한도·TTL·타임아웃 |
| `src/analyzer/pure/registry.py` | Analyzer Protocol + REGISTRY + register() + AnalyzeContext + AnalysisIssue + Category/Severity StrEnum |
| `src/analyzer/io/tools/*.py` | 개별 분석기 — 모듈 로드 시 자동 register() 호출 (Phase S.3-B 이후 `pure/` vs `io/` 분리) |
| `src/notifier/_common.py` | notifier 공통 헬퍼 — format_ref, get_all_issues, truncate_message |
| `src/notifier/_http.py` | HTTP_CLIENT_TIMEOUT 적용 httpx 클라이언트 빌더 |
| `src/webhook/_helpers.py` | `get_webhook_secret()` + `_webhook_secret_cache` per-repo TTL 캐시(5분) |
| `src/webhook/loop_guard.py` | 자기 분석 루프 방지 3-layer (kill switch / bot sender / skip marker + rate limit). `is_whitelisted_bot()` 헬퍼로 화이트리스트 봇만 BotInteractionLimiter 적용 |
| `src/webhook/router.py` | Webhook 라우터 aggregator — providers 3개 include |
| `src/gate/engine.py` | 3-옵션 Gate + GateDecision upsert (중복 INSERT 방지) + MergeAttempt 관측(Phase F.1) |
| `src/gate/merge_reasons.py` | auto-merge 실패 사유 정규 태그 상수 (Phase F QW5) |
| `src/gate/merge_failure_advisor.py` | `get_advice(reason)` — reason tag → 권장 조치 텍스트 (Phase F.3, 순수 함수) |
| `src/notifier/merge_failure_issue.py` | `create_merge_failure_issue()` — auto-merge 실패 GitHub Issue (Phase F.3, dedup 24h) |
| `src/models/merge_attempt.py` | MergeAttempt ORM — score/threshold 스냅샷 + failure_reason 태그 (Phase F.1, append-only) |
| `src/shared/merge_metrics.py` | parse_reason_tag + log_merge_attempt — DB INSERT + 구조화 로그 (Phase F.1) |
| `src/repositories/` | DB 접근 계층 11종 — repository_repo (`find_by_full_name` + Phase H 신규 `find_by_full_name_with_owner` opt-in joinedload), analysis_repo, analysis_feedback_repo, merge_attempt_repo, gate_decision_repo, repo_config_repo, user_repo, merge_retry_repo, insight_narrative_cache_repo (0033: `record_error` / `record_error_repo` 에러 빈도 추적), security_alert_log_repo, issue_registration_repo |
| `src/worker/pipeline.py` | 분석 파이프라인 + build_analysis_result_dict |
| `src/models/merge_retry.py` | MergeRetryQueue ORM — 재시도 큐 (append-only claim 패턴) |
| `src/repositories/merge_retry_repo.py` | enqueue_or_bump · claim_batch · mark_succeeded/terminal/expired — 원자적 SKIP LOCKED 클레임 |
| `src/gate/retry_policy.py` | 순수 함수: parse_reason_tag · should_retry · compute_next_retry_at · is_expired |
| `src/github_client/checks.py` | get_ci_status · get_required_check_contexts (5분 TTL 캐시) |
| `src/services/merge_retry_service.py` | process_pending_retries — CI-aware 재시도 워커 |
| `src/gate/native_automerge.py` | enable_or_fallback() — GraphQL `enablePullRequestAutoMerge` 우선 + REST `merge_pr` 폴백 (Tier 3 PR-A, 그룹 52) |
| `src/gate/_merge_attempt_states.py` | MergeAttempt.state lifecycle 정규 상수 (LEGACY/ENABLED_PENDING_MERGE/ACTUALLY_MERGED/DISABLED_EXTERNALLY) — Phase 3 PR-B1 도입 |
| `src/github_client/graphql.py` | GraphQL POST 래퍼 + `enablePullRequestAutoMerge` mutation + `EnableAutoMergeResult` 분류 + 5xx 자동 재시도 (Phase H PR-1B-2, `_GRAPHQL_*` 상수) |
| `src/static/vendor/chart.umd.min.js` | Chart.js 4.4.0 UMD min vendoring (UI 감사 Step C) — CDN 차단/오프라인 환경에서 빈 차트 회피. `src/main.py` 의 CachedStaticFiles (`Cache-Control: no-cache` ETag 재검증) mount 로 노출. 사용 페이지: repo_detail / analysis_detail / dashboard. 운영 가이드: `docs/runbooks/static-assets.md` (PR-D4) |
| `src/main.py` `CachedStaticFiles` | `StaticFiles` 서브클래스 — 200/304 응답에 `Cache-Control: no-cache`(ETag 재검증) 자동 주입. 🔴 무버전 URL(`/static/js/effects.js` 등)이라 immutable 장기 캐시 금지 — immutable+1년은 배포 후 구 JS/CSS 를 최대 1년 서빙(2026-06-18 stale 사고, #936 라이브 미반영). `no-cache` 는 변경 시 즉시 전파·미변경 시 304(본문 재다운로드 없음) (사이클 97 #423 도입, 2026-06-18 immutable→no-cache 정정) |
| `src/services/repo_insight_service.py` `compute_score_kpi` | 공유 헬퍼 — cur/prev 분석 리스트 → avg_score/score_delta/grade 계산. `repo_kpi` + `dashboard_service.repo_insight_cards` 양쪽에서 재사용 (CPD 제거 목적, 사이클 97 #423) |
| `src/services/dashboard_service.py` `_fetch_analyses_for_window` / `_group_analyses_by_repo` | N+1 제거 배치 헬퍼 — repo_ids IN 절 단일 쿼리 + per-repo cap 그룹화 (사이클 97 #423) |
| `tests/conftest.py` | 환경변수 주입 + _webhook_secret_cache autouse 클리어 + _user_repos_cache autouse 클리어 |

## 작업 이력

- **그룹 13~61** (2026-04 ~ 2026-05-02): [docs/_archive/STATE-groups-13-61-2026-05.md](_archive/STATE-groups-13-61-2026-05.md)
- **사이클 60~166** (2026-05-03 ~ 2026-06-09): [docs/cycle-history.md](cycle-history.md)
- **정합성 감사 P2 백로그 처리 — 6 PR (#874~#879)** (2026-06-12): 사용자 "C1 + P2 전부" 결정 → integrity-audit full 백로그를 테마별 cohesive 배치로 처리. **#874** dead-code(C23 `review_prompt.has_test_files`·C24/C25 `merge_reasons` has_hooks/clean dead 엔트리+OPTIONAL_CHECK_ONLY dead 상수·C26 expired↔abandoned docstring) · **#875** webhook/notifier 보안(C13 railway exc·C20 telegram repo.full_name 로그 sanitize·C27 telegram 봇 응답[parse_mode=HTML] 사용자값 전수 html.escape) · **#876** pipeline(C10 pylint/bandit 파서 item[] 직접 subscript→`.get()` 방어[키 누락/None KeyError fail-open 차단]·C18 AI리뷰 `detect_languages_from_patches` content 전달[shebang-only 스크립트 감지]) · **#877** db/i18n/관측(C14 insight 캐시 `invalidate` .first()→bulk delete[cross-lang eviction]·C28 i18n `loader.format` ValueError 포착[렌더 500 차단]·C11 merge_retry 관측 threshold snapshot→live `cfg.merge_threshold`) · **#878** test/UI(C30 dashboard grade dead assertion `==B`·U3 analysis_detail 트렌드 nav `encodeURIComponent(REPO_NAME)`) · **#879** CodeQL fix-up(#873 ORM import py/unused-import #507~514 → 클래스 import+`_REGISTERED_MODELS` 참조). 전 PR TDD·Codex mutual OK(다수 NG[C10 None·C11 failure 경로·C9 주석·C30 테스트 등]→동일 PR 수정→재검증 OK, 정책 18 §3b)·CI green·pylint 10.00. 단위 4907→4915·통합 154·전체 5069. **🔴 백로그 보류 5건** ([[project-audit-backlog-2026-06-12]]): C1(`save_gate_decision` dead wrapper+55 inert patch — git -S 로 #712 마지막 호출 제거 reconcile, 2026-06-11 deep-research FP-기각이 부정확 확인; 대규모 테스트 리팩토링이라 별도 세션) · C12(OTP brute-force rate-limit — 상태 설계 feature) · C22(AI리뷰 diff 16000자 무음 절단 score-integrity 비대칭 — AI리뷰→pipeline→gate 다층 신호 전파 + 정책 16 제외 영역) · U1(0027 RLS legacy user_id NULL 제외 — migration 민감·미검증) · U2(effects.js hx-boost 애니메이션 미재실행 — E2E 필요).
- **전체 정합성 감사 — 보안/correctness P1 4 PR (#868~#871)** (2026-06-12): 사용자 "다음 작업" 위임 → `/integrity-audit full`(wf_7fb7c8f8, 179 에이전트·3라운드·14M 토큰) → 확정 32(P0 1·P1 9·P2 22)+미검증 4·위양성 11. 사용자 결정 = 보안/correctness P1만 수정 + 나머지 백로그. **#868** P0 — hook `/result` 빈/NULL 토큰 인증 우회(`secure_str_compare(None,"")`=`b""==b""`=True → hook_token NULL 행에 빈 토큰 인증 통과 → 위조 Analysis/점수 오염; `not body.token or not config.hook_token` 가드, verify 엔드포인트 대칭; 🔴 `secure_str_compare` **8 호출처 전수** — 나머지 7 상류 가드 안전). **#869** U0 — dashboard security 모드 cross-tenant 노출(라우트 require_login+user_id 전달하나 `dashboard_security` 무시 → 전체 테넌트 알림 노출; `security_alert_log_repo._apply_owner_filter`(repo_id→Repository.user_id) + user_id 전달; alert.user_id 는 pending 시 NULL이라 repo 소유권 격리; RLS owner-bypass라 앱 필터 유일 방어). **#870** C6+C2 AI-fail fail-open 2건 — approve `_run_semi_auto`에 static_incomplete+ai_review_failed 가드 미러링(_run_auto 대칭) + ai_review `_parse_response` commit/direction keys_present 검사(hook `_coerce_ai_scores` parity — 키 누락 시 인플레 default success 위장 차단). **#871** C3 — merge_retry per-row broad-except 격리(단일 행 예상외 예외→전체 배치 중단 방지; 🔴 Codex NG→terminal 행 status='pending' 가드 추가→재검증 OK). 전 PR TDD(+12: #868+3·#869+1·#870+6·#871+2)·Codex mutual OK·CI green·pylint 10.00. 단위 4895→4907·통합 154·전체 5061. **백로그 정정**: 커버리지 95→97%(8497줄/291 실측, C4)·pytest-asyncio 1.3.0→1.4.0(C29)·env-vars.md VERIFIER 상수 2종+`.env.example` MERGE_VERIFIER_DISABLED(C7/C9). **나머지 백로그**(P1 tests: C1 test_engine dead assert·C5 migration import / P2 22 / 미검증 4) = [[project-audit-backlog-2026-06-12]] 차기 세션.
- **잔여/후속 세션 — #865 검증자 봉인 P1-1 반자동 parity (engine 단일출처화)** (2026-06-12): 사용자 "순서대로 수행 → 머지후 다음작업" → #863(P1-4)·#864 sync 머지 후 ② **#865 머지**: 2nd-LLM 검증자 봉인 P1-1 — 검증 가드(should_verify→verify_merge_safety→차단 시 PR 코멘트)를 `merge_verifier.verifier_blocks_merge()` 로 추출 + `engine._run_auto_merge` 진입부(threshold 직후·`SessionLocal` 전) **단일출처화** → 자동(AutoMergeAction)·반자동(telegram handle_gate_callback) 양 경로 공유(이전엔 자동만 → 반자동 검증자 우회 = parity 갭). 호출자 `result` 전달(자동 `ctx.result`·반자동 `result_dict`). 🔴 retry 서비스(`process_pending_retries`)는 `_run_auto_merge` 미경유(merge_pr 직접)라 CI 재시도 재검증 안 됨(초기 머지 1회만). 사용자 **Option A confirm**(정책 18 §3a + gate High tier). TDD 14(verifier_blocks_merge 7 + engine 가드 6 + telegram parity 1, 기존 auto_merge_verifier 5 교체), pipeline-reviewer **APPROVE**(P0 0 — 동작 보존·fail-closed·NPE 안전·import 순환 0 실측), Codex mutual **OK**(5항목). `.claude/rules/api.md`·`docs/architecture.md` sync. 검증자 **INACTIVE라 운영 동작 영향 0**(`OPENAI_API_KEY` 미설정 시 `should_verify=False` → 활성화 시 발효). pylint 10.00, flake8 신규 0. 단위 4886→4895(+9)·통합 154·전체 5049. **검증자 봉인 P1 코드상 3건 전부 완료**(interpret_verdict #861·diff cap #863·parity #865) → 활성화(`OPENAI_API_KEY` BYO, 사용자)만 잔여. P1(reviewer 권고 — 검증자 차단 시 MergeAttempt DB 미기록)은 #859 회고 P2 백로그 "verifier-blocked DB 기록"으로 활성화 PR 재검토.
- **잔여/후속 세션 — #863 머지 (검증자 봉인 P1-4)** (2026-06-12): 사용자 "잔여/후속 작업 확인 → 순서대로 수행" → 다중 소스 적대 교차검증(워크플로 rate-limit → 6 도메인 인라인 대체 실측) 후 ① **#863 머지**(검증자 활성화 봉인 P1-4 — diff/token cap): `diff_exceeds_cap(patches)` True 시 OpenAI **미호출 + fail-closed 차단**(`VERIFIER_BLOCKED`, 비용 0, Codex Option A) · `_assemble_diff_text` 단일출처 · `VERIFIER_DIFF_CHAR_CAP=60000` · `VERIFIER_MAX_OUTPUT_TOKENS=8192` · `max_completion_tokens`(SDK+httpx). Codex mutual OK(머지 전)·CI 8/8 green. 단위 4880→4886(+6)·전체 5040·pylint 10.00. **잔여 = #2 RLS owner-bypass Phase 4 운영 전환(사용자 — PW 교체+`DATABASE_URL[_WORKER]` 설정+pooler `SET LOCAL` 검증, [`runbooks/rls-role-separation.md`](runbooks/rls-role-separation.md) §Phase 4) + 검증자 활성화 봉인 P1-1 반자동 Telegram parity(`engine._run_auto_merge` 단일출처화, 설계 confirm 대기) + 검증자 활성화(`OPENAI_API_KEY` BYO, 사용자)**.
- **잔여/후속 세션 — docs 정합·사이클 166~#859 회고·P2 하드닝** (2026-06-11, #860/#861): 사용자 "잔여 작업 및 후속 작업 확인" → read-only sweep(wf_08d70ad8) 후 결정 C. 메인 CI flake(`httpcore==1.*` pypi timeout) 재실행 green 복구. **#860**(docs): db-migration 0032~0041·SMTP_FROM 유령 제거·reports/INDEX 13건 백필·.env.example OPENAI 주석 이중용도+검증자 env·env-vars 내부상수 2건 + **사이클 166~#859 5+1 회고** 아카이브(`docs/_archive/reports/2026-06-11-cycle-166-859-retrospective.md`, P0 1·P1 8·P2 다수, cross-verify 22/24 TRUE). **#861**(fix): `interpret_verdict` fail-closed 엄격 파싱(`raw["safe"] is True`/`manipulation_detected is not False`, bool() truthy 함정 차단, 회귀 +16)·`_call_via_http` httpx fallback 테스트·CI `-rs`+pip `--retries 5 --timeout 60` + CodeQL fix-up(테스트 URL 부분문자열→정확 비교, py/incomplete-url-substring HIGH). Code Scanning #504(`openai_metrics.py:32` `await` FP) dismiss→open 0. Secret Scanning #1 `telegram_bot_token` revoked/resolved 확정(dangling `7d0fa1fe` 실위험 0). 사용자 결정 3건: 검증자 INACTIVE 유지(봉인 P1 2건 백로그)·secret purge 보류 권고·자율 후속 수행. 전 PR Codex mutual OK·CI 8/8 green. 단위 4864→4880(+16)·전체 5034·pylint 10.00.
- **2nd-LLM 머지 검증자 (cross-vendor AI 거버넌스 가드)** (2026-06-11, feat/merge-verifier): 사용자 "분석/동작 체계가 AI 거버넌스인가, 2nd LLM 상호보완 필요한가" 문의 → SCAManager 는 "AI 활용 거버넌스 + 부분 AI 거버넌스 속성"이나 **AI 판단 자체 미검증** 갭 식별 → 옵션 B(2nd LLM 검증자) 선택. 브레인스토밍 스킬→spec→writing-plans→subagent-driven 9 task 구현. **OpenAI GPT 가 경계밴드 자동머지 후보**(merge_threshold ≤ score < +band)의 (1)머지 안전성 (2)Claude 리뷰 조작/환각을 적대적 판정 → unsafe/조작/검증자오류 시 **자동머지 차단 + PR 코멘트**(재채점 X, fail-closed). 🔴 **순수 opt-in**: `OPENAI_API_KEY` 미설정 시 완전 비활성(비용 0, 동작 변화 0 — BYO key + 경계밴드 표적화 + 저가 소형 모델, 정책 16 정합). OpenAI SDK 우선 + httpx fallback. 인젝션 방어 `<untrusted-data>` 경계 + 시스템 프롬프트 역할 고정. 신규 3 모듈(`gate/merge_verifier.py`·`verifier/openai_client.py`·`shared/openai_metrics.py`) + `AutoMergeAction` 가드 + `post_plain_pr_comment` + `merge_reasons` 태그 2 + config 4 env. 최종 코드리뷰 P0 0건(fail-closed·비용불변 코드 검증) → Important 2건 반영(band ge=1 validator·차단 로그 태그). 단위 4838→4864(+26), 전체 5018, pylint 10.00, 발신 한국어 가드 #155 준수. 절차/설계: `docs/superpowers/specs/2026-06-11-merge-verifier-design.md`(gitignore). 🔍 사용자 검증: `OPENAI_API_KEY` 운영 설정 시 실 PR 경계점수 차단 동작 + 모델 ID(`gpt-5-mini` 기본 — 운영 시 확정).
- **정합성 감사 + deep-research follow-up: pipeline AI-fail 점수 NULL-persist (P1) + literal→상수 (P2)** (2026-06-11): 다이나믹 워크플로우(integrity-audit full) + deep-research 검토에서 발견한 미검증 후보를 직접 실측 검증 후 수정. **P1**: AI 리뷰 genuine 실패(`ai_review_status ∈ {api_error, parse_error}`) 시 webhook pipeline(`_save_and_gate`)이 인플레 기본 점수(17/17/7→~89/B)를 `Analysis.score/grade` 에 영속 → 대시보드/리더보드 집계 오염. hook 경로(#25/#814)는 이미 NULL-persist 였으나 **pipeline 경로 비대칭** → `ai_review_failed(result_dict)` 게이트로 NULL 저장 통일(result dict 의 status/breakdown 보존). `no_api_key`/`empty_diff`(의도적 미수행)는 점수 유지(회귀 방지). **P2**: `ai_review.py::_default_result` literal 17/17/7 → `AI_DEFAULT_*_RAW` 상수(값 동일, drift 방지). 회귀 가드 4(api_error/parse_error→NULL·success/skip→유지). pipeline.md NULL-persist 대칭 규칙 추가. pylint 10.00, flake8 0. 단위 4826→4830(+4), 통합 154 불변, 전체 4984. (verdict: 미검증 후보 10건 중 REAL 4·하드닝 P2 4·borderline 1·FP 1 — 본 PR=pipeline 그룹, 나머지 후속 PR.)
- **정합성 감사 + deep-research follow-up 후속 4 PR — webhook/api/ui 하드닝 + docs 정합** (2026-06-11, #854~856·#852): 위 #853(pipeline) 의 "나머지 후속 PR". **#854(webhook P1+P2)**: pre-auth secret 캐시 무한 증가 차단(`_store_secret` forged `full_name` 마다 엔트리 → DoS) `WEBHOOK_SECRET_CACHE_MAX=2048` 상한 + telegram/railway 로그 `sanitize_for_log`. Codex 5/5. **#855(api P2)**: webhook URL SSRF 검증 storage 단일출처화 `shared/ssrf.py::is_safe_webhook_url`(settings+repos field_validator 공유) + email To 헤더 CRLF strip. Codex 5/5. **#856(ui P2)**: base.html `_kpiCountupHandler` 를 `htmx:historyRestore` 에도 등록(#473 대칭, 뒤로가기 KPI count-up 재초기화). Codex 4/4. **#852(docs)**: architecture WorkerSessionLocal·STATE 60~166·i18n 쿠키명 정정 + runbook §4 Phase 4 pooler(`SET LOCAL`) 검증 항목. 전 PR Codex mutual OK·CI green. 단위 4830→4838(+8: #854 +3·#855 +3·#856 +2·#852 0, 각 머지 커밋 collect-only 실측), 통합 154 불변, 전체 4992, E2E 113, pylint 10.00. **borderline/FP 기각 2건**(`merge_retry_service.py:142` observability nit · `test_engine.py:39` 'dead patch' 오탐 — `save_gate_decision` auto 경로 사용 중). **최종 잔여 = #2 RLS owner-bypass Phase 4 운영 전환만**(코드 Phase 1~4 완료, 사용자 — 임시 PW 교체 + `DATABASE_URL`/`DATABASE_URL_WORKER` 설정 + pooler `SET LOCAL` 테넌트 격리 실측). 절차: [`docs/runbooks/rls-role-separation.md`](runbooks/rls-role-separation.md) §Phase 4.
- **RLS #2 Phase 4 admin 대시보드 cross-tenant 보존 — api/admin·ui/routes/admin hybrid** (2026-06-10, #849 후속): #849 머지 후 사용자 "이후 작업 수행" → 마지막 코드 가능 #2 항목 해소. admin 은 `require_admin` 세션이 있지만 `tenants`(`tenant_inventory` — User/Repository/Analysis)·`operations`(`operations_kpi` — MergeAttempt/User)는 **전체 테넌트 집계**라 Phase 4 admin 세션 RLS(`app.user_id=admin`)가 admin 본인 행만 남겨 under-report. 두 엔드포인트만 신규 `_get_worker_db`(BYPASSRLS) 의존성으로 분기 — cross-tenant 가시성 보존. 🔴 **`rls-audit` 는 `_get_db`(web) 유지**: `connection_bypasses_rls` 가 현재 connection 의 rolbypassrls 를 실측하는 진단이라 웹 app role 로 평가돼야 정확(worker=BYPASSRLS 면 항상 우회=TRUE 오진단). api/admin·ui/routes/admin 2 모듈 `_HYBRID_DB_MODULES` 편입(github.py 와 동일 hybrid 패턴 — bare+worker 둘 다 import). 엔드포인트별 세션 라우팅 = sentinel 회귀 가드 3(distinct worker/web 세션 주입 후 각 서비스가 받는 db 검증 — 정적 import 가드가 못 잡는 endpoint swap 차단). Codex mutual 검증(6/6). pylint 10.00, flake8 0. 단위 4817→4826(+9: test 10 hybrid 계약 확장 4 + sentinel 3 + `_get_worker_db` 제너레이터 커버 2[CI coverage-on-new-code fix-up]), 통합 154 불변, 전체 4980. **Phase 4 코드 차단 경로 전부 해소(OAuth 콜백 + 시스템 API 3종 + admin 2종) — #2 잔여 = Phase 4 운영 전환만**(사용자 — PW 교체 + `DATABASE_URL`/`DATABASE_URL_WORKER` 설정). 절차: [`docs/runbooks/rls-role-separation.md`](runbooks/rls-role-separation.md) §Phase 4.
- **RLS #2 Phase 4 OAuth 로그인 blocker 해소 — auth_callback worker 세션 전환 (옵션 ②)** (2026-06-10, 사용자 결정 위임): 적대적 검증 P1 blocker(OAuth 콜백 users self-RLS 차단 → `scamanager_app` 비-owner 전환 시 전원 로그인 장애) 해소. `auth_callback`(`src/auth/github.py`)의 users upsert(`find_by_github_id` SELECT + 신규 User INSERT)만 `WorkerSessionLocal`(BYPASSRLS worker role, 시스템 컨텍스트)로 전환 — 콜백 시점 `session["user_id"]` 부재(`app.user_id=''`)로 인한 차단 우회. `logout` 은 세션 존재(본인 행)라 bare `SessionLocal`(웹 RLS 경로) 유지. Phase 2 라우팅 가드 재설계: `_HYBRID_DB_MODULES`(github.py = bare+worker 둘 다 import 허용) 카테고리 도입 + test 6 예외 + test 7a expected 확장 + hybrid 계약 가드 2종(test_worker_session_routing 테스트 10 — 양 import 존재 + `as SessionLocal` alias 금지). test_github.py 콜백 patch 8건 `WorkerSessionLocal` 전환(logout 2건 `SessionLocal` 유지). **+ Codex mutual(정책 18) 발견 — 시스템 API 라우트 3종**(`api/repos`·`stats`·`repo_report`, `require_api_key` 세션 없이 cross-tenant 조회)도 Phase 4 차단 대상 → `WorkerSessionLocal as SessionLocal`(background 패턴, 모듈 심볼 유지로 기존 patch 불변) 전환 + `_SYSTEM_API_MODULES` 라우팅 가드 신설(`_WEB_API_MODULES` 에서 분리). `DATABASE_URL_WORKER` 미설정 동안 `WorkerSessionLocal is SessionLocal` = 현행 완전 동일(Phase 4 worker URL 설정 시 자동 발효). pylint 10.00, flake8 0. 단위 4809→4817(+8: hybrid 2 + 시스템 API 재라우팅 가드 6), 통합 154 불변, 전체 4971. **#2 잔여 = Phase 4 URL 전환(운영, 사용자 — PW 교체) + `/admin/tenants` under-report(admin 세션 RLS = 권한 상승 별도 결정)**. 절차: [`docs/runbooks/rls-role-separation.md`](runbooks/rls-role-separation.md) §Phase 4.
- **RLS Phase 1 운영 + Phase 3 — 0041 FORCE + 실측 가시화** (2026-06-10): Phase 1 = Supabase role 분리 운영 완료(Claude MCP 직접 실행, 정책 12 승인 — `scamanager_app` NOBYPASSRLS + `scamanager_worker` BYPASSRLS + GRANT 12/12·11/11 + DEFAULT PRIVILEGES, 기능 실증 app 0/8·worker 8/8, 임시 PW 교체 대기). Phase 3 = alembic `0041_rls_force`(11 테이블 리터럴 FORCE, PG-only, downgrade NO FORCE) + `rls_coverage_summary(db)` 실측 2종(`force_applied` pg_class 11/11 bound-param + `connection_bypasses_rls` rolbypassrls OR rolsuper) + `/admin/rls-audit` BYPASSRLS 우회 경고 배너(3 locale — Phase 3~4 거짓 안심 창 봉인) + scripts/backfill worker alias + ENABLE↔FORCE bijection 가드 + PG-live round-trip(ci.yml 핀). 적대적 3-렌즈 verify 적발 **Phase 4 P1 blocker**(OAuth 콜백 users self-RLS 차단 → 전원 로그인 장애) → runbook §Phase 4 blocker + 로그인/admin smoke + pre-flight 봉인 — 근본 전략(정책 컨텍스트 키/auth worker 세션/WITH CHECK 분리) = 사용자 결정 대기. 단위 4787→4809(+22)·전체 4963. **#2 잔여 = Phase 4(운영, 사용자 — PW 교체 + OAuth 전략 결정 선행)**.
- **RLS Phase 2 — background 전용 worker 세션 분리** (2026-06-10, 옵션 A — 사용자 "가장 권장하는 옵션" 위임, #847): 정합성 감사 #2 의 선행 코드 — `DATABASE_URL_WORKER` env(+postgres:// 변환 validator) + `database.py::_build_worker_session_factory`/`WorkerSessionLocal`(미설정 시 `SessionLocal` **동일 객체** 재사용 = 현행 동작 보존, 설정 시 독립 단일 엔진 — failover 없음·RLS SET LOCAL listener 미등록·활성 로그 1줄) + background 16 모듈 `WorkerSessionLocal as SessionLocal` alias 전환(모듈 심볼 유지 = 기존 테스트 patch 불변 — worker/pipeline·webhook providers 3+_helpers·gate engine+actions 2·notifier lazy 6·internal_cron·hook) + ast 정적 라우팅 가드 52 테스트(TDD Red 45→Green 48 + Codex R1/R2 강화 4: 전수 inventory 양방향 bijection[미분류 신규 import 자동 fail] + 재바인딩 금지 + 모듈 객체 import 금지). pipeline-reviewer **APPROVE**(P0 0) — P1 docs 2건(env-vars/.env.example)·P2-1 활성 로그 반영. db.md/deploy.md/runbook/env-vars/.env.example/README env 표 sync. 단위 4735→4787(+52), 통합 154 불변, pylint 10.00, flake8 E501 15(baseline 동일). **#2 잔여 = Phase 1 role 생성·Phase 4 URL 전환(운영, 사용자) + Phase 3 FORCE 마이그레이션(운영 선행 후 코드)**.
- **잔여 정리 라운드 A옵션** (2026-06-10): 전수 스캔 워크플로우(5+1 read-only, wf_851d8529) 신규 발견 정리 — PR #838~#845 본문 8건 리터럴 `@-` 소실 → squash 커밋 메시지 복원(GitHub-side, #839 정책 11 섹션 회복 포함) + **정책 10 진화: PR 본문 `--body-file` 전달 + 생성 직후 길이 검증 의무** 신설(#846) + Code Scanning 12건 dismiss+사유(open 12→0, 전건 FP/의도 패턴) + smoke-checks §8.7 RLS stale 행 + `rls_session.py` docstring stale 참조 정정 + 메모리 dangling commit 상충 동기화. 주석/docs-only, 테스트 4889 불변. 잔여 = #2 불변.
- **사이클 166** (2026-06-09): Task9 full 감사 P2 백로그 해소 5 PR(#820~#824) — 사용자 "잔여 작업 확인" → 잔여 백로그 코드 실측 검증 워크플로우(wf_d1e440d5, 6에이전트 read-only) 후 자율 가능 P2 10건 처리. **빠른 정합 묶음**: #820 docs/rule(#19 README cron 2개·#20 architecture scripts 2파일·#21 env-vars config.py line·#28 security.md SESSION_SECRET 기본값) · #821 db/test(#17 insight_cache repo_id index=True 유령 인덱스 제거+가드 Red2→Green1·#31 test_0029 dead-branch) · #822 effects.js dead-code(#33 setupTabs/setupNavMagnet, BEM 셀렉터 템플릿 사용 0, −91줄, 고아 CSS follow-up 분리). **UI Medium 묶음**: #823 hx-boost 리스너 누적(#35 add_repo pagehide·#36 tweaks keydown remove-before-add + 정적 가드) · #824 i18n_args 이중이스케이프(#34, 보안 민감 — 사용자 Option A: 수동 escape 제거 autoescape 위임, B′ Markup 래핑은 literal-< 번역문 깨짐 기각, safe-contract allowlist 방어선 가드). 전 PR Codex true mutual OK(5/5), 모든 CI green. 단위 4718→4723(+5), 통합 154 불변, pylint 10.00. 🔴 #34 학습: 감사 에이전트가 overview.html 1곳만 보고 |safe 22건 놓침 → 보안 수정은 호출처 전수 grep + Codex mutual 필수. **후속 머지(사용자 '순차적 작업')**: #826(#15/#16 ORM↔alembic 인덱스 정합)·#827(#33 고아 CSS −156줄)·#828(#14 analyses.repo_id FK CASCADE 0038, PG-only round-trip CI 검증), 단위 4723→4726. **적대 재검증 후속 (2026-06-09, wf_688dd1f2 — 4 PR #838~#841 머지)**: STATE '#2 외 전부 해소' overclaim + #32 'resolved' 위양성 적발 → #838(docs 정합 정정)·#839(#32 settings JS-리터럴 tojson 17곳)·#840(drift ④ repositories.user_id FK SET NULL+고아정리, 0039)·#841(drift ① users 인덱스명 rename, 0040, #840 스택). 전 PR Codex mutual OK·PG-only CI green. 단위 4730→4733(+3). **잔여작업 라운드 (2026-06-09~10, 사용자 결정 C — 2 PR #843/#844)**: #843(drift ③④' ORM 부분 인덱스 선언 — analyses/insight_cache `postgresql_where`+`sqlite_where` 양 방언, allowlist ③④ 제거, PG compare_metadata 정합·**WHERE-FP 미발생**, 단위 4733→4735) · #844(#2 RLS owner-bypass 근본 해결 운영 runbook — `docs/runbooks/rls-role-separation.md`: role 분리+background 전략 3옵션+FORCE 절차, docs-only). 전 PR Codex mutual OK. **최종 잔여 = #2(RLS·SaaS — role 분리 runbook 준비완료, 운영 실행+Phase 2 background 코드 PR 대기)만** — full 감사 36건 drift①③④'·#32 전부 해소. 결정 완료 #14/#22/#23/#2(SaaS).
- **사이클 165** (2026-06-08~09): Task9 골든 리메디에이션 11 PR(#802~#814) — P1 9/10 + P2 보안·파이프라인 하드닝 클러스터 5/5. **P1 (#802~810)**: telegram authz(#1)·`secure_str_compare` 전 compare_digest 적용(#9/#10 + 비-ASCII 변종 #26/#27/#29/#30)·AI-fail auto-merge 차단(#8)·content-fetch/tool-timeout incomplete fail-open 봉인(#6/#7)·hook SHA race-safe(#5)·check_suite force-due(#4)·docs(#3)·RLS FORCE 갭 가시화(#2-D). **P2 클러스터 (#811~814, 사용자 AskUserQuestion 선택)**: **#811(#11+#13)** 게이트 콜백 리플레이 가드 `gate_decision_repo.claim_decision`(insert-only UNIQUE analysis_id first-writer-wins, save_gate_decision upsert 대체 — 결정 뒤집기 차단) + webhook 본문 파싱 400 · **#812(#24)** ai_review per-field 안전변환 `_coerce_score`(hook `_coerce_raw_score` PARITY GUARD 인라인, feedback 보존 + parse_error 유지) · **#813(#12)** SSRF DNS-rebinding docstring 정직화(옵션B, 코드 0줄) · **#814(#25)** hook parse_error 점수 NULL 저장(집계 오염 차단) + overview F-오분류 회귀 수정. **🔴 Codex true mutual(정책 18) 실결함 4건 적발**(#811 비원자 TOCTOU·타 파일 stale mock 3건·#814 overview F — 단독 진행 시 운영 반영됐을 회귀). 11 PR 사용자 위임 순차 머지. 단위 4655→4713(+58), 통합 153 불변, pylint 10.00, 전 PR CI green(#813 codecov tiny-diff non-blocking). **회고 + follow-up**: 5+1 다중 에이전트 회고(6 에이전트, P0 0/확정 P1 6~7, Codex cross-verify) → **(A) #816** docs/rule 정합(architecture 흐름·pipeline.md auto-merge fail-open 봉인 3종·정책18 §3 NG 2-tier·claim docstring) + **(B) #817** 회고 P1 테스트 갭 봉인(claim_decision PG first-writer-wins invariant·broad IntegrityError 흡수·seam 실DB). Codex mutual 이 follow-up 에서도 정합오류 적발(#816 정책18 off-by-one·#817 invariant 과장 표현). 단위 4713→4715·통합 153→154.
- **사이클 164** (2026-06-08): area=gate 감사 잔여 6 결함 — 사용자 Q1~Q4 결정 (#794~#796) — 이전 세션 중단 복구. read-only 워크플로우(7에이전트)로 잔여 6 결함 재분석(전부 still_present + 라인 drift 보정 + tier 분류) → 사용자 4 결정 → 3 PR. **#795 (Q2+Q3)**: `_run_static_analysis`/`_run_static_with_timeout` 재구성 — 파일 단위 격리(실패 파일 빈 결과·나머지+AI리뷰 보존) + 타임아웃 부분결과 보존(`loop.time()` deadline 파일별 순차) + self-verify 발견 안전망(전량 실패→incomplete fail-closed). pipeline-reviewer APPROVE×2, 단위 +3. **#796 (Q1)**: telegram `handle_gate_callback` 인라인 단발 merge → `engine._run_auto_merge` 위임(자동/반자동 완전 대칭 — retry 큐잉·SHA 가드·CI 재판별·terminal/deferred 알림·관측 단일 출처). 가드 AutoMergeAction 미러링, reject+고score 오머지 잠재 버그 차단, except RuntimeError 보강 + dead code 제거 + api.md sync, 단위 −2. **#794 (Q4)**: `_regate_pr_if_needed` first-writer-wins 통일(`_race_recover_existing` 대칭) — 동일 head SHA 멀티 PR 잘못된 PR auto-merge 차단, 단위 +1. ⚠️ #794 codecov/patch tiny-diff 아티팩트(SonarCloud New Code 100%, non-blocking). Codex exec 401 만료 지속 → 정책 18 standing 승인 하 Claude 적대적 self-verify(pipeline-reviewer 4 + skeptic 7렌즈) fallback. 전 PR P0·P1 0건. 단위 4648→4650. **회고 follow-up (#798/#799)**: 5+1 회고 P1 즉시 반영 — #798 PR 코멘트 incomplete 경고 배너(인플레 점수 무경고 노출 차단, i18n 3언어) + #799 회귀 가드 3건(incomplete 종단·_race_recover 대칭·semi-auto 임계 layer). **Codex mutual 복구**(2026-06-08 재로그인, exec CODEX_OK)로 #798/#799 push 전 정상 mutual 검증 — 6사이클 self-verify fallback 종료. 단위 4650→4655.
- **사이클 163** (2026-06-07): area=gate P2 백로그 해소 (#783~#787) — integrity-audit area=gate 리포트 confirmed P2 중 자기완결적 5건을 단일 도메인 PR + TDD + 적대적 self-verify(독립 skeptic 5~7렌즈)로 해소. **#783**: ApproveAction `_run_auto` 정적분석 불완전(`static_analysis_incomplete`) 가드 — #779 auto_merge 가드의 approve 경로 확장(인플레 점수 간접 머지 차단). **#784**: CLI hook 점수 필드 비숫자/None/Infinity 안전 변환 — `_coerce_raw_score`/`_coerce_ai_scores`(int() TypeError/ValueError/OverflowError 흡수 → default 폴백 + parse_error, 500 차단). self-verify 가 OverflowError(Infinity) 갭 발견·차단. **#785**: merge_retry 백오프 config 검증자 — `Field(ge=1)` 양수 5필드 + `model_validator` max>=initial 경계(백오프 단조성 보장) + env-vars.md sync. **#786**: branch/tag 삭제 push zero-SHA 조기 종료 — `_is_blank_sha`(빈/all-zeros), `_ensure_repo`/`_collect_files` 진입 전 차단(GitHub 404 반복 제거). **#787**: `_ensure_repo` 동시 INSERT race IntegrityError 복구 — rollback+재조회(다른 워커 repo 사용), 재조회 None re-raise(워커 abort 차단). pipeline-reviewer APPROVE(#786/#787). Codex 토큰 만료 지속 → 사용자 standing 승인 하 Claude 적대적 self-verify fallback(사이클 119/125/161/162 전례). 단위 +20(4628→4648).
- **사이클 161** (2026-06-06): 정합성 감사 P1 백로그 해소 (#764~#767) — 직전 #759 full 감사 confirmed P1 3건을 적대적 분석 워크플로우(analyze+refute, 3건 `diff_is_safe=true) 선행 후 단일 도메인 PR + TDD 로 해소. **#764**: CLI hook(`POST /api/hook/result`) AI raw 점수 미클램핑 → `ai_review.py:184` 패턴 미러 `max(0,min(AI_RAW_*_MAX,..))` 적용 (범위 밖 값이 calculator 스케일링 거쳐 breakdown cap 초과/음수 되는 정합 위반 차단, 회귀 가드 2). **#765**: `merge_reasons.is_retriable_tag()` dead code(src 호출처 0) 활성화 — `engine.py:202` 하드코딩 `(UNSTABLE_CI, UNKNOWN_STATE_TIMEOUT)` 튜플 → `is_retriable_tag()` (frozenset/tuple 동치 동작 보존, import 원자 정리). **#766**: `base.html` `_initReveal`/`_finishProgress` 가 `htmx:afterSettle`/`historyRestore` 에 remove-before-add 없이 등록돼 hx-boost 재방문마다 누적 → `document._initRevealHandler`/`_finishProgressHandler` 단일 슬롯 가드 (E2E 회귀 1). **#767**: `test_doc_review_gate` 절대경로 테스트가 하드코딩 `d:/source/scamanager/` 를 비-루트로 가정했으나 본 리포 실제 루트와 일치 → 루트가 `d:\Source\SCAManager` 인 머신에서만 실패(CI Linux 통과로 은폐) → 런타임 루트 파생 경로로 독립화. Codex 토큰 만료(`refresh token already used`) → 사용자 승인 하 Claude 직접검증 fallback (사이클 119/125 전례). 단위 +2 / E2E +1.
- **사이클 160** (2026-06-06): integrity-audit 워크플로우 검증 세션 (#760~#763) — integrity-audit 다이나믹 워크플로우 옵션 검증(scope/dryRun/changedFiles 정상) → 현재 main 전수 감사 → 정합성 fix 4건. **#760** 운영 문서 stale 경로 7건 (CLAUDE.md 메모리-grep 경로 등). **#761** n8n issue 릴레이 GitHub 토큰 유출 차단 — 전역 env `N8N_RELAY_REPO_TOKEN`(default off) opt-in + `n8n_webhook_secret` 둘 다 충족 시에만 전송. **#762** timeout 단일출처 정리(미사용 `constants.py` 300 제거, `pipeline.py` 60 유지, 동작 무변경) + merge off-by-one(N-1) 문서화. **#763** `issue_registrations` RLS policy alembic 0037 (repo_id→repositories.user_id 1-hop, PG 전용). ⚠️ STATE.md 동기화는 본 세션(161)에서 일괄 수행 — 사이클 160 당시 미반영(stale)이었음.
- **사이클 129** (2026-05-24): AI 분석 결과 GitHub Issue 등록 기능 (Phase 1+2) (#614 머지) — **Issue 등록 API 3종**: `POST /api/issues/register` (중복 감지: `issue_key` SHA-256 64자 dedup + TOCTOU `IntegrityError` 처리) + `GET /api/issues/status` (analysis_detail TTL 동기화) + `GET /api/issues/repo-summary` (repo_detail 일괄 동기화). **ORM/마이그레이션**: `IssueRegistration` 모델 (`issue_type`/`issue_key`/`github_issue_number`/`github_issue_state`/`github_issue_synced_at`) + `alembic/0035_issue_registrations.py` (CASCADE FK, `UniqueConstraint(repo_id, issue_key)`, PG 인덱스). **GitHub 연동**: `create_issue`/`get_issue_state` + TTL 300s 캐시 (`_sync_state_if_stale` 헬퍼 — naive datetime UTC 정규화 포함). **UI**: `analysis_detail.html` `#issueRegPanel` (AI/Static 탭 + 등록 모달) + `repo_detail.html` `#repoBulkPanel` (일괄 등록 체크박스). **CI 수정 3단계**: ① SonarCloud Coverage 75%/83% → 100% (21 tests 추가 — `_get_analysis_and_repo`·`_get_repo_or_404`·TOCTOU·HTTPError·naive-datetime 경로 커버) → ② SonarCloud CPD 4.8% → `_sync_state_if_stale` 헬퍼 추출 + `sonar.cpd.exclusions=tests/**,src/templates/**` → 0.0% → ③ CodeQL 자동수정 2건 rebase 흡수. Codex mutual OK (2회). 단위 2948→3007.
- **사이클 125** (2026-05-23): 회고 P1/P2 전수 이행 (#600). **P1-1**: `src/auth/session.py` `get_current_user` DB 조회 `try/except Exception: return None` 추가 — INT_MAX+1 (2^31) 세션 주입 → PG DataError 미처리 500 차단. **P1-2**: `tests/unit/auth/test_session_security.py` `os.environ.setdefault` → 직접 대입 교체 (testing.md 규칙) + `test_get_current_user_with_overflow_user_id_returns_none` 신규 (P1-1 페어). **P1-3**: `src/constants.py` BREAKDOWN_KEY_* 주석 "calculator·notifier·gate 공유" → "현재 calculator 전용, notifier·gate 확장 예정" (선언/현실 불일치 해소). **P2**: `tests/unit/auth/test_github.py` `test_oauth_state_consumed_after_use_reuse_fails` docstring 목적 명시. Codex mutual: Codex CLI 런타임 오류 → 사용자 승인 하에 Claude 직접 검증 대체. 단위 2947→2948, 누적 3098→3099.
- **사이클 124** (2026-05-23): B+C 작업. C Phase 2 — SonarCloud S1192 해소: `src/constants.py` `BREAKDOWN_KEY_*` 5개 상수 신설 + `src/scorer/calculator.py` 중복 리터럴 (`"code_quality"` ×3 / `"security"` ×3) → `Category`/`Severity` StrEnum + `BREAKDOWN_KEY_*` 상수 교체 (#597). B P2-A — session fixation 경계 테스트: `test_session_security.py` INT_MAX(2^31-1) user_id → None 검증. B P2-B — OAuth state 재사용 차단 명시: `test_github.py` `MismatchingStateError` → `/?error=oauth_failed` 302 문서화. 단위 2945→2947, 누적 3096→3098.
- **사이클 123** (2026-05-23): B+C 작업. B — P3 보안 심층 테스트: `test_email.py` `test_build_html_escapes_xss_in_issue_message` 추가 (Telegram ↔ Email XSS parity 해소). C — SonarCloud S6540: `src/middleware/locale.py` `Optional[str]` → `str | None` 2곳 + import 제거. `src/notifier/_language.py` `Optional[Session/str]` → `Session|None/str|None` 3곳 + import 제거 (#595). 단위 2944→2945, 누적 3095→3096.
- **사이클 117** (2026-05-22): /login 중간 단계 제거 — GitHub OAuth 직행 + 오류 배너 (#565, #567, #568, #570, #572, #574, #575, #578) — `/login` 301 redirect to `/auth/github` (하위 호환), `require_login` Location `/login` → `/auth/github`, `auth_callback` try/except (`OAuthError` → `/?error=oauth_failed` / `Exception` → `/?error=auth_failed`), 이메일 폴백 `noreply.github.com`, `landing.html` nav 링크 + 오류 배너 CSS/HTML, `overview` `error` param. 통합 테스트 11건 동작 변경 반영 (`/login` 301 / auth_callback 302 / dependency_overrides 격리 추가). 신규 단위 테스트 4건. 단위 2938→2942. **#568 (follow-up)**: `logout` 리다이렉트 `/login` → `/` — `/login` 301 체인으로 인해 로그아웃 후 GitHub OAuth 화면 진입 버그 해소. **#570 (follow-up)**: `logout` HX-Redirect 적용 — hx-boost body-swap 시 landing.html 독립 `<head>` CSS 미적용 레이아웃 깨짐 해소. `HX-Request` 헤더 감지 시 `200 + HX-Redirect: /` 반환으로 전체 페이지 재로드 유도. 단위 +1 (2942→2943). **#572 (follow-up)**: landing.html nav 우상단 "로그인" 버튼 제거 — 히어로 "지금 시작 →" CTA 와 중복으로 혼선 유발, 단일 CTA 로 단순화. **#574 (follow-up)**: `github.py` 미사용 `get_current_user` import 제거 + 테스트 불필요한 patch 정리. **#575 P0**: `test_callback_no_email_anywhere_uses_noreply_address` 추가 (email=None + 빈 이메일 목록 → noreply fallback 경로 커버, 단위 2943→2944) + `landing.html` 배너 닫기 onclick `history.replaceState` 추가 (?error= URL 잔존 → 새로고침 배너 재표시 버그 수정). **#578 P2 + CI 수정**: `login.html` 삭제 (사이클 117 standalone `landing.html` 통합으로 dead template — 196줄) + `overview.py` `_ALLOWED_ERRORS` 화이트리스트 (`oauth_failed`/`auth_failed`) + `login.html` 참조 테스트 3파일 8건 제거 (test_form_mobile_sweep 3 / test_i18n_template_render 4 / test_illustration_markup 1). 단위 2944→2941, 통합 154→151, 전체 3098→3092.
- **사이클 111** (2026-05-20): analysis_detail 차트 검정 배경 + 피드백 버튼 핸들러 누적 수정 (#533) — **Bug 1**: `backgroundColor: accent + '22'` → `accentRgb ? ('rgba(' + accentRgb + ', 0.13)') : (accent + '22')`. 모든 테마가 `--accent-rgb: 99,102,241` 형식 정의 → `accent = 'rgb(99,102,241)'` → `'rgb(99,102,241)22'` INVALID CSS → Chart.js fallback rgba(0,0,0,0.1) → 차트 영역 검정. **Bug 2**: 피드백 IIFE → `_initFeedback()` named function + `feedbackInit` data attribute guard + `document._adFeedbackHandler` remove-before-add (htmx-boost 재방문 시 핸들러 중복 등록 방지). 회귀 가드 13건 신설 (케이스 A/B/C). 단위 2860→2873.
- **사이클 110** (2026-05-20): AI Insight 에러 빈도 추적 (#530, #531) — **#530**: AI 리뷰 모델 선택기를 Card ⑥ 에서 위험 구역 바로 위 독립 카드로 이동 (settings.html). **#531**: InsightNarrativeCache 에러 추적 컬럼 3개 (last_error_at/error_count/last_error_type) + 0033 마이그레이션 (PG 인덱스 포함) + `record_error` / `record_error_repo` 헬퍼 신설 — dashboard_service/repo_insight_service 에러 경로 3곳 호출 추가 + `log_claude_api_call` error_type 인수 누락 버그 수정. 42건 신규 테스트. Codex mutual: 샌드박스 차단 → 사용자 직접 push 승인. 단위 2818→2860.
- **사이클 109** (2026-05-19): 전체 페이지 정밀 감사 + P0/P1/P2 14건 수정 (#516~#519) — 5+1 다중 에이전트 감사로 14건 식별 (P0 1 + P1 7 + P2 6). **#516 P0**: `database.py` RLS f-string SQL → 파라미터화 쿼리 (SQL injection 방어). **#517 P1**: `base.html` RAF 경쟁조건 + 이벤트 핸들러 누적등록 4곳 remove-before-add 전환 + window.pageshow/beforeunload 핸들러 remove-before-add. **#518 P1**: `merge_retry_repo.py` `claim_batch` FOR UPDATE SKIP LOCKED (PostgreSQL 조건부) + `auth/github.py` 로그아웃 시 DB github_access_token NULL 처리 + `i18n/filters.py` XSS 가드 (markupsafe.escape — str kwargs HTML 이스케이프, Markup passthrough) + `settings.html` countdown `{% set %}` Markup 객체화. CI fail (codecov/patch 69.23%) → 테스트 2건 추가 fix-up 후 pass. **#519 P1~P2**: `analysis_detail.html`/`dashboard.html`/`repo_insights.html` htmx:afterSettle+historyRestore 핸들러 추가 (뒤로가기 시 차트 미복원 버그) + animate=false 일관 적용 + `dashboard.html` phantom CSS 토큰 `--accent-primary` 제거. Codex mutual 검증: #518 NG(assert 약함)→강화→OK / #519 NG(animate=false 누락)→추가→OK. 단위 2816→2818.
- **사이클 108** (2026-05-19): E2E 성능 테스트 assert 강화 + `/api/github/repos` TTL 캐시 (#504, #505) — **#504**: `e2e/test_performance.py` FCP/LCP `assert is not None` 선행 추가 (null 메트릭 silent skip → 명시 실패) + `test_health_ttfb` Playwright → `requests.Session` 전환 (Windows IPv6→IPv4 DNS fallback ~667ms/req 해소, warmup 1회 선행). Codex NG (한국어 단독 warmup 주석) → 이중 언어 주석 수정 → OK. **#505**: `/api/github/repos` 5분 TTL 캐시 (`_user_repos_cache`) 추가 — `existing_names` 항상 최신 DB 조회 (fresh) + `tests/conftest.py` `_clear_user_repos_cache` autouse fixture + 캐시 hit/expire 2 테스트. Codex OK. 단위 2814→2816.
- **사이클 107** (2026-05-19): 회고 반영 + 문서·코드 정비 (#503) — P0 수정: `make perf-report` Windows UnicodeEncodeError (`PYTHONIOENCODING=utf-8`). `/health` 임계값 e2e/scripts 통일 (`health_ttfb: 300ms` + `_is_health_page()` urlsplit 헬퍼, Codex NG→수정). CLAUDE.md 6-step `cycle-history.md` 동기화 추가. `docs/cycle-history.md` 사이클 95~106 이력 12건 추가. `docs/architecture.md` `scripts/` 미등재 파일 5개. `.claude/rules/testing.md` `@pytest.mark.perf` 지침. **P1 잔여 → 사이클 108**: FCP/LCP 조건부 assert → `assert is not None` 선행 추가 + `/health` Playwright → requests 전환.
- **사이클 106** (2026-05-18): 페이지 성능 측정 E2E + 독립 스크립트 (#500) — `e2e/test_performance.py` 12개 `@pytest.mark.perf` 테스트 (TTFB/FCP/LCP/DCL/Load, 3회 avg/min/max, Playwright Chromium headless, `analysis_page` fixture + `seeded_analysis` session-scope) + `scripts/perf_measure.py` 독립 실행 스크립트 (로컬 SQLite 서버 자동 시작·종료, 운영 Railway TTFB, `--local-only`/`--prod-only`, `_http_ttfb` stream=True strict TTFB, Markdown 리포트 `docs/reports/perf-*.md`) + `make test-perf`/`make perf-report` Makefile 타깃. Codex mutual 검증 OK (P1 fd 미닫음 + P2 TTFB 정확도 수정). 첫 리포트: 로컬 11 페이지 전부 ✅, `/api/github/repos` ~510ms 느린 API 포착.
- **사이클 105** (2026-05-18): CI TruffleHog BASE==HEAD 오류 수정 (#498) — push-to-main 이벤트에서 `base: default_branch`가 HEAD와 동일 커밋을 가리켜 exit 1하던 문제 해소. 인라인 삼항 조건식 → PR/push 이벤트별 2-step 분리 + zero-SHA(첫 push/force-push) if 가드 추가. Codex mutual 검증 2회 (1차 NG→수정→2차 OK). CI TruffleHog + pytest 전체 success 확인.
- **사이클 104** (2026-05-18): 5+1 다중 에이전트 전체 정합성 감사 + Anthropic 표준 준수 검증 (#490) — P0 5건: `docs/architecture.md` tailwind.css ghost entry 명시 + 함수명 2개 정정 (`enable_or_fallback` / `build_notification_tasks`) + github_client·railway_client 미등재 파일 추가 + E2E/메모리/배지 수치 갱신 (96→99, 18→19, 2967→2968). P1 1건: CLAUDE.md doc-consistency-reviewer 사용 범위 명확화 (cross-verify 6차 금지 → 단독 목적 허용). false-positive 2건 제거. Anthropic 표준 준수도 **93/100** 확인.
- **사이클 103** (2026-05-18): MCP Supabase 직접 조회로 auto-merge 실패 원인 분석 → 3건 코드 수정 + Codex mutual 검증 OK 후 머지 (#479) — **P0-fix**: `_trigger_retry_for_sha`의 `if not rows: return` 조기 반환 제거 + overdue sweep 무조건 실행 (cron failure fallback 보장) + `has_hooks` terminality `terminal` → `retriable` (CI hook 대기 → CI 완료 자동 해소) + `should_retry(UNSTABLE_CI, "unknown")` → `True` (transient GitHub API 오류 시 영구 terminal 방지). 테스트 3파일 갱신 (test_retry_policy · test_check_suite_handler · test_auto_merge_enqueue) + 신규 `test_trigger_retry_for_sha_sweeps_overdue_when_no_sha_rows` 추가. 단위 2813→2814.
- **사이클 102** (2026-05-17): 잔여 작업 확인 + `.codex/rules/ui.md` 테마명 정정 (`glass/claude-dark` → `pastel/catppuccin` — #462 CLAUDE.md 정정과 동기화, Codex 규칙 drift 해소, #475). CLAUDE.md P2-1 압축 완료 — **#476 Phase 1** (~10줄: 정책 8 관점 분리 목록·사이클 67 조건·단일관점 ROI·정책 18 하단) + **#477 Phase 2** (~7줄: 정책 1 서문·중복 cross-ref·cross-verify 에이전트 설명 1줄 합침·진화 요약 heading 제거) + `docs/policies/history.md` 이전 3건 (정책 1 Q4 회귀 가드 전체 · 사이클 67 생략 조건 폐기 이력 · 단일관점 ROI 삭제 배경). 5+1 에이전트 검증 + Codex mutual OK. 총 절감 ~17줄.
- **사이클 95** (2026-05-14): 문서 정비 P2 (#410~#413) — testing.md SessionLocal 경고 추가 / architecture.md mockup-polar.html 항목 / AGENTS.md 중복 표 → 링크 3건 / STATE.md 그룹 13-61 아카이브 / CLAUDE.md HTML 주석 7블록 + line 361 압축 (59줄 절감).
- **사이클 96** (2026-05-14): pylint 10.00/10 달성 (#415) — C0415 17건 inline disable + C0301/R0913/R0917/W0718/W0613/E0401 처리. PR-D5 (#417): CLAUDE.md HTML 블록 3·5 → docs/policies/active.md 이전 (#정책-17-why-how + #정책-5-phase-종료-cross-reference 신설).
- **사이클 97** (2026-05-14): P0 경로 하드코딩 수정 (#420) — `.codex/hooks.json` + `doc_review_gate.py` × 2 + `.claude/settings.json` + `CLAUDE.md` `f:/` → `d:/` 전수 교체. 문서 정비 (#421) — AGENTS.md 완료 6-step 정정 + `.codex/rules/deploy.md` nixpacks exit127 경고 추가. Issue #408 기능 구현 (#423) — MPA 네비게이션 진행 바 (`#page-progress` CSS+JS IIFE, 같은 경로 앵커 가드) + `CachedStaticFiles` (1년 캐시 immutable) + `dashboard_service.repo_insight_cards` N+1 → 배치 IN 쿼리 2건 (`_fetch_analyses_for_window` + `_group_analyses_by_repo`) + `compute_score_kpi` 공유 헬퍼 추출 (CPD 5.5%→0.0%) + `recurring_issue_count` count≥2 수정. 단위 테스트 2912→2914. scripts 하드코딩 경로 제거 + dashboard async 블로킹 수정 (#426) — `scripts/i18n_comments/` 4 파일 `f:/…` → `Path(__file__).resolve().parents[2]` 동적 경로 + `dashboard.py` security/usage/overview `run_in_threadpool` 래핑 (이벤트 루프 블로킹 방지) + `_DASHBOARD_TEMPLATE` 상수 추출 (SonarCloud S1192 해소). STATE.md 갱신 (#427). 랜딩 페이지 추가 (#428) — `src/templates/landing.html` Vercel+Stripe 스타일 프리미엄 랜딩 (애니메이션 메시 그라데이션 + 4-orb float + fadeUp stagger + 미인증 분기 `overview.py` → `get_current_user` Optional 교체) + `test_overview_landing.py` 2건 (미인증→landing / 인증→overview). 랜딩 phantom token 수정 (#429) — `--bg-base-rgb` → `var(--bg-nav)` (4-테마 정의 token) + `docs/architecture.md` templates 목록 `landing` 추가. CodeQL unused import 해소 (#431) — `test_0031_repo_insights_cache.py` 미사용 `from-import` 제거 (alerts #387+#389) + side-effect import #386/#384 false-positive dismiss.
- **사이클 99 CodeQL 정비** (2026-05-15): PR #445 머지 — `tests/unit/ui/test_dashboard_repos_mode.py` `import pytest` 삭제 (알림 #397 실제 수정) + `tests/integration/test_repo_report_api.py` side-effect import `# noqa: F401` 알림 #395/#396 false-positive dismiss. CodeQL open alert 0건.
- **사이클 99 후속** (2026-05-15): 회고 문서 정비 (#docs/cycle99-retro-followup) — `.claude/rules/testing.md` settings 싱글톤 mock 패턴 추가 + `.claude/rules/ui.md` Jinja2 `lower+default` None 함정 경고 추가 + `docs/architecture.md` `/api/repos/report` 엔드포인트 2건 + repo_insight_service 7 함수 + find_all_by_user + repositories 설명 갱신 + `docs/policies/active.md` PR 템플릿 Codex 검증 의뢰 섹션 추가 (정책 18 강화). ⚠️ cherry-pick 경위: PR #442/443은 feat 브랜치를 base로 생성 → GitHub MERGED 표시됐으나 main 미반영 → `git cherry-pick 2cf0bf4 78fd6de b7b1b07 df54294` + `git push origin main` 으로 사용자(xzawed)가 직접 수습.
- **사이클 101 회고 자유 발언 이행** (2026-05-17): 메모리 슬러그 자동 검증 + 에이전트 도메인 분리 (#469~#470) — **#469**: `scripts/check_memory_refs.py` 신설 (CLAUDE.md/active.md/history.md 참조 슬러그 ↔ 실제 파일 비교, 스테일 `(현재 미생성)` 어노테이션 탐지, UTF-8 강제, S3776 CC ≤ 15) + `Makefile` `check-memory-refs` 타깃 추가 + `active.md` 스테일 어노테이션 8건 제거. **#470**: `docs/policies/active.md` `#정책-8-doc-audit-agent-domain` 신설 — 5+1 문서 감사 에이전트 비중복 5 도메인 분리 표 (Agent-1: CLAUDE.md/.claude / Agent-2: policies/ / Agent-3: architecture+STATE / Agent-4: .claude/rules/ / Agent-5: reference+runbooks / Agent-6: cross-verify). 메모리 18건 실측 확인 (이전 35건 = 산정 오류 정정).
- **사이클 101 문서 감사 후속** (2026-05-17): 5+1 cross-verify 식별 항목 정리 (#466~#467) — **#466**: `.claude/rules/testing.md` `tests/test_ui_router.py` → `tests/unit/ui/test_router.py` 경로 + `.claude/rules/api.md` SDK timeout 명확화 (Anthropic 60s / httpx 10s) + `.claude/rules/deploy.md` SMTP_PORT coerce_smtp_port 자동처리 정정. **#467**: `docs/reference/env-vars.md` `STRICT_TOKEN_ENCRYPTION` 누락 항목 추가 + SMTP_PORT 설명 정정 + `docs/STATE.md` "2967 passed" → "2963 passed, 4 skipped" 실측 반영. 누락 메모리 파일 4개 생성 (P0 — feedback-architecture-decision-pre-confirm / feedback-codex-post-validation-mandatory / feedback-fixture-model-sync-discipline / feedback-pr-scoped-ci). 허위 양성 6건 제거 (architecture.md 분석기 수 / Policy 13 smoke 카운트 / DB_FORCE_IPV4 / MERGE_UNKNOWN_RETRY_LIMIT / CLAUDE.md 줄수 / api.md timeout 평면 불일치).
- **사이클 101** (2026-05-17): 5+1 멀티에이전트 감사 전 항목 수정 (#460~#464) — **#460 P0**: `test_grade_a_for_score_90_plus` dead assertion 수정 (AI 최대점 강제로 total=100 보장) + `_extract_event_metadata` 브랜치 삭제 push KeyError 방어 (`or {}` 패턴) + README 배지 갱신 (2917→2964). **#461 P1**: `GITHUB_API` URL 상수 9개 파일 일괄 치환 + Gate 임계값 3종 (`GATE_DEFAULT_APPROVE/REJECT/MERGE_THRESHOLD`) constants.py 단일 출처 → ORM·dataclass·API·UI 4곳 sync. **#462 P1**: CLAUDE.md·active.md 테마명 `glass/claude-dark` → `pastel/catppuccin` 정정 + test_semgrep 2개 isinstance-only 어설션 강화 (`filename`·`issues` 검증 추가). **#463 P1**: lifespan startup `TELEGRAM_WEBHOOK_SECRET` 미설정 시 prod-only SECURITY 경고 + test_main 3건 커버리지. **#464 P2**: `validate_external_url` async화 + `socket.getaddrinfo` → `asyncio.to_thread` (이벤트 루프 블로킹 방지) + 5개 notifier caller `await` 추가 + test_http.py async 변환 + test_retry_concurrency_postgres 2곳 assertion 강화 (is not None → len==1/isinstance list). 단위 2810→2813, 통합 125→154.
- **사이클 100** (2026-05-17): pytest 성능 병목 해소 (#449) — `pytest.ini` `--timeout=30` 전역 적용 (subprocess hang 방지) + starlette/fastapi DeprecationWarning 필터 + `pytest-timeout>=2.3.0` 추가 + `Makefile` `test-local` 타겟 (slow 제외 Windows 로컬용) + `tests/integration/test_static_analyzer.py` module-scope fixtures 도입 (`_clean_result`/`_eval_result` — analyze_file subprocess 모듈당 1회 제한). 테스트 수 변동 없음 (34 passed).
- **사이클 98** (2026-05-15): CodeQL py/exit-from-finally 수정 (#433) — `tests/unit/ui/test_router.py` finally 블록 람다 2건 → 명시 함수 `_restore_get_current_user()` / `_restore_require_login()` 교체 (CodeQL alert #390 해소). HTMX hx-boost 네비게이션 (#434) — `htmx.min.js` 1.9.12 vendoring + `base.html` `<body hx-boost="true">` + `htmx:afterSettle` 프로그레스바 연동 + `analysis_detail.html` Chart.js `destroy()` 가드 + `test_htmx_vendor.py` 회귀가드 3건 (단위 2914→2917). hx-boost 후속 수정 (#435) — `docs/architecture.md` htmx.min.js 항목 추가 + `repo_detail.html`/`dashboard.html` `themechange` boolean 플래그 가드 (stale closure 버그 발생 — #436으로 즉시 수정). themechange stale closure 완전 수정 (#436) — boolean 플래그 → remove-before-add 패턴 3개 파일 (`_repoThemeHandler`/`_dashThemeHandler`/`_riThemeHandler`) + `.claude/rules/ui.md` themechange 패턴 갱신.
